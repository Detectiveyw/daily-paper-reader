---
title: General Techniques for Reducing Key-Switching Overhead in Privacy-Preserving Two-Party Transformer Inference
title_zh: 隐私保护两方 Transformer 推理中降低密钥切换开销的通用技术
authors: "Wenshao Yang, Zhenhua Liu, Dongdong Yao"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25349v1"
tags: ["query:fss-llm"]
score: 9.0
evidence: 安全两方Transformer推理与FHE优化
tldr: 本研究针对隐私保护两方Transformer推理中全同态加密（FHE）密钥切换带来的巨大计算开销，提出了一套通用的优化技术。通过将注意力机制分解为预计算与在线交互、引入存储与通信权衡机制，以及融合乘法后的旋转操作，该方法在不改变现有打包策略的前提下，显著降低了在线推理阶段的计算负担，为资源受限环境下的安全推理提供了灵活的部署方案。
selection_source: fresh_fetch
motivation: 现有的FHE矩阵乘法优化高度依赖特定的打包算法，且密钥切换操作在Transformer推理中占据了主要的计算开销。
method: 提出了预计算辅助的注意力计算方法、存储-通信权衡技术以及针对乘法-旋转模式的融合密钥切换技术。
result: 该技术在不修改现有打包策略的情况下，显著减少了在线阶段的密钥切换开销，并实现了存储与通信之间的灵活平衡。
conclusion: 本研究通过通用的预处理和算子融合技术，有效提升了隐私保护Transformer推理的效率，具有较强的普适性和实用价值。
---

## 摘要
在安全两方 Transformer 推理中，线性层通常使用全同态加密（FHE）通过明文-密文或密文-密文矩阵乘法进行评估。在基于 FHE 和混合 FHE-MPC 系统中，密钥切换主要发生于此，并占据了主要的计算开销。现有的优化方案严重依赖于特定的打包算法，限制了它们的通用适用性。针对这一开销，我们从独立于打包方式的角度出发，提出了一种用于安全注意力计算的预处理辅助方法。通过将注意力机制分解为可预计算的操作和在线交互，该方法在不修改现有打包策略的情况下，减少了在线推理阶段的密钥切换。然而，第一种将密钥切换转移到离线的方法引入了额外的存储需求。为解决这一问题，我们提出了存储-通信权衡技术，用适度的在线通信取代大型预计算密文，从而实现在不同资源约束下的灵活部署。虽然在混合方案和基于 FHE 方案的第一层中，密文-密文矩阵乘法被卸载到了预处理阶段，但这些操作在离线阶段和后续 FHE 层中仍然存在。为了进一步优化，我们针对现有 RNS-CKKS 矩阵乘法方案中频繁出现的“先乘法后旋转”模式，提出了一种融合密钥切换技术。通过将重线性化和旋转合并为一个过程，该技术降低了相关的计算成本。分析评估表明，我们提出的技术显著降低了在线密钥切换开销，并在无需修改现有打包策略的情况下，提供了存储与通信之间的灵活权衡。

## Abstract
In secure two-party Transformer inference, linear layers are typically evaluated using Fully Homomorphic Encryption (FHE) through plaintext-ciphertext or ciphertext-ciphertext matrix multiplications, where key switching primarily occurs and dominates computational overhead in both FHE-based and hybrid FHE-MPC systems. Existing optimizations rely heavily on packing-specific algorithms, limiting their general applicability.   Targeting this overhead from a packing-independent perspective, we propose a preprocessing-assisted method for secure attention computation. By decomposing attention into precomputable operations and online interactions, this method reduces online inference-phase key switching without modifying existing packing strategies.   However, the first method shifting key switching offline introduces additional storage requirements. To address this, we propose storage-communication trade-off techniques that replace large precomputed ciphertexts with modest online communication, enabling flexible deployment under varying resource constraints.   While ciphertext-ciphertext matrix multiplication is offloaded to the preprocessing phase in hybrid schemes and the first layer of FHE-based schemes, these operations still persist in the offline stage and subsequent FHE layers. To further optimize it, we propose a fused key-switch technique targeting the multiplication-followed-by-rotation pattern, which frequently arises in existing RNS-CKKS matrix multiplication schemes. By combining relinearization and rotation into a single procedure, this technique reduces the associated computation costs.   Analytical evaluations demonstrate that our proposed techniques significantly reduce online key-switch overhead and provide flexible trade-offs between storage and communication without requiring modifications to existing packing strategies.

---

## 论文详细总结（自动生成）

这是一份关于论文《General Techniques for Reducing Key-Switching Overhead in Privacy-Preserving Two-Party Transformer Inference》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在隐私保护的两方 Transformer 推理场景中（客户端持有数据，服务器持有模型），全同态加密（FHE）和混合 FHE-MPC 技术被广泛采用。然而，FHE 中的**密钥切换（Key-Switching）**操作是计算开销的主要来源，直接决定了推理延迟。
*   **研究动机**：现有的优化方案（如 BLB, THOR, ARION 等）大多与特定的“明文打包策略”（Packing Strategy）深度绑定，缺乏通用性。
*   **核心问题**：如何设计一种独立于打包策略的通用技术，以降低在线推理阶段的密钥切换开销，并平衡存储与通信之间的矛盾。

### 2. 论文提出的方法论
论文提出了三项核心技术，旨在从不同维度优化密钥切换开销：

*   **预处理辅助的注意力分解（Algorithm 1）**：
    *   **核心思想**：利用加法同态特性，将输入 $X$ 分解为随机掩码 $U$ 和掩码后的明文 $X-U$。
    *   **技术细节**：将注意力分数计算 $QK^\top$ 展开为四个部分。其中涉及纯掩码矩阵的复杂密文乘法（如 $Q_u K_u^\top$）被移至离线预处理阶段；在线阶段仅需处理密文与明文（$X-U$ 相关项）的乘法，从而大幅减少在线密钥切换次数。
*   **存储-通信权衡策略（Algorithm 2, 3, 4）**：
    *   **核心思想**：预处理虽然减少了计算，但会产生大量预计算密文，导致存储压力。
    *   **技术细节**：通过引入额外的随机掩码和少量在线明文通信，替代存储大型旋转后的密文集合。该方法允许系统根据带宽和内存情况灵活调整部署方案。
*   **融合重线性化与旋转技术（Algorithm 5）**：
    *   **核心思想**：针对 RNS-CKKS 方案中常见的“乘法后紧跟旋转”的计算模式进行底层算子融合。
    *   **技术细节**：传统流程需要先进行重线性化（包含一次密钥切换）再进行旋转（包含另一次密钥切换）。该技术通过修改算法流程，将两者合并为一个单一的密钥切换过程，省去了一次昂贵的 ModDown 操作。

### 3. 实验设计
*   **场景与 Benchmark**：论文选择了两个具有代表性的安全推理框架作为基准：
    1.  **Arion**：基于纯 FHE 的非交互式推理方案。
    2.  **BLB**：基于 FHE-MPC 混合协议的推理方案。
*   **对比方法**：对比了原始方案（Original）、集成预处理分解后的方案（Variant 1）以及集成存储-通信权衡后的方案（Variant 2）。
*   **评估指标**：主要采用**分析性评估**，对比指标包括：在线密钥切换次数（Key Switch Counts）、预处理阶段的密文存储量（Ciphertext Storage）、在线通信流量（Data Traffic）。

### 4. 资源与算力
*   **算力说明**：论文明确指出，本版本（arXiv v1）侧重于**分析性评估（Analytical Evaluation）**。
*   **硬件信息**：文中**未提供**具体的 GPU 型号、数量或端到端训练/推理时长。作者在文中备注，包含端到端实现结果和实验评估的更新版本将在后续修订中发布。

### 5. 实验数量与充分性
*   **实验规模**：论文针对 Transformer 的三个关键阶段（Q/K/V 投影、注意力分数计算、值聚合）进行了详细的操作计数对比。
*   **充分性评价**：从理论分析的角度看，实验对比了不同安全模型（纯 FHE 与混合 MPC）下的性能变化，覆盖了主要的计算瓶颈。
*   **客观性**：通过数学分解和算子计数的硬性指标进行对比，结果较为客观。但由于缺乏真实硬件上的延迟（Latency）数据，目前尚无法完全评估其在实际网络环境下的表现。

### 6. 论文的主要结论与发现
*   **显著降低计算压力**：通过预处理分解，在线阶段的密钥切换次数可大幅减少。例如在 Arion 框架的注意力分数计算中，可将 1536 次在线密钥切换降至 0 次。
*   **灵活的资源配置**：存储-通信权衡技术非常有效。在 BLB 框架中，通过增加少量在线通信，可将预处理存储需求从 50 个密文降至 6 个。
*   **底层优化增益**：融合密钥切换技术通过减少 ModDown 操作，直接提升了 RNS-CKKS 矩阵乘法的底层效率，且该优化对所有层均有效。

### 7. 优点
*   **打包无关性（Packing-independent）**：这是最大的亮点。该方法不要求修改现有的数据打包方式，具有极强的普适性，可以轻松集成到现有的各种安全推理框架中。
*   **数学分解巧妙**：通过对注意力机制公式的代数重组，精准地将昂贵的密文-密文操作转化为廉价的密文-明文操作。
*   **部署灵活性**：提供了存储与通信的权衡方案，适应了从高性能服务器到资源受限设备的多种部署场景。

### 8. 不足与局限
*   **缺乏实测性能数据**：目前仅有操作计数分析，缺乏在真实 GPU/CPU 上的端到端运行时间对比，难以直观判断其对整体推理速度的提升幅度。
*   **通信开销增加**：虽然减少了存储，但权衡方案引入了额外的在线明文传输，在极低带宽环境下可能会抵消计算优化带来的收益。
*   **适用范围限制**：在纯 FHE 方案中，预处理分解主要应用于第一层；对于后续层，除非引入交互，否则优化效果有限。

（完）
