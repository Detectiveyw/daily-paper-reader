---
title: Layer-Parallel Inference Reduces Encrypted Nonlinear Depth in Transformers
title_zh: 层并行推理降低了 Transformer 中的加密非线性深度
authors: "Ligong Han, Kai Xu, Hao Wang, Ruijiang Gao, Akash Srivastava"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04819v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 使用全同态加密和层并行技术的加密Transformer推理
tldr: 针对全同态加密（FHE）下Transformer推理因非线性层顺序堆叠导致的计算瓶颈，本文研究了结构化牛顿层并行（SNLP）方法。该方法将L层顺序非线性深度转化为少数求解器迭代和线性修正，显著减少了加密推理中的自举（bootstrap）次数。实验表明，SNLP在大幅降低非线性深度的同时保持了较低的误差放大率，为高效加密大模型推理提供了新路径。
selection_source: fresh_fetch
motivation: 解决全同态加密中Transformer推理因大量非线性块顺序组合导致的计算延迟和深度瓶颈。
method: 采用结构化牛顿层并行（SNLP）技术，将层间顺序组合替换为少量的求解器迭代与线性结构修正。
result: "在0.5B模型上将自举次数减少了2.65倍，困惑度仅增加1.2%，且误差放大率低于传统的顺序推理。"
conclusion: SNLP能有效降低加密Transformer的非线性深度，且与块级FHE友好型算子设计具有互补性。
---

## 摘要
全同态加密 (FHE) 能够对加密数据进行计算，但实际的加密 Transformer 推理受到许多非线性块顺序组合的瓶颈限制。我们研究了结构化牛顿层并行 (SNLP) 是否能使这种层间组合对 FHE 更加友好：虽然每个 Transformer 块仍需对 softmax 和 RMSNorm 等操作进行多项式逼近，但 SNLP 将逐层顺序非线性深度从 L 个阶段减少为少量的求解器迭代加上线性结构化修正。利用基于切比雪夫多项式逼近的仿真框架，我们测量了 8 个模型和 4 个架构系列在顺序推理与 SNLP 推理下的误差累积。在一个 0.5B 参数的 IDN 训练模型上，SNLP 将符号自举 (symbolic bootstraps) 从 53 次减少到 20 次 (2.65 倍)，困惑度仅增加 1.2%，同时降低了误差放大率 (1.36x vs. 1.42x)。在所有测试模型中，SNLP 的放大率均低于顺序推理。消融实验表明，softmax 逼近占据了主要的误差预算，而 CKKS 算术噪声在我们的设置中可以忽略不计，这表明 SNLP 是对块级 FHE 友好算子设计的补充，而非替代。

## Abstract
Fully homomorphic encryption (FHE) enables computation on encrypted data, but practical encrypted Transformer inference is bottlenecked by the sequential composition of many nonlinear blocks. We study whether Structured Newton Layer Parallelism (SNLP) can make this inter-layer composition more FHE-friendly: each Transformer block still requires polynomial approximations for operations such as softmax and RMSNorm, but SNLP reduces the layerwise sequential nonlinear depth from L stages to a small number of solver iterations plus linear structured corrections. Using a simulation framework based on Chebyshev polynomial approximations, we measure error accumulation under sequential versus SNLP inference across 8 models and 4 architecture families. On a 0.5B IDN-trained model, SNLP reduces symbolic bootstraps from 53 to 20 (2.65x) with only +1.2% perplexity degradation, while lowering error amplification (1.36x vs. 1.42x). Across all tested models, SNLP has lower amplification than sequential inference. Ablations show that softmax approximation dominates the error budget and CKKS arithmetic noise is negligible in our setting, suggesting that SNLP is complementary to block-level FHE-friendly operator design rather than a replacement for it.

---

## 论文详细总结（自动生成）

这是一份关于论文《Layer-Parallel Inference Reduces Encrypted Nonlinear Depth in Transformers》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：全同态加密（FHE）允许在加密数据上进行计算，是隐私保护推理的核心技术。然而，Transformer 模型在 FHE 下的推理效率极低。
*   **核心痛点**：Transformer 包含大量非线性操作（如 Softmax、LayerNorm、激活函数），在 FHE 中必须使用多项式逼近。在传统的 $L$ 层顺序推理中，这些近似误差会逐层复合放大，且消耗大量的“模数预算”，导致必须频繁执行极其耗时的“自举”（Bootstrapping）操作（占总延迟的 50-86%）。
*   **研究动机**：探索是否可以通过改变层间计算拓扑结构，降低顺序非线性深度，从而减少自举次数并提升加密推理的精度稳定性。

### 2. 方法论
*   **核心思想**：引入**结构化牛顿层并行（SNLP）**。将 Transformer 的层级依赖视为一个非线性方程组，通过并行评估和线性修正来替代传统的顺序执行。
*   **关键技术细节**：
    *   **计算图重构**：将模型分为前 $S$ 层的顺序前缀和后 $N$ 层的并行后缀。
    *   **迭代求解**：在每次迭代 $k$ 中，并行计算所有后缀层的输出，然后利用结构化牛顿修正公式更新隐藏状态：$h_l^{(k+1)} = \tilde{h}_l^{(k)} + A_l^{(k)}(h_{l-1}^{(k+1)} - h_{l-1}^{(k)})$。
    *   **结构化代理（IDN & HCN）**：为了适配 FHE，修正矩阵 $A_l$ 被设计为极简形式。**IDN（恒等牛顿）**令 $A_l = I$，使修正变为纯加法（FHE 中加法成本极低且不消耗深度）；**HCN** 则针对流式混合架构使用线性混合矩阵。
*   **评价指标（NFE）**：定义“非线性前向评估次数”（NFE）为 $(L-N) + K$。NFE 直接决定了 FHE 中的自举次数。

### 3. 实验设计
*   **数据集**：使用 ClimbMix 预训练数据集，在 2048 序列长度的验证集上评估困惑度（PPL）。
*   **模型与架构**：测试了 8 个模型，涵盖 0.5B 和 3B 参数量，包括标准 Transformer、IDN 正则化模型、以及流式超连接（mHC）架构。
*   **Benchmark**：以标准的顺序推理（Sequential Inference）作为基准。
*   **仿真框架**：开发了一个基于 PyTorch 的仿真环境，用不同阶数（8-14 阶）的切比雪夫多项式替换所有非线性算子，并模拟 CKKS 方案的算术噪声。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **规模参考**：实验涉及 0.5B 至 3B 参数规模的模型，属于中等规模的学术研究实验。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度的消融实验，包括多项式阶数扫描、不同算子（Softmax vs Norm）的误差贡献分析、CKKS 噪声鲁棒性测试以及跨架构对比。
*   **充分性评价**：实验设计较为严谨。通过“误差放大率”（PPL_HE / PPL_exact）这一指标，客观地隔离了结构改进带来的增益。涵盖了从 0.5B 到 3B 的模型，证明了结论在不同规模下的连贯性。

### 6. 主要结论与发现
*   **大幅减深**：在 0.5B 模型上，SNLP 将自举次数从 53 次减少到 20 次（**2.65 倍缩减**），而 PPL 仅上升 1.2%。
*   **误差抑制**：在所有测试配置中，SNLP 的误差放大率均低于顺序推理，说明并行修正结构比顺序堆叠更能容忍多项式近似带来的扰动。
*   **误差源识别**：Softmax 逼近是误差的主要来源，而 FHE 的算术噪声在多项式近似误差面前几乎可以忽略不计。
*   **架构启示**：线性残差混合架构（如 mHC）在加密环境下表现出天然的友好性，其顺序误差放大率最低。

### 7. 优点
*   **系统级优化**：不同于以往研究只关注单个算子的多项式优化，本文从模型拓扑结构入手，提供了互补的优化维度。
*   **FHE 适配性极强**：IDN 修正不增加乘法深度，完美契合 FHE 的计算特性。
*   **精度与效率双赢**：证明了在降低计算开销的同时，还能获得比顺序模型更好的数值稳定性。

### 8. 不足与局限
*   **硬件实测缺失**：实验基于多项式仿真，未在真实的 FHE 硬件加速器或底层库（如 OpenFHE）上测量实际端到端延迟。
*   **训练依赖性**：为了获得最佳效果，通常需要进行“SNLP 感知训练”，这限制了其在现成预训练模型上的直接应用。
*   **算子覆盖**：未考虑 Softmax 中最大值减法所需的比较电路成本，这在真实 FHE 实现中也是一个不小的开销。
*   **模型规模**：虽然测试了 3B 模型，但对于超大规模参数（如 70B+）的有效性仍待验证。

（完）
