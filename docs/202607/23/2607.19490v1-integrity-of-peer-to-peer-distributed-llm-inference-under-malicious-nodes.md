---
title: Integrity of peer-to-peer distributed LLM inference under malicious nodes
title_zh: 恶意节点环境下点对点分布式大语言模型推理的完整性
authors: "Mert Cihangiroglu, Antonino Nocera"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19490v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 恶意节点下分布式LLM推理的完整性
tldr: 本文针对P2P分布式大模型推理中的恶意节点篡改问题，提出了一种基于“金丝雀”输入的完整性检查方法。该方法通过在正常请求中混入秘密输入，并测量节点间传递的激活值偏差，利用概率测试区分恶意篡改与良性硬件噪声。实验表明，该方法在多种配置下均实现了1.0的AUROC，能精准识别恶意节点，为去中心化推理提供了安全保障。
selection_source: fresh_fetch
motivation: 在多方参与的P2P分布式推理中，恶意节点可能篡改输出，且现有方法难以区分恶意攻击与正常的硬件计算误差。
method: 通过混入预先知晓结果的秘密“金丝雀”输入，并根据激活值的漂移分布进行概率性检测，从而识别篡改行为。
result: 在408个实验配置中，该检测器达到了1.0的AUROC，在所有测试中均能完美地将恶意节点与良性节点区分开。
conclusion: 该研究证明了通过激活值偏差分析可以有效且低成本地保障分布式LLM推理的完整性，解决了去中心化环境下的信任难题。
---

## 摘要
点对点（P2P）分布式推理通过将大语言模型（LLM）的层分散到多个节点上，在汇集的消费级硬件上执行。每个请求都会经过由多个独立方拥有和控制的节点。然而，在这种环境下，任何一方都可以篡改其所属层的输出，从而破坏最终结果。在可信硬件上重新计算前向传播可以发现这种行为，但这会引入额外的计算成本。科学文献中包含了几种先前的完整性检查方法，例如用于图像分类器的已知答案陷阱和密码学承诺。然而，这些解决方案仅测试精确的正确性，而没有考虑到良性节点之间可能产生的常规变异。在本文中，我们提出了一种通过测量每个节点传递给下一个节点的激活值变异来检查输出完整性的方法。想要使用该网络的节点会选择一小组秘密的“金丝雀”输入，这些输入的正确激活值是预先已知的，并将其混入常规流量中。由于节点无法区分金丝雀输入和真实查询，任何篡改节点也会破坏它们。与已知参考值的偏差随后会揭示恶意活动：良性节点仅表现出由硬件噪声引起的微小变异，而篡改节点的偏差则大得多。我们将恶意节点的识别视为一种区分两种漂移分布的概率测试，而不依赖于固定阈值。我们研究了 408 种配置，并在实验运行前确定了指标和成功标准；检测器的 AUROC 达到 1.0，在每种配置的每个金丝雀输入上，都能正确地将恶意分片的排名置于每个良性分片之上。

## Abstract
Peer-to-peer distributed inference executes a Large Language Model (LLM) on pooled consumer hardware by spreading its layers across many nodes. Every request passes through nodes that are owned and controlled by multiple independent parties. However, in this setting, any party can tamper with the output of its layers to corrupt the end result. Recomputing the forward pass on trusted hardware can catch this, but it introduces additional computational cost. The scientific literature includes several prior integrity-checking approaches, such as known-answer traps for image classifiers and cryptographic commitments. However, these solutions test only the exact correctness and do not account for the ordinary variation that may arise between benign nodes. In this paper, we propose a method that checks the output integrity by measuring the variation in the activations that each node passes to the next. A peer who wants to use the network selects a small set of secret canary inputs whose correct activations are known in advance and mixes them into regular traffic. Because the peers cannot tell a canary from a real query, any tampering node corrupts them as well. The deviation from the known reference then reveals malicious activity: benign nodes exhibit only minor variation from hardware-induced noise, whereas tampered nodes deviate far more. We treat the identification of malicious nodes as a probabilistic test that separates two drift distributions, without relying on a fixed threshold. We study 408 configurations with metrics and success criteria fixed before any experiment ran; the detector reaches AUROC 1.0, correctly ranking the malicious shard above every benign shard on every canary in every configuration.

---

## 论文详细总结（自动生成）

这篇论文针对点对点（P2P）分布式大语言模型（LLM）推理环境下的安全性问题提出了创新的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在 P2P 分布式推理（如 Petals 项目）中，LLM 的不同层被部署在由不同参与方控制的消费级硬件上。这种去中心化架构面临一个核心安全威胁：**恶意节点可能篡改其负责层的中间激活值（Activations）**，从而破坏最终输出。
*   **挑战**：由于硬件异构性、浮点数舍入误差（fp16 vs fp32）以及非结合律计算，即使是良性节点也会产生微小的数值漂移。
*   **痛点**：传统的完整性检查（如冗余重计算）成本极高；而简单的精确匹配无法区分“良性硬件噪声”与“恶意篡改”。

### 2. 论文提出的方法论：金丝雀陷阱（Canary Trap）
论文提出了一种基于统计测试的轻量级检测机制，核心思想是利用“金丝雀”输入作为探测器。
*   **核心思想**：验证者预先在可信环境下计算一小组秘密输入（金丝雀）的精确 fp32 激活值作为参考。在实际推理时，将这些金丝雀随机混入正常查询流中。由于恶意节点无法区分金丝雀和真实查询，其篡改行为会同时作用于两者。
*   **关键技术细节**：
    *   **相对 L2 错配度（Relative-L2 Mismatch）**：计算实时激活值 $h_i$ 与参考值 $a^*_i$ 之间的偏差：$m_i = \|h_i - a^*_i\|_F / \|a^*_i\|_F$。
    *   **概率排名测试**：不设定固定阈值，而是将检测视为一个排名问题。通过计算 **AUROC**（受试者工作特征曲线下面积）来衡量恶意节点偏差是否始终高于良性节点的噪声水平。
    *   **硬件噪声建模**：论文建立了一个高斯噪声模型来模拟良性硬件产生的数值漂移，确保检测器在噪声环境下依然稳健。

### 3. 实验设计
*   **模型架构**：使用了 GPT-2 (124M)、Pythia-160M 和 Pythia-410M。
*   **数据集**：LAMBADA（测试长程上下文预测）和 WikiText-2（常规文本）。
*   **攻击策略（6种）**：包括四种强度的**高斯扰动**（$\epsilon$ 从 0.02 到 0.20）、**符号翻转**（Sign flip）和**缩放攻击**（Rescale）。
*   **基准对比（Benchmark）**：
    *   **随机基准**：模拟随机猜测，AUROC 期望值为 0.5。
    *   **标签混淆控制**：随机分配节点标签，验证信号是否真实来自金丝雀机制。
    *   **冗余重计算上限**：作为性能天花板，代表支付双倍计算成本后的检测效果。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练/模拟时长。由于该研究侧重于推理阶段的模拟分析而非大规模模型训练，其计算开销主要集中在 408 种配置的推理模拟上。

### 5. 实验数量与充分性
*   **实验规模**：总共进行了 **408 组配置**的实验。
    *   **因子实验（216组）**：交叉组合了模型、数据集、攻击类型和检测器设置。
    *   **专项子研究（192组）**：深入探讨了信噪比、模型深度（最高24层）、攻击者位置、节点共谋（最多4个恶意节点）以及逃逸边界。
*   **客观性**：论文采用了**预注册（Pre-registered）协议**，在实验运行前就固定了成功标准（AUROC > 0.9）和评估指标，有效避免了事后挑选数据的偏差，实验设计非常充分且客观。

### 6. 主要结论与发现
*   **检测精度极高**：在所有 72 种核心因子配置中，检测器的 **AUROC 均达到了 1.000**，意味着它能完美地将恶意节点排在所有良性节点之上。
*   **性能对等**：该方法的检测效果达到了“冗余重计算”的水平，但计算成本极低（仅需读取一次激活值，无需重新计算）。
*   **鲁棒性强**：检测效果不受模型深度、攻击者位置或恶意节点数量的影响。
*   **逃逸与伤害的权衡**：实验证明，如果攻击者想要通过减小篡改强度 $\epsilon$ 来逃避检测，那么其对模型输出准确率的破坏力也会降至接近零。

### 7. 优点
*   **低成本高效率**：无需昂贵的加密证明或双倍计算，适合资源受限的 P2P 环境。
*   **阈值无关**：通过排名机制规避了因硬件差异导致难以设定统一阈值的难题。
*   **预注册机制**：科学严谨性高，实验结果具有很强的说服力。

### 8. 不足与局限
*   **噪声模型简化**：实验中使用的各向同性高斯噪声可能无法完全模拟真实 GPU 硬件中复杂的非线性舍入误差。
*   **模型规模限制**：实验主要在 124M 到 410M 参数的模型上进行，尚未在千亿级（如 BLOOM-176B）模型上验证其激活值统计特性的变化。
*   **自适应攻击风险**：虽然论文讨论了金丝雀不可区分性，但如果攻击者能够通过高级手段识别出金丝雀输入，该防御机制可能会失效。
*   **存储开销**：验证者需要存储所有金丝雀在每一层的参考激活值，对于超大规模模型，这会带来一定的存储压力。

（完）
