---
title: "ATLAS: Automated Approximation of Transformers for Efficient Homomorphic Inference in One Hour"
title_zh: ATLAS：一小时内实现高效同态推理的 Transformer 自动化近似框架
authors: "Jianhang Xie, Sicheng Tan, Vishnu Naresh Boddeti, Zhichao Lu"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23478v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 使用同态加密进行Transformer模型的隐私推理
tldr: 针对全同态加密（FHE）下Transformer推理成本极高的问题，本文提出ATLAS框架。它通过多目标优化自动配置各层的多项式近似参数，打破了以往手动统一配置的局限。ATLAS采用两阶段优化策略和代理模型，在极大的搜索空间中高效寻找延迟与精度的平衡点，显著提升了同态推理的效率并降低了部署门槛。
selection_source: fresh_fetch
motivation: 现有的同态加密推理依赖手动且统一的层配置，无法利用不同层对近似误差的差异化容忍度来优化延迟。
method: 将层级近似配置建模为延迟与精度的多目标优化问题，并结合两阶段优化策略与代理模型来加速搜索过程。
result: ATLAS能在极短时间内从海量空间中找到优化配置，显著降低了BERT、ViT及LLaMA3等模型的同态推理开销。
conclusion: 自动化且差异化的层级近似配置是实现高效同态加密大模型推理的关键路径。
---

## 摘要
全同态加密（FHE）为隐私推理提供了强大的密码学保障，但在 FHE 环境下部署 Transformer 模型的开销依然极其昂贵。一个核心瓶颈在于，Softmax、归一化和激活函数等非线性操作必须替换为与 CKKS 方案兼容的多项式近似，而这些近似操作所消耗的乘法深度主导了推理成本。尽管近期的框架提升了近似技术，但它们均依赖于手动配置的近似超参数（如迭代次数、多项式阶数），并将其统一应用于所有层。这种统一配置的方法虽然简便，但过于僵化：不同层对近似误差的容忍度不同，且不会降低预测准确率，而统一配置无法利用这种差异性来降低延迟。然而，允许每层采用独立配置会导致搜索空间随模型深度爆炸式增长，BERT/ViT（12 层）约为 10^84 种配置，LLaMA3（32 层）则高达 10^225 种，使手动探索变得几乎不可能。我们提出了 ATLAS，一个通过将问题建模为延迟与预测准确率的多目标优化来自动配置逐层近似设置的框架。该优化问题具有内在难度：1) 在庞大的决策空间（BERT/ViT 为 120 个变量，LLaMA3 为 320 个变量）中平衡竞争目标；2) 评估代价高昂，即使在明文下每个配置也需 70-1,000 秒；3) 优化信号稀疏，35-50% 的候选配置会产生数值无效解。ATLAS 通过一种逐步放宽逐层约束的两阶段优化策略，并结合代理模型加速评估，成功应对了这些挑战。

## Abstract
Fully homomorphic encryption (FHE) provides strong cryptographic guarantees for private inference, but deploying transformer models under FHE remains prohibitively expensive. A key bottleneck is that non-linear operations such as softmax, normalization, and activation must be replaced with polynomial approximations compatible with the CKKS scheme, and the multiplicative depth consumed by these approximations dominates inference cost. Recent frameworks have advanced approximation techniques, yet all rely on manually configured approximation hyperparameters (e.g., number of iterations, polynomial degree), applied uniformly across all layers. While convenient, this uniform-configuration approach is overly rigid: different layers can tolerate different levels of approximation error without degrading predictive accuracy, and uniform configurations cannot exploit this variability to reduce latency. Allowing each layer to adopt its own configuration, however, causes the search space to explode with model depth, reaching roughly $10^{84}$ configurations for BERT/ViT (12 layers) and $10^{225}$ for LLaMA3 (32 layers), rendering manual exploration practically impossible. We present ATLAS, an automated framework that configures per-layer approximation settings by formulating the problem as a multi-objective optimization over latency and predictive accuracy. The resulting problem is inherently difficult: 1) competing objectives over a large decision space (120 or 320 variables for BERT/ViT or LLaMA3); 2) expensive evaluation, as each configuration takes 70-1,000 seconds even in cleartext; and 3) sparse optimization signals, as 35-50% of candidate configurations yield numerically invalid solutions. ATLAS addresses these challenges through a two-stage optimization strategy that progressively relaxes layer-wise constraints, combined with surrogate models to accelerate evaluation.

---

## 论文详细总结（自动生成）

以下是对论文《ATLAS: Automated Approximation of Transformers for Efficient Homomorphic Inference in One Hour》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在全同态加密（FHE）环境下，Transformer 模型的非线性操作（如 Softmax、LayerNorm、GELU）必须通过多项式近似实现。现有的近似方案通常由专家手动设计，并**统一应用**于模型的所有层。
*   **研究动机**：
    *   **层间差异性**：模型不同层对近似误差的容忍度不同，统一配置会导致严重的计算资源浪费。
    *   **搜索空间爆炸**：若允许每层独立配置，搜索空间将达到 $10^{84}$（BERT）甚至 $10^{225}$（LLaMA3），手动优化完全不可行。
    *   **评估成本高**：FHE 推理极慢，即使在明文下评估一个配置的精度也需耗时数分钟，导致传统的自动化搜索难以实施。

### 2. 方法论
*   **核心思想**：将层级近似配置建模为一个**多目标组合优化问题**，目标是同时最小化乘法深度（影响延迟）和最小化平均绝对误差（MAE，影响精度）。
*   **关键技术细节**：
    *   **搜索空间编码**：将 Softmax 的迭代次数与阶数、归一化的 Newton-Goldschmidt 迭代次数、激活函数的多项式阶数编码为整数向量。
    *   **两阶段进化搜索（NSGA-II）**：
        *   **第一阶段（LayerProblem）**：强制所有层共享相同配置，快速缩小搜索范围，寻找高质量的全局基准。
        *   **第二阶段（NetworkProblem）**：以第一阶段结果为种子，放宽约束，允许各层独立微调，探索异构的最优配置。
    *   **加速策略**：
        *   **数据子集代理**：仅使用 10 个随机样本评估 MAE，通过实验证明其与全量数据的排名相关性极高（Kendall $\tau > 0.85$）。
        *   **前层代理（Early-Layer Proxy）**：在第一阶段，仅计算前几层的输出误差作为整体误差的近似，减少前向传播计算量。
        *   **分布式评估**：利用进化算法的并行性，在多 GPU 上同步评估种群。

### 3. 实验设计
*   **实验场景**：涵盖了三种主流架构：Encoder-only (BERT-base)、Decoder-only (LLaMA3-8B) 和 Vision Transformer (ViT-base)。
*   **数据集**：
    *   **搜索阶段**：IMDB（文本）、ImageNet-1K（图像）的极小种子集。
    *   **评估阶段**：GLUE 任务（SST-2, RTE, QNLI）、ImageNet-1K 验证集、MMLU、GSM8K、HumanEval。
*   **Benchmark 与对比方法**：
    *   **Iterative Softmax (Cho et al., CCS 2024)**：目前最先进的手动近似方案。
    *   **THOR (Moon et al., CCS 2025)**：针对 Transformer 的 FHE 推理框架。
    *   **NEXUS (Zhang et al., NDSS 2025)**：非交互式 FHE 推理框架。

### 4. 资源与算力
*   **硬件环境**：服务器配备 NVIDIA RTX 4090 (48GB GPU)、AMD EPYC 7343 (16核 CPU) 及 256GB RAM。
*   **搜索时长**：
    *   **BERT/ViT**：搜索过程在 **30 分钟内**完成。
    *   **LLaMA3-8B**：搜索过程在 **1 小时内**完成。
*   **FHE 后端**：基于 GPU 加速的 Phantom-FHE 库实现。

### 5. 实验数量与充分性
*   **实验规模**：论文针对三种不同类型的 Transformer 架构进行了详尽测试，每种架构都生成了完整的 Pareto 前沿曲线（精度 vs. 延迟）。
*   **消融实验**：对比了单阶段 vs. 两阶段搜索、顺序优化 vs. 联合优化，以及各项加速技术对搜索效率的提升。
*   **公平性**：作者在相同的 FHE 后端和硬件环境下重新实现了对比方法（如 THOR 和 NEXUS 的近似逻辑），确保了对比的客观性。实验结果显示 ATLAS 在相同精度下具有更低的深度和延迟。

### 6. 主要结论与发现
*   **效率提升**：相比于专家设计的基准（Iterative Softmax），ATLAS 将乘法深度降低了约 **35%**，端到端推理延迟降低了约 **25%**。
*   **精度保持**：在大幅降低计算开销的同时，模型在下游任务（如 GLUE、MMLU）上的精度损失几乎可以忽略不计。
*   **层级冗余**：实验发现，手动配置通常预留了过高的“安全边际”，ATLAS 发现许多中间层可以使用极低阶的多项式而不影响最终结果。
*   **通用性**：ATLAS 是框架无关的，可以作为后处理步骤直接应用于现有的 FHE 推理系统。

### 7. 优点
*   **自动化程度高**：解决了 FHE 部署中最耗时的手动调参问题，实现了“一小时内”自动配置。
*   **搜索策略巧妙**：通过两阶段搜索和代理模型，成功将原本不可行的超大规模搜索问题转化为实用工具。
*   **跨领域验证**：不仅限于 NLP，还首次在 FHE 环境下对 ViT 和 8B 规模的大模型进行了深度优化。

### 8. 不足与局限
*   **代理数据偏差风险**：搜索依赖于极小规模的样本集（10个样本），虽然实验证明相关性强，但在极端分布不均的数据集上可能失效。
*   **FHE 方案局限**：目前主要针对基于 CKKS 方案的乘法深度优化，未讨论其在 TFHE 等其他加密方案下的表现。
*   **绝对延迟挑战**：尽管 ATLAS 提升了 25% 的效率，但 FHE 推理的绝对耗时（如 BERT 仍需数百秒）距离实时应用仍有距离。

（完）
