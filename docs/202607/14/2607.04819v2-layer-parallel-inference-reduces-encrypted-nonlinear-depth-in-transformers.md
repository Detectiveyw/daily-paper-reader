---
title: Layer-Parallel Inference Reduces Encrypted Nonlinear Depth in Transformers
title_zh: 层并行推理降低 Transformer 中的加密非线性深度
authors: "Ligong Han, Kai Xu, Hao Wang, Ruijiang Gao, Han Gao, Akash Srivastava"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04819v2"
tags: ["query:fss-llm"]
score: 8.0
evidence: 加密Transformer推理与非线性深度优化
tldr: 针对全同态加密（FHE）在 Transformer 推理中因非线性层顺序堆叠导致的计算瓶颈，本文研究了结构化牛顿层并行（SNLP）的应用。SNLP 将 L 层顺序非线性深度转化为少数求解器迭代和线性修正，显著减少了加密推理所需的自举（bootstrap）次数。实验表明，该方法在 0.5B 模型上将自举次数降低了 2.65 倍，且误差累积更低，为高效加密大模型推理提供了新路径。
selection_source: fresh_fetch
motivation: 全同态加密下的 Transformer 推理受限于大量非线性层顺序组合带来的高昂计算成本和深度。
method: 采用结构化牛顿层并行（SNLP）技术，通过并行化处理层间组合，将顺序非线性深度转化为迭代求解过程。
result: 在 0.5B 模型上将自举次数从 53 次降至 20 次，且误差放大率低于传统顺序推理。
conclusion: SNLP 能有效降低加密推理的非线性深度，且与现有的块级 FHE 友好算子设计具有互补性。
---

## 摘要
全同态加密 (FHE) 允许在加密数据上进行计算，但实际的加密 Transformer 推理受到多个非线性块顺序组合的瓶颈限制。我们研究了结构化牛顿层并行 (SNLP) 是否能使这种层间组合对 FHE 更加友好：尽管每个 Transformer 块仍需对 softmax 和 RMSNorm 等操作进行多项式逼近，但 SNLP 将逐层顺序非线性深度从 L 个阶段减少为少量的求解器迭代加上线性结构化修正。利用基于切比雪夫多项式逼近的模拟框架，我们测量了 8 个模型和 4 个架构系列在顺序推理与 SNLP 推理下的误差累积。在一个 0.5B 参数的 IDN 训练模型上，SNLP 将符号自举 (symbolic bootstraps) 从 53 次减少到 20 次 (2.65 倍)，困惑度仅增加 1.2%，同时降低了误差放大率 (1.36 倍 vs. 1.42 倍)。在所有测试模型中，SNLP 的误差放大率均低于顺序推理。消融实验表明，softmax 逼近在误差预算中占主导地位，而 CKKS 算术噪声在我们的设置中可以忽略不计，这表明 SNLP 是对块级 FHE 友好算子设计的补充，而非替代。

## Abstract
Fully homomorphic encryption (FHE) enables computation on encrypted data, but practical encrypted Transformer inference is bottlenecked by the sequential composition of many nonlinear blocks. We study whether Structured Newton Layer Parallelism (SNLP) can make this inter-layer composition more FHE-friendly: each Transformer block still requires polynomial approximations for operations such as softmax and RMSNorm, but SNLP reduces the layerwise sequential nonlinear depth from L stages to a small number of solver iterations plus linear structured corrections. Using a simulation framework based on Chebyshev polynomial approximations, we measure error accumulation under sequential versus SNLP inference across 8 models and 4 architecture families. On a 0.5B IDN-trained model, SNLP reduces symbolic bootstraps from 53 to 20 (2.65x) with only +1.2% perplexity degradation, while lowering error amplification (1.36x vs. 1.42x). Across all tested models, SNLP has lower amplification than sequential inference. Ablations show that softmax approximation dominates the error budget and CKKS arithmetic noise is negligible in our setting, suggesting that SNLP is complementary to block-level FHE-friendly operator design rather than a replacement for it.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何通过改变 Transformer 的层间计算结构，来优化全同态加密（FHE）环境下的推理效率。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心瓶颈**：在全同态加密（FHE）中，Transformer 的推理速度极慢。主要原因在于 Transformer 包含大量非线性操作（如 Softmax、RMSNorm、激活函数），这些操作在 FHE 中必须通过高阶多项式逼近。
*   **深度累积效应**：Transformer 的 $L$ 个层是顺序执行的，这意味着非线性逼近产生的误差会随深度逐层累积，且每一层都会消耗加密模数预算，导致必须频繁进行极其耗时的“自举”（Bootstrapping）操作（占总延迟的 50-86%）。
*   **研究目标**：探索**结构化牛顿层并行（SNLP）**能否通过减少“顺序非线性深度”来降低 FHE 推理成本，同时控制误差放大。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将 Transformer 的层间计算视为求解一个非线性残差方程组。SNLP 不再逐层顺序计算，而是先顺序计算前 $S$ 层（前缀），然后对剩余的 $N$ 层（后缀）进行并行评估，并通过迭代修正来逼近顺序执行的结果。
*   **关键技术细节**：
    *   **计算图重构**：将 $L$ 层的顺序非线性深度转化为 $(L-N) + K$ 个阶段，其中 $K$ 是求解器的迭代次数。
    *   **结构化修正**：
        *   **IDN (Identity Newton)**：使用恒等矩阵作为雅可比矩阵的替代，修正过程仅涉及加法，在 FHE 中具有零乘法深度。
        *   **HCN (HC Newton)**：针对具有线性混合残差连接的架构（如 mHC），使用小的线性混合矩阵进行修正。
    *   **NFE 指标**：定义“非线性前向评估次数”（NFE）作为 FHE 成本的代理指标，实验证明 NFE 与 FHE 自举次数高度线性相关（比率接近 1:1）。
*   **FHE 模拟框架**：使用切比雪夫多项式（Chebyshev Polynomials）在校准区间内拟合非线性函数，并在 PyTorch 前向传播中模拟这些逼近误差。

### 3. 实验设计
*   **模型与架构**：评估了 8 个模型，涵盖 4 种架构家族（标准 Transformer、IDN 正则化模型、无 $x_0$ 连接模型、mHC 架构），参数量从 0.5B 到 3B。
*   **数据集**：在 ClimbMix 数据集上从头训练，并在 2048 序列长度的验证集上评估困惑度（PPL）。
*   **Benchmark**：以标准的**顺序加密推理（Sequential Inference）**作为基准。
*   **对比维度**：不同多项式阶数（8 到 14 阶）、不同 SNLP 配置（$N$ 与 $K$ 的组合）、不同架构的误差放大率（PPL HE / PPL Exact）。

### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 型号、数量或总训练时长。
*   **规模参考**：实验涉及 0.5B 和 3B 规模的模型训练与推理模拟，属于中等规模的学术研究实验。

### 5. 实验数量与充分性
*   **实验规模**：进行了多项式阶数扫描、CKKS 算术噪声鲁棒性测试、跨模型对比、Pareto 前沿分析以及逐层误差累积分析。
*   **充分性评价**：实验设计较为全面。通过消融实验确定了 Softmax 是主要的误差来源，并验证了 SNLP 在不同架构下的普适性。使用了 200k 至 1M token 的评估量，确保了统计结果的稳定性。

### 6. 主要结论与发现
*   **显著降本**：在 0.5B 模型上，SNLP 将自举次数从 53 次降至 20 次（**2.65 倍减少**），而加密 PPL 的损失仅为 1.2%。
*   **误差放大率更低**：在所有测试模型中，SNLP 的误差放大率均低于顺序推理，表明其在减少深度的同时并没有加剧多项式逼近的负面影响。
*   **架构友好性**：**mHC 架构**被证明对 FHE 最友好，因为其层间连接是纯线性的，不增加非线性深度。
*   **规模效应**：较大的模型（3B）比较小的模型（0.5B）对多项式逼近误差更具鲁棒性。
*   **噪声无关性**：在当前精度下，多项式逼近误差远大于 CKKS 算术噪声，后者几乎可以忽略。

### 7. 优点
*   **视角独特**：不同于以往专注于改进单个算子（如优化单个 Softmax 的多项式）的研究，本文从**层间计算图**的角度切入，提供了互补的优化方向。
*   **FHE 适配性强**：提出的 IDN 修正方法在 FHE 中几乎是“免费”的（仅涉及加法），极大地利用了 FHE 的特性。
*   **理论与实践结合**：通过 NFE 指标建立了算法复杂度与 FHE 实际开销之间的清晰联系。

### 8. 不足与局限
*   **模拟局限性**：实验是在模拟环境下进行的，而非真实的加密环境。虽然模拟了多项式误差，但未完全考虑 FHE 中的比较电路（用于 Softmax 稳定性）等额外成本。
*   **模型规模限制**：实验最高仅达到 3B 参数，尚未在超大规模模型（如 70B+）上验证 SNLP 的有效性。
*   **应用限制**：SNLP 需要特定的训练策略（SNLP-aware training）才能在低 NFE 下保持高性能，这意味着现有预训练模型可能需要微调或重新训练才能发挥最大效用。

（完）
