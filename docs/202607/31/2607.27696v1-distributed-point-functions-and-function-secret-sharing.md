---
title: Distributed Point Functions and Function Secret Sharing
title_zh: 分布式点函数与函数秘密共享
authors: "Elette Boyle, Niv Gilboa, Yuval Ishai, Peter Scholl"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.27696v1"
tags: ["query:fss-llm"]
score: 10.0
evidence: 关于 DPF 和 FSS 定义、构造及应用的全面综述
tldr: 本文综述了分布式点函数（DPF）及其扩展形式函数秘密共享（FSS）的定义、构造与应用。DPF 是一种轻量级密码原语，支持对权重为 1 的秘密向量进行压缩加法共享，具有极高的计算效率。该技术已广泛应用于隐私信息检索、匿名消息传递及安全多方计算等领域，通过 FSS 进一步扩展了支持的函数类别，为复杂密码学关联的生成提供了高效方案。
selection_source: fresh_fetch
motivation: 旨在解决如何在多方之间高效、压缩地共享特定函数（如点函数）并应用于隐私保护和安全计算任务。
method: 通过系统性地综述 DPF 的轻量级构造方法及其向支持更丰富函数类的 FSS 的扩展技术。
result: 展示了 DPF 在隐私信息检索、匿名通信和伪随机关联生成器等多个前沿领域的成功应用成果。
conclusion: DPF 和 FSS 作为高效的密码学工具，为实现大规模、低开销的安全计算和隐私保护提供了关键的技术支撑。
---

## 摘要
分布式点函数（DPF）是一种密码学原语，它能够在两个或多个参与方之间实现秘密权重为1的向量（等价于点函数）的压缩加法共享。DPF 构造极具吸引力的轻量级结构使其在广泛的领域得到了应用。这些应用包括隐私信息检索、匿名消息传递、带预处理的安全计算，以及用于将小型相关种子扩展为大规模密码学相关伪随机实例的伪随机相关生成器。在本文中，我们综述了 DPF 的定义、构造和应用。我们还讨论了 DPF 向函数秘密共享（FSS）的扩展，后者将点函数推广到支持更丰富的函数类。高效的 FSS 方案为大多数 DPF 应用提供了类似的推广。

## Abstract
A distributed point function (DPF) is a cryptographic primitive that enables compressed additive sharing of a secret weight-1 vector (equivalently, a point function) across two or more parties. The appealing lightweight structure of DPF constructions has enabled a wide range of applications. These include private information retrieval, anonymous messaging, secure computation with preprocessing, and pseudorandom correlation generators for expanding small correlated seeds into large pseudorandom instances of cryptographic correlations.   In this article, we survey definitions, constructions, and applications of DPFs. We also discuss the extension of DPF to function secret sharing (FSS), which generalizes point functions to support richer function classes. Efficient FSS schemes yield a similar generalization for most of the applications of DPFs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Distributed Point Functions and Function Secret Sharing》（分布式点函数与函数秘密共享）的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的秘密共享方案在处理长向量（长度为 $N$）时，每个份额的大小通常也与 $N$ 成正比，这在处理大规模数据（如隐私检索或匿名通信）时会导致巨大的通信开销。
*   **核心问题**：如何在保证计算安全性的前提下，对特定类别的函数（如仅在一点有值的点函数）进行“压缩”共享，使得生成的密钥（份额）大小远小于向量长度 $N$？
*   **整体含义**：本文是一篇综述，系统性地总结了分布式点函数（DPF）和函数秘密共享（FSS）的定义、构造方法、性能边界以及在隐私保护和安全多方计算（MPC）中的广泛应用。

### 2. 论文提出的方法论
*   **核心思想**：利用伪随机生成器（PRG）的计算安全性，将函数 $f$ 拆分为两个压缩的函数份额 $f_0$ 和 $f_1$，满足对于任意输入 $x$，$f_0(x) + f_1(x) = f(x)$，且单个份额不泄露关于 $f$ 的任何信息。
*   **关键技术细节**：
    *   **树状 DPF 构造（BGI16）**：这是目前最先进的方案。它基于 GGM 树结构，每一层通过 PRG 将种子扩展，并引入“修正词”（Correction Word, CW）来控制路径。在秘密点 $\alpha$ 的路径上，两个参与方的种子保持独立且伪随机；在非 $\alpha$ 路径上，通过 CW 使得两方的种子完全一致，从而在加法重构时相互抵消（输出为 0）。
    *   **同态特性**：利用加法秘密共享的线性同态性，支持对共享结果进行本地聚合（如求和），这是实现隐私信息检索（PIR）的关键。
    *   **扩展构造**：
        *   **DCF（分布式比较函数）**：支持 $x < \alpha$ 的判断。
        *   **DMPF（多点函数）**：支持权重为 $t$ 的稀疏向量共享，利用概率批处理码（PBC）或遗忘键值存储（OKVS）进行优化。
        *   **多方 DPF**：探讨了在 $m > 2$ 参与方下的构造，指出在全门限安全下存在“平方根”大小的性能瓶颈。

### 3. 实验设计
*   **场景与 Benchmark**：作为综述论文，文中并未进行单一的新实验，而是对比了过去十年间多种方案的理论复杂度和已知实验表现。
*   **对比方法**：
    *   **DMPF 性能对比**：对比了 Naive（简单叠加）、Big-state、OKVS-based 和 PBC-based 四种构造在不同权重 $t$ 下的 FullEval（全域评估）时间。
    *   **PIR 应用**：讨论了 DPF 在关键字搜索 PIR 中的通信效率，对比了传统线性扫描方案。
    *   **匿名消息系统**：如 Riposte 系统，展示了 DPF 如何处理数百万用户的匿名投稿。

### 4. 资源与算力
*   **算力说明**：文中未提及具体的 GPU 或服务器集群型号，因为 DPF 的核心优势在于其**轻量级**。
*   **计算开销**：主要计算量在于 PRG 调用（通常使用 AES-NI 指令集加速）。例如，树状 DPF 的评估开销仅为 $n$ 次 PRG 调用（$n$ 为输入比特长度），在现代 CPU 上仅需微秒级时间。

### 5. 实验数量与充分性
*   **充分性**：论文引用了大量实证研究（如 Poplar、Doplar、Prio 等系统），涵盖了从理论复杂度分析到实际系统部署的各个方面。
*   **客观性**：论文客观地指出了不同 DMPF 构造的适用范围（例如：$t \le 70$ 时 Big-state 最优，$t > 10^4$ 时 PBC 最优），提供了公平的性能权衡指导。

### 6. 论文的主要结论与发现
*   **压缩极限**：对于 $n$ 比特输入、$\lambda$ 安全参数的 DPF，目前最优密钥大小约为 $n \cdot \lambda$ 比特。
*   **假设依赖**：DPF 的高效构造仅依赖于对称加密原语（PRG/OWF），这使其比基于公钥加密的方案快几个数量级。
*   **应用潜力**：DPF 已成为伪随机相关生成器（PCG）的核心组件，能够以极低的通信成本生成大量的 OT（离线不经意传输）或 Beaver 三元组，极大地优化了 MPC 的预处理阶段。

### 7. 优点
*   **结构清晰**：从基础定义到高级扩展，再到实际应用，逻辑严密。
*   **实用性强**：重点介绍了能够直接投入工业应用的轻量级构造，而非仅停留在理论层面。
*   **前瞻性**：总结了大量开放性问题，为后续研究指明了方向。

### 8. 不足与局限
*   **多方瓶颈**：在超过两个参与方且存在高比例共谋的情况下，DPF 的密钥压缩效率显著下降（从对数级退化为平方根级），目前尚无基于对称加密的突破性进展。
*   **复杂函数支持**：对于通用电路的 FSS，目前仍需依赖昂贵的公钥原语（如 LWE 假设），在计算开销上尚无法与 DPF 相比。
*   **恶意安全性**：虽然提到了可验证 DPF，但在处理恶意客户端时的验证开销（通常与域大小 $N$ 成线性关系）仍然是实际部署中的挑战。

（完）
