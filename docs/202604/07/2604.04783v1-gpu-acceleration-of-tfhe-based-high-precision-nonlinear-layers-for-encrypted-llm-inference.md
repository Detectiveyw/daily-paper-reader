---
title: GPU Acceleration of TFHE-Based High-Precision Nonlinear Layers for Encrypted LLM Inference
title_zh: 面向加密 LLM 推理的基于 TFHE 的高精度非线性层 GPU 加速
authors: "Guoci Chen, Xiurui Pan, Qiao Li, Bo Mao, Congming Gao, Chengying Huan, Mingzhe Zhang, Jie Zhang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04783v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 使用TFHE进行非线性层加密LLM推理
tldr: 针对云端大语言模型推理中的隐私泄露问题，本文提出TIGER框架，这是首个利用GPU加速TFHE实现高精度非线性层的方案。针对现有FHE方法在精度与效率上的权衡难题，TIGER通过优化WoP-PBS方法并结合数值算法，突破了原生查找表的精度限制，并利用批处理设计提升GPU并行效率。该框架为加密LLM推理提供了高效的GELU、Softmax和LayerNorm实现，显著提升了密文计算性能。
selection_source: fresh_fetch
motivation: 解决现有全同态加密方法在处理大语言模型非线性层时面临的精度受限、计算开销大以及GPU资源利用率不足的问题。
method: 提出TIGER框架，通过GPU优化的WoP-PBS技术结合数值算法提升精度，并采用批处理驱动设计以增强并行计算效率。
result: 实验表明，TIGER在GELU、Softmax和LayerNorm层上的处理速度较CPU基准分别提升了7.17倍、16.68倍和17.05倍。
conclusion: TIGER证明了通过GPU加速的TFHE方案可以高效实现高精度非线性层，为隐私保护下的LLM推理提供了实用化路径。
---

## 摘要
将大语言模型（LLM）部署为云服务引发了隐私担忧，因为推理过程可能会泄露敏感数据。全同态加密（FHE）允许在加密数据上进行计算，但目前的 FHE 方法在高效且精确的非线性函数评估方面面临挑战。具体而言，基于 CKKS 的方法需要高阶多项式逼近，当目标精度提高时，其开销会变得非常昂贵。相比之下，TFHE 的可编程自举（PBS）通过提供精确的查找表评估，性能优于 CKKS。但它缺乏 LLM 非线性层的高精度实现，且对 GPU 资源的利用不足。我们提出了 TIGER，这是首个用于基于 TFHE 的高精度非线性 LLM 层评估的 GPU 加速框架。TIGER 提供：(1) 结合数值算法的 GPU 优化 WoP-PBS 方法，以突破非线性函数上原生查找表的精度限制；(2) 关键非线性层的高精度且高效的实现，使实用的加密推理成为可能；(3) 利用输入间并行性的批处理驱动设计，以提升 GPU 效率。与 CPU 基准相比，TIGER 在 GELU、Softmax 和 LayerNorm 上分别实现了 7.17 倍、16.68 倍和 17.05 倍的加速。

## Abstract
Deploying large language models (LLMs) as cloud services raises privacy concerns as inference may leak sensitive data. Fully Homomorphic Encryption (FHE) allows computation on encrypted data, but current FHE methods struggle with efficient and precise nonlinear function evaluation. Specifically, CKKS-based approaches require high-degree polynomial approximations, which are costly when target precision increases. Alternatively, TFHE's Programmable Bootstrapping (PBS) outperforms CKKS by offering exact lookup-table evaluation. But it lacks high-precision implementations of LLM nonlinear layers and underutilizes GPU resources.   We propose \emph{TIGER}, the first GPU-accelerated framework for high-precision TFHE-based nonlinear LLM layer evaluation. TIGER offers: (1) GPU-optimized WoP-PBS method combined with numerical algorithms to surpass native lookup-table precision limits on nonlinear functions; (2) high-precision and efficient implementations of key nonlinear layers, enabling practical encrypted inference; (3) batch-driven design exploiting inter-input parallelism to boost GPU efficiency. TIGER achieves 7.17$\times$, 16.68$\times$, and 17.05$\times$ speedups over a CPU baseline for GELU, Softmax, and LayerNorm, respectively.

---

## 论文详细总结（自动生成）

这是一份关于论文《GPU Acceleration of TFHE-Based High-Precision Nonlinear Layers for Encrypted LLM Inference》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证隐私的前提下，高效且高精度地执行大语言模型（LLM）中的非线性层（如 Softmax, LayerNorm, GELU）的密文推理。
*   **研究动机**：
    *   **隐私需求**：云端 LLM 推理存在敏感数据泄露风险，全同态加密（FHE）是理想方案。
    *   **现有方案缺陷**：基于 CKKS 的方案在处理非线性函数时依赖高阶多项式逼近，精度越高开销越大；基于 TFHE 的方案虽然支持查找表（LUT），但原生精度受限（通常 < 20位），且缺乏针对 GPU 并行性的优化，导致推理速度极慢。

### 2. 方法论
TIGER 框架通过“密码学原语+数值算法+硬件优化”的协同设计解决上述问题：
*   **核心思想**：利用 **WoP-PBS**（无填充可编程自举）作为基础，结合**数值精细化（Numerical Refinement）**技术突破查找表的物理精度限制。
*   **关键技术细节**：
    *   **查找表与数值协同设计**：将输入拆分为高位和低位。高位通过 WoP-PBS 进行 LUT 查找获得初始值，低位利用**一阶泰勒展开**进行修正，从而以较低的计算代价获得极高的数值精度。
    *   **复合非线性层构建**：将 Softmax 和 LayerNorm 分解为一系列定点运算（加、减、乘、倒数平方根等）和高精度函数评估。
    *   **定点运算优化**：设计了“乘法调度器”，通过离线调度和在线执行分离，剪枝无效的部分乘积，并优化了定点除法的实现。
    *   **GPU 底层加速**：
        *   **FFT 优化**：采用基-4（Radix-4）分解和 Karatsuba 复数乘法减少同步和计算开销。
        *   **批处理拆分策略**：针对 GPU 缓存压力，将大批量 PBS 任务拆分为最优吞吐量区间（192-320）的子批次，避免资源竞争。

### 3. 实验设计
*   **模型与数据集**：以 **GPT-2** 为代表模型，使用 **WikiText-2** 数据集评估不同精度对模型困惑度（Perplexity）的影响。
*   **Benchmark（基准）**：
    *   **CPU-WoP**：基于 CPU 的 WoP-PBS 实现（使用 16 线程 OpenMP 加速）。
    *   **GPU-FBT**：将 TIGER 中的 WoP-PBS 替换为传统的函数自举（FBT-TFHE）算法。
*   **对比维度**：层级延迟（GELU, Softmax, LayerNorm）、端到端 Transformer 块延迟、消融实验、可扩展性分析。

### 4. 资源与算力
*   **硬件环境**：
    *   **GPU**：NVIDIA RTX 6000 Ada Generation (48GB 显存)。
    *   **CPU**：Intel Xeon Gold 5320 (2.20GHz)。
    *   **内存**：4TB DRAM。
*   **软件实现**：使用 CUDA C++ 编写，核心代码超过 9500 行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了针对 GPT-2 困惑度的精度敏感性分析。
    *   对三个核心非线性层进行了详尽的延迟测试。
    *   **消融实验**：测试了 12 种不同的优化组合（FFT 变体、Karatsuba、批处理拆分等）。
    *   **可扩展性测试**：涵盖了从 4 到 768 的不同隐藏层维度。
*   **充分性评价**：实验设计较为充分，不仅展示了加速比，还深入分析了 GPU 性能瓶颈（如 L2 缓存命中率、DRAM 吞吐量等），对比实验涵盖了算法级（FBT vs WoP）和硬件级（CPU vs GPU）的对比，结果客观。

### 6. 主要结论与发现
*   **显著加速**：相比 CPU 基准，TIGER 在 GELU 上实现 **7.17×** 加速，Softmax 实现 **16.68×** 加速，LayerNorm 实现 **17.05×** 加速。
*   **端到端提升**：对于单个 GPT-2 Transformer 块，TIGER 实现了 **15.54×** 的整体加速。
*   **精度至关重要**：实验证明非线性层对精度极度敏感，低精度（4位）会导致困惑度飙升数百倍，而 TIGER 的高精度方案能保持模型性能。
*   **批处理策略有效性**：通过将 PBS 批次限制在特定范围内，可有效缓解 GPU 显存层次结构的压力，提升 4.35× 的利用率。

### 7. 优点与亮点
*   **首创性**：全球首个针对 TFHE 高精度非线性层评估的 GPU 加速框架。
*   **算法创新**：巧妙结合了密码学 PBS 和经典数值分析（泰勒展开），解决了 FHE 中“精度-开销”的固有矛盾。
*   **硬件感知**：深入分析了 GPU 架构对 PBS 计算的影响，提出的批处理拆分和 FFT 优化具有很强的工程参考价值。

### 8. 不足与局限
*   **绝对延迟仍然较高**：尽管有十倍以上的加速，单个 Transformer 块的密文推理仍需约 985 秒（约 16 分钟），距离实时交互式 LLM 推理仍有巨大差距。
*   **模型覆盖范围**：主要针对 GPT-2 结构进行优化，对于更新的架构（如使用 SwiGLU 或不同归一化方式的模型）虽有通用性，但未在文中直接验证。
*   **通信开销未深究**：在多 GPU 扩展讨论中仅停留在理论层面，未实际测试多卡间的通信瓶颈。

（完）
