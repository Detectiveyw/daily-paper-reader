Title: Cryptanalysis of the Legendre Pseudorandom Function over Extension Fields

URL Source: https://arxiv.org/pdf/2604.04833v2

Published Time: Thu, 09 Apr 2026 01:21:00 GMT

Number of Pages: 10

Markdown Content:
# Cryptanalysis of the Legendre Pseudorandom Function over Extension Fields 

Daksh Pandey 

Indian Institute of Technology, Roorkee 

Abstract. The Legendre Pseudorandom Function (PRF) is a highly ef-ficient cryptographic primitive built upon the Legendre symbol, recently gaining significant traction due to its low multiplicative complexity in Multi-Party Computation (MPC) and Zero-Knowledge Proof (ZKP) pro-tocols. While the security of the Legendre PRF over prime fields Fp has been extensively studied, recent interest has shifted toward its instantia-tion over extension fields Fpr . In this paper, we present the first compre-hensive cryptanalysis of the single-degree Legendre PRF operating over 

Fpr .First, we analyze the structural properties of polynomial input encod-ing under a standard passive threat model (sequential additive counter queries). We demonstrate that while the absence of polynomial carry-overs causes an asynchronous "no-carry fracture" that successfully neu-tralizes classical sliding-window collision attacks, the fracture itself is de-terministically periodic. By introducing a novel "Differential Signature" bucketing technique, we show that an attacker can systematically group fractured sequences by their structural shapes. This allows an adversary to bypass the no-carry defense entirely and passively recover the secret key in O(U · pr /M ) operations, where U is the unicity distance. Second, we evaluate the PRF under an active Chosen-Query threat model. We demonstrate that an adversary can completely circumvent the additive fracture by evaluating the PRF along a geometric sequence generated by a primitive polynomial. This query structure invokes strict multiplicative homomorphism over F∗ 

> pr

, permitting the direct general-ization of the state-of-the-art table collision attack to extract the key in purely O(pr /M ) operations. Finally, we evaluate the cryptographic boundaries of these attacks, formally establishing the absolute necessity of higher-degree key variants ( d ≥ 2) to achieve exponential security against structural reduction in extension fields. 

Keywords: Legendre PRF · Cryptanalysis · Extension Fields · Collision Attack · Pseudorandom Functions. 

## 1 Introduction and Mathematical Foundations 

The Legendre Pseudorandom Function (PRF) was originally proposed as a highly efficient primitive designed to minimize multiplicative complexity in Multi-Party Computation (MPC) and symmetric Zero-Knowledge Proof (ZKP) sys-tems [3]. While the baseline construction operates over prime fields Fp [2], opti-mizing throughput in specific cryptographic protocols often necessitates shifting  

> arXiv:2604.04833v2 [cs.CR] 8 Apr 2026 2Daksh P.

operations to extension fields Fpr . In this section, we formalize the algebraic foundations required to construct and evaluate the Legendre PRF over these extension fields. 

1.1 The Standard Legendre PRF ( d = 1 )

In its original, single-degree instantiation over a prime field Fp (where p is an odd prime), the Legendre PRF is evaluated using a secret key K ∈ Fp and an input x ∈ Fp. The function maps the result of the standard Legendre symbol to a single binary bit: 

LK (x) = 

 x + Kp



where the mathematical outputs +1 (quadratic residue) and −1 (quadratic non-residue) are mapped to the binary values 0 and 1, respectively. The sim-plicity of the linear shift (x + K) is the primary source of the function’s MPC efficiency, but it also forms the structural basis for sliding-window collision at-tacks [4,1]. 

1.2 Extension Field Construction ( Fpr )

To port this function to an extension field, we transition from scalar integer arithmetic to polynomial arithmetic. The field Fpr is defined by a base prime p

and an extension degree r.The elements of this field are polynomials with a maximum degree of r − 1,taking the form: 

A(x) = ar−1xr−1 + · · · + a1x + a0

where each coefficient ai ∈ Fp.

To ensure the field is closed under multiplication, operations are generally performed modulo an irreducible polynomial I(x) of degree exactly r. Therefore, for any two elements A(x), B (x) ∈ Fpr , field addition and multiplication are defined as: 

A(x) ⊕ B(x) ≡ (A(x) + B(x)) (mod p)

A(x) ⊗ B(x) ≡ (A(x) · B(x)) (mod I(x), p )

Crucially, polynomial addition over Fp operates strictly coefficient-wise with no “carry-over” to higher degrees, a property that fundamentally alters the be-havior of sequential additive queries. 

1.3 The Generalized Legendre Symbol 

To evaluate the PRF, we must generalize the Legendre symbol for Fpr . An element A(x) ∈ F∗ 

> pr

is considered a quadratic residue if there exists some element 

Y (x) such that Y (x)2 ≡ A(x) (mod I(x), p ).Cryptanalysis of the Legendre Pseudorandom Function over Extension Fields 3

By Euler’s Criterion generalized for finite fields [5], the Legendre symbol of a polynomial A(x) can be deterministically computed via exponentiation: 

L(A(x)) ≡ A(x) pr −12 (mod I(x), p )

This operation yields +1 if A(x) is a quadratic residue, and −1 if it is a non-residue. For the PRF output, we maintain the standard binary mapping: 

PRF( A) = 

(

0 if L(A(x)) ∈ { 0, 1}

1 if L(A(x)) = −1

## 2 Implementation Constraints and Statistical Security 

Before evaluating the structural vulnerabilities of the Legendre PRF, we must rigorously define the mapping between sequential integer queries and the ex-tension field Fpr , and formally bound the statistical pseudorandomness of the resulting keystream. 

2.1 Polynomial Input Encoding and Periodicity 

In standard Counter Mode, the server generates a keystream by evaluating the PRF on a sequentially incrementing integer counter n ∈ N. To evaluate the function over an extension field, this integer must be deterministically injected into the polynomial ring. 

Definition 1 (Base-p Polynomial Encoding). Let the extension field Fpr

consist of polynomials of maximum degree r − 1. We define the encoding map 

ϕ : Zpr → Fpr such that for an integer n with base-p expansion n = Pr−1 

> i=0

dipi,the corresponding field element is: 

ϕ(n) = Xn(x) = dr−1xr−1 + · · · + d1x + d0

where di ∈ Fp.

This encoding enforces a strict physical boundary on the allowable query space, as the map ϕ is only bijective over the finite interval [0 , p r − 1] .

Proposition 1 (Periodicity Constraint). Let M be the total number of se-quential queries. If M ≥ pr , the input sequence Xn (mod pr ) silently wraps, rendering the resulting PRF keystream perfectly periodic with a maximum period of pr .

Consequently, for cryptographic viability, the extension field parameters must be selected such that pr ≫ M , ensuring the adversary never observes a periodic cycle. 4 Daksh P. 

2.2 Pseudorandomness and the Extension Field Weil Bound 

To ensure the PRF resists statistical distinguishing attacks, the generated bit-stream must approximate a uniform distribution. Over prime fields, the distri-bution of Legendre sequences is bounded by the Weil theorem for multiplicative characters [6]. We extend this bound to the generalized Legendre symbol over 

Fpr .

Lemma 1 (Weil Bound for Multiplicative Characters over Fpr ). Let χ

be the quadratic character of Fpr (i.e., the generalized Legendre symbol L), and let f (T ) ∈ Fpr [T ] be a polynomial of degree d ≥ 1 that is not a perfect square in 

Fpr [T ]. Then the character sum over Fpr satisfies: 

X

> A∈Fpr

χ(f (A)) ≤ (d − 1) √pr

where the sum excludes the d roots of f (at which χ vanishes), and the bound holds for squarefree f of degree d ≥ 2. For the PRF’s linear input f (A) = A + K

(degree d = 1 ), the map A 7 → A + K is a bijection on Fpr , so the character sum P 

> A

L(A + K) equals P 

> A∈Fpr

L(A) = 0 exactly, by the orthogonality of characters. 

Using Lemma 1, we can formally bound the expected frequency of any sliding-window pattern within the keystream, proving its resistance to statistical biases. 

Theorem 1 (Statistical Indistinguishability). Let S be a PRF keystream generated over Fpr . If an adversary analyzes sliding-window patterns of length 

l, the absolute frequency of any specific l-bit binary sequence deviates from the uniform expectation by at most O(√pr ). Specifically, the expected occurrence Nl

of the pattern in the query space satisfies: 

Nl = pr

2l + O(√pr )

The error term O(√pr ) follows from applying the Weil bound to degree-d poly-nomial combinations ( d ≥ 2) that arise when counting joint occurrences of l-bit patterns; for l ≥ 2, the relevant character sums involve polynomials of degree 

l ≥ 2, to which the bound (d − 1) √pr ≤ (l − 1) √pr = O(√pr ) applies correctly. 

As the field size pr → ∞ , the fractional error term bounded by the Weil theorem becomes negligible, and the distribution converges perfectly to the uni-form distribution. Therefore, any successful cryptanalysis of the Legendre PRF over Fpr cannot rely on statistical bias, but must instead exploit the underlying algebraic structure of the evaluation. Cryptanalysis of the Legendre Pseudorandom Function over Extension Fields 5

## 3 Structural Analysis and Passive Cryptanalysis 

The security of the Legendre PRF against classical collision attacks [4] relies strictly on the continuity of the input sequence differential. In this section, we formally evaluate the structural behavior of polynomial base-p encoding and prove that the sequence can be compromised passively despite the failure of constant arithmetic progressions. 

3.1 The Additive Differential and Sequence Fracture 

In a passive threat model, the server generates a keystream S = {LK (Xn)}Mn=1 

by evaluating the PRF on a sequentially incrementing counter n, mapped to polynomials Xn ∈ Fpr as defined in Section 2.1. Classical table collision attacks over Fp necessitate a constant differential 

∆n = Xn+1 − Xn = 1 . We demonstrate that the polynomial mapping over extension fields inherently fractures this progression. 

Lemma 2 (Asynchronous Polynomial Carry). Let Xn ∈ Fpr be the poly-nomial encoding of an integer n. The differential ∆n = Xn+1 ⊕ Xn (mod p) is not constant, but rather takes values from the set 

( kX

> i=0

xi 0 ≤ k < r 

)

,

determined strictly by the p-adic valuation of n + 1 .Proof. Let n = Pr−1 

> i=0

dipi mapping to Xn = Pr−1 

> i=0

dixi. The standard integer addition n + 1 induces a carry cascading up to the k-th digit if and only if 

di = p − 1 for all 0 ≤ i < k (and dk̸ = p − 1). In that case, incrementing n sets 

di → 0 for 0 ≤ i < k and dk → dk + 1 .Because polynomial addition in Fpr operates coefficient-wise modulo p with-out algebraic carry-over, each reset di = p − 1 → 0 contributes a coefficient difference of 0 − (p − 1) ≡ 1 (mod p), and the increment dk → dk + 1 contributes a difference of +1 at degree k. Therefore all coefficients x0, x 1, . . . , x k increase by 1, yielding 

∆n = xk + xk−1 + · · · + x + 1 = 

> k

X

> i=0

xi.

When k = 0 (no carry, i.e. d0̸ = p − 1), only the constant coefficient increments and ∆n = 1 , consistent with the formula for k = 0 .Lemma 2 proves that the differential ∆n takes at most r distinct values, one for each possible carry depth k ∈ { 0, . . . , r − 1}. Consequently, the input sequence fractures into disjointed arithmetic sub-blocks of length p, rendering the contiguous sliding-window reference required by Beullens et al. [1] mathe-matically impossible. 6 Daksh P. 

3.2 Passive Key Recovery via Differential Signatures 

While the constant progression is fractured, the sequence of differentials (∆n)n∈N

is deterministically periodic. We exploit this determinism to mount a passive recovery attack. 

Definition 2 (Differential Signature). For a target sequence window Wn of unicity length U starting at index n, the differential signature δn is the sequence of cumulative polynomial differences relative to Xn:

δn = (0 , X n+1 ⊖ Xn, X n+2 ⊖ Xn, . . . , X n+U −1 ⊖ Xn)

Theorem 2 (Passive Key Recovery). Let M be the number of observed keystream bits, and U be the unicity distance such that 2U ≫ pr . An adver-sary can recover the secret key K passively in expected O(U · pr /M ) operations. Proof. By Definition 2, the target window Wn evaluates the PRF as: 

Wn = ( L(Xn + K + δ0), . . . , L (Xn + K + δU −1)) 

Letting Z = Xn + K, the sequence reduces to L(Z + δi) for 0 ≤ i < U , which depends exclusively on the universal shape δn and the scalar polynomial Z.The number of unique differential signatures for a window of length U is strictly bounded by p⌈log p U ⌉. The adversary extracts all M − U + 1 overlapping windows from the keystream and partitions them into equivalence classes based on their signature δ.The adversary selects the maximally populated equivalence class, expected to contain M ′ ≈ M/U sequence targets, and constructs a hash table H. The adversary then randomly samples Z ∈ Fpr , evaluates the reference window using the associated signature δmax , and queries H. A collision occurs with probability 

M ′/p r , requiring pr /M ′ iterations. Upon collision, the table yields the target index n, and the key is recovered algebraically via K = Z ⊖ Xn (mod p). The total time complexity is bounded by O(pr /(M/U )) = O(U · pr /M ).

## 4 Active Cryptanalysis via Geometric Sequences 

While Theorem 2 establishes a passive bypass of the polynomial carry, the com-plexity suffers a linear penalty proportional to U . In this section, we formalize an active chosen-query attack that restores strict algebraic homomorphism, elimi-nating the fracture entirely. 

4.1 Multiplicative Homomorphism over F∗

> pr

To completely circumvent the additive limitations of Fp[x], an active adver-sary with oracle access to the server abandons polynomial addition. Instead, the adversary selects a primitive generator g(x) of the multiplicative group F∗

> pr

and queries the server with a geometric sequence Xi = gi (mod I(x), p ) for 

1 ≤ i ≤ M .Cryptanalysis of the Legendre Pseudorandom Function over Extension Fields 7

Lemma 3 (Factoring Identity of the Legendre PRF). For a geometric query sequence gi, the Legendre PRF evaluation factors into a secret-dependent constant and a universal, keyless reference sequence shifted by a discrete loga-rithm j.Proof. The Legendre symbol generalized over Fpr is completely multiplicative. Thus, the evaluation L(gi + K) can be factored as: 

L(gi + K) ≡ L  K ⊗ (K−1gi + 1)  ≡ L(K) · L(K−1gi + 1) (mod I(x), p )

Since g(x) generates F∗ 

> pr

, there exists a unique integer j such that gj ≡ K−1

(mod I(x), p ). Substituting gj yields: 

L(gi + K) = L(K) · L(gi+j + 1) 

The sequence is thereby algebraically isolated into an unknown shift j and a sign coefficient L(K) ∈ {− 1, +1 }, operating over the universal reference L(gm + 1) .

4.2 Geometric Table Collision Attack 

Leveraging Lemma 3, we generalize the state-of-the-art table collision attack [1] to operate over the multiplicative group of the extension field. 

Theorem 3 (Active Key Recovery). Given M chosen geometric queries, an adversary can extract the secret polynomial K ∈ Fpr in purely O(pr /M )

operations. Proof. The adversary intercepts the geometric keystream and populates a hash table T mapping U -bit contiguous windows to their starting index i. To mitigate spurious collisions, U is chosen such that 2U ≫ pr .The adversary defines the universal reference function L0(m) = L(gm + 1) .By randomly sampling exponents m ∈ { 1, . . . , p r − 1}, the adversary generates reference windows of length U . By Lemma 3, the target sequence is a strict shift of the reference sequence, potentially inverted if L(K) = −1. Therefore, the adversary queries T for both the generated reference window and its bitwise complement. Because T contains M −U ≈ M targets, a collision is guaranteed in expected 

pr /M guesses. Let the collision match target index i and reference index m. By the factoring identity, the exponent relation is: 

m ≡ i + j (mod pr − 1) = ⇒ j ≡ m − i (mod pr − 1) 

The secret polynomial K is then extracted via the field inversion: 

K ≡ (gj )−1 (mod I(x), p )

This process requires exactly O(pr /M ) Legendre symbol evaluations, effectively reducing the active security of the single-degree PRF in Fpr to its theoretical lower bound. 8 Daksh P. 

## 5 Cryptographic Boundaries and Future Work 

The cryptanalysis presented in Sections 3 and 4 demonstrates that the single-degree ( d = 1 ) Legendre PRF is fundamentally insecure over extension fields under both passive and active threat models. However, the original PRF frame-work [4] allows for the use of higher-degree polynomials to mitigate algebraic weaknesses. As part of our future work, we intend to formally evaluate the se-curity boundaries of these generalized constructions. 

5.1 The Higher-Degree Variant ( d ≥ 2)

To defend against structural reduction, the secret key can be expanded from a single element into a set of d independent polynomials: K = ( Kd−1, K d−2, . . . , K 0).The PRF evaluates a monic polynomial of degree d:

LK (x) = L(xd + Kd−1xd−1 + · · · + K1x + K0)

For example, the degree-2 (quadratic) variant evaluates: 

LK (x) = L(x2 + K1x + K0)

When an adversary attempts to execute the active geometric attack (Section 4) by querying Xi = gi, the evaluation becomes: 

L(( gi)2 + K1(gi) + K0)

Unlike the single-degree variant, it is mathematically impossible to factor this expression into a secret-dependent constant and a universal reference sequence. The presence of the K1x term inherently binds the sequence to the specific, un-known combination of (K1, K 0). Because the variables cannot be cleanly isolated, the O(pr /M ) table collision attack is effectively neutralized. 

5.2 Future Cryptanalysis and Security Bounds 

While the geometric sequence attack fails against d ≥ 2, the algebraic structure of the extension field may still permit advanced reduction techniques. Future research will explore the application of Tschirnhaus transformations and “com-pleting the square” methodologies to eliminate intermediate key coefficients. Preliminary analysis suggests that by executing Meet-in-the-Middle (MITM) attacks over these algebraically reduced forms, an adversary can decrease the brute-force complexity from O(pdr ) down to approximately O(p⌊d/ 2⌋r ). Conse-quently, while the degree-2 variant may still be vulnerable to optimized searches in O(pr ) time, deploying the Legendre PRF with degrees d ≥ 3 or 4 appears necessary to establish a robust, exponential security margin against structural reduction in Fpr .Cryptanalysis of the Legendre Pseudorandom Function over Extension Fields 9

## Availability of Source Code 

The Python implementations for all cryptanalytic attacks described in this pa-per, including the randomized geometric table collision and the passive shape-bucketing optimization, are open-source and publicly available on GitHub at: 

https://github.com/DA1729/lprf_ext_fields_analysis .

## 6 Conclusion 

In this paper, we presented a comprehensive cryptanalysis of the single-degree Legendre Pseudorandom Function (PRF) operating over extension fields Fpr .We established that while the transition from prime fields to extension fields disrupts the continuity of standard additive counter queries—resulting in an asynchronous “no-carry fracture” that effectively thwarts classical sliding-window collision attacks—this structural boundary is fundamentally insufficient to guar-antee cryptographic security. Under a standard passive threat model, we demonstrated that the deter-ministic periodicity of polynomial input encoding allows an adversary to cluster fractured sequence windows by their differential signatures. By leveraging this novel shape-bucketing optimization, the adversary can bypass the additive frac-ture entirely and passively recover the secret key in O(U · pr /M ) operations. Furthermore, by elevating the threat model to a Chosen-Query Attack, we proved that an active adversary can completely circumvent the additive group limitations. By querying the PRF along a geometric sequence generated by a primitive polynomial, the adversary invokes strict multiplicative homomorphism over F∗ 

> pr

. This mathematical reduction factors the secret key into a pure exponent shift, permitting the direct generalization of the state-of-the-art table collision attack [1] and extracting the key in precisely O(pr /M ) operations. Ultimately, our findings formally break the d = 1 Legendre PRF over ex-tension fields under both passive and active threat models. To maintain an exponential security margin against structural and algebraic reduction, future deployments of the Legendre PRF in Fpr for Multi-Party Computation and Zero-Knowledge applications must inherently rely on higher-degree key variants (d ≥ 2) to mathematically bind the polynomial coefficients and resist generalized collision cryptanalysis. 

## 7 LLM Usage Disclosure 

Gemini 3.1 Pro was used to assist in generating Python scripts for validating the mathematical methods and techniques developed in this work, as well as in refining and structuring the manuscript from preliminary notes into a formal research paper. 10 Daksh P. 

## References 

1. Beullens, W., Beyne, T., Udovenko, A., Vitto, G.: Cryptanalysis of the Legendre PRF and generalizations. IACR Transactions on Symmetric Cryptology 2020 (1), 313–330 (2020). https://doi.org/10.13154/tosc.v2020.i1.313-330 

2. Damgård, I.B.: On the randomness of Legendre and Jacobi sequences. In: Gold-wasser, S. (ed.) CRYPTO 1988, LNCS, vol. 403, pp. 163–172. Springer, Heidelberg (1989). https://doi.org/10.1007/0-387-34799-2_13 

3. Grassi, L., Rechberger, C., Rotaru, D., Scholl, P., Smart, N.P.: MPC-friendly sym-metric key primitives. In: Weippl, E.R., et al. (eds.) Proceedings of the 2016 ACM SIGSAC Conference on Computer and Communications Security (CCS ’16), pp. 430–443. ACM, New York (2016). https://doi.org/10.1145/2976749.2978370 

4. Khovratovich, D.: Key recovery from Legendre symbol. IACR Cryptology ePrint Archive, Report 2019/147 (2019). https://eprint.iacr.org/2019/147 

5. Lidl, R., Niederreiter, H.: Finite Fields. Encyclopedia of Mathematics and its Ap-plications, vol. 20, 2nd edn. Cambridge University Press, Cambridge (1997). 6. Weil, A.: Sur les courbes algébriques et les variétés qui s’en déduisent. Actualités Sci. Ind., vol. 1041. Hermann, Paris (1948).