---
title: Efficient DPF-based Error-Detecting Information-Theoretic Private Information Retrieval Over Rings
title_zh: 基于 DPF 的环上高效检错信息论私有信息检索
authors: "Pengzhen Ke, Liang Feng Zhang, Huaxiong Wang, Li-Ping Wang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00411v1"
tags: ["query:fss-llm"]
score: 9.0
evidence: 使用分布式点函数 (DPF) 在环上实现可检错的隐私信息检索
tldr: 本研究针对现有认证私有信息检索（APIR）在有限域下 DPF 密钥过大及双密钥设计导致通信开销高的问题，提出了一种基于环的信息论纠错 PIR 方案（itED-PIR）。通过利用素数幂次环上的 itDPF，该方案打破了域结构的限制，并采用单密钥设计将查询通信量减半。该成果为分布式存储和后量子隐私协议提供了更高效、可扩展的恶意抗性 PIR 框架。
selection_source: fresh_fetch
motivation: 现有的 APIR 方案受限于有限域结构，导致在信息论安全设置下密钥尺寸过大且通信冗余。
method: 提出一种基于素数幂次环的 itED-PIR 方案，并引入单 itDPF 密钥设计以优化查询过程。
result: 显著降低了密钥增长速度，并将查询侧通信开销减半，同时保持了隐私性和可验证性。
conclusion: 该方案为构建轻量级、灵活的恶意抗性私有信息检索系统提供了高效的新框架。
---

## 摘要
认证私有信息检索 (APIR) 是目前最先进的检错私有信息检索 (ED-PIR) 技术，利用分布式点函数 (DPF) 实现亚多项式复杂度和隐私性。然而，其有限域结构限制其仅能使用素数阶 DPF，导致在信息论设置下密钥尺寸过大，且其双 DPF 密钥设计引入了不必要的通信开销，限制了其在大规模部署中的实用性。本文提出了一种新型的基于环的信息论 ED-PIR (itED-PIR) 方案，通过利用素数幂阶信息论 DPF (itDPF) 克服了这些局限。该方案构建在素数幂环之上，打破了 APIR 的域诱导约束，实现了更高效的 DPF 利用，显著减缓了密钥尺寸的增长，使该方案在高度安全场景下具有可行性。此外，单 itDPF 密钥设计通过消除 APIR 冗余的双密钥设置，在不损害隐私或可验证性的前提下，将查询侧通信开销减半。除了直接的效率提升外，这项工作还为构建基于 DPF 的抗恶意攻击私有信息检索建立了一个轻量级、灵活的框架，为分布式存储系统和后量子隐私协议中的隐私保护数据检索开辟了新途径。

## Abstract
Authenticated private information retrieval (APIR) is the state-of-the-art error-detecting private information retrieval (ED-PIR), using Distributed Point Functions (DPFs) for subpolynomial complexity and privacy. However, its finite field structure restricts it to prime-order DPFs, leading to prohibitively large key sizes under information-theoretic settings, while its dual-DPF-key design introduces unnecessary communication overhead, limiting its practicality for large-scale deployments. This paper proposes a novel ring-based information-theoretic ED-PIR (itED-PIR) scheme that overcomes these limitations by leveraging prime-power-order information-theoretic DPFs (itDPFs). Built over a prime-power ring, the proposed scheme breaks APIR's field-induced constraint to enable more efficient DPF utilization, significantly reducing key size growth and rendering the scheme feasible for high-security scenarios. Additionally, a single-itDPF-key design halves query-side communication overhead by eliminating APIR's redundant dual-key setup, without compromising privacy or verifiability. Beyond immediate efficiency gains, this work establishes a lightweight, flexible framework for constructing DPF-based malicious-resilient private information retrieval, opening new avenues for privacy-preserving data retrieval in distributed storage systems and post-quantum privacy protocols.

---

## 论文详细总结（自动生成）

这篇论文题为《Efficient DPF-based Error-Detecting Information-Theoretic Private Information Retrieval Over Rings》（基于 DPF 的环上高效检错信息论私有信息检索），由上海科技大学、南洋理工大学等机构的研究者完成。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在多服务器私有信息检索（PIR）中，为了应对恶意服务器返回错误数据的问题，研究者提出了检错 PIR（ED-PIR）。目前最先进的方案是认证 PIR（APIR），它利用分布式点函数（DPF）实现了亚多项式级的通信复杂度。
*   **核心问题**：
    1.  **结构限制**：APIR 严格受限于有限域结构，只能使用素数阶 DPF。在信息论安全设置下，这导致密钥尺寸随安全强度呈指数级增长。
    2.  **通信冗余**：APIR 采用双 DPF 密钥设计（每个服务器两个密钥），引入了不必要的通信开销。
*   **研究意义**：本文旨在通过引入“环”结构和“单密钥”设计，打破现有方案的效率瓶颈，使其在大规模分布式存储和高安全需求场景下更具实用性。

### 2. 方法论
*   **核心思想**：将 PIR 的代数结构从“有限域”扩展到“素数幂环” $R = (\mathbb{Z}_{p^\tau}, +, \cdot)$，并利用单密钥 DPF 配合环上的乘法特性实现结果验证。
*   **关键技术细节**：
    *   **单 itDPF 密钥设计**：客户端不再发送两个密钥，而是随机选择一个可逆元素 $\beta \in R^*$，生成一个针对点函数 $f_{\alpha, \beta}$ 的 DPF 密钥发送给服务器。
    *   **环上验证逻辑**：服务器计算数据库的加权和并返回。客户端收到响应后进行聚合得到 $R_{agg}$，利用辅助信息 $\beta$ 计算 $\beta^{-1} \cdot R_{agg}$。如果结果属于合法集合（如二进制数据库的 $\{0, 1\}$），则接受；否则判定为服务器篡改。
    *   **通用性框架**：该方案不限制底层 DPF 的具体实现，只要其输出群是加法群 $(\mathbb{Z}_{p^\tau}, +)$ 即可。
*   **算法流程**：
    1.  **查询（Que）**：采样 $\beta \in R^*$，生成单密钥 $k_j$。
    2.  **回答（Ans）**：服务器执行 DPF 求值并与数据库项相乘求和。
    3.  **重构（Rec）**：客户端聚合结果，通过环乘法逆元进行合法性校验。

### 3. 实验设计
*   **场景与 Benchmark**：本文属于理论密码学研究，主要通过**渐近复杂度分析**和**具体参数实例化**进行对比。
*   **对比方法**：主要对比对象是 **APIR (Colombo et al., USENIX Security 2023)**。
*   **对比维度**：
    *   服务器数量（3, 4, 8 及通用 $d(t+1)$ 台）。
    *   隐私级别（统计隐私 vs. 完美隐私）。
    *   通信复杂度（查询开销、响应开销）。
    *   安全性参数（如错误检测概率 $\epsilon$）。

### 4. 资源与算力
*   **说明**：论文中**未提及**具体的 GPU 或 CPU 硬件实验、训练时长或算力消耗。
*   **原因**：该研究侧重于算法的数学构造、安全性证明（信息论安全）以及通信复杂度的理论推导，而非工程实现。

### 5. 实验数量与充分性
*   **实验形式**：论文提供了 4 个核心推论（Corollary 1-4），分别对应不同的服务器架构和隐私需求。
*   **充分性评价**：
    *   **理论充分性**：通过严谨的数学证明（定理 1-7）验证了方案的正确性、隐私性和可验证性。
    *   **对比客观性**：在 Table 1 中详细列出了不同方案在相同安全参数下的复杂度公式。特别是在高安全强度（如 $\epsilon = 2^{-128}$）下，通过对比展示了本方案相比 APIR 的压倒性优势（APIR 在此参数下复杂度不可行）。

### 6. 主要结论与发现
*   **通信效率翻倍**：通过单密钥设计，查询侧的通信开销直接减半。
*   **突破域限制**：利用素数幂环，可以使用更小的素数（如 $p=2$）通过增加幂次 $\tau$ 来提升安全性，避免了有限域下必须寻找大素数的难题。
*   **完美隐私实现**：实现了首个在 4 服务器环境下具有亚多项式通信复杂度的完美 1-隐私 itED-PIR 方案。
*   **量子抗性**：由于基于信息论安全，该方案天然具有抗量子攻击的特性。

### 7. 优点
*   **轻量级**：显著降低了 DPF 密钥的大小，减少了带宽占用。
*   **灵活性**：支持从二进制数据库到多位（m-bit）数据库的通用扩展。
*   **高安全性**：在提供检错能力的同时，支持完美隐私（Perfect Privacy），不依赖计算困难性假设。

### 8. 不足与局限
*   **客户端假设**：目前模型假设客户端是“诚实但好奇”的，尚未深入讨论恶意客户端可能带来的安全风险。
*   **缺乏工程实测**：虽然理论复杂度优越，但未在真实的分布式存储系统（如 Hadoop 或 IPFS）中进行端到端的延迟测试。
*   **计算成本**：与所有基于 DPF 的 PIR 方案一样，服务器端仍需对整个数据库进行线性扫描，计算开销随数据库增大而线性增加。

（完）
