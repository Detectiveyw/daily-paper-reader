---
title: "LLM-Redactor: An Empirical Evaluation of Eight Techniques for Privacy-Preserving LLM Requests"
title_zh: LLM-Redactor：八种隐私保护型大语言模型请求技术的实证评估
authors: "Justice Owusu Agyemang, Jerry John Kponyo, Elliot Amponsah, Godfred Manu Addo Boakye, Kwame Opuni-Boachie Obour Agyekum"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12064v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 评估用于大模型隐私保护请求的多方计算秘密共享和全同态加密技术
tldr: 本研究针对LLM应用中敏感数据泄露至云端API的问题，系统评估了本地推理、脱敏、语义重述等八种隐私保护技术。通过开发开源中间件并利用1300个样本的基准测试，研究发现单一技术难以兼顾，而结合本地路由、脱敏与重述的组合方案（A+B+C）在保护个人信息（PII）和专有代码方面表现最优，并提出了基于威胁模型和工作负载的决策准则。
selection_source: fresh_fetch
motivation: 现有的网络加密和组织级数据防泄露工具无法有效处理发送至云端LLM API的提示词内容隐私问题。
method: 评估了包括本地推理、脱敏恢复、语义重述、TEE、分割推理、同态加密、多方计算和差分隐私在内的八种技术，并实现了一个兼容OpenAI API的开源中间件。
result: "实验表明组合方案（A+B+C）在PII泄露率上仅为0.6%，且在500个样本中实现了PII零精确泄露。"
conclusion: 没有单一技术能完全胜任，应根据威胁模型和工作负载特征，通过决策准则选择最合适的隐私保护技术组合。
---

## 摘要
编程代理和基于大语言模型（LLM）的应用通常会将潜在的敏感内容发送到云端 LLM API，这些内容可能会被记录、保留、用于训练或被传唤。现有的隐私工具侧重于网络级加密和组织级数据泄露防护（DLP），但两者都无法处理提示词（prompt）本身的内容。我们对八种隐私保护型 LLM 请求技术进行了系统的实证评估：(A) 仅本地推理，(B) 带占位符还原的脱敏，(C) 语义改写，(D) 受信执行环境（TEE）托管推理，(E) 分割推理，(F) 全同态加密，(G) 通过多方计算实现的秘密共享，以及 (H) 差分隐私噪声。我们在一个与 MCP 和任何 OpenAI 兼容 API 兼容的开源中间层（shim）中实现了所有八种技术（或在尚无法部署的情况下实现了易于处理的研究阶段子集）。我们使用包含 1,300 个样本和 4,014 个标注的真值标记泄露基准，在四类工作负载中评估了四种实用选项（A、B、C、H）及其组合。我们的核心发现是，没有任何单一技术占据绝对优势：A+B+C 的组合（尽可能在本地路由，对其余部分进行脱敏和改写）在个人身份信息（PII）上的综合泄露率为 0.6%，在专有代码上为 31.3%，且在 500 个样本中实现了 PII 的零精确泄露。我们提出了一种决策规则，可根据威胁模型预算和工作负载特征选择合适的选项。代码、基准测试和评估框架已发布在 https://github.com/jayluxferro/llm-redactor。

## Abstract
Coding agents and LLM-powered applications routinely send potentially sensitive content to cloud LLM APIs where it may be logged, retained, used for training, or subpoenaed. Existing privacy tooling focuses on network-level encryption and organization-level DLP, neither of which addresses the content of prompts themselves. We present a systematic empirical evaluation of eight techniques for privacy-preserving LLM requests: (A) local-only inference, (B) redaction with placeholder restoration, (C) semantic rephrasing, (D) Trusted Execution Environment hosted inference, (E) split inference, (F) fully homomorphic encryption, (G) secret sharing via multi-party computation, and (H) differential-privacy noise. We implement all eight (or a tractable research-stage subset where deployment is not yet feasible) in an open-source shim compatible with MCP and any OpenAI-compatible API. We evaluate the four practical options (A, B, C, H) and their combinations across four workload classes using a ground-truth-labelled leak benchmark of 1,300 samples with 4,014 annotations. Our headline finding is that no single technique dominates: the combination A+B+C (route locally when possible, redact and rephrase the rest) achieves 0.6% combined leak on PII and 31.3% on proprietary code, with zero exact leaks on PII across 500 samples. We present a decision rule that selects the appropriate option(s) from a threat-model budget and workload characterisation. Code, benchmarks, and evaluation harness are released at https://github.com/jayluxferro/llm-redactor.

---

## 论文详细总结（自动生成）

这篇论文对保护大语言模型（LLM）请求隐私的八种技术进行了深入的实证评估，并提出了一个名为 **LLM-Redactor** 的开源中间件框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：开发者在使用云端 LLM API（如 OpenAI、Anthropic）时，提示词（Prompts）中常包含个人身份信息（PII）、商业秘密、专有代码等敏感数据。现有的网络加密（TLS）只能防止中间人攻击，无法防止云端厂商获取内容；而传统的 DLP 工具又无法处理动态的 API 请求。
*   **研究动机**：填补 LLM 请求流水线中隐私保护技术的系统性评估空白，探索如何在保证模型实用性的同时，最大限度地减少敏感数据向云端的泄露。

### 2. 论文提出的方法论
论文系统化地评估了八种技术（A-H），并将其集成在一个兼容 OpenAI API 和 MCP 协议的中间件中：
*   **核心思想**：通过“本地预处理 + 智能路由 + 隐私增强”的流水线处理请求。
*   **关键技术细节**：
    *   **(A) 本地推理**：使用本地轻量级模型（如 3B 参数量）处理简单任务，完全避免数据外流。
    *   **(B) 脱敏与还原**：利用 NER（命名实体识别）和正则匹配识别敏感词，替换为占位符（如 `<EMAIL_1>`），并在响应回传时还原。
    *   **(C) 语义重述**：本地模型重写提示词，保留技术核心但移除身份特征。
    *   **(D) TEE 托管推理**：在受信任执行环境（如 AWS Nitro Enclaves）中运行模型。
    *   **(E) 分割推理**：本地运行前几层，仅发送中间激活值。
    *   **(F) 全同态加密 (FHE)**：在加密数据上直接进行推理（目前处于研究阶段）。
    *   **(G) 多方计算 (MPC)**：通过秘密共享在多个非共谋服务器间分配计算。
    *   **(H) 差分隐私 (DP) 噪声**：在词级别注入校准噪声。

### 3. 实验设计
*   **数据集/基准测试**：构建了一个包含 **1,300 个样本**、**4,014 个真值标注**的基准测试，分为四类工作负载：
    *   **WL1 (PII 密集型)**：包含姓名、邮箱、地址等。
    *   **WL2 (密钥密集型)**：包含 API Key、Token、配置文件。
    *   **WL3 (隐性身份)**：不含直接 PII 但通过上下文可推断身份（如“某公司的 CFO”）。
    *   **WL4 (专有代码)**：包含内部函数名、数据库架构等。
*   **对比方法**：对比了上述八种技术及其组合（如 A+B, A+B+C），并以“无保护基准”作为参照。
*   **评估指标**：精确泄露率（Exact Leak Rate）、部分泄露率、延迟（Latency）、实用性损耗（Utility Delta）。

### 4. 资源与算力
*   **本地模型**：使用了 **Ollama** 运行 **Llama 3.2 (3B)** 和 **Qwen 3.5 (4B)** 模型进行本地分类、重述和评估。
*   **硬件环境**：提到了使用 **AWS Nitro Enclaves** 进行 TEE 演示。
*   **算力说明**：论文未明确给出具体的 GPU 数量和总训练时长，但指出本地推理和重述阶段会增加约 1-2 秒的延迟，而 FHE/MPC 等技术目前在算力开销上极其高昂（比明文慢 10,000 倍以上），仅作原理性验证。

### 5. 实验数量与充分性
*   **实验规模**：针对 1,300 个样本进行了多轮测试，涵盖了从正则匹配到复杂的本地模型重述等多种配置。
*   **充分性**：实验设计较为全面，不仅测试了隐私保护效果，还通过“法官模型（Judge Model）”评估了脱敏对回答质量的影响。
*   **客观性**：通过引入“隐性身份（WL3）”这一极具挑战性的类别，客观地揭示了当前基于内容的脱敏技术的局限性。

### 6. 论文的主要结论与发现
*   **无“银弹”技术**：没有任何单一技术能完美平衡隐私、性能和实用性。
*   **最优组合**：**A+B+C 组合**（本地路由 + 脱敏 + 重述）表现最佳，在 PII 任务上实现了 **0.6% 的极低泄露率**，且在 500 个样本中实现了零精确泄露。
*   **隐性身份难题**：对于 WL3（隐性身份），所有基于内容转换的技术（B, C, H）表现均不佳（泄露率 >95%），证明了语义层面的隐私保护极难通过简单的词汇替换实现。
*   **决策准则**：提出了根据“威胁模型预算”选择技术的规则，例如零容忍场景必须使用本地推理或 TEE。

### 7. 优点
*   **系统性强**：首次在统一基准下对比了从工程实用到前沿密码学的八类技术。
*   **实用价值高**：提供了开源中间件，兼容主流 API，开发者可直接部署。
*   **分类科学**：提出的四类工作负载非常贴合实际开发场景（尤其是代码和密钥保护）。

### 8. 不足与局限
*   **数据合成性**：测试样本均为合成数据，可能无法完全覆盖真实世界中复杂多变的提示词模式。
*   **检测器依赖**：脱敏效果高度依赖于 Presidio 和 spaCy 等底层 NER 工具的准确率，对非英语内容的支持有限。
*   **研究阶段技术的局限**：FHE、MPC 和分割推理仅进行了原理性模拟或小规模测试，尚未达到生产级评估。
*   **质量评估规模较小**：实用性评估（Judge Model）仅针对 50 个样本进行了抽样，结论的统计显著性有待加强。

（完）
