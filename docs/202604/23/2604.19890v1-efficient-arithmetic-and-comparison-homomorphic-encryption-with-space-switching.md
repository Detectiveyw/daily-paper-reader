---
title: Efficient Arithmetic-and-Comparison Homomorphic Encryption with Space Switching
title_zh: 基于空间切换的高效算术与比较同态加密
authors: "Erwin Eko Wahyudi, Yan Solihin, Qian Lou"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19890v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 同态加密中高效的算术和比较运算
tldr: 全同态加密（FHE）在处理算术和比较运算时面临效率与精度的权衡。本文提出一种针对FV类方案的“空间切换”方法，通过在数值空间和数字空间之间进行转换，实现了算术与比较运算的高效集成。该方法避免了传统方案切换的高昂开销和多项式逼近的精度问题，在数据库工作负载下性能提升显著，为隐私保护计算提供了实用方案。
selection_source: fresh_fetch
motivation: 现有的全同态加密方案难以在统一框架下高效且准确地同时支持算术运算和比较运算。
method: 提出空间切换（Space Switching）技术，通过规约和模数提升步骤在数值空间与数字空间之间进行转换。
result: 实验表明，该方法在数据库任务中比方案切换快17倍，比直接比较快15倍。
conclusion: 空间切换方法有效解决了FHE中算术与比较运算的集成难题，显著提升了隐私保护计算的实用性。
---

## 摘要
全同态加密（FHE）能够在不解密的情况下对加密数据进行计算，使其成为隐私保护应用的核心。然而，现有的方案都无法在一个统一的框架内高效地同时支持算术和比较操作。先前的方法（如方案切换和多项式逼近）面临严重的局限性：方案切换在大输入下会产生极高的开销，而逼近方法在临界点附近会引入误差，限制了其在精度敏感任务中的应用。我们提出了一种空间切换方法，旨在将算术和比较计算无缝集成到 FV 类方案中。我们的方法识别出这两类操作需要不同的明文空间，并引入了两个过程：一个是从数值空间 $\mathbb{Z}_{p^r}$ 转换到数字空间 $\mathbb{Z}_{p}$ 的归约步骤，以及一个将结果映射回 $\mathbb{Z}_{p^r}$ 的模数提升步骤。这种设计使得在同一方案内能够连续进行算术和比较的求值。实验表明，在数据库工作负载上，我们的方法比方案切换快 17 倍，比直接比较快 15 倍，证明了其在现实世界隐私保护计算中的实用性。代码和构件可在 https://github.com/UCF-Lou-Lab-PET/Universal-BGV 获取。

## Abstract
Fully homomorphic encryption (FHE) enables computation on encrypted data without decryption, making it central to privacy-preserving applications. However, no existing scheme efficiently supports both arithmetic and comparison operations in a unified framework. Prior approaches such as scheme switching and polynomial approximation face serious limitations: switching incurs prohibitive overhead for large inputs, while approximation methods introduce errors near critical points, restricting use in accuracy-sensitive tasks. We propose space switching method to integrate arithmetic and comparison computation seamlessly within FV-style schemes. Our approach identifies that the two types of operations require different plaintext spaces and introduces two procedures: a reduction step to transition from the number space $\mathbb{Z}_{p^r}$ to the digit space $\mathbb{Z}_{p}$, and a modulus-raising step to map results back to $\mathbb{Z}_{p^r}$. This design enables continuous evaluation of arithmetic and comparison within the same scheme. Experiments show that our method achieves up to $17\times$ faster performance than scheme switching and $15\times$ faster than direct comparison on database workloads, demonstrating its practicality for real-world privacy-preserving computation. Code and artifacts are available at https://github.com/UCF-Lou-Lab-PET/Universal-BGV.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Arithmetic-and-Comparison Homomorphic Encryption with Space Switching》的深度结构化分析报告：

### 1. 论文的核心问题与整体含义
全同态加密（FHE）领域长期存在“算术与逻辑运算难兼顾”的矛盾：
*   **背景**：以 BGV/BFV 为代表的“字级（Word-wise）”方案擅长高吞吐量的算术运算（如矩阵乘法），但在处理比较（Comparison）等非线性逻辑时效率极低；以 TFHE 为代表的“位级（Bit-wise）”方案擅长逻辑运算，但算术运算开销巨大。
*   **核心问题**：现有的融合方案中，“方案切换（Scheme Switching）”在大位宽输入下开销惊人，而“多项式逼近（Polynomial Approximation）”在临界点（如零点附近）存在精度误差，无法满足基因组分析或金融计算等对**精确比较**的需求。
*   **研究动机**：在不切换加密方案的前提下，如何在 BGV/BFV 框架内高效、精确地实现算术与比较运算的无缝集成。

### 2. 方法论：空间切换（Space Switching）
论文提出了“空间切换”技术，其核心思想是利用不同的明文空间处理不同的运算：

*   **核心思想**：将算术运算保留在原生的**数值空间 $\mathbb{Z}_{p^r}$** 中，而将比较运算转换到更小的**数字空间 $\mathbb{Z}_p$** 中执行。
*   **关键技术流程**：
    1.  **规约步骤（Reduction）**：将位于 $\mathbb{Z}_{p^r}$ 的加密数值 $a$ 提取为其 $p$ 进制的数字表示 $\{a_i\}_{i=0}^{r-1}$，其中每个 $a_i \in \mathbb{Z}_p$。论文改进了传统的数字提取算法，通过避免中间值的重复计算，将多项式求值次数从 $O(r^2)$ 降低到 $O(r)$。
    2.  **数字级比较（Digit-wise Comparison）**：在 $\mathbb{Z}_p$ 空间内，利用费马小定理构造低次插值多项式（如 $1-x^{p-1}$ 用于等值测试），对提取出的数字进行并行比较。
    3.  **结果聚合（Aggregation）**：将各数字位的比较结果通过同态乘法和加法逻辑合并，得到最终的比较布尔值。
    4.  **模数提升（Modulus Raising）**：将位于 $\mathbb{Z}_p$ 的比较结果（0 或 1）重新映射回 $\mathbb{Z}_{p^r}$ 空间，以便后续继续进行算术运算（如 `SUM` 聚合）。

### 3. 实验设计
*   **数据集与场景**：
    *   **基础算子测试**：针对不同位宽（8, 12, 16, 20 bit）的“小于（LT）”和“等于（EQ）”运算。
    *   **数据库工作负载**：使用 **TPC-H Benchmark** 的 Query 6，模拟加密数据库的过滤（WHERE 子句，含比较）与聚合（SUM 运算，含算术）。
*   **对比方法（Baselines）**：
    *   **方案切换**：HE3DB（结合 BGV 和 TFHE 的 SOTA 方案）。
    *   **直接比较**：Morimura 等人提出的在单一 $\mathbb{Z}_{p^r}$ 空间直接进行多项式插值的方法。
    *   **多项式逼近**：基于 CKKS 的逼近方法（用于精度对比）。

### 4. 资源与算力
*   **硬件环境**：Intel(R) Xeon(R) W-2235 CPU @ 3.80GHz，配备 128GB RAM。
*   **软件实现**：基于生产级 FHE 库 **HElib** 和 NTL 数论库实现。
*   **算力细节**：实验采用**单线程**运行，以确保对比的公平性。文中未提及 GPU 加速，所有结果均基于 CPU 计算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 8 位到 20 位的多种位宽测试，甚至扩展到了 32 位和 56 位的大位宽场景。
    *   数据库实验涵盖了 $2^{12}$ 到 $2^{14}$ 行数据的规模。
    *   提供了详细的运行时间分解（Breakdown），分析了规约、比较、聚合、提升各阶段的耗时。
*   **充分性评价**：实验设计较为充分。作者不仅对比了运行时间，还对比了内存占用、评估密钥（Evaluation Key）大小，并讨论了与 CKKS 逼近方案在精度上的差异。对比基准选择了近年来的顶级会议成果，具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：在基础 LT 算子上，空间切换比方案切换快达 **40 倍**，比直接比较快达 **130 倍**（摊销后）。
*   **数据库效率**：在 TPC-H 查询任务中，空间切换比方案切换快 **17 倍**，比直接比较快 **15 倍**。
*   **可扩展性**：随着位宽增加，直接比较方案的开销呈指数级增长，而空间切换方案表现出近乎线性的增长趋势，能够处理 32 位甚至 56 位的高精度输入。
*   **资源优势**：相比方案切换，空间切换在内存占用和密钥大小上具有显著优势（例如内存占用仅为后者的 1/8 到 2/3）。

### 7. 优点
*   **统一框架**：无需在不同的加密方案（如 BGV 和 TFHE）之间进行复杂的转换，降低了系统实现的复杂度。
*   **精确性**：基于有限域插值，保证了比较结果的 100% 准确，优于 CKKS 的逼近法。
*   **高效利用 SIMD**：充分利用了 BGV 方案的槽（Slots）并行特性，大幅提升了摊销后的吞吐量。
*   **算法优化**：对数字提取算法的改进具有通用性，可推广至其他 FHE 优化场景。

### 8. 不足与局限
*   **规约开销**：尽管进行了优化，但“规约（Reduction）”和“插值比较”步骤仍占据了总耗时的 75%-85%，是主要的计算瓶颈。
*   **应用限制**：该方法主要针对 FV 类（BGV/BFV）方案，对于需要浮点数原生支持的场景，仍需依赖 CKKS，而空间切换在处理非整数比较时的表现未被深入探讨。
*   **深度消耗**：空间切换涉及多次多项式求值，对乘法深度（Multiplicative Depth）有一定消耗，可能需要更大的加密参数或更频繁的自举（Bootstrapping）。

（完）
