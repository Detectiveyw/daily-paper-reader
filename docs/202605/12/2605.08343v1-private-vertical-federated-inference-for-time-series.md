---
title: Private Vertical Federated Inference for Time-Series
title_zh: 针对时间序列的隐私保护纵向联邦推理
authors: "Lucas Fenaux, Larris Xie, Aditya Bang, Alex Zhang, Kevin Wilson, Florian Kerschbaum"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08343v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 基于MPC的Transformer架构安全推理
tldr: 针对时间序列数据的纵向联邦推理，传统多方计算在处理大规模Transformer时开销巨大且存在隐私泄露风险。本文提出PPHH-VFL架构，通过将模型头拆分为公共明文部分和私有MPC部分，并结合对抗训练，在保证隐私的同时大幅提升了推理效率和模型性能，实现了通信量和延迟的显著降低。
selection_source: fresh_fetch
motivation: 现有的多方计算方案在处理大规模Transformer模型时效率极低，而纵向联邦学习在嵌入层存在隐私泄露风险。
method: 提出PPHH-VFL混合架构，将模型头分为高效的明文公共头和轻量级的MPC私有头，并对公共嵌入进行对抗训练以抑制隐私泄露。
result: "相比端到端MPC提速达六个数量级，相比VFL+MPC基准在广域网下提速44.4倍，通信开销降低91.2%，且提升了分类准确率和回归精度。"
conclusion: PPHH-VFL在保证强隐私保护的前提下，为大规模时间序列模型的纵向联邦推理提供了一种高效且高精度的实用方案。
---

## 摘要
机构可以从时间序列数据的协作推理中获益。在需要隐私保护的场景下，多方计算（MPC）是提供强有力保障的直接方法，但其成本极高，且在现代 Transformer 架构上的扩展性较差。纵向联邦学习（VFL）具有较高的效率，但在嵌入（embedding）层面存在隐私泄露问题，且对于大型模型，通过 MPC 保护整个 VFL 模型头部的做法仍然极其缓慢且通信开销巨大。为了实现实用且大规模的安全推理，我们提出了“公有/私有混合头部纵向联邦学习”（PPHH-VFL）。这种混合架构将模型头部拆分为一个高效的明文公有头部和一个安全的轻量级 MPC 私有头部。通过对公有嵌入应用对抗训练，我们缓解了隐私泄露；同时，小型私有头部安全地保留了实现高下游效用所需的敏感信息流。对参数量高达 8600 万的模型进行的实证评估表明，与端到端 MPC 相比，PPHH-VFL 将推理速度提升了多达六个数量级。与标准的 VFL+MPC 基准相比，我们的方法具有显著更好的扩展性，在广域网（WAN）中实现了高达 44.4 倍的加速，通信成本降低了 91.2 倍（每批次从 1.7 GB 降至 19 MB），同时将下游分类准确率提高了 2.50%，回归均方根误差（RMSE）降低了 40.7%。

## Abstract
Institutions may benefit from collaborative inference on time-series data. In settings where privacy is necessary, multi-party computation (MPC) is a straightforward approach to providing strong guarantees, yet it remains prohibitively expensive and scales poorly with modern transformer architectures. Vertical Federated Learning (VFL) offers efficiency but suffers from privacy leakage at the embedding level, and securing the entire VFL model head via MPC remains prohibitively slow and communication-heavy for larger models. To enable practical, secure inference at scale, we propose "Public/Private Hybrid Head-VFL" (PPHH-VFL). This hybrid architecture splits the model head into an efficient plaintext public head and a secure, lightweight MPC private head. By applying adversarial training to the public embeddings, we mitigate privacy leakage; concurrently, the small private head securely preserves the flow of sensitive information needed for high downstream utility. Empirical evaluations on models ranging up to 86 million parameters demonstrate that PPHH-VFL accelerates inference by up to six orders of magnitude compared to end-to-end MPC. Compared to a standard VFL+MPC baseline, our approach scales significantly better, achieving a speedup of up to 44.4x in WAN and a 91.2x reduction in communication costs (dropping from 1.7 GB to 19 MB per batch), while simultaneously improving downstream classification accuracy by 2.50% and regression RMSE by 40.7%.

---

## 论文详细总结（自动生成）

这是一份关于论文《Private Vertical Federated Inference for Time-Series》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**时间序列数据在纵向联邦学习（VFL）环境下的隐私保护推理**。
*   **研究动机**：金融欺诈检测、临床监测等领域需要多机构协作推理，但数据极度敏感。
*   **现有技术瓶颈**：
    *   **多方计算（MPC）**：虽然提供强理论隐私保证，但在处理现代 Transformer 架构时（尤其是 Softmax 和激活函数），计算和通信开销巨大，难以扩展到大模型。
    *   **纵向联邦学习（VFL）**：推理效率高，但客户端发送的嵌入（Embedding）存在隐私泄露风险，且若用 MPC 保护整个模型头部，在大模型下依然极慢。
*   **核心问题**：如何在保证隐私的前提下，实现大规模 Transformer 模型的高效、低延迟协作推理？

### 2. 论文提出的方法论：PPHH-VFL
论文提出了 **公有/私有混合头部纵向联邦学习（Public/Private Hybrid Head-VFL）** 架构：
*   **核心思想**：将模型头部（Head）拆分为两部分：一个大的**公有头部（Public Head）**在明文下运行以保证效率，一个小的**私有头部（Private Head）**在 MPC 下运行以处理敏感信息。
*   **关键技术细节**：
    1.  **嵌入拆分**：客户端将本地编码器的输出拆分为公有嵌入和私有嵌入。
    2.  **对抗训练（Adversarial Training）**：为了防止公有嵌入泄露隐私（如识别出数据来源机构），引入一个判别器（Discriminator）进行对抗训练。目标是让判别器无法分辨嵌入来自哪个客户端，从而实现“经验隐私”。
    3.  **混合推理流程**：
        *   公有嵌入 $\rightarrow$ 公有头部（明文计算） $\rightarrow$ 公有预测值。
        *   私有嵌入 $\rightarrow$ 私有头部（MPC 计算） $\rightarrow$ 私有预测值。
        *   **融合头部（Fusion Head）**：在 MPC 环境下将公有和私有预测值结合，输出最终结果。
    4.  **时间维度拆分**：不同于传统 VFL 拆分特征维度，本文提出按**时间步（Time-steps）**拆分序列，即不同客户端持有同一序列的不同时间段。

### 3. 实验设计
*   **数据集**：
    1.  **信用卡交易欺诈检测（Credit Card Fraud）**：合成数据集，二分类任务。
    2.  **Rossmann 销售预测（Rossmann Sales）**：真实零售数据集，回归任务。
*   **基准测试（Benchmarks）**：
    1.  **End-to-End MPC (E2E)**：全流程使用 MPC 推理中央模型（理论隐私上限，性能下限）。
    2.  **VFL + MPC**：标准 VFL 架构，但模型头部完全由 MPC 保护。
*   **场景设置**：对比了局域网（LAN，1ms 延迟）和广域网（WAN，40ms 延迟）两种网络环境。

### 4. 资源与算力
*   **硬件环境**：使用配备 **NVIDIA A100 GPU (80GB)** 的服务器，拥有 32 个 CPU 核心和 1TB RAM。
*   **配置**：每个客户端进程分配一个独立的 GPU。
*   **软件**：基于 **CrypTen** 框架实现 MPC 协议。
*   **训练时长**：文中未详细列出总训练时长，但提到模型参数量从 100 万扩展到 **8600 万**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同规模的架构（P1-P4, H1-H4），参数量跨度大。
*   **重复性**：推理时间结果基于 **9 次运行的平均值**，并提供了 **95% 置信区间**。
*   **充分性**：实验对比了推理延迟、通信带宽、下游任务准确率/RMSE 以及针对判别器的隐私分析。涵盖了从局域网到广域网的性能表现，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **推理加速**：相比端到端 MPC，PPHH-VFL 提速高达 **6 个数量级**（将 157 天的推理缩短至 8.6 秒）。
*   **通信优化**：相比 VFL+MPC 基准，在处理 86M 参数模型时，通信量从 1.7 GB 降至 19 MB（**减少 91.2%**），WAN 环境下提速 **44.4 倍**。
*   **效用提升**：PPHH-VFL 的分类准确率比 VFL+MPC 高出 **2.50%**，回归 RMSE 降低了 **40.7%**。作者认为对抗训练起到了正则化作用。
*   **隐私有效性**：判别器准确率接近随机猜测（约 33.3%），证明了对抗训练能有效抑制嵌入层的身份泄露。

### 7. 优点
*   **扩展性极强**：通过将大部分计算移出 MPC，解决了 Transformer 模型在安全推理中的扩展性难题。
*   **混合隐私模型**：巧妙结合了 MPC 的理论安全性和对抗训练的经验安全性，在效率和隐私间找到了平衡点。
*   **通信效率**：极大地降低了联邦推理对带宽的依赖，使其在现实广域网环境下具有实用价值。

### 8. 不足与局限
*   **效用差距**：虽然优于 VFL+MPC，但相比端到端 MPC（中央模型），仍有约 10% 的准确率损失，这主要是由于 VFL 分布式注意力机制的局限性。
*   **经验隐私风险**：对抗训练提供的是经验隐私而非数学上的可证明隐私（如差分隐私），在面对未知的攻击手段时可能存在风险。
*   **应用限制**：目前主要针对时间序列和 Transformer 结构，对于其他数据模态（如图像、图数据）的适用性尚待验证。

（完）
