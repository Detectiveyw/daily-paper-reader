---
title: On the (In-)Security of the Shuffling Defense in the Transformer Secure Inference
title_zh: 论 Transformer 安全推理中洗牌防御的（不）安全性
authors: "Zhengyi Li, Yakai Wang, Kang Yang, Yu Yu, Jiaping Gui, Yu Feng, Ning Liu, Minyi Guo, Jingwen Leng"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04901v1"
tags: ["query:fss-llm"]
score: 9.0
evidence: Transformer模型的密码学安全推理及非线性层效率
tldr: 本文研究了Transformer安全推理中打乱防御（Shuffling Defense）的安全性。为了平衡效率与隐私，现有方案常将打乱后的中间激活值暴露给客户端，以加速非线性层计算。作者提出一种新颖的攻击方法，通过将不同打乱顺序的激活值对齐到统一排列，成功实现了模型权重的逆向提取。实验表明，该攻击能以极低成本高精度恢复Pythia和GPT-2的模型权重，证明了打乱防御在保护模型隐私方面的局限性。
selection_source: fresh_fetch
motivation: 评估Transformer安全推理中广泛使用的激活值打乱防御机制是否能有效防止模型权重被恶意客户端提取。
method: 提出一种对齐攻击算法，将多次推理中随机打乱的激活值映射回一致的排列顺序，从而利用线性关系推导模型参数。
result: 在Pythia-70m和GPT-2模型上，攻击者仅需约1美元的查询成本即可实现极低误差的权重恢复。
conclusion: 研究证明了打乱防御机制并不像此前声称的那样稳固，现有的Transformer安全推理协议在保护模型权重方面存在严重安全隐患。
---

## 摘要
对于 Transformer 模型，密码学安全推理确保客户端仅获知最终输出，而服务器对客户端的输入一无所知。然而，由于需要大量的通信轮数和数据传输，安全地计算非线性层仍然是一个主要的效率瓶颈。为了解决这一问题，先前的工作向客户端公开中间激活值，从而允许以明文形式计算非线性操作。尽管这种方法显著提高了效率，但暴露激活值使得攻击者能够提取模型权重。为了降低这一风险，现有工作采用了一种洗牌（shuffling）防御机制，仅向客户端展示随机置换后的激活值。在本研究中，我们证明了洗牌防御并不像之前声称的那样稳健。我们提出了一种攻击方法，该方法将经过不同洗牌处理的激活值对齐到同一个置换，并随后利用它们来提取模型权重。在 Pythia-70m 和 GPT-2 上的实验表明，所提出的攻击可以将洗牌后的激活值对齐，均方误差在 $10^{-

## Abstract
For Transformer models, cryptographically secure inference ensures that the client learns only the final output, while the server learns nothing about the client's input. However, securely computing nonlinear layers remains a major efficiency bottleneck due to the substantial communication rounds and data transmission required. To address this issue, prior works reveal intermediate activations to the client, allowing nonlinear operations to be computed in plaintext. Although this approach significantly improves efficiency, exposing activations enables adversaries to extract model weights. To mitigate this risk, existing works employ a shuffling defense that reveals only randomly permuted activations to the client. In this work, we show that the shuffling defense is not as robust as previously claimed. We propose an attack that aligns differently shuffled activations to a common permutation and subsequently exploits them to extract model weights. Experiments on Pythia-70m and GPT-2 demonstrate that the proposed attack can align shuffled activations with mean squared errors ranging from $10^{-9}$ to $10^{-6}$. With a query cost of approximately \$1, the adversary can recover model weights with L1-norm differences ranging from $10^{-4}$ to $10^{-2}$ compared to the oracle weights.

---

## 论文详细总结（自动生成）

这篇论文对 Transformer 安全推理中广泛采用的“洗牌防御（Shuffling Defense）”机制进行了深入的安全评估，并证明了该防御在面对特定攻击时存在严重漏洞。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：在 Transformer 的安全推理（如使用多方计算 MPC）中，非线性层（Softmax, GELU, LayerNorm）是主要的性能瓶颈，占总延迟的 75%~90%。
*   **现状**：为了提速，现有方案（如 LOE 推理）将中间激活值暴露给客户端以进行明文计算。为了防止客户端通过激活值提取模型权重，研究者提出了“洗牌防御”，即在暴露激活值前对其进行随机置换（Shuffling）。
*   **核心问题**：论文挑战了“洗牌防御足以保护模型权重”的假设。作者试图证明，即使不知道具体的置换顺序，攻击者仍能通过对齐技术恢复模型参数。

### 2. 方法论
攻击的核心思想是**向量对齐（Vector Alignment）**与**线性系统求解**：
*   **向量对齐**：如果两个激活向量在数值上非常接近，即使它们被独立且随机地打乱，攻击者也可以通过寻找元素间的最小距离来恢复它们的相对对应关系。
*   **构造近似输入**：由于 Transformer 输入是离散 Token，难以直接构造微小差异的输入。作者巧妙地利用了安全推理协议中**安全截断（Secure Truncation）产生的随机 1-bit 误差**。通过多次请求同一 Prompt，截断误差会产生微小扰动，从而生成数值极度接近但打乱顺序不同的激活向量。
*   **对齐算法**：将对齐问题转化为“指派问题（Assignment Problem）”，使用**匈牙利算法（Hungarian Algorithm）**最小化两个打乱向量之间的距离，从而将所有观测到的激活值对齐到同一个（虽然仍是未知的）置换顺序。
*   **权重提取**：一旦激活值对齐，攻击者即可构建线性方程组 $X'W' = Y'$。求解出的权重 $W'$ 与原始权重 $W$ 仅存在行/列置换差异，但在神经网络中这种差异具有**功能等效性**，不影响前向传播结果。

### 3. 实验设计
*   **受害者模型**：Pythia-70m 和 GPT-2（安全推理研究中常用的基准模型）。
*   **实验场景**：针对 Transformer 中的各类线性层（$W_{qkv}, W_o, W_{h1}, W_{h2}$）进行提取。
*   **对比与指标**：
    *   **对齐精度**：使用均方误差（MSE）和正确匹配元素的计数。
    *   **权重恢复精度**：计算提取权重与原始权重之间的 L1 范数差异。
    *   **下游任务表现**：在 Wikitext 数据集上测试提取模型的困惑度（Perplexity）。
*   **基准（Benchmark）**：以原始模型权重作为 Oracle（金标准）。

### 4. 资源与算力
*   **算力说明**：文中未详细列出具体的 GPU 型号或数量。
*   **攻击成本**：作者强调了攻击的低成本性。对于 Pythia-70m 和 GPT-2，总查询次数在 3 万至 5 万次左右。按商业 API 价格计算，**总查询成本仅约 1 美元**。
*   **微调时间**：提取后的模型仅需不到 6 分钟的微调即可恢复到接近原始模型的性能。

### 5. 实验数量与充分性
*   **实验维度**：涵盖了不同的模型架构、不同的层类型、以及不同的定点数精度（14-bit, 16-bit, 18-bit）。
*   **充分性**：实验覆盖了 Transformer 的核心组件，并分析了奇异值（Singular Value）对求解稳定性的影响。通过对齐误差和权重误差的双重验证，证明了攻击的有效性。实验设计客观，考虑了数值计算中的病态矩阵问题。

### 6. 主要结论与发现
*   **洗牌防御不安全**：洗牌防御无法阻止模型提取攻击，攻击者可以实现 $10^{-9}$ 到 $10^{-6}$ 级别的激活值对齐精度。
*   **高精度恢复**：提取的权重与原始权重的 L1 差异在 $10^{-4}$ 到 $10^{-2}$ 之间，足以支持下游黑盒攻击或模型分析。
*   **功能等效性**：即使不知道置换矩阵，只要层与层之间保持一致的置换，提取的模型在功能上与原模型几乎等价。

### 7. 优点（亮点）
*   **攻击视角独特**：首次指出“对齐”而非“破解置换”才是提取权重的关键。
*   **利用协议缺陷**：巧妙利用了密码学协议中看似无害的“截断误差”作为扰动源，解决了离散输入难以构造近似激活值的难题。
*   **成本极低**：证明了这种攻击在现实商业环境（如 MLaaS API）中是完全可行的。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 70M~124M 规模的小模型上。随着模型增大，隐藏层维度增加，线性系统的病态问题可能会加剧，导致提取精度下降。
*   **对齐复杂度**：匈牙利算法在处理极高维度（如数万维）的向量对齐时，计算开销会显著增加。
*   **防御对策**：虽然提出了增加噪声或部分私密计算 LayerNorm 等防御建议，但尚未深入验证这些防御对性能的具体影响。

（完）
