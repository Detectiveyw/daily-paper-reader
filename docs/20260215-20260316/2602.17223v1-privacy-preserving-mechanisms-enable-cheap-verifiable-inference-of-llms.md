---
title: Privacy-Preserving Mechanisms Enable Cheap Verifiable Inference of LLMs
title_zh: 隐私保护机制实现大语言模型（LLM）的低成本可验证推理
authors: "Arka Pal, Louai Zahran, William Gvozdjak, Akilesh Potti, Micah Goldblum"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17223v1"
tags: ["query:fss-llm"]
score: 9.0
evidence: 利用隐私保护机制实现大模型的可验证推理
tldr: 针对大语言模型第三方托管中存在的推理真实性验证难题，本文提出了一种利用隐私保护机制实现廉价可验证推理的新思路。由于传统的零知识证明（ZKP）开销巨大，研究者设计了两种新协议，通过在隐私推理过程中增加极少量计算开销（仅需几个额外 token），在保证隐私的同时实现了对推理过程的验证。该方法比 ZKP 更快，且对下游任务几乎无负面影响，揭示了隐私与可验证性之间的内在联系。
selection_source: fresh_fetch
motivation: 解决第三方托管服务中推理提供商可能使用廉价弱模型欺骗用户，且现有加密验证方法计算开销过大的问题。
method: 提出两种利用隐私保护推理机制的新协议，通过在推理过程中引入少量额外 token 的计算来实现对模型执行过程的验证。
result: 该方法在仅增加极低计算成本的情况下实现了可验证性，且验证速度显著优于传统的零知识证明方法。
conclusion: 研究揭示了隐私与可验证性之间的紧密联系，证明了隐私保护机制是实现高效、廉价 LLM 可验证推理的有效途径。
---

## 摘要
随着大语言模型（LLM）规模的不断扩大，能够本地托管和运行模型的用户越来越少。这导致第三方托管服务的使用日益增加。然而，在这种环境下，推理提供商所执行的计算缺乏保障。例如，不诚实的提供商可能会用运行成本更低的弱模型替换昂贵的大模型，并将弱模型的结果返回给用户。现有的推理验证工具通常依赖于零知识证明（ZKP）等密码学方法，但这些方法会增加显著的计算开销，且对于大模型而言仍然不可行。在这项工作中，我们提出了一个新的见解——即给定一种执行隐私 LLM 推理的方法，就可以以极低的额外成本获得某种形式的可验证推理。具体而言，我们提出了两种新协议，利用隐私保护 LLM 推理来为所执行的推理提供保证。我们的方法成本低廉，仅需增加少量额外的 token 计算，且对下游几乎没有影响。由于最快的隐私保护推理方法通常比零知识（ZK）方法更快，所提出的协议还缩短了验证运行时间。我们的工作为 LLM 推理中隐私性与可验证性之间的联系提供了新颖的见解。

## Abstract
As large language models (LLMs) continue to grow in size, fewer users are able to host and run models locally. This has led to increased use of third-party hosting services. However, in this setting, there is a lack of guarantees on the computation performed by the inference provider. For example, a dishonest provider may replace an expensive large model with a cheaper-to-run weaker model and return the results from the weaker model to the user. Existing tools to verify inference typically rely on methods from cryptography such as zero-knowledge proofs (ZKPs), but these add significant computational overhead, and remain infeasible for use for large models. In this work, we develop a new insight -- that given a method for performing private LLM inference, one can obtain forms of verified inference at marginal extra cost. Specifically, we propose two new protocols which leverage privacy-preserving LLM inference in order to provide guarantees over the inference that was carried out. Our approaches are cheap, requiring the addition of a few extra tokens of computation, and have little to no downstream impact. As the fastest privacy-preserving inference methods are typically faster than ZK methods, the proposed protocols also improve verification runtime. Our work provides novel insights into the connections between privacy and verifiability in LLM inference.

---

## 论文详细总结（自动生成）

这是一份关于论文《Privacy-Preserving Mechanisms Enable Cheap Verifiable Inference of LLMs》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着大语言模型（LLM）规模剧增，用户不得不依赖第三方推理提供商。然而，提供商可能为了节省成本而进行“模型替换攻击”（用廉价弱模型冒充昂贵大模型）。
*   **研究背景**：现有的验证手段主要依赖零知识证明（ZKP），但其计算开销极大（比原始推理慢数千倍），在超大规模模型上几乎不可行。
*   **核心见解**：论文发现“隐私保护”与“可验证性”之间存在互补关系。如果推理过程已经通过隐私机制（如SMPC、FHE或TEE）进行了加密，那么用户可以利用这种“不可见性”植入秘密的验证标记，从而以极低的成本实现推理验证。

### 2. 论文提出的方法论
论文提出了两种基于隐私保护机制的协议：

*   **协议 1：Logit 指纹识别 (Logit Fingerprinting)**
    *   **核心思想**：利用 LLM 的 Logit 向量作为模型的唯一“指纹”。
    *   **技术细节**：
        1.  **哨兵 Token 植入**：用户在原始 Prompt 中随机位置插入 $K$ 个哨兵 Token。
        2.  **注意力掩码修改**：构造特殊的 2D 注意力掩码，使哨兵 Token 之间可以相互注意（自回归），但与原始 Prompt Token 互不干扰，确保不影响下游生成结果。
        3.  **隐私混淆**：利用隐私机制（如 SMPC）隐藏哨兵 Token 的位置和内容。
        4.  **验证**：推理完成后，用户将返回的哨兵位置 Logit 与本地预计算的缓存进行匹配。
*   **协议 2：带噪声的 Logit 指纹识别 (Logit Fingerprinting with Noise)**
    *   **核心思想**：针对协议 1 可能遭受的“子集攻击”（提供商只计算部分 Tensor），引入噪声预测机制。
    *   **技术细节**：
        1.  **噪声注入**：在 Token Embedding 中加入随机采样的噪声 $b$。
        2.  **模块训练**：预先训练轻量级的 `NoiseEmbedder`（注入噪声）和 `NoisePredictor`（从隐藏状态预测噪声）。
        3.  **双重验证**：既验证哨兵 Token 的 Logit 指纹，又验证非哨兵位置返回的隐藏状态是否包含正确的噪声特征。

### 3. 实验设计
*   **数据集/场景**：
    *   使用 **FineWeb-Edu** 数据集进行 `NoisePredictor` 的训练和评估。
    *   构建了一个包含 50,000 个序列的 **Logit 缓存**用于指纹匹配实验。
*   **测试模型**：Llama 3.2 (1B, 3B, 8B) 和 Qwen 2.5 (0.5B, 1.5B, 3B, 7B)。
*   **对比方法 (Benchmark)**：
    *   **安全性对比**：模拟了多种攻击手段，包括模型量化（8-bit）、低秩近似（LoRA 风格投影）、单步微调、同系列不同尺寸模型替换。
    *   **性能对比**：与最先进的 ZKP 推理框架 **zkLLM** 进行端到端耗时对比。

### 4. 资源与算力
*   **硬件设备**：实验主要在 **NVIDIA A100 GPU** 上运行。
*   **训练细节**：
    *   对于协议 2，在 FineWeb-Edu 的 40,000 个样本上微调了 500 步（Batch Size 64）。
    *   使用了 AdamW 优化器，学习率为 5e-4。
    *   基础模型权重保持冻结，仅训练极轻量级的线性投影层。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **指纹唯一性实验**：在多个模型家族上进行了 5 万次采样，验证了 Logit 指纹的区分度。
    *   **鲁棒性实验**：覆盖了量化、微调、低秩分解等多种精细的“模型近似”攻击场景。
    *   **性能实验**：测试了从 1 到 100 个 Token 生成长度下的延迟，并考虑了 LAN（局域网）和 WAN（广域网）两种网络环境。
*   **充分性评价**：实验设计较为全面，不仅验证了方法的有效性，还通过模拟多种隐蔽攻击证明了其安全性边界。对比 zkLLM 的实验展示了显著的工程实用价值。

### 6. 论文的主要结论与发现
*   **Logit 是强指纹**：即使是同一家族的模型或经过微调的模型，其 Logit 差异（L1 距离）也远大于硬件非确定性产生的误差（15-20 倍以上差距）。
*   **极高性能提升**：在 Llama-2-7B 上，协议 2 结合 SMPC 框架（SIGMA）的推理速度比 zkLLM 快约 **15 倍**。
*   **验证成本极低**：验证过程仅涉及简单的缓存查找或轻量级线性层计算，对用户端（校验端）几乎无压力。
*   **隐私与验证的协同**：证明了隐私机制提供的“输入不可见性”是实现廉价验证的关键前提。

### 7. 优点
*   **开销极低**：相比 ZKP 动辄数千倍的开销，该方法仅增加几个 Token 的计算量，几乎可以忽略不计。
*   **非侵入性**：不需要修改 LLM 的核心架构，且通过掩码设计保证了不影响模型原始的输出质量。
*   **兼容性强**：协议是通用的，可以与 SMPC、FHE 或 TEE 等多种隐私技术结合使用。

### 8. 不足与局限
*   **统计学保证而非密码学保证**：与 ZKP 提供的确定性数学证明不同，该方法提供的是统计学上的高概率保证，理论上存在极小概率的漏报。
*   **协议 1 的脆弱性**：如果隐私机制保留了 Tensor 结构（如某些 SMPC），协议 1 容易受到子集攻击（只计算部分 Token）。
*   **预训练依赖**：协议 2 需要针对每个模型预先训练噪声预测模块，虽然开销小，但仍需可信第三方参与初始化。
*   **采样挑战**：在温度 $T > 0$ 的随机采样场景下，验证生成结果的正确性仍具有挑战（文中建议结合极简的 ZKP 处理采样步骤）。

（完）
