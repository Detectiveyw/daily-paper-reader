---
title: "Guarding the Middle: Protecting Intermediate Representations in Federated Split Learning"
title_zh: 守护中间层：保护联邦拆分学习中的中间表示
authors: "Obaidullah Zaland, Sajib Mistry, Monowar Bhuyan"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17614v1"
tags: ["query:ppml"]
score: 9.0
evidence: 隐私保护学习方法和联邦拆分学习
tldr: 针对联邦拆分学习（UFSL）中中间表示易泄露隐私的问题，本文提出KD-UFSL框架。该框架结合了k-匿名微聚合和差分隐私技术，旨在减少从客户端传输到服务器的数据泄露。实验证明，该方法在有效抵御数据重构攻击、显著降低重构图像相似度的同时，仍能保持全局模型的预测性能，为大规模大数据应用提供了隐私与效能平衡的解决方案。
selection_source: fresh_fetch
motivation: 联邦拆分学习中传输的中间表示（smashed data）面临被恶意服务器通过重构攻击恢复出原始私有数据的风险。
method: 提出KD-UFSL方案，利用微聚合技术实现k-匿名，并结合差分隐私对中间表示进行扰动处理以增强安全性。
result: "实验表明该方法使重构图像的均方误差提升高达50%，结构相似度降低40%，且有效保留了全局模型的分类性能。"
conclusion: KD-UFSL在显著增强隐私保护能力的同时兼顾了模型实用性，非常适合需要平衡隐私与效能的大规模大数据应用场景。
---

## 摘要
在大数据场景下，海量异构数据集分布于各客户端，这需要可扩展且保护隐私的学习方法。联邦学习 (FL) 能够在不进行数据中心化的情况下，在各客户端之间实现机器学习 (ML) 模型的去中心化训练。然而，去中心化训练给客户端设备带来了计算负担。U型联邦拆分学习 (UFSL) 将部分客户端计算卸载到服务器，同时将数据和标签保留在客户端。然而，客户端与服务器共享的中间表示（即 smashed data）容易泄露客户端的隐私数据。为了减少通过中间数据表示泄露客户端数据的风险，本研究提出了 k-匿名差分隐私 UFSL (KD-UFSL)，该方法利用微聚合和差分隐私等隐私增强技术，最大限度地减少传输至服务器的 smashed data 中的数据泄露。我们首先证明了攻击者可以通过数据重构攻击从中间表示中获取客户端隐私数据，随后提出了一种隐私增强解决方案 KD-UFSL 来降低这一风险。实验结果表明，在四个基准数据集上，KD-UFSL 在某些情况下将原始图像与重构图像之间的均方误差提高了多达 50%，同时将其结构相似性降低了多达 40%。更重要的是，KD-UFSL 在提高隐私性的同时保留了全局模型的效用。这突显了其在必须平衡隐私与效用的大规模大数据应用中的适用性。

## Abstract
Big data scenarios, where massive, heterogeneous datasets are distributed across clients, demand scalable, privacy-preserving learning methods. Federated learning (FL) enables decentralized training of machine learning (ML) models across clients without data centralization. Decentralized training, however, introduces a computational burden on client devices. U-shaped federated split learning (UFSL) offloads a fraction of the client computation to the server while keeping both data and labels on the clients' side. However, the intermediate representations (i.e., smashed data) shared by clients with the server are prone to exposing clients' private data. To reduce exposure of client data through intermediate data representations, this work proposes k-anonymous differentially private UFSL (KD-UFSL), which leverages privacy-enhancing techniques such as microaggregation and differential privacy to minimize data leakage from the smashed data transferred to the server. We first demonstrate that an adversary can access private client data from intermediate representations via a data-reconstruction attack, and then present a privacy-enhancing solution, KD-UFSL, to mitigate this risk. Our experiments indicate that, alongside increasing the mean squared error between the actual and reconstructed images by up to 50% in some cases, KD-UFSL also decreases the structural similarity between them by up to 40% on four benchmarking datasets. More importantly, KD-UFSL improves privacy while preserving the utility of the global model. This highlights its suitability for large-scale big data applications where privacy and utility must be balanced.

---

## 论文详细总结（自动生成）

这篇论文《Guarding the Middle: Protecting Intermediate Representations in Federated Split Learning》针对联邦拆分学习中的隐私泄露问题提出了创新的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：联邦学习（FL）虽然保护了本地数据，但对客户端设备计算压力大；拆分学习（SL）通过模型切分减轻了客户端负担。U型联邦拆分学习（UFSL）结合了两者优点，将模型分为头、身、尾三部分，确保数据和标签均留在客户端。
*   **核心问题**：在UFSL中，客户端发送给服务器的中间表示（称为“粉碎数据”，Smashed Data）并非绝对安全。诚实但好奇的服务器可以利用“数据重构攻击”（Data Reconstruction Attack），通过训练反转网络从这些中间特征中恢复出客户端的原始私有图像。
*   **研究动机**：如何在保持拆分学习计算效率的同时，增强中间表示的隐私性，防止原始数据被恶意重构。

### 2. 方法论：KD-UFSL 框架
论文提出了 **KD-UFSL**（k-Anonymous Differentially Private UFSL），核心思想是结合**数据层面的差分隐私**与**特征层面的k-匿名**：
*   **差分隐私 (DP)**：在客户端进行前向传播前，直接在原始输入数据上添加高斯噪声（Gaussian Noise），实现 $(\epsilon, \delta)$-DP 保护。
*   **k-匿名微聚合 (Microaggregation)**：
    *   服务器将参与的客户端分为若干组，每组至少包含 $k$ 个客户端。
    *   客户端计算出中间特征（Head Network 的输出）后发送给服务器。
    *   服务器对同组内的特征进行**微聚合（取平均值）**。
    *   服务器的 Body Network 处理的是聚合后的特征，这使得单个客户端的贡献在组内 $k$ 个成员中不可区分。
*   **训练流程**：客户端处理 Head 和 Tail 部分，服务器处理中间的 Body 部分。通过微聚合后的特征进行反向传播，更新各部分参数。

### 3. 实验设计
*   **数据集**：使用了四个主流基准数据集：**CIFAR10**（彩色）、**SVHN**（彩色）、**EMNIST**（灰度）、**Fashion-MNIST**（灰度）。
*   **网络架构**：对比了 **ResNet18**、**ResNet50** 以及轻量级 **ConvNet**（4层卷积）。
*   **对比基准 (Baselines)**：
    1.  **Vanilla UFSL**：传统的U型联邦拆分学习（无隐私保护）。
    2.  **UFSL + DP**：仅添加差分隐私。
    3.  **UFSL + KA**：仅应用模型级 k-匿名。
*   **评估指标**：
    *   **隐私性**：均方误差（MSE $\uparrow$ 表示重构越难）、结构相似性指数（SSIM $\downarrow$ 表示重构图像与原图越不像）。
    *   **效用性**：全局模型的分类准确率（Accuracy）。

### 4. 资源与算力
*   **算力资源**：论文明确提到使用了瑞典国家超级计算机中心（NSC）提供的 **Berzelius** 超级计算资源。
*   **硬件细节**：文中**未明确列出**具体的 GPU 型号（如 A100 或 H100）的数量，也未记录具体的训练总时长。

### 5. 实验充分性与客观性
*   **实验规模**：做了多组深入的实验，包括不同数据集的横向对比、不同模型架构的鲁棒性测试。
*   **消融与参数分析**：
    *   研究了噪声强度 $\sigma^2$ 和匿名参数 $k$ 对结果的影响。
    *   探讨了 Head 网络深度（切分层位置）对隐私的影响。
    *   测试了客户端数量（从 5 到 50）的可扩展性。
*   **客观性**：实验涵盖了 RGB 和灰度两种数据类型，并使用了可视化重构图直观展示防御效果，评估指标全面（隐私 vs 效用），实验设计较为公平客观。

### 6. 主要结论与发现
*   **隐私增强显著**：KD-UFSL 相比 Vanilla UFSL，能将重构图像的 MSE 提升高达 50%，SSIM 降低多达 40%，有效阻断了重构攻击。
*   **效用损失微小**：在显著提升隐私的同时，全局模型的准确率仅下降了约 **2% - 2.5%**，证明了该方法在隐私与效用之间的良好平衡。
*   **可扩展性强**：随着客户端数量增加，隐私保护能力和模型准确率保持稳定。
*   **切分层影响**：Head 网络越深（本地计算越多），隐私性越好，但会增加客户端的计算负担。

### 7. 优点与亮点
*   **创新性组合**：首次将模型级 k-匿名（微聚合）引入联邦拆分学习领域，并与差分隐私结合形成双重防御。
*   **防御直观**：通过可视化实验证明，经过 KD-UFSL 处理后，攻击者重构出的图像基本为噪点或模糊块，无法辨认原始物体。
*   **实用性高**：在不显著增加客户端计算负担的前提下（相比纯本地训练），提供了极强的隐私保障。

### 8. 不足与局限
*   **通信协调**：k-匿名微聚合需要服务器对客户端进行分组同步，这可能在网络环境不稳定或客户端掉线（Stragglers）时增加系统复杂性。
*   **数据集差异**：实验显示该方法在彩色数据集（CIFAR10, SVHN）上的表现优于灰度数据集，对于极简特征的数据集可能存在保护效力波动的风险。
*   **攻击模型局限**：主要针对基于反转网络的数据重构攻击，未深入探讨其对成员推理攻击（Membership Inference）或属性推理攻击的防御效果。

（完）
