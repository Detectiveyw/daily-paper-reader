---
title: "LRD-MPC: Efficient MPC Inference through Low-rank Decomposition"
title_zh: LRD-MPC：通过低秩分解实现高效的 MPC 推理
authors: "Tingting Tang, Yongqin Wang, Murali Annavaram"
date: 2026-02-16
pdf: "https://arxiv.org/pdf/2602.14397v1"
tags: ["query:ppml"]
score: 9.0
evidence: 使用秘密共享的深度神经网络MPC推理
tldr: 本文提出LRD-MPC，旨在优化安全多方计算（MPC）在机器学习推理中的效率。针对矩阵乘法带来的巨大开销，该方法利用低秩分解将大矩阵运算简化，并创新性地引入截断跳过和线性层级联技术，有效解决了分解导致的额外通信轮数和精度维护问题。实验表明，该方案在多种协议下显著提升了推理速度并降低了能耗。
selection_source: fresh_fetch
motivation: MPC在执行深度学习推理时，其核心的矩阵乘法操作会产生极高的计算与通信成本。
method: 通过低秩分解简化线性层，并结合截断跳过与线性层级联优化来消除额外的通信和计算开销。
result: "在n-PC和3-PC协议下分别获得最高25%和33%的加速，并减少了高达52%的GPU能耗。"
conclusion: LRD-MPC为提升MPC安全推理性能提供了一种高效且通用的优化框架。
---

## 摘要
安全多方计算（MPC）使互不信任的各方能够在不泄露输入的情况下共同计算一个函数。其在机器学习（ML）中的应用受到了广泛关注，特别是部署在多个云虚拟机（VM）上的安全推理服务，其中每个虚拟机充当一个 MPC 参与方。模型提供者对模型权重进行秘密共享，用户对输入进行秘密共享，从而确保每个服务器仅对随机份额进行操作。虽然 MPC 提供了强大的密码学保证，但它会产生巨大的计算和通信开销。深度神经网络严重依赖卷积层和全连接层，这些层在 MPC 中需要昂贵的矩阵乘法。为了降低这一成本，我们建议对线性层利用低秩分解（LRD），将一个大型矩阵乘法替换为两个较小的矩阵乘法。在 MPC 中，每次矩阵乘法都会产生一轮通信，这意味着将一个矩阵乘法分解为两个会导致额外的一轮通信。其次，增加的矩阵乘法需要额外的截断步骤以维持数值精度。由于截断本身需要通信和计算，这些开销可能会抵消分解带来的收益。为了解决这个问题，我们引入了两种互补的优化技术：截断跳过和高效线性层拼接。截断跳过消除了由 LRD 引起的额外截断，而线性层拼接则通过流水线操作来隐藏额外的通信轮次。这些技术共同减轻了 MPC 中 LRD 的主要开销，并提高了整体效率。我们的方法广泛适用于各种 MPC 协议。实验表明，与全秩基准相比，该方法在 n-PC 协议中实现了高达 25% 的加速，在 3-PC 协议中实现了 33% 的加速，同时节省了高达 52% 的 GPU 能耗，并减少了 88% 的离线阶段延迟。

## Abstract
Secure Multi-party Computation (MPC) enables untrusted parties to jointly compute a function without revealing their inputs. Its application to machine learning (ML) has gained significant attention, particularly for secure inference services deployed across multiple cloud virtual machines (VMs), where each VM acts as an MPC party. Model providers secret-share model weights, and users secret-share inputs, ensuring that each server operates only on random shares. While MPC provides strong cryptographic guarantees, it incurs substantial computational and communication overhead. Deep neural networks rely heavily on convolutional and fully connected layers, which require costly matrix multiplications in MPC. To reduce this cost, we propose leveraging low-rank decomposition (LRD) for linear layers, replacing one large matrix multiplication with two smaller ones. Each matrix multiplication in MPC incurs a round of communication, meaning decomposing one matrix multiplication into two leads to an additional communication round. Second, the added matrix multiplication requires an additional truncation step to maintain numerical precision. Since truncation itself requires communication and computation, these overheads can offset the gains from decomposition. To address this, we introduce two complementary optimizations: truncation skipping and efficient linear layer concatenation. Truncation skipping removes the extra truncation induced by LRD, while linear layer concatenation pipelines operations to hide the additional communication round. Together, these techniques mitigate the main overheads of LRD in MPC and improve overall efficiency. Our approach is broadly applicable across MPC protocols. Experiments show up to 25% speedup in n-PC and 33% in 3-PC protocols over full-rank baselines, along with up to 52% GPU energy savings and 88% reduction in offline-phase latency.

---

## 论文详细总结（自动生成）

这是一份关于论文《LRD-MPC: Efficient MPC Inference through Low-rank Decomposition》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在安全多方计算（MPC）环境下，深度学习推理的线性层（卷积和全连接层）面临极高的计算和通信开销。
*   **研究动机**：
    *   MPC 中的矩阵乘法比明文运算复杂得多（$n$-PC 协议下乘法量增加约 3 倍），且每层都需要通信轮次和截断（Truncation）操作来维持定点数精度。
    *   虽然**低秩分解（LRD）**在明文模型中能有效减少计算量，但在 MPC 中直接应用会产生副作用：将一个大矩阵乘法分解为两个小矩阵乘法会**增加一轮通信**和**额外的截断步骤**，这些开销往往会抵消计算量减少带来的收益。
    *   本文旨在通过优化手段，使低秩分解在 MPC 环境下真正实现性能提升。

### 2. 方法论
LRD-MPC 提出了两种核心优化技术来解决低秩分解带来的额外开销：

*   **低秩分解应用**：将权重矩阵 $W$ 分解为两个较小的矩阵 $U$ 和 $V$（即 $W \approx U \odot V$），将一次大矩阵乘法转变为两次连续的小矩阵乘法。
*   **截断跳过（Truncation Skipping）**：
    *   **核心思想**：跳过第一次小矩阵乘法后的截断步骤，仅在第二次乘法后进行一次总的截断。
    *   **技术细节**：在定点数运算中，三次输入相乘会累积 $3f$ 的精度位。通过在最后执行一次 $2f$ 位的截断（$\Pi_{2fTrunc}$），可以直接获得正确的精度结果。
    *   **收益**：由于 MPC 截断协议的成本与截断位数无关，此举直接消除了一轮通信和相关的计算开销，且不增加最后一步的成本。
*   **高效线性层级联（Efficient Linear Layer Concatenation）**：
    *   **核心思想**：利用低秩分解产生的两个连续线性层，通过流水线（Pipelining）技术重叠计算与通信。
    *   **技术细节**：
        *   在 **3-PC (Trio)** 协议中，利用参与方之间计算任务的不对称性，让计算较快的参与方在等待通信时提前开始下一层的计算。
        *   在 **$n$-PC** 协议中，利用模型权重（静态数据）的独立性，将第二层权重的广播与第一层的局部计算重叠，从而隐藏通信延迟。

### 3. 实验设计
*   **数据集与场景**：
    *   **视觉任务**：CIFAR10（WideResNet18）、ImageNet（VGG19）。
    *   **图神经网络**：Cora（GCN）。
    *   **自然语言处理**：IMDb（BERT LARGE）。
*   **Benchmark（基准）**：全秩（Full-rank）模型的 MPC 推理性能。
*   **对比协议**：
    *   **$n$-PC 协议**：基于 CrypTen 框架实现。
    *   **3-PC 协议**：基于 Trio 协议和 PIGEON 框架实现。
*   **网络环境**：模拟了 LAN（10Gbps）、MAN（5Gbps, 5ms 延迟）和 WAN（5Gbps, 35ms 延迟）三种场景。

### 4. 资源与算力
*   **硬件配置**：
    *   **CPU**：AMD EPYC 7502。
    *   **GPU**：Nvidia Quadro RTX 5000。
*   **软件框架**：CrypTen（用于 $n$-PC）和 PIGEON（用于 3-PC）。
*   **训练/推理时长**：文中未明确给出总训练时长（因为主要关注推理阶段），但详细记录了各模型的在线/离线推理延迟。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了四种主流模型架构（CNN、GCN、Transformer），并在两种不同的 MPC 协议族（$n$-PC 和 3-PC）上进行了验证。
*   **消融实验**：分别展示了“仅低秩（LR）”、“低秩+截断跳过（LR+TS）”以及“完整优化（Combined）”的效果，清晰地证明了每项技术的贡献。
*   **充分性评价**：实验设计较为全面，考虑了不同网络带宽和延迟对 MPC 性能的巨大影响，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：LRD-MPC 在 $n$-PC 协议下实现高达 **25%** 的加速，在 3-PC 协议下实现 **33%** 的加速。
*   **能效比**：显著降低了 GPU 能耗，最高可节省 **52%** 的在线阶段功耗。
*   **离线阶段优化**：由于减少了乘法总数，离线阶段（生成 Beaver 三元组）的延迟降低了高达 **88%**。
*   **精度保持**：截断跳过对模型精度的影响极小，所有测试模型的精度损失均控制在 **0.5%** 以内。

### 7. 优点
*   **通用性强**：提出的优化技术（截断跳过和层级联）不依赖于特定 MPC 协议，可广泛应用于各种基于秘密共享的框架。
*   **多维度优化**：不仅关注推理延迟，还深入分析了 MPC 环境下极易被忽视的能耗问题和离线预处理成本。
*   **软硬件结合视角**：通过流水线设计巧妙地解决了 MPC 协议在现代硬件（GPU）上的通信瓶颈。

### 8. 不足与局限
*   **秩的选择（Rank Selection）**：论文中低秩分解的秩比例（如 0.5 或 0.25）是预设的经验值，缺乏一种自动搜索最优秩与精度平衡点的机制。
*   **精度风险**：虽然实验中精度损失较小，但截断跳过理论上会减少中间层的整数位表示范围，对于某些对动态范围极其敏感的模型，可能会增加溢出风险。
*   **应用限制**：主要针对线性层占比较高的模型，对于非线性层（如 ReLU、Softmax）占主导的模型，提升效果会受限。

（完）
