---
title: Integrating Homomorphic Encryption and Synthetic Data in FL for Privacy and Learning Quality
title_zh: 在联邦学习中集成同态加密与合成数据以提升隐私与学习质量
authors: "Yenan Wang, Carla Fabiana Chiasserini, Elad Michael Schiller"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02969v1"
tags: ["query:ppml"]
score: 9.0
evidence: 联邦学习中的同态加密与合成数据
tldr: "本研究针对联邦学习中同态加密（HE）带来的高计算开销与模型质量平衡难题，提出了Alt-FL方案。该方案通过交替执行基于真实数据的加密训练轮次和基于合成数据的明文训练轮次，在保障隐私的同时引入合成数据增强学习效果。实验表明，Alt-FL能有效抵御数据泄露攻击，相比现有方案提升了13.4%的准确率，并降低了48%的加密开销，实现了隐私、性能与成本的优化平衡。"
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中同态加密导致的高资源消耗与模型学习质量之间的矛盾。
method: 提出Alt-FL框架，通过交替使用真实数据（加密传输）和合成数据（明文传输）进行模型训练。
result: "实验显示该方法比Selective HE准确率高出13.4%，且同态加密相关成本降低了约48%。"
conclusion: Alt-FL在增强隐私保护的同时，显著提升了联邦学习的训练效率和模型性能。
---

## 摘要
联邦学习 (FL) 能够在不共享敏感客户端数据的情况下实现机器学习模型的协作训练，使其成为隐私敏感型应用的核心。然而，FL 面临着在确保学习质量和稳健隐私保护的同时降低资源消耗的双重挑战，尤其是在使用同态加密 (HE) 等计算开销巨大的技术时。在本研究中，我们通过集成合成数据生成和交替策略，增强了基于 HE 的隐私保护 FL 流程。具体而言，我们提出的解决方案名为交替联邦学习 (Alt-FL)，它在基于真实数据的本地训练（真实轮次）与基于合成数据的本地训练（合成轮次）之间交替进行，并分别在真实轮次和合成轮次中传输加密和明文形式的模型参数。我们的方法通过合成数据增强的数据集提升了学习质量（如模型准确率），利用 HE 保护了客户端数据隐私，并通过交替策略使加解密成本保持在可控范围内。我们针对 DLG 攻击等数据泄露攻击评估了该方案，证明了其稳健的隐私保护能力。此外，与选择性 HE (Selective HE) 相比，Alt-FL 的模型准确率提升了 13.4%，且 HE 相关成本降低了高达 48%。

## Abstract
Federated learning (FL) enables collaborative training of machine learning models without sharing sensitive client data, making it a cornerstone for privacy-critical applications. However, FL faces the dual challenge of ensuring learning quality and robust privacy protection while keeping resource consumption low, particularly when using computationally expensive techniques such as homomorphic encryption (HE). In this work, we enhance an FL process that preserves privacy using HE by integrating it with synthetic data generation and an interleaving strategy. Specifically, our solution, named Alternating Federated Learning (Alt-FL), consists of alternating between local training with authentic data (authentic rounds) and local training with synthetic data (synthetic rounds) and transferring the encrypted and plaintext model parameters on authentic and synthetic rounds (resp.). Our approach improves learning quality (e.g., model accuracy) through datasets enhanced with synthetic data, preserves client data privacy via HE, and keeps manageable encryption and decryption costs through our interleaving strategy. We evaluate our solution against data leakage attacks, such as the DLG attack, demonstrating robust privacy protection. Also, Alt-FL provides 13.4% higher model accuracy and decreases HE-related costs by up to 48% with respect to Selective HE.

---

## 论文详细总结（自动生成）

以下是对论文《Integrating Homomorphic Encryption and Synthetic Data in FL for Privacy and Learning Quality》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：联邦学习（FL）在保护隐私的同时面临三大挑战：**学习质量（准确率）**、**隐私保护强度**与**资源消耗（计算和带宽）**之间的矛盾。
*   **研究动机**：
    *   虽然FL不共享原始数据，但共享的模型梯度仍可能通过“深度梯度泄露”（DLG）攻击恢复出原始图像。
    *   同态加密（HE）能有效防御此类攻击，但其计算开销巨大且会导致密文膨胀。
    *   合成数据虽能提升模型性能，但若简单叠加在HE流程中，会进一步增加训练负担。
*   **目标**：设计一种既能利用HE保护隐私，又能通过合成数据提升质量，同时显著降低资源开销的FL框架。

### 2. 方法论：Alt-FL 框架
*   **核心思想**：提出**交替联邦学习（Alt-FL）**策略，将训练过程分为“真实轮次”和“合成轮次”交替进行。
*   **关键技术细节**：
    *   **真实轮次（Authentic Rounds）**：客户端使用本地真实敏感数据训练。为了保护隐私，模型参数使用**选择性同态加密（Selective HE）**进行加密后上传至服务端。
    *   **合成轮次（Synthetic Rounds）**：客户端使用非敏感的合成数据训练。由于合成数据不含隐私信息，模型参数以**明文（Plaintext）**形式传输，从而省去加解密开销。
    *   **可调比例 $\rho$**：引入参数 $\rho$（合成轮次占总轮次的比例），允许系统根据资源约束和性能需求动态调整。
    *   **算法流程**：服务端根据 $\rho$ 决定当前轮次类型；客户端根据轮次类型选择数据集并决定是否加密；服务端进行聚合（加密或明文聚合）并广播。

### 3. 实验设计
*   **数据集与场景**：
    *   使用 **CIFAR-10** 图像分类数据集。
    *   设置 3 个客户端，通过狄利克雷分布（$\alpha=0.5$）模拟非独立同分布（Non-IID）的真实数据分布。
    *   **合成数据生成**：为简化流程，将原始训练集对半拆分，一半视为真实数据，另一半视为IID的合成数据。
*   **Benchmark（基准方法）**：
    *   **FE (Full Encryption)**：全加密方案（$\eta=1, \rho=0$），所有轮次均全量加密。
    *   **S-HE (Selective HE)**：选择性加密方案（$\eta=0.2, \rho=0$），所有轮次仅加密部分敏感参数。
*   **评估指标**：模型准确率、隐私鲁棒性（DLG攻击下的图像相似度指标：UQI, MSSSIM, VIF）、执行时间、传输带宽。

### 4. 资源与算力
*   **硬件环境**：实验在 **Intel Xeon Platinum 8358 CPU** 上运行，每个训练进程分配 4 个核心。
*   **GPU使用**：论文中**未明确提到使用 GPU**，主要侧重于 CPU 上的计算开销评估（这符合 HE 研究通常关注 CPU 瓶颈的特征）。
*   **训练时长**：未给出具体总时长，但记录了单次加解密的时间（约 0.01s-0.07s 级别）和收敛所需的轮数（约 77-92 轮）。

### 5. 实验数量与充分性
*   **实验规模**：每种配置（不同的加密比例 $\eta$ 和交替比例 $\rho$）均独立运行 **10 次**。
*   **数据处理**：去除了最高和最低的异常值后取平均值，并使用滑动窗口（窗口大小 12）平滑准确率曲线以判定收敛。
*   **充分性评价**：实验覆盖了隐私攻击测试、准确率对比和资源消耗分析，逻辑闭环。但由于客户端数量较少（3个）且合成数据生成方式较为简化，其在更大规模异构网络下的表现仍有待验证。

### 6. 主要结论与发现
*   **准确率提升**：Alt-FL 相比于传统的 S-HE 方案，模型准确率提升了 **13.4%**，这归功于合成数据对非IID数据集的平衡作用。
*   **开销降低**：通过在合成轮次使用明文传输，Alt-FL 将 HE 相关的加解密计算开销降低了约 **48%**。
*   **隐私保护**：实验证明，即使合成轮次使用明文，攻击者也无法从合成模型中恢复出真实图像，隐私保护水平与全加密方案相当。
*   **通信权衡**：虽然 Alt-FL 达到收敛所需的总轮数略有增加（约增加 20%），但由于单轮密文传输量大幅减少，总密文传输量反而下降了 **39.1%**。

### 7. 亮点
*   **策略新颖**：首次提出在 FL 中通过“数据类型”与“加密策略”交替挂钩的方法，巧妙绕过了 HE 的性能瓶颈。
*   **双重收益**：合成数据不仅被用作性能增强工具，还被用作“隐私安全”的通信载体，实现了功能复用。
*   **开源支持**：提供了 GitHub 代码实现，增强了研究的可重复性。

### 8. 不足与局限
*   **合成数据假设**：实验中的合成数据是从原始真实数据集中拆分出来的，这在现实中可能难以获得。实际应用中，生成高质量且不泄露隐私的合成数据本身也需要计算资源。
*   **规模受限**：仅在 3 个客户端的小规模环境下验证，未探讨在大规模（如成千上万客户端）场景下的同步与调度问题。
*   **攻击模型单一**：主要针对 DLG 攻击，未深入探讨针对合成数据轮次的更高级推理攻击或成员推理攻击。
*   **收敛延迟**：交替策略导致收敛轮数增加，在对实时性要求极高的场景下可能存在限制。

（完）
