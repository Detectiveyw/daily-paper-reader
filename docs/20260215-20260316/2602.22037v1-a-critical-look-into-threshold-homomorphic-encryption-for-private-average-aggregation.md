---
title: A Critical Look into Threshold Homomorphic Encryption for Private Average Aggregation
title_zh: 对用于隐私平均聚合的阈值同态加密的深入审视
authors: "Miguel Morona-Mínguez, Alberto Pedrouzo-Ulloa, Fernando Pérez-González"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.22037v1"
tags: ["query:ppml"]
score: 9.0
evidence: 用于隐私聚合的门限同态加密
tldr: 本研究探讨了阈值同态加密（Threshold HE）在联邦学习私有平均聚合中的应用及其安全性。针对主流HE库中协作解密过程可能遭受的解密预言机攻击，论文评估了引入大方差“涂抹噪声”作为防御措施对系统性能的影响。通过对基于RLWE的BFV和CKKS阈值变体进行详细对比，研究证明了在满足安全需求的前提下，CKKS方案在聚合任务中表现出与BFV相当的性能，为联邦学习提供了更安全的加密选择。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中阈值同态加密方案因协作解密机制而面临的安全漏洞问题。
method: 调研了基于RLWE的阈值加密技术，并重点分析了采用涂抹噪声技术抵御攻击时的性能开销。
result: 实验结果显示，在联邦平均聚合场景下，CKKS阈值方案的执行效率与BFV方案基本持平。
conclusion: 阈值HE方案在加强安全性（如添加涂抹噪声）后，CKKS仍是实现高效联邦学习聚合的有力竞争者。
---

## 摘要
阈值同态加密（Threshold HE）非常适合实现隐私联邦平均聚合，这是联邦学习（FL）中的一项关键操作。尽管具有潜力，但最近的研究表明，如果攻击者能够访问受限解密预言机，主流同态加密库中提供的阈值方案可能会引入意外的安全漏洞。该预言机反映了联邦学习客户端在不知道密钥的情况下协作解密聚合结果的能力。本研究调查了基于 RLWE 的阈值同态加密在联邦平均聚合中的应用，并研究了使用大方差涂抹噪声作为防御措施对性能的影响。我们对 BFV 和 CKKS 的阈值变体进行了详细比较，发现基于 CKKS 的聚合性能与基于 BFV 的解决方案相当。

## Abstract
Threshold Homomorphic Encryption (Threshold HE) is a good fit for implementing private federated average aggregation, a key operation in Federated Learning (FL). Despite its potential, recent studies have shown that threshold schemes available in mainstream HE libraries can introduce unexpected security vulnerabilities if an adversary has access to a restricted decryption oracle. This oracle reflects the FL clients' capacity to collaboratively decrypt the aggregated result without knowing the secret key. This work surveys the use of threshold RLWE-based HE for federated average aggregation and examines the performance impact of using smudging noise with a large variance as a countermeasure. We provide a detailed comparison of threshold variants of BFV and CKKS, finding that CKKS-based aggregations perform comparably to BFV-based solutions.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Critical Look into Threshold Homomorphic Encryption for Private Average Aggregation》的结构化深入分析报告：

### 1. 核心问题与研究背景
*   **研究动机**：在联邦学习（FL）中，中央聚合器通常能接触到所有客户端的原始模型更新，这带来了严重的隐私风险。虽然同态加密（HE）可以解决此问题，但传统的单密钥 HE 依赖于不可信的第三方或强假设。
*   **核心问题**：阈值同态加密（Threshold HE）允许客户端协作解密聚合结果，但在最新的密码学研究中，这种“协作解密”被证明存在安全漏洞。如果攻击者能访问解密结果（即解密预言机），可能通过密钥恢复攻击获取客户端私钥。
*   **研究目标**：本文旨在探讨如何在满足更严苛的 **IND-CPA D** 安全模型（即抵御基于解密反馈的攻击）下，安全且高效地利用 BFV 和 CKKS 两种主流加密方案执行隐私平均聚合。

### 2. 方法论
*   **核心思想**：在协作解密阶段引入**涂抹噪声（Smudging Noise）**。通过添加一个方差极大（与安全参数 $\lambda$ 相关）的统计噪声，掩盖解密过程中泄露的 RLWE 内部误差，从而阻断密钥恢复攻击。
*   **关键技术细节**：
    *   **多方协议流程**：包括设置阶段（生成集体公钥）、输入阶段（客户端加密更新）、评估阶段（聚合器执行同态加法）和输出阶段（协作解密）。
    *   **安全对策**：在解密份额生成时，每个客户端 $C_i$ 采样涂抹噪声 $e_{smg,i}$。根据 Smudging Lemma，为了达到 $\lambda$ 位安全性，涂抹噪声的界限需设为 $B_{smg} \approx 2^{\lambda/2} B_{ct}$。
    *   **方案对比**：对比了处理精确整数运算的 **BFV** 方案和处理近似浮点数运算的 **CKKS** 方案在阈值设置下的表现。
*   **公式逻辑**：论文推导了满足解密正确性所需的最小密文模数 $q$。对于 CKKS，其近似误差被重新定义为包含涂抹噪声的部分，并以此计算其相对于 BFV 的密文扩张率。

### 3. 实验设计
*   **场景设定**：跨孤岛联邦学习（Cross-silo FL），客户端数量 $L$ 在 8 到 128 之间，模型参数量设定为 1,638,400。
*   **Benchmark 与对比方法**：
    *   **对比对象**：阈值 BFV (MBFV) 与 阈值 CKKS (MCKKS)。
    *   **评估指标**：密文模数 $q$ 的大小（决定了计算和通信开销）、位精度（Bit precision）、各步骤运行时间（加密、聚合、协作解密）。
*   **实现工具**：使用 Go 语言编写的 **Lattigo** 同态加密库。

### 4. 资源与算力
*   **硬件环境**：Intel Core i7-10750H CPU @ 2.60GHz（单线程测试）。
*   **内存**：31.1 GB RAM。
*   **算力消耗**：实验未涉及大规模 GPU 训练，侧重于加密运算的 CPU 耗时。例如，在 16,384 维向量下，一次完整的隐私聚合协议（含加密、聚合、解密）总耗时在 3.9 秒到 8.7 秒之间（取决于参数集）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   通过解析推导给出了 $q$ 的理论下界。
    *   绘制了多组对比曲线，涵盖了不同的安全参数 $\lambda \in \{32, 64, 96, 128\}$ 和客户端数量 $L \in \{8, 128\}$。
    *   提供了 3 组典型参数下的详细运行时间表。
*   **充分性评价**：实验在理论分析和基础性能基准测试上非常充分。它客观地展示了在极端安全需求（大方差噪声）下两种方案的权衡，但未在真实的分布式网络环境（考虑带宽延迟）中进行测试。

### 6. 主要结论与发现
*   **打破固有认知**：此前学界普遍认为 CKKS 在 IND-CPA D 模型下由于需要巨大的涂抹噪声会导致精度丧失或效率极低。本文证明，在联邦平均聚合场景下，**CKKS 的性能实际上与 BFV 相当**，甚至在高精度需求下表现更好。
*   **密文扩张**：当要求的位精度提高时，CKKS 的模数 $q$ 增长速度在某些区间内优于 BFV。
*   **额外隐私收益**：CKKS 的近似性质天然地在结果中引入了噪声，这可以被视为一种“免费”的差分隐私（DP）保护，有助于抵御针对聚合结果的推理攻击。

### 7. 优点
*   **安全性前瞻**：紧跟密码学前沿，针对最新的 IND-CPA D 安全漏洞提出了实用的 FL 部署建议。
*   **深入的对比分析**：不仅停留在理论，还通过 Lattigo 库给出了具体的参数配置和运行耗时，对工程实践有指导意义。
*   **视角独特**：指出了 CKKS 近似误差在隐私保护上的潜在正面作用。

### 8. 不足与局限
*   **功能局限**：研究仅限于“平均聚合”（加法），未探讨更复杂的非线性聚合（如中位数聚合或拜占庭容错聚合），后者在 HE 下的开销会显著增加。
*   **通信开销未量化**：虽然分析了密文模数 $q$（间接反映通信量），但未直接给出在不同网络带宽下的通信延迟实验数据。
*   **应用范围**：主要针对跨孤岛（Cross-silo）场景，对于资源受限的移动设备（Cross-device）场景，大方差噪声带来的计算负担可能依然过重。

（完）
