---
title: Efficient Multi-Party Secure Comparison over Different Domains with Preprocessing Assistance
title_zh: 预处理辅助下跨不同域的高效多方安全比较
authors: "Kaiwen Wang, Xiaolin Chang, Yuehan Dong, Ruichen Zhang"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19604v1"
tags: ["query:ppml"]
score: 9.0
evidence: 用于机器学习和数据分析的多方安全比较
tldr: 本研究针对多方计算中安全比较协议预处理开销大的瓶颈，提出了一种利用非合谋辅助方（Dealer）的高效n方LTBits和MSB提取协议。该方案支持有限域和环域，通过充分利用辅助方生成的关联随机性优化在线阶段，并采用黑盒构造确保了跨后端的可移植性。实验表明，该协议在性能上显著优于现有最先进框架，为隐私保护机器学习等应用提供了更实用的比较原语。
selection_source: fresh_fetch
motivation: 现有安全比较协议的预处理阶段成本极高，且缺乏针对辅助方模式的在线阶段优化及跨域通用性。
method: 提出基于辅助方的黑盒构造协议，在有限域实现常数轮在线复杂度，在环域实现对数级轮数复杂度。
result: 实验结果显示，该协议相比现有最先进的MPC框架实现了1.79倍至19.4倍的性能提升。
conclusion: 该研究通过优化关联随机性的利用，显著提升了多方安全比较的效率，为大规模隐私计算提供了高性能基础组件。
---

## 摘要
安全比较是多方计算（MPC）中的一种基础原语，支持机器学习和数据分析等隐私保护应用。比较协议的一个关键性能瓶颈在于其预处理阶段，这主要是由于生成必要的关联随机性（correlated randomness）的成本很高。近期的框架引入了一个被动的、非共谋的辅助方（dealer）来加速预处理。然而，仍存在两个关键问题：首先，现有的辅助方辅助方法仅将辅助方视为传统预处理的直接替代品，而没有重新设计比较协议以优化在线阶段；其次，大多数协议专门针对特定的代数域、对手模型或参与方配置，缺乏广泛的通用性。在这项工作中，我们提出了首个在 $\mathbb{F}_p$ 和 $\mathbb{Z}_{2^k}$ 上均可运行的辅助方辅助 $n$ 方 LTBits（Less-Than-Bits）和 MSB（最高有效位）提取协议，并在协议层面实现了完美安全性。通过充分利用辅助方生成丰富关联随机性的能力，我们的 $\mathbb{F}_p$ 构造实现了常数轮在线复杂度，而 $\mathbb{Z}_{2^k}$ 构造实现了具有可调分支因子的 $O(\log_n k)$ 轮复杂度。所有协议均通过扩展的 ABB 模型表述为黑盒构造，确保了在不同 MPC 后端和对手模型之间的可移植性。实验结果表明，与最先进的 MPC 框架相比，该协议实现了 $1.79\times$ 到 $19.4\times$ 的加速，突显了我们的协议在比较密集型 MPC 应用中的实用性。

## Abstract
Secure comparison is a fundamental primitive in multi-party computation, supporting privacy-preserving applications such as machine learning and data analytics. A critical performance bottleneck in comparison protocols is their preprocessing phase, primarily due to the high cost of generating the necessary correlated randomness. Recent frameworks introduce a passive, non-colluding dealer to accelerate preprocessing. However, two key issues still remain. First, existing dealer-assisted approaches treat the dealer as a drop-in replacement for conventional preprocessing without redesigning the comparison protocol to optimize the online phase. Second, most protocols are specialized for particular algebraic domains, adversary models, or party configurations, lacking broad generality. In this work, we present the first dealer-assisted $n$-party LTBits (Less-Than-Bits) and MSB (Most Significant Bit) extraction protocols over both $\mathbb{F}_p$ and $\mathbb{Z}_{2^k}$, achieving perfect security at the protocol level. By fully exploiting the dealer's capability to generate rich correlated randomness, our $\mathbb{F}_p$ construction achieves constant-round online complexity and our $\mathbb{Z}_{2^k}$ construction achieves $O(\log_n k)$ rounds with tunable branching factor. All protocols are formulated as black-box constructions via an extended ABB model, ensuring portability across MPC backends and adversary models. Experimental results demonstrate $1.79\times$ to $19.4\times$ speedups over state-of-the-art MPC frameworks, highlighting the practicality of our protocols for comparison-intensive MPC applications.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Multi-Party Secure Comparison over Different Domains with Preprocessing Assistance》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在多方安全计算（MPC）中，**安全比较**（如判断 $x < y$）是性能瓶颈。其主要开销源于预处理阶段生成复杂的关联随机性（如 daBits, edaBits）。
*   **研究动机**：
    *   **辅助方利用不足**：虽然近期研究引入了“被动辅助方（Dealer）”来加速预处理，但大多只是将其作为传统预处理的简单替代，未针对辅助方的能力重新设计在线协议。
    *   **缺乏通用性**：现有协议往往绑定特定的代数域（如仅支持素数域 $\mathbb{F}_p$ 或仅支持环 $\mathbb{Z}_{2^k}$）、特定的参与方数量或特定的安全模型（被动/主动）。
*   **整体含义**：本文旨在通过充分挖掘辅助方的预处理能力，重新设计在不同代数域下通用的、高性能的 $n$ 方安全比较协议。

### 2. 论文提出的方法论
论文通过扩展的**算术黑盒（ABB）模型**，提出了两种核心协议：

*   **核心思想**：利用辅助方生成“更丰富、更复杂”的关联随机性（如随机掩码的幂序列、多输入门预处理数据），从而极大地压缩在线阶段的通信轮数。
*   **关键技术细节**：
    *   **$\mathbb{F}_p$ 域协议 ($\Pi_{MSB}^p$)**：采用基于多项式的构造。辅助方在预处理阶段生成随机掩码的算术和位分解，并提供幂序列。在线阶段通过一次揭露（Open）和本地多项式求值，将复杂度降至**常数轮（2轮）**。
    *   **$\mathbb{Z}_{2^k}$ 域协议 ($\Pi_{MSB}^{2^k}$)**：针对环环境，提出**多输入 AND 门优化**。辅助方预先生成 $2^m-1$ 个秘密份额来支持 $m$ 输入 AND 门。通过构建具有可调分支因子 $n$ 的前缀 AND 树，将在线轮数优化至 $O(\log_n k)$。
    *   **完美安全性**：由于辅助方提供了完全一致的算术/位分解，消除了 edaBits 等方案中的统计误差，在协议层面实现了**完美安全性（Perfect Security）**。

### 3. 实验设计
*   **Benchmark（基准）**：对比了目前最先进的（SOTA）通用安全比较框架 **Rabbit** (Makri et al., 2021)。
*   **实验场景**：
    *   **网络环境**：模拟了 LAN（10Gbps 带宽, 1ms 延迟）和 WAN（100Mbps 带宽, 100ms 延迟）。
    *   **安全模型**：涵盖了被动安全（Semi-honest）和主动安全（Malicious）。
    *   **代数域**：测试了不同位宽的素数域（16/31/61 bit）和环（32/64 bit）。
*   **对比指标**：在线运行时间（Runtime）和通信量（Communication Cost）。

### 4. 资源与算力
*   **硬件环境**：使用了一台运行 Ubuntu 20.04 的服务器，配置为 **AMD Ryzen Threadripper 3970X** (32核/64线程，2.2GHz)，内存为 **251GB RAM**。
*   **软件实现**：使用 C++20 编写，基于类似 MD-ML 的模块化架构实现辅助方预处理逻辑。
*   **训练/执行时长**：文中未提及具体的训练时长（因为是推理/计算协议而非模型训练），但详细记录了单次及批量比较操作的毫秒级/秒级响应时间。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 $\mathbb{F}_p$ 做了 3 种不同素数规模、2 种安全模型、2 种网络环境下的批量实验（10 到 10,000 次比较）。
    *   针对 $\mathbb{Z}_{2^k}$ 做了不同分支因子 $n$（从 2 到 10）的消融实验，以寻找不同网络下的最优参数。
*   **充分性评价**：实验设计非常**充分且客观**。它不仅对比了总时长，还深入分析了分支因子对性能的影响，并明确指出了在何种场景（如大素数域、大批量）下本方案可能不如基准方案，体现了学术严谨性。

### 6. 论文的主要结论与发现
*   **显著加速**：在 $\mathbb{F}_p$ 域下，尤其在 WAN 环境和小批量场景中，比 Rabbit 快 **1.79x 到 19.4x**。
*   **轮数优势**：证明了在辅助方模式下，可以将 $\mathbb{F}_p$ 上的比较压缩至 2 轮，这在延迟较高的网络（WAN）中具有决定性优势。
*   **最优分支因子**：发现 $\mathbb{Z}_{2^k}$ 协议的最优分支因子 $n$ 随网络环境变化：WAN 环境下由于延迟高，更大的 $n$（如 8）更优；LAN 环境下 $n=5$ 左右性能最佳。
*   **安全性提升**：在提供高性能的同时，实现了比 Rabbit（在 $\mathbb{F}_p$ 上仅具统计安全性）更高的完美安全性。

### 7. 优点
*   **理论突破**：首次在辅助方模式下实现了常数轮的 $n$ 方 LTBits 协议。
*   **高度通用**：通过 ABB 黑盒模型，协议可以无缝迁移到不同的 MPC 后端（如 SPDZ, TinyOT）。
*   **实用性强**：针对现实中的网络延迟（WAN）进行了深度优化，非常适合跨地域的隐私计算任务。
*   **参数可调**：$\mathbb{Z}_{2^k}$ 协议的分支因子可根据带宽和延迟灵活配置。

### 8. 不足与局限
*   **辅助方信任假设**：虽然辅助方是被动且非合谋的，但在某些严苛的去中心化场景下，引入额外的辅助实体可能会增加系统复杂性和信任负担。
*   **存储开销**：为了换取在线阶段的极速，辅助方需要生成并分发较大量的预处理数据（尤其是 $\mathbb{Z}_{2^k}$ 协议中随分支因子指数增长的数据），这可能增加预处理阶段的存储和通信压力。
*   **计算密集度**：在 $\mathbb{F}_p$ 协议中，本地多项式求值的计算量随位宽增加，在大规模位宽（如 64-bit 以上）且网络极快的环境下，计算开销可能会抵消通信轮数减少带来的收益。

（完）
