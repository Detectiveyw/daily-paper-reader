---
title: Towards Privacy-Preserving LLM Inference via Collaborative Obfuscation (Technical Report)
title_zh: 迈向通过协作混淆实现隐私保护的大语言模型推理（技术报告）
authors: "Yu Lin, Qizhi Zhang, Wenqiang Ruan, Daode Zhang, Jue Hong, Ye Wu, Hanning Xia, Yunlong Mao, Sheng Zhong"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01499v1"
tags: ["query:fss-llm"]
score: 9.0
evidence: 通过协作混淆实现隐私保护的LLM推理
tldr: 针对云端大模型推理中的隐私泄露风险，本文提出AloePri，这是首个面向工业应用的隐私保护推理方法。它通过协变混淆技术共同转换数据和模型参数，在保证推理精度和效率的同时，实现了与现有基础设施的完全兼容。在Deepseek-V3.1-Terminus（671B参数）上的实验证明，该方法在保持极低精度损失和等同于明文推理效率的前提下，能有效抵御先进的隐私攻击。
selection_source: fresh_fetch
motivation: 现有的隐私保护推理方法难以同时满足低精度损失、高效率、异构硬件支持和现有基础设施兼容性这四个工业级核心需求。
method: 提出AloePri方法，利用协变混淆技术对输入输出数据及模型参数进行联合转换，确保在不改变现有推理架构的情况下保护隐私。
result: "在671B参数的大模型上实现了0.0%至3.5%的极低精度损失，推理效率与明文相当，且仅有不到5%的Token能被攻击者恢复。"
conclusion: AloePri是首个在真实工业系统和超大规模模型中展现出实用性的隐私保护LLM推理方案。
---

## 摘要
大语言模型（LLMs）的快速发展推动了基于云的 LLM 推理服务的广泛采用，同时也带来了与远程推理中私有数据传输和处理相关的显著隐私风险。为了使隐私保护 LLM 推理技术在工业场景中得到实际应用，必须同时满足三个核心要求：（1）应尽量减少准确性和效率损失，以减轻服务体验的下降；（2）推理过程可以在由异构遗留 xPU 组成的大规模集群上运行；（3）应确保与现有 LLM 基础设施的兼容性，以复用其工程优化。据我们所知，现有的隐私保护 LLM 推理方法中，没有一种能在提供有意义的隐私保证的同时满足上述所有约束。在本文中，我们提出了 AloePri，这是首个面向工业应用的隐私保护 LLM 推理方法。AloePri 通过协变混淆（covariant obfuscation）保护输入和输出数据，该方法联合转换数据和模型参数，以实现更好的准确性和隐私性。我们为每个模型组件精心设计了转换方案，在确保推理准确性和数据隐私的同时，保持与现有语言模型即服务（LMaaS）基础设施的完全兼容。AloePri 已集成到一个工业系统中，用于评估主流 LLM。对 Deepseek-V3.1-Terminus 模型（671B 参数）的评估表明，AloePri 导致的准确率损失仅为 0.0%~3.5%，且表现出与明文推理相当的效率。同时，AloePri 成功抵御了最先进的攻击，恢复的 token 比例不到 5%。据我们所知，AloePri 是首个在真实系统中对大规模模型表现出实际适用性的方法。

## Abstract
The rapid development of large language models (LLMs) has driven the widespread adoption of cloud-based LLM inference services, while also bringing prominent privacy risks associated with the transmission and processing of private data in remote inference. For privacy-preserving LLM inference technologies to be practically applied in industrial scenarios, three core requirements must be satisfied simultaneously: (1) Accuracy and efficiency losses should be minimized to mitigate degradation in service experience. (2) The inference process can be run on large-scale clusters consist of heterogeneous legacy xPUs. (3) Compatibility with existing LLM infrastructures should be ensured to reuse their engineering optimizations. To the best of our knowledge, none of the existing privacy-preserving LLM inference methods satisfy all the above constraints while delivering meaningful privacy guarantees. In this paper, we propose AloePri, the first privacy-preserving LLM inference method for industrial applications. AloePri protects both the input and output data by covariant obfuscation, which jointly transforms data and model parameters to achieve better accuracy and privacy. We carefully design the transformation for each model component to ensure inference accuracy and data privacy while keeping full compatibility with existing infrastructures of Language Model as a Service. AloePri has been integrated into an industrial system for the evaluation of mainstream LLMs. The evaluation on Deepseek-V3.1-Terminus model (671B parameters) demonstrates that AloePri causes accuracy loss of 0.0%~3.5% and exhibits efficiency equivalent to that of plaintext inference. Meanwhile, AloePri successfully resists state-of-the-art attacks, with less than 5\% of tokens recovered. To the best of our knowledge, AloePri is the first method to exhibit practical applicability to large-scale models in real-world systems.

---

## 论文详细总结（自动生成）

这篇技术报告介绍了一种名为 **AloePri** 的新型隐私保护大语言模型（LLM）推理方法。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在“语言模型即服务”（LMaaS）场景下，用户需将私有数据发送至云端，面临严重的隐私泄露风险。
*   **研究动机**：现有的隐私保护技术难以同时满足工业界的三个核心约束：
    1.  **精度与效率**：不能有明显的性能下降或感知延迟（延迟需控制在100ms以内）。
    2.  **硬件兼容性**：必须能在异构的旧款 xPU（GPU/NPU/FPGA）集群上运行，无需特殊硬件（如 TEE）。
    3.  **软件兼容性**：必须兼容现有的推理框架（如 vLLM、SGLang），以复用 KV Cache 等工程优化。
*   **现状**：现有的密码学方法（如 FHE、MPC）太慢，基于 TEE 的方法硬件受限，而传统的混淆方法在隐私与精度之间平衡极差。

### 2. 方法论：协变混淆（Covariant Obfuscation）
*   **核心思想**：不同于仅对数据进行混淆，AloePri 采用**协变混淆**，即**联合转换输入数据和模型权重**。通过调整模型参数来抵消输入混淆带来的精度损失，从而在保持高精度的同时提供更强的隐私保证。
*   **关键技术细节**：
    *   **离线模型混淆**：客户端生成秘密置换 $\tau$ 和可逆矩阵。
        *   **嵌入层与输出头**：应用 Token 级置换，并加入高斯噪声和可逆键矩阵（Key Matrix）进行双边变换。
        *   **注意力机制**：设计了“头内变换”（RoPE 块置换、缩放）和“头间置换”，打乱注意力分数和隐藏状态。
        *   **FFN 层**：利用缩放和置换矩阵确保非线性操作的正确性。
    *   **组合定理**：提出了顺序、并行和求和三种组合定理，证明了将各组件的混淆组合后，整个模型的推理误差依然受控。
    *   **在线推理流程**：客户端本地将 Prompt 转换为混淆 Token，服务器在混淆模型上推理，客户端最后对返回的混淆响应进行解码。

### 3. 实验设计
*   **数据集/场景**：
    *   **通用任务**：MMLU, C-Eval, PIQA。
    *   **代码生成**：HumanEval。
    *   **指令遵循**：IFEval。
    *   **隐私评估**：PUPA（PII 保护）、CCI3/Huatuo26M（Token 频率分析）。
*   **Benchmark 模型**：Qwen2.5/3 (14B/32B), Llama3 (8B), Deepseek-R1-Distill, 以及超大规模的 **Deepseek-V3.1-Terminus (671B)**。
*   **对比方法**：SANTEXT, RANTEXT（Token 替换类）；DP-Forward, SGT（嵌入转换类）。
*   **攻击模型**：包括词汇匹配攻击（VMA）、不变性攻击（IA）、内部状态反演（ISA）、训练反演攻击（IMA）以及 Token 频率分析。

### 4. 资源与算力
*   **客户端**：仅需 CPU 环境（Intel Xeon Platinum 8457C, 96核）。
*   **服务器端**：使用了高性能 GPU 集群。
*   **具体算力**：文中提到在多节点 GPU 集群上部署，但未给出具体的 GPU 型号（如 H100 或 A100）和确切数量。
*   **耗时**：对于 32B 模型，离线混淆约需 10 分钟；对于 671B 的 Deepseek-V3.1，离线混淆约需 8 小时。在线推理效率与明文推理几乎一致。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 8B 到 671B 不同架构（Dense 和 MoE）的模型，实验组数非常丰富。
*   **充分性**：
    *   进行了广泛的精度测试（5个主流 Benchmark）。
    *   针对 5 种不同类型的先进攻击进行了防御性验证。
    *   包含消融实验（验证内部状态保护的有效性）和参数敏感性分析（如噪声系数 $\alpha$ 和矩阵系数 $\lambda$ 的影响）。
*   **客观性**：对比了多种 SOTA 混淆方法，并使用了互信息（MI）等理论指标进行量化，实验设计较为客观公平。

### 6. 主要结论与发现
*   **极低损失**：在 671B 模型上，精度损失仅为 0.0%~3.5%，几乎不影响用户体验。
*   **高效性**：推理延迟（TTFT 和 TPOT）与明文推理相当，支持高并发。
*   **强安全性**：成功抵御了多种反演攻击，攻击者恢复的 Token 比例低于 5%，PII 信息恢复率极低。
*   **工业可用性**：首次证明了隐私保护推理可以扩展到超大规模模型并直接集成到 vLLM 等现有框架中。

### 7. 优点
*   **理论完备**：提出了协变混淆的数学定义及组合定理，并给出了基于 PAC 隐私框架的安全界限证明。
*   **工程友好**：不改变模型结构，不依赖特殊硬件，兼容现有软件栈，部署成本极低。
*   **扩展性强**：是目前已知唯一能处理 600B 以上参数规模的隐私推理方案。

### 8. 不足与局限
*   **离线开销**：虽然是一次性的，但对于超大规模模型，客户端仍需数小时进行模型混淆处理。
*   **数值稳定性**：在 BF16 精度下，如果混淆矩阵的系数 $\lambda$ 设置过大，可能会导致数值溢出，限制了混淆强度的上限。
*   **确定性映射风险**：由于 Token 映射是确定性的，理论上仍存在遭受极端分布已知的 Token 频率攻击的风险（尽管实验显示实际威胁较小）。
*   **客户端参与度**：客户端需要存储混淆后的部分参数或执行 Token 转换，对客户端软件有一定侵入性。

（完）
