---
title: "RobPI: Robust Private Inference against Malicious Client"
title_zh: RobPI：针对恶意客户端的鲁棒隐私推理
authors: "Jiaqi Xue, Mengxin Zheng, Qian Lou"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19918v1"
tags: ["query:ppml"]
score: 9.0
evidence: 针对恶意客户端的鲁棒隐私推理
tldr: 针对现有私有推理（PI）协议在半诚实模型下易受恶意客户端攻击的问题，本文提出了一种名为RobPI的鲁棒私有推理协议。研究首先展示了一种新型推理操纵攻击，能以极低查询量篡改模型输出。为此，RobPI通过在逻辑值和特征中引入加密兼容噪声，构建了防御机制。实验表明，该协议能显著降低攻击成功率并大幅增加攻击成本，有效提升了私有推理的安全性。
selection_source: fresh_fetch
motivation: 现有的私有推理协议大多假设客户端是半诚实的，无法抵御恶意客户端通过操纵输入来篡改模型输出的攻击。
method: 提出RobPI协议，通过在私有推理的逻辑值和特征中织入与加密兼容的噪声，来防御恶意客户端的攻击。
result: "实验显示RobPI将攻击成功率降低了约91.9%，并将恶意攻击所需的查询次数提升了10倍以上。"
conclusion: RobPI为抵御恶意客户端提供了高效且鲁棒的私有推理方案，在保证隐私的同时显著增强了系统的安全性。
---

## 摘要
机器学习推理在各种应用中的广泛部署引发了隐私担忧。为此，隐私推理（PI）协议应运而生，允许各方在不泄露敏感数据的情况下进行推理。尽管 PI 的效率在近期取得了显著进展，但目前大多数方法都假设一个半诚实（semi-honest）威胁模型，即数据所有者是诚实的并遵守协议。然而在现实中，数据所有者可能具有不同的动机并以不可预测的方式行事，使得这一假设并不符合实际。为了展示恶意客户端如何破坏半诚实模型，我们首先针对一系列最先进的隐私推理协议设计了一种推理操纵攻击。该攻击允许恶意客户端以比现有黑盒攻击少 3 到 8 倍的查询次数来修改模型输出。受此启发，我们提出并实现了 RobPI，这是一种能够抵御恶意客户端的鲁棒且具有弹性的隐私推理协议。RobPI 集成了一种独特的密码学协议，通过将加密兼容的噪声融入隐私推理的 Logits 和特征中来增强安全性，从而有效抵御恶意客户端攻击。在各种神经网络和数据集上的广泛实验表明，RobPI 实现了约 91.9% 的攻击成功率降低，并将恶意客户端攻击所需的查询次数增加了 10 倍以上。

## Abstract
The increased deployment of machine learning inference in various applications has sparked privacy concerns. In response, private inference (PI) protocols have been created to allow parties to perform inference without revealing their sensitive data. Despite recent advances in the efficiency of PI, most current methods assume a semi-honest threat model where the data owner is honest and adheres to the protocol. However, in reality, data owners can have different motivations and act in unpredictable ways, making this assumption unrealistic. To demonstrate how a malicious client can compromise the semi-honest model, we first designed an inference manipulation attack against a range of state-of-the-art private inference protocols. This attack allows a malicious client to modify the model output with 3x to 8x fewer queries than current black-box attacks. Motivated by the attacks, we proposed and implemented RobPI, a robust and resilient private inference protocol that withstands malicious clients. RobPI integrates a distinctive cryptographic protocol that bolsters security by weaving encryption-compatible noise into the logits and features of private inference, thereby efficiently warding off malicious-client attacks. Our extensive experiments on various neural networks and datasets show that RobPI achieves ~91.9% attack success rate reduction and increases more than 10x the number of queries required by malicious-client attacks.

---

## 论文详细总结（自动生成）

以下是对论文《RobPI: Robust Private Inference against Malicious Client》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着机器学习即服务（MLaaS）的普及，隐私推理（Private Inference, PI）技术（如基于全同态加密 FHE 的方案）被用于保护用户数据和模型参数。
*   **核心问题**：现有的 PI 协议大多基于“半诚实（Semi-honest）”假设，即假设客户端会严格遵守协议。然而，现实中客户端可能是恶意的，通过操纵输入（对抗样本）来篡改模型输出（例如在人脸识别系统中伪造身份）。
*   **研究动机**：作者发现现有的黑盒攻击在 PI 场景下效率较低，且现有的防御手段（如在输入端加噪）由于 PI 特有的多项式激活函数（如平方函数）会导致噪声衰减，从而失效。

### 2. 论文提出的方法论
论文提出了攻击（PI-Attack）和防御（RobPI）两套方案：
*   **PI-Attack（推理操纵攻击）**：
    *   **核心思想**：在频率域而非空间域进行搜索。利用离散余弦变换（DCT）将图像转为频率系数。
    *   **关键技术**：采用“弧形搜索顺序”，优先扰动低频分量；引入“余弦退火扰动调度”，为低频分量分配更大的步长，从而以极少的查询次数实现攻击。
*   **RobPI（鲁棒隐私推理协议）**：
    *   **核心思想**：在模型的输出层（Logits）或特征层织入“加密兼容噪声”。
    *   **关键技术**：
        *   **输出端加噪**：绕过 PI 网络中非线性层对噪声的衰减，直接干扰攻击者的搜索方向。
        *   **动态噪声训练（DNT）**：在训练阶段引入随机强度的噪声，增强模型对噪声的容忍度，平衡清洁准确率与鲁棒性。
        *   **防御平均推理攻击**：针对攻击者通过多次查询取平均值来抵消噪声的策略，RobPI 提出了使用非零均值噪声或在倒数第二层加噪的变体。

### 3. 实验设计
*   **数据集**：MNIST（手写数字）、CIFAR-10（通用物体）、Diabetic Retinopathy（糖尿病视网膜病变医疗影像）。
*   **攻击 Benchmark**：对比了 SimBA-DCT 和 Square Attack（目前最强的黑盒攻击）。
*   **防御 Benchmark**：对比了 RND（随机噪声防御）、RND-GF（带高斯增强的 RND）和对抗训练（AT）。
*   **评估指标**：攻击成功率（ASR）、防御成功率（DSR）、平均查询次数、平均 $\ell_2$ 扰动范数、清洁准确率（ACC）。

### 4. 资源与算力
*   **硬件环境**：实验运行在 Azure 标准 B8ms 虚拟机上，配置为 8 vCPUs 和 32GB DRAM。
*   **软件实现**：基于 Microsoft SEAL 库（BFV 方案）实现加密计算。
*   **算力说明**：文中未详细列出模型训练的具体 GPU 型号或时长，但考虑到 PI 协议的评估重点在于推理延迟和加密开销，其算力消耗主要集中在 CPU 端的同态运算。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单（MNIST）到复杂（医疗影像）的三个不同领域数据集，针对目标攻击（Targeted）和非目标攻击（Untargeted）均做了详尽对比。
*   **消融实验**：对噪声因子 $\sigma$、扰动调度、DNT 技术以及针对“平均推理攻击”的防御变体进行了消融分析。
*   **充分性评价**：实验设计较为全面，不仅验证了所提攻击的有效性，还通过理论分析（扰动成功概率 DSP）与实验数据相互印证，客观地展示了 RobPI 在准确率损失极小的情况下大幅提升安全性的能力。

### 6. 主要结论与发现
*   **攻击效能**：PI-Attack 相比现有黑盒攻击，查询次数减少了 3x 至 8x，且扰动更小。
*   **防御效能**：RobPI 将攻击成功率降低了约 91.88%，并将攻击所需的查询次数提升了 10 倍以上。
*   **关键发现**：在 PI 网络中，由于多项式激活函数的存在，在输出层加噪比在输入层加噪更有效，且通过 DNT 技术可以进一步优化准确率与安全性的权衡。

### 7. 优点
*   **针对性强**：专门解决了 PI 协议中“半诚实”假设的漏洞，填补了恶意客户端防御的空白。
*   **效率高**：RobPI 的防御机制不需要复杂的网络修改或高昂的计算开销，仅通过加噪和训练优化即可实现。
*   **理论支撑**：对噪声如何干扰攻击搜索方向给出了严谨的数学证明（基于高斯分布的累积分布函数）。

### 8. 不足与局限
*   **应用限制**：RobPI 主要针对基于查询的黑盒攻击，对于白盒攻击（假设攻击者已知模型参数）的防御效果未做深入探讨（尽管在 PI 场景下模型参数通常对客户端加密）。
*   **博弈风险**：虽然提出了非零均值噪声来对抗“平均推理攻击”，但如果攻击者已知防御策略的统计特性，理论上仍可能存在更高级的统计攻击手段。
*   **实验覆盖**：实验主要集中在卷积神经网络（CNN），对于目前流行的 Transformer 等架构在 PI 场景下的表现提及较少。

（完）
