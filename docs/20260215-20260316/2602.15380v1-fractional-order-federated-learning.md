---
title: Fractional-Order Federated Learning
title_zh: 分数阶联邦学习
authors: "Mohammad Partohaghighi, Roummel Marcia, YangQuan Chen"
date: 2026-02-17
pdf: "https://arxiv.org/pdf/2602.15380v1"
tags: ["query:ppml"]
score: 9.0
evidence: 隐私保护联邦学习架构
tldr: 针对联邦学习中收敛慢、通信成本高及非独立同分布（non-IID）数据带来的挑战，本文提出了一种新型算法 FOFedAvg。该方法通过引入分数阶随机梯度下降（FOSGD），利用分数阶导数的记忆特性捕捉长程相关性和深层历史信息。实验证明，FOFedAvg 在多个基准数据集上显著提升了通信效率和收敛速度，并增强了对异构数据的鲁棒性，同时在理论上证明了其收敛性。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习在处理非独立同分布数据时存在的收敛缓慢、通信开销大及训练不稳定等问题。
method: 提出 FOFedAvg 算法，将分数阶随机梯度下降集成到联邦平均框架中，利用记忆感知更新捕捉历史梯度信息。
result: 在 MNIST、CIFAR-10 等多个数据集上的实验表明，FOFedAvg 在测试性能和收敛速度上均优于多种主流联邦优化算法。
conclusion: 分数阶记忆感知更新能有效提升联邦学习在异构数据环境下的鲁棒性与效率，为分布式训练提供了新路径。
---

## 摘要
联邦学习 (FL) 允许远程客户端在保护隐私的同时协作训练全局模型。尽管具有隐私保护优势，但 FL 仍存在显著缺点，包括收敛速度慢、通信成本高以及非独立同分布 (non-IID) 数据问题。在本研究中，我们提出了一种名为分数阶联邦平均 (FOFedAvg) 的新型 FedAvg 变体，它结合了分数阶随机梯度下降 (FOSGD) 以捕捉长程关系和更深层的历史信息。通过引入具有记忆感知能力的分数阶更新，FOFedAvg 提高了通信效率并加速了收敛，同时缓解了由异构、非独立同分布客户端数据引起的不稳定性。我们在 MNIST、FEMNIST、CIFAR-10、CIFAR-100、EMNIST、Cleveland 心脏病数据集、Sent140、PneumoniaMNIST 和 Edge-IIoTset 等基准数据集上，将 FOFedAvg 与一系列成熟的联邦优化算法进行了对比。在一系列非独立同分布划分方案下，FOFedAvg 在测试性能和收敛速度方面与这些基准算法相比具有竞争力，且通常表现更优。在理论方面，我们证明了在标准平滑性和方差有界假设下，当分数阶 $0 < α \le 1$ 时，FOFedAvg 收敛至驻点。综上所述，这些结果表明分数阶、记忆感知更新可以显著提高联邦学习的鲁棒性和有效性，为异构数据上的分布式训练提供了一条切实可行的路径。

## Abstract
Federated learning (FL) allows remote clients to train a global model collaboratively while protecting client privacy. Despite its privacy-preserving benefits, FL has significant drawbacks, including slow convergence, high communication cost, and non-independent-and-identically-distributed (non-IID) data. In this work, we present a novel FedAvg variation called Fractional-Order Federated Averaging (FOFedAvg), which incorporates Fractional-Order Stochastic Gradient Descent (FOSGD) to capture long-range relationships and deeper historical information. By introducing memory-aware fractional-order updates, FOFedAvg improves communication efficiency and accelerates convergence while mitigating instability caused by heterogeneous, non-IID client data. We compare FOFedAvg against a broad set of established federated optimization algorithms on benchmark datasets including MNIST, FEMNIST, CIFAR-10, CIFAR-100, EMNIST, the Cleveland heart disease dataset, Sent140, PneumoniaMNIST, and Edge-IIoTset. Across a range of non-IID partitioning schemes, FOFedAvg is competitive with, and often outperforms, these baselines in terms of test performance and convergence speed. On the theoretical side, we prove that FOFedAvg converges to a stationary point under standard smoothness and bounded-variance assumptions for fractional order $0<α\le 1$. Together, these results show that fractional-order, memory-aware updates can substantially improve the robustness and effectiveness of federated learning, offering a practical path toward distributed training on heterogeneous data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fractional-Order Federated Learning》（分数阶联邦学习）的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
联邦学习（FL）在实际应用中面临三大核心挑战：**非独立同分布（non-IID）数据导致的客户端漂移（Client Drift）**、**收敛速度缓慢**以及**高昂的通信成本**。传统的联邦优化算法（如 FedAvg）及其变体大多基于整数阶梯度下降，这种方法本质上是“无记忆”的，仅依赖当前梯度，容易在异构数据环境下产生不稳定的更新轨迹。

本论文的研究动机在于引入**分数阶微积分（Fractional Calculus）**。分数阶导数具有非局部性（Non-locality）和记忆效应（Memory Effect），能够捕捉参数更新的长程时间相关性和历史信息。研究者希望通过这种“记忆感知”的更新机制，平滑异构数据带来的梯度波动，从而提高 FL 的鲁棒性和效率。

### 2. 论文提出的方法论
论文提出了 **FOFedAvg（Fractional-Order Federated Averaging）** 算法，其核心是将分数阶随机梯度下降（FOSGD）集成到联邦学习框架中。

*   **核心思想**：利用 Caputo 型分数阶导数（阶数 $0 < \alpha \le 1$）来替代传统的整数阶梯度。
*   **关键技术细节**：
    *   **分数阶更新公式**：在客户端本地训练时，参数更新不再仅仅是 $\Theta - \eta \nabla f(\Theta)$，而是引入了一个基于参数变化量的缩放因子：
        $$\Theta_{t+1} = \Theta_t - \frac{\mu_t}{\Gamma(2-\alpha)} (\|\Theta_t - \Theta_{t-1}\| + \delta)^{1-\alpha} \nabla \ell(\Theta_t)$$
        其中，$\Gamma$ 是 Gamma 函数，$\delta$ 是正则化常数，$(\|\cdot\| + \delta)^{1-\alpha}$ 项实现了对历史轨迹的幂律加权。
    *   **记忆感知**：当 $\alpha < 1$ 时，更新步长会根据参数的历史演化进行动态调整。这种机制在 non-IID 场景下能起到类似“动量”但比动量更复杂的平滑作用。
    *   **算法流程**：服务端下发全局模型；客户端利用 FOSGD 进行多轮本地迭代；服务端收集各客户端更新并进行加权平均。
*   **理论保证**：论文在数学上证明了在标准平滑性和方差有界假设下，FOFedAvg 能够收敛至驻点。

### 3. 实验设计
*   **数据集与场景**：涵盖了极广泛的领域，包括：
    *   **图像分类**：MNIST, EMNIST, FEMNIST, CIFAR-10, CIFAR-100。
    *   **医疗诊断**：Cleveland 心脏病数据集, PneumoniaMNIST（肺炎检测）。
    *   **文本处理**：Sent140（情感分析）。
    *   **工业物联网**：Edge-IIoTset（网络攻击检测）。
*   **Non-IID 划分**：采用了 Dirichlet 划分和病理划分（Pathological partitioning），模拟了轻微、中等和严重三种数据异构程度。
*   **对比方法（Baselines）**：共对比了 10 种主流算法，包括经典方法（FedAvg, FedProx, SCAFFOLD）、现代优化方法（FedNova, FedAdam, MOON）、隐私保护方法（DPSGD）以及通信高效方法（SparseSecAgg, FDSE, FedBM）。

### 4. 资源与算力
论文**未明确说明**具体的硬件资源（如 GPU 型号、数量）以及总训练时长。但文中提到，FOFedAvg 的计算复杂度与 FedAvg 相当，每个 mini-batch 仅增加了 $O(d)$ 的轻量级向量运算开销（用于计算参数差的范数），因此在资源受限的边缘设备上具有良好的可行性。

### 5. 实验数量与充分性
*   **实验规模**：论文在 9 个不同的基准数据集上进行了实验，涵盖了视觉、文本、医疗和 IoT 等多种模态，实验组数非常庞大。
*   **消融实验**：详细探讨了分数阶阶数 $\alpha$、正则化参数 $\delta$、客户端参与比例、Batch Size 以及本地迭代轮数（Epochs）对性能的影响。
*   **充分性与客观性**：实验设计考虑了不同规模的客户端（从 10 到 1000 个），并针对 non-IID 的极端情况进行了专门测试。对比算法涵盖了从 2017 年到 2025 年的最新研究成果，评估指标包括准确率、收敛轮数和通信数据量（MB），实验结果具有较高的客观性和说服力。

### 6. 论文的主要结论与发现
*   **性能优越性**：在绝大多数 non-IID 场景下，FOFedAvg 的最终测试准确率均优于或等同于现有最强基准。
*   **通信效率**：FOFedAvg 达到目标准确率所需的通信轮数显著减少。在某些配置下，相比 FedAvg 可减少高达 10% 以上的通信开销。
*   **鲁棒性**：在极端 non-IID 情况下（如 PneumoniaMNIST），传统 FedAvg 可能会失效，而 FOFedAvg 仍能保持稳定的高准确率。
*   **参数敏感性**：发现 $\alpha$ 接近 1（如 0.97 或 0.99）时，通常能在保持记忆效应的同时获得最快的收敛速度。

### 7. 优点
*   **理论与实践结合**：不仅提出了受物理启发的新算法，还提供了严谨的收敛性证明。
*   **低开销高收益**：通过极小的计算增量（计算范数），获得了显著的收敛加速和鲁棒性提升。
*   **广泛的适用性**：实验覆盖了从简单数字识别到复杂医疗和 IoT 安全的多种现实场景。
*   **兼容性**：该方法作为一种本地优化器，理论上可以与现有的差分隐私（DP）和安全聚合技术无缝集成。

### 8. 不足与局限
*   **超参数调节**：引入了额外的超参数 $\alpha$，虽然论文给出了建议范围，但在不同任务中寻找最优 $\alpha$ 可能需要额外的调优成本。
*   **过时梯度的风险**：在数据分布随时间剧烈波动的动态环境中，长程记忆可能会引入过时的梯度信息，导致模型调整滞后。
*   **隐私分析尚浅**：虽然文中讨论了与隐私机制的兼容性，但未深入定量分析分数阶更新本身对梯度逆向攻击（Gradient Inversion）的防御能力。
*   **算力细节缺失**：缺乏具体的硬件运行时间对比，难以直观评估其在特定硬件上的能效比。

（完）
