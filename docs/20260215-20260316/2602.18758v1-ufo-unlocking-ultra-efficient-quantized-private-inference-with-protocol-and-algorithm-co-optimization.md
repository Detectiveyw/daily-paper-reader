---
title: "UFO: Unlocking Ultra-Efficient Quantized Private Inference with Protocol and Algorithm Co-Optimization"
title_zh: UFO：通过协议与算法协同优化解锁超高效量化隐私推理
authors: "Wenxuan Zeng, Chao Yang, Tianshi Xu, Bo Zhang, Changrui Ren, Jin Dong, Meng Li"
date: 2026-02-21
pdf: "https://arxiv.org/pdf/2602.18758v1"
tags: ["query:ppml"]
score: 9.0
evidence: 基于安全两方计算的量化隐私推理
tldr: UFO是一个针对安全两方计算（2PC）隐私推理的优化框架，通过协议与算法的协同设计，将Winograd卷积与量化技术结合。它解决了Winograd变换带来的通信开销和权重离群值导致的精度下降问题。通过图级协议优化和混合精度量化感知训练，UFO在显著降低通信开销的同时提升了模型精度，为高效隐私推理提供了新方案。
selection_source: fresh_fetch
motivation: 现有的基于2PC的隐私CNN推理面临巨大的通信和延迟开销，且直接结合Winograd卷积与量化会带来效率和精度上的双重挑战。
method: 提出了一种协同优化框架，包含减少通信的图级协议优化、基于层敏感度的混合精度量化算法，以及处理权重离群值的位重加权算法。
result: "实验表明，UFO相比SiRNN、COINN和CoPriv等前沿框架，通信量分别减少了11.7倍、3.6倍和6.3倍，且准确率提升了1.16%至1.29%。"
conclusion: UFO通过协议与算法的深度融合，成功实现了超高效且高精度的量化隐私推理，显著优于现有的SOTA方案。
---

## 摘要
基于安全两方计算（2PC）的隐私卷积神经网络（CNN）推理面临高昂的通信和延迟开销，尤其是来自卷积层的开销。在本文中，我们提出了 UFO，一个联合优化 2PC 协议和量化算法的量化 2PC 推理框架。UFO 的特点是采用了一种新型 2PC 协议，该协议系统地将高效的 Winograd 卷积算法与量化相结合，以提高推理效率。然而，我们观察到，简单地将量化与 Winograd 卷积结合面临以下挑战：1) 从推理角度看，Winograd 变换引入了大量的加法，并需要频繁的位宽转换以避免推理溢出，从而导致不可忽视的通信开销；2) 从训练角度看，Winograd 变换引入了权重离群值，使得量化感知训练（QAT）变得困难，导致模型准确率下降。为了应对这些挑战，我们对协议和算法进行了协同优化。1) 在协议层面，我们为 2PC 推理提出了一系列图级优化，以最小化通信。2) 在算法层面，我们开发了一种基于层敏感度的混合精度 QAT 算法，以在通信约束下优化模型准确率。为了处理离群值，我们进一步引入了一种 2PC 友好的位重加权算法，在不显式增加位宽的情况下扩大表示范围。通过大量实验，UFO 与最先进的框架 SiRNN、COINN 和 CoPriv 相比，通信量分别减少了 11.7 倍、3.6 倍和 6.3 倍，且准确率分别提高了 1.29%、1.16% 和 1.29%。

## Abstract
Private convolutional neural network (CNN) inference based on secure two-party computation (2PC) suffers from high communication and latency overhead, especially from convolution layers. In this paper, we propose UFO, a quantized 2PC inference framework that jointly optimizes the 2PC protocols and quantization algorithm. UFO features a novel 2PC protocol that systematically combines the efficient Winograd convolution algorithm with quantization to improve inference efficiency. However, we observe that naively combining quantization and Winograd convolution faces the following challenges: 1) From the inference perspective, Winograd transformations introduce extensive additions and require frequent bit width conversions to avoid inference overflow, leading to non-negligible communication overhead; 2) From the training perspective, Winograd transformations introduce weight outliers that make quantization-aware training (QAT) difficult, resulting in inferior model accuracy. To address these challenges, we co-optimize both protocol and algorithm. 1) At the protocol level, we propose a series of graph-level optimizations for 2PC inference to minimize the communication. 2) At the algorithm level, we develop a mixed-precision QAT algorithm based on layer sensitivity to optimize model accuracy given communication constraints. To accommodate the outliers, we further introduce a 2PC-friendly bit re-weighting algorithm to increase the representation range without explicitly increasing bit widths. With extensive experiments, UFO demonstrates 11.7x, 3.6x, and 6.3x communication reduction with 1.29%, 1.16%, and 1.29% higher accuracy compared to state-of-the-art frameworks SiRNN, COINN, and CoPriv, respectively.

---

## 论文详细总结（自动生成）

这是一份关于论文《UFO: Unlocking Ultra-Efficient Quantized Private Inference with Protocol and Algorithm Co-Optimization》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：基于安全两方计算（2PC）的隐私深度学习推理面临极高的通信开销和延迟，尤其是卷积层。
*   **研究动机**：
    *   **现有技术的局限**：Winograd 卷积能减少乘法次数，但会引入大量本地加法；量化能降低位宽，但两者简单结合时，Winograd 变换会导致数值溢出（需增加位宽转换协议，增加通信）和权重离群值（导致量化精度大幅下降）。
    *   **协同优化需求**：现有的隐私推理框架（如 SiRNN, CoPriv）未能有效解决 Winograd 变换与低比特量化结合时的效率与精度冲突。

### 2. 方法论：核心思想与关键技术
UFO 提出了**协议与算法协同优化**的框架，核心包含以下三个维度：

*   **量化 Winograd 卷积协议 (QWinConv)**：
    *   系统性地将 Winograd 变换嵌入 2PC 流程，在离线阶段处理权重变换，在线阶段处理特征和输出变换。
    *   通过引理推导确定了位扩展（Bit Extension）的边界，以严格保证推理正确性。
*   **图级协议优化 (Graph-level Optimization)**：
    *   **协议融合 (Protocol Fusion)**：识别并合并连续的位宽转换操作（如扩展、截断、重量化），消除冗余的在线通信。
    *   **简化残差协议 (Simplified Residual)**：直接将残差分支的位宽和缩放比例对齐到卷积输出，减少主干路径上的转换开销。
    *   **MSB 已知优化**：利用 ReLU 后激活值非负的特性，优化截断和扩展协议。
*   **算法层面优化**：
    *   **2PC 感知混合精度 QAT**：利用 Hessian 矩阵迹（Trace）衡量层敏感度，通过整数线性规划（ILP）在通信约束下分配最优位宽。
    *   **位重加权算法 (Bit Re-weighting)**：针对 Winograd 引入的权重离群值，不增加总位宽，而是调整最高有效位（MSB）的权重（如将 $2^{lw-1}$ 调整为 $2^{lw}$），在不增加通信的前提下扩大数值表示范围。

### 3. 实验设计
*   **数据集**：CIFAR-10, CIFAR-100, Tiny-ImageNet, ImageNet。
*   **模型**：MiniONN, ResNet-18, ResNet-20, ResNet-32。
*   **Benchmark 与对比方法**：
    *   **通用隐私推理框架**：SiRNN, CrypTFlow2, Gazelle, Delphi。
    *   **量化/线性化优化方法**：COINN, CoPriv, DeepReduce, SNL, SAFENet, SENet。
    *   **Winograd 量化算法**：Legendre, BQW, PAW, WinoVidiVici。
*   **安全模型**：半诚实（Honest-but-curious）威胁模型。

### 4. 资源与算力
*   **硬件环境**：Intel Xeon Gold 5220R CPU @ 2.20GHz。
*   **网络环境**：LAN 模式（带宽 377 MBps，延迟 0.3ms）。
*   **软件框架**：基于 EzPC 库（C++）实现推理协议；使用 PyTorch 进行量化感知训练（QAT）。
*   **训练细节**：文中未详细列出具体的 GPU 型号和训练总时长，但提到在训练中将 Winograd 变换矩阵 G 设为可学习参数。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型数据集（CIFAR）到大规模数据集（ImageNet）的完整评估。
*   **实验类型**：
    *   **端到端性能对比**：准确率 vs. 通信量/延迟。
    *   **微基准测试 (Micro-benchmark)**：针对卷积协议和残差协议的专项测试。
    *   **消融实验**：逐项验证协议融合、残差优化、位重加权等模块的贡献。
*   **充分性评价**：实验设计非常充分，对比了多种不同技术路线（HE-based, OT-based, ReLU-reduced）的 SOTA 方法，且在不同位宽配置下进行了详尽测试，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **通信大幅降低**：在 CIFAR-100 上，UFO 相比 SiRNN 通信量减少了 **11.7倍**，相比 CoPriv 减少了 **6.3倍**。
*   **精度不降反升**：通过位重加权和混合精度优化，UFO 在大幅减小通信的同时，准确率比对比方法高出 **1.16% - 1.29%**。
*   **在线效率提升**：协议融合和简化残差协议显著降低了在线阶段的负担，解决了量化 Winograd 带来的额外开销问题。

### 7. 优点
*   **协同设计思路**：不仅优化协议，还从算法端（位重加权）解决了硬件/协议层面的痛点（离群值）。
*   **通用性强**：提出的图级优化（如协议融合）不仅适用于 Winograd，也可推广至其他混合精度隐私推理场景。
*   **性能卓越**：在 ImageNet 等大规模任务上依然保持了极高的通信压缩比和精度稳定性。

### 8. 不足与局限
*   **架构限制**：目前主要针对 CNN 架构设计，Winograd 算法无法直接应用于 Transformer 架构（如 Attention 机制）。
*   **安全模型局限**：仅针对半诚实模型，未讨论在恶意（Malicious）安全模型下的表现和开销。
*   **硬件依赖**：虽然是 CPU 推理，但未讨论在移动端或更受限边缘设备上的内存占用情况。

（完）
