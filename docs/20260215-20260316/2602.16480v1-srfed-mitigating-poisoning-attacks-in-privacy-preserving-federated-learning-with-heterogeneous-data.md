---
title: "SRFed: Mitigating Poisoning Attacks in Privacy-Preserving Federated Learning with Heterogeneous Data"
title_zh: SRFed：在异构数据环境下缓解隐私保护联邦学习中的投毒攻击
authors: Yiwen Lu
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16480v1"
tags: ["query:ppml"]
score: 9.0
evidence: 具有防御性聚合的隐私保护联邦学习
tldr: 联邦学习面临服务器推理攻击和客户端投毒攻击的双重威胁，且在非独立同分布（Non-IID）数据下表现不佳。本文提出SRFed框架，通过去中心化高效函数加密（DEFE）保护模型隐私并消除第三方依赖，同时结合层级投影与聚类分析实现隐私保护下的鲁棒聚合。实验证明，SRFed在提升拜占庭容错能力的同时，显著降低了计算与通信开销。
selection_source: fresh_fetch
motivation: 针对现有联邦学习防御方案在处理异构数据时效率低下且难以兼顾隐私保护与投毒攻击防御的问题。
method: 提出DEFE加密方案实现非交互式解密，并结合层级投影与聚类分析技术在加密状态下过滤恶意模型。
result: 实验结果显示SRFed在隐私保护、拜占庭鲁棒性以及计算通信效率方面均优于现有主流基准方案。
conclusion: SRFed为非独立同分布环境下的联邦学习提供了一种高效且安全的隐私保护与防御投毒攻击的综合解决方案。
---

## 摘要
联邦学习（FL）能够在不暴露客户端隐私数据的情况下实现协作模型训练，已在隐私敏感场景中得到广泛应用。然而，联邦学习面临两个关键的安全威胁：一是好奇的服务器可能发起推理攻击以重构客户端的隐私数据；二是受损的客户端可能发起投毒攻击以破坏模型聚合。现有解决方案通过将主流隐私保护技术与防御性聚合策略相结合来缓解这些攻击。然而，这些方案要么产生高昂的计算和通信开销，要么在非独立同分布（Non-IID）数据设置下表现不佳。为了应对这些挑战，我们提出了 SRFed，这是一个针对 Non-IID 场景的高效拜占庭容错且隐私保护的联邦学习框架。首先，我们设计了一种去中心化的高效函数加密（DEFE）方案，以支持高效的模型加密和非交互式解密。DEFE 还消除了对第三方的依赖，并能防御服务器端的推理攻击。其次，我们开发了一种基于 DEFE 的隐私保护防御性模型聚合机制。该机制通过逐层投影和基于聚类的分析，在 Non-IID 数据环境下过滤投毒模型。理论分析和广泛的实验表明，SRFed 在隐私保护、拜占庭容错性和效率方面均优于现有的基准方案。

## Abstract
Federated Learning (FL) enables collaborative model training without exposing clients' private data, and has been widely adopted in privacy-sensitive scenarios. However, FL faces two critical security threats: curious servers that may launch inference attacks to reconstruct clients' private data, and compromised clients that can launch poisoning attacks to disrupt model aggregation. Existing solutions mitigate these attacks by combining mainstream privacy-preserving techniques with defensive aggregation strategies. However, they either incur high computation and communication overhead or perform poorly under non-independent and identically distributed (Non-IID) data settings. To tackle these challenges, we propose SRFed, an efficient Byzantine-robust and privacy-preserving FL framework for Non-IID scenarios. First, we design a decentralized efficient functional encryption (DEFE) scheme to support efficient model encryption and non-interactive decryption. DEFE also eliminates third-party reliance and defends against server-side inference attacks. Second, we develop a privacy-preserving defensive model aggregation mechanism based on DEFE. This mechanism filters poisonous models under Non-IID data by layer-wise projection and clustering-based analysis. Theoretical analysis and extensive experiments show that SRFed outperforms state-of-the-art baselines in privacy protection, Byzantine robustness, and efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《SRFed: Mitigating Poisoning Attacks in Privacy-Preserving Federated Learning with Heterogeneous Data》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：联邦学习（FL）虽然避免了直接共享原始数据，但仍面临两大威胁：一是**好奇的服务器**通过模型更新推断隐私数据（推理攻击）；二是**恶意的客户端**上传伪造更新以破坏全局模型（投毒攻击）。
*   **核心痛点**：现有的隐私保护联邦学习（PPFL）方案在处理**非独立同分布（Non-IID）**数据时表现不佳，且往往伴随着高昂的计算/通信开销，或依赖于不可信的第三方密钥生成机构。
*   **研究目标**：提出一个名为 **SRFed** 的框架，旨在 Non-IID 环境下同时实现高效的隐私保护和拜占庭鲁棒性（Byzantine-robust）。

### 2. 方法论
SRFed 的核心由两个关键组件构成：
*   **去中心化高效函数加密（DEFE）**：
    *   **核心思想**：改进了现有的函数加密（FE）方案，实现了去中心化的密钥管理。
    *   **技术细节**：通过分布式密钥生成消除对第三方的依赖；修改密文结构以支持**非交互式解密**，避免了传统方案中耗时的离散对数运算。
    *   **隐私增强**：在加密过程中引入受控噪声，防止服务器通过中间聚合结果推断客户端隐私。
*   **隐私保护鲁棒聚合机制**：
    *   **层级投影（Layer-wise Projection）**：服务器在不解密本地模型的前提下，利用 DEFE 计算本地模型每一层与全局模型对应层的内积，生成投影向量。
    *   **聚类分析**：利用 **K-Means** 算法对所有客户端的投影向量进行聚类。
    *   **过滤策略**：计算各簇内向量与质心的平均余弦相似度，识别并剔除相似度异常的簇（即恶意投毒模型），仅对良性簇进行加权聚合。

### 3. 实验设计
*   **数据集**：MNIST（手写数字）和 CIFAR-10（彩色物体分类）。
*   **场景设置**：通过 Dirichlet 分布（参数 $\alpha$ 取 0.2, 0.6, 0.8）模拟不同程度的 **Non-IID** 数据分布。
*   **攻击模型**：
    *   **有针对性投毒**：标签翻转攻击（Label-flipping）。
    *   **无针对性投毒**：高斯攻击（Gaussian Attack）。
    *   **恶意比例**：设置 0% 到 50% 的恶意客户端占比。
*   **基准方法（Benchmarks）**：对比了 FedAvg（基础）、ShieldFL（基于同态加密）、PBFL（基于区块链）、Median（鲁棒聚合）、Biscotti（基于差分隐私）、FoolsGold（基于余弦相似度）和 ESB-FL（基于函数加密）。

### 4. 资源与算力
*   **硬件配置**：
    *   **CPU**: Intel Xeon E5-1650 v4。
    *   **内存**: 32 GB RAM。
    *   **GPU**: NVIDIA GeForce GTX 1080 Ti。
    *   **网络带宽**: 40 Mbps。
*   **训练参数**：20个客户端，100轮训练，Batch size 为 64，本地训练 10 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文针对两个数据集分别在多种异构程度（$\alpha$ 值）、多种攻击类型及不同恶意比例下进行了详尽测试。
*   **充分性评价**：实验设计较为**充分且客观**。它不仅评估了模型精度（OA），还引入了源类准确率（SA）和攻击成功率（ASR）来专门衡量防御效果。此外，还专门进行了效率分析，对比了不同加密方案的计算耗时。

### 6. 主要结论与发现
*   **鲁棒性极强**：在极端的 Non-IID 场景（$\alpha=0.2$）且恶意比例高达 50% 时，SRFed 仍能保持较高的源类准确率（SA 约 70%），而其他对比方法（如 ShieldFL, Median）的 SA 几乎降至 0%。
*   **高效率**：相比于基于同态加密的 ShieldFL，SRFed 的总训练时间缩短了 **58%**；相比于同类 FE 方案 ESB-FL，其聚合阶段效率提升了 **71%**。
*   **安全性**：理论证明了该方案在 DCR（判定复合剩余性）假设下满足“诚实但好奇”的服务器安全性。

### 7. 优点
*   **细粒度检测**：采用层级投影而非全局统计，能更敏锐地捕捉到隐藏在特定模型层中的投毒行为。
*   **去中心化信任**：DEFE 方案解决了 PPFL 中常见的“受信任第三方”安全隐患。
*   **性能无损**：与差分隐私（DP）方法不同，SRFed 在无攻击情况下不会损失模型精度（具有 Fidelity 特性）。

### 8. 不足与局限
*   **模型复杂度限制**：实验仅在较小的 CNN 模型上完成，未验证其在超大规模模型（如大语言模型或深层 ResNet）上的内存压力和计算延迟。
*   **攻击多样性**：未探讨更隐蔽的后门攻击（Backdoor Attacks）或自适应攻击（即攻击者已知防御策略后的针对性绕过）。
*   **扩展性验证**：客户端数量仅设为 20，在成千上万个客户端的大规模真实场景下的聚类开销和通信瓶颈尚待观察。

（完）
