---
title: Efficient Privacy-Preserving Sparse Matrix-Vector Multiplication Using Homomorphic Encryption
title_zh: 基于同态加密的高效隐私保护稀疏矩阵-向量乘法
authors: "Yang Gao, Gang Quan, Wujie Wen, Scott Piersall, Qian Lou, Liqiang Wang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04742v1"
tags: ["query:ppml"]
score: 9.0
evidence: 使用同态加密的隐私保护稀疏矩阵向量乘法
tldr: 本研究针对科学计算和机器学习中核心的稀疏矩阵-向量乘法（SpMV）操作，提出了首个结合同态加密（HE）的高效隐私保护框架。通过引入创新的压缩稀疏排序列（CSSC）矩阵格式，该框架有效解决了加密计算中的存储和计算开销问题。实验证明，该方法在保持数据隐私的同时，显著提升了处理速度并降低了内存占用，为联邦学习和加密数据库等安全应用提供了重要支持。
selection_source: fresh_fetch
motivation: 针对同态加密在稀疏矩阵-向量乘法（SpMV）应用中缺乏高效实现方案的现状，探索平衡隐私保护与计算效率的方法。
method: 提出了一种专为加密计算设计的压缩稀疏排序列（CSSC）矩阵格式，通过优化密文打包减少计算开销。
result: 在真实数据集上的实验结果显示，该方法在处理时间和内存使用效率上均取得了显著提升。
conclusion: 该框架为联邦学习、加密数据库及科学计算等领域的隐私保护应用奠定了坚实的技术基础。
---

## 摘要
稀疏矩阵-向量乘法 (SpMV) 是科学计算、数据分析和机器学习中的一项基础操作。当处理的数据具有敏感性时，保护隐私变得至关重要，而同态加密 (HE) 已成为应对这一挑战的主要方法。尽管 HE 能够实现隐私保护计算，但其在 SpMV 中的应用在很大程度上仍未得到解决。据我们所知，本文提出了第一个将 HE 与 SpMV 高效集成的框架，解决了计算效率和数据隐私的双重挑战。特别地，我们引入了一种名为压缩稀疏排序列 (Compressed Sparse Sorted Column, CSSC) 的新型矩阵压缩格式，该格式专门为优化加密稀疏矩阵计算而设计。通过保留稀疏性并实现高效的密文打包，CSSC 显著降低了存储和计算开销。我们在真实数据集上的实验结果表明，所提出的方法在处理时间和内存使用方面都取得了显著提升。本研究推进了隐私保护 SpMV 的发展，并为联邦学习、加密数据库、科学计算等领域的安全应用奠定了基础。

## Abstract
Sparse matrix-vector multiplication (SpMV) is a fundamental operation in scientific computing, data analysis, and machine learning. When the data being processed are sensitive, preserving privacy becomes critical, and homomorphic encryption (HE) has emerged as a leading approach for addressing this challenge. Although HE enables privacy-preserving computation, its application to SpMV has remained largely unaddressed. To the best of our knowledge, this paper presents the first framework that efficiently integrates HE with SpMV, addressing the dual challenges of computational efficiency and data privacy. In particular, we introduce a novel compressed matrix format, named Compressed Sparse Sorted Column (CSSC), which is specifically designed to optimize encrypted sparse matrix computations. By preserving sparsity and enabling efficient ciphertext packing, CSSC significantly reduces storage and computational overhead. Our experimental results on real-world datasets demonstrate that the proposed method achieves significant gains in both processing time and memory usage. This study advances privacy-preserving SpMV and lays the groundwork for secure applications in federated learning, encrypted databases, scientific computing, and beyond.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Privacy-Preserving Sparse Matrix-Vector Multiplication Using Homomorphic Encryption》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在保证数据隐私的前提下，高效地执行**稀疏矩阵-向量乘法（SpMV）**。
*   **研究动机**：SpMV 是科学计算和机器学习的核心算子。虽然同态加密（HE）允许在加密数据上进行计算，但现有的 HE 矩阵运算方案几乎完全针对**稠密矩阵**设计。
*   **背景挑战**：直接将稠密矩阵的 HE 算法应用于稀疏矩阵会导致严重的性能退化：
    1.  **无效计算**：对大量零元素进行昂贵的加密运算。
    2.  **格式不匹配**：传统的稀疏格式（如 CSR/CSC）在密文域中会导致频繁且耗时的密文旋转（Rotation）和槽位对齐操作。
    3.  **存储膨胀**：HE 密文本身具有巨大的空间开销，不合理的打包方式会耗尽内存。

### 2. 论文提出的方法论
论文提出了首个支持**双操作数加密（密文矩阵 × 密文向量）**的 SpMV 框架，核心技术包括：
*   **CSSC 矩阵格式（核心创新）**：
    *   **行重排**：按每行非零元素（NNZ）数量降序排列，使非零元素分布更规整。
    *   **左对齐**：将每行的非零元素向左靠拢，消除行内空隙。
    *   **列优先打包**：按列提取元素并打包进密文槽位，确保矩阵元素与向量元素的槽位在计算时自然对齐。
*   **端到端流水线**：
    *   **客户端 A（矩阵方）**：将矩阵转为 CSSC 格式，加密非零值，并将列索引发送给客户端 B。
    *   **客户端 B（向量方）**：根据列索引重组向量，使其与 CSSC 格式对齐后加密。
    *   **云端（计算方）**：执行一次密文-密文乘法，随后通过**对数级深度的二叉树聚合算法（Total-Sum）**和轻量级掩码（Masking）完成跨块求和。
*   **低深度设计**：通过优化布局，将乘法深度控制在 1（或极低），避免了昂贵的自举（Bootstrapping）操作。

### 3. 实验设计
*   **数据集**：选自 **SuiteSparse Matrix Collection**，涵盖了从小型（130x130）到大型（62,586x62,586）的多种真实世界稀疏矩阵，涉及电路仿真、社交网络、优化问题等领域。
*   **Benchmark 与对比方法**：
    *   **Diag (Diagonal Method)**：经典的对角线打包法（常用于稠密矩阵）。
    *   **HEGMM**：高效的密文通用矩阵乘法框架。
    *   **HETAL**：针对迁移学习优化的 HE 矩阵运算。
    *   **Rhombus**：一种针对“明文矩阵-密文向量”场景优化的最新方法（作为跨场景参考）。

### 4. 资源与算力
*   **硬件环境**：Intel Xeon Silver 4114 CPU (2.20 GHz, 10 核心)，配有 256 GB RAM。
*   **软件框架**：使用 **Pyfhel** 库实现，底层基于 **BFV** 同态加密方案。
*   **算力说明**：实验主要在单线程模式下运行以确保公平性。文中未提及使用 GPU 加速，这符合目前大多数基于格（Lattice）的 HE 研究在通用 CPU 上评估算法逻辑的惯例。

### 5. 实验数量与充分性
*   **实验规模**：论文对数十个具有不同稀疏模式和维度的矩阵进行了测试，详细记录了运行时间、内存占用、通信开销和噪声增长。
*   **充分性与公平性**：
    *   实验对比了多种主流 HE 矩阵算法，并针对大型矩阵无法直接处理的问题，为基准方法实现了分块（Blocking）处理，确保了对比的客观性。
    *   每组实验独立重复 5 次取平均值，消除了系统波动影响。
    *   涵盖了从计算效率到内存安全（噪声预算分析）的全方位评估。

### 6. 论文的主要结论与发现
*   **性能飞跃**：相比于最先进的基准方法，该方案实现了平均 **100 倍以上**的加速，在某些超大规模稀疏矩阵上，加速比达到了 **3 到 5 个数量级**（最高超过 5000 倍）。
*   **内存优化**：内存占用平均降低了 **5 倍以上**，解决了大型加密矩阵运算容易导致内存溢出的问题。
*   **可扩展性**：证明了通过 CSSC 格式和分块策略，HE 可以处理维度达数万级的稀疏矩阵，而传统方法在这些场景下往往因超时（超过 3 天）或内存耗尽而失败。

### 7. 优点（亮点）
*   **首创性**：填补了密文-密文 SpMV 高效实现方案的空白。
*   **HE 感知设计**：CSSC 格式不是简单的压缩，而是深度结合了 HE 的 SIMD 特性，极大地减少了最耗时的“密文旋转”操作。
*   **隐私强度高**：支持矩阵和向量同时加密，适用于参与方互不信任的严苛隐私场景。

### 8. 不足与局限
*   **结构信息泄露**：该方法假设矩阵的**稀疏模式（非零元素的位置）是公开的**。虽然在很多科学计算场景中这是可接受的，但在某些极端隐私场景（如病人诊断矩阵）中，位置信息本身可能也是敏感的。
*   **静态限制**：CSSC 格式目前针对静态稀疏模式设计，如果矩阵结构频繁发生动态变化，重新压缩和加密的开销会很高。
*   **通信开销**：虽然计算和内存大幅优化，但 HE 密文本身的传输量依然很大（MB 级别），在带宽受限的环境下仍面临挑战。

（完）
