Title: Gecko: Fast Private Inference via Secure Public Encoder Offloading

URL Source: https://arxiv.org/pdf/2608.02378v1

Published Time: Tue, 04 Aug 2026 02:35:47 GMT

Number of Pages: 12

Markdown Content:
> 1

# Gecko: Fast Private Inference via Secure Public Encoder Offloading 

## Cheng’an Wei, Kai Chen, Yue Zhao, Congyi Li, and Shenchen Zhu 

Abstract —Private inference protects both user inputs and server models during neural network inference, but existing solutions remain too slow for practical deployment. This motivates recent efforts to run a public encoder, such as a pretrained backbone, outside the protection boundary and evaluate only a small private predictor cryptographically. While appealing for efficiency, this design is not inherently secure: naively offloading a public encoder may create a feature-space shortcut : an extraction adversary may learn the remaining private predictor’s feature-to-output mapping more easily than the original model’s input-to-output behavior. We present Gecko , designed to limit this additional risk while retaining a compact encrypted predictor. We leverage a frozen backbone that contributes hierarchical features, fixed Fastfood projections that compress them, and private feature gating that prepares them for prediction. We formalize ideal independence and information-preservation conditions as design guidance, then separately evaluate component-reuse extraction attacks. Across image and audio tasks, Gecko achieves 0.4–2.2-second inference with at most 10.8 MB communication and accuracy comparable to transfer-learning baselines. Under the evaluated attacks, reusing the offloaded public encoder provides no significant advantage to model-extraction adversaries. Source code and a demo are available at https://github.com/CassiniHuy/gecko-infer. 

Index Terms —Private inference, homomorphic encryption, model extraction, public encoders, privacy-preserving machine learning. 

✦

## 1 INTRODUCTION 

Deep learning inference is increasingly offered as a remote service for sensitive applications such as biomedical im-age classification [1], [2]. Private inference aims to protect both parties in this setting: the server should not learn the user’s input or output, and the user should not learn the server’s proprietary model beyond the intended prediction. Fully Homomorphic Encryption (FHE) provides this con-fidentiality by allowing the server to compute directly on ciphertexts [3]. The main obstacle is cost. Neural-network operations are far more expensive under encryption than in plaintext, and the feature-extraction backbone usually dominates mod-ern inference. Even optimized systems still require about one minute for a ResNet50 image [4], [5], [6], [7]. Public-encoder offloading offers a different optimization: execute a generic pretrained encoder outside FHE and protect only a lightweight task-specific predictor [8], [9], [10]. This fol-lows a public-resource assumption : the offloaded encoder is independently available and contains no proprietary task adaptation. Consider a chest-X-ray service. The user can run a frozen public image encoder locally, but its representation is not a pneumonia diagnosis; the service-specific mapping remains in the server’s proprietary predictor. The user therefore encrypts the public representation, the server evaluates the private predictor under FHE, and the user decrypts the returned result. Offloading eliminates the need to protect the 

• The authors are with the Institute of Information Engineering, Chi-nese Academy of Sciences, China, and the School of Cyber Security, University of Chinese Academy of Sciences, China (e-mail: weichen-gan@iie.ac.cn; chenkai@iie.ac.cn; zhaoyue@iie.ac.cn; licongyi@iie.ac.cn; zhushenchen@iie.ac.cn). Corresponding author: Kai Chen. 

expensive generic encoder without eliminating the server’s proprietary contribution. 

Feature-space shortcut problem. Ordinary prediction ac-cess already entails model-extraction risk: an adversary can query class probabilities and train a transfer-learning surrogate [11]. While we cannot eliminate this baseline risk, we should not introduce incremental risk through offload-ing. A naive split commonly exposes the exact final-layer representation consumed by the private predictor. Because deep features are compact and selective [12], this interface may reveal a narrow task-aligned feature space and reduce extraction from learning the full input–output behavior to learning a simpler feature-to-output mapping. We call this additional simplification the feature-space shortcut .The basic remedy is to enrich the public representation, but doing so introduces two challenges. First, the public rep-resentation should be richer without becoming task-adapted or forcing a large private predictor. Second, representation richness needs a principled assessment: task accuracy alone indicates utility but not the feature richness the public representation has. 

Approach. We propose Gecko , a private-inference frame-work designed to limit the additional shortcut created by public encoding. A frozen public backbone contributes fea-tures from multiple abstraction levels, retaining low-, mid-, and high-level characteristics rather than only its selective final representation. Fixed Fastfood-based random projec-tions [13] compress each level, and a private hierarchical feature-gating layer balances the projected groups before being fused into a lightweight predictor. Only this task-specific predictor is evaluated under FHE. We also formulate two ideal design criteria: prediction-artifact independence , which excludes task-specific private artifacts from the public component, and information preser-arXiv:2608.02378v1 [cs.LG] 3 Aug 2026 2

vation , which prevents the exposed interfaces from col-lapsing the inference behavior. These criteria characterize prediction-functional uncertainty, which, while not a direct measure of model extraction hardness, helps us gauge it. In practice, we use estimated mutual information as an offline, relative diagnostic of representation richness and then empirically evaluate extraction risk by measuring the difference in attack accuracy after offloading. Across general-image, biomedical, and audio tasks [14], [15], [16], [17], [18], [19], [2], Gecko completes inference in 0.4–2.2 seconds with at most 10.8 MB communication while maintaining accuracy comparable to transfer-learning base-lines. Under our evaluated attacks, direct reuse of the public encoder and projection offered no consistent extraction ad-vantage over free surrogate tuning. Our contributions are:  

> •

We develop a private-inference partition that executes an independently public encoder outside FHE while retain-ing the task-specific predictor under FHE, substantially reducing protected computation.  

> •

We construct a compact hierarchical representation using multi-level features, fixed Fastfood projections, and pri-vate feature gating, so the encrypted predictor remains lightweight without relying on a selective final-layer in-terface.  

> •

We formalize ideal information-theoretic criteria for this partition and evaluate its efficiency, utility, and incremen-tal extraction risk across realistic tasks and component-reuse attack settings. The source code is available at https://github.com/ CassiniHuy/gecko-infer. 

## 2 PROBLEM SETTING AND CHALLENGES 

2.1 Private Inference with Public Encoders 

We consider a client–server classification service. The user holds a private input, and the server holds a proprietary task-specific predictor. Conventional private inference eval-uates the complete model on encrypted inputs, but protect-ing a large generic feature extractor dominates the cost. As Figure 1 illustrates, public-encoder offloading instead lets the user execute an independently available encoder in plaintext, encrypt its representation, and send only that ciphertext to the proprietary predictor. The encoder cannot produce the service output by itself: it supplies generic features but contains no private task-specific mapping. This partition changes the model-protection boundary. The public encoder needs no model-side confidentiality, whereas the predictor weights and internal state remain private. Our question is therefore whether exposing the encoder and its representation gives an adversary additional 

extraction leverage beyond normal prediction access. 

2.2 Threat Model 

Following prior private-inference work [20], [21], [8], [22], [23], [24], we assume two semi-honest parties without a trusted third party. Both follow the prescribed protocol while attempting to infer additional information.  

> •

The curious server receives only encrypted representations and returns encrypted predictions. It does not hold the decryption key and should learn neither the user’s input User  Server                  

> Encrypt
> Decrypt
> Output 𝑌
> Encrypted
> Output [𝑌 ]
> Encrypted
> Input [𝑋 ]
> Input 𝑋
> Traditional Public -Encoder Offloading
> …
> Entire proprietary
> model under FHE
> Heavy encrypted
> computation
> Slow Inference
> User
> Input 𝑋
> …
> Public encoder
> outside FHE
> Representation ෨𝑋
> Encode
> Output 𝑌
> Server
> Small proprietary
> predictor under
> FHE
> Light encrypted
> computation
> Encrypted
> Output [෨𝑌 ]
> Encrypt
> Fast Inference
> Under FHE Private (light FHE) Private (heavy FHE) Public (outside FHE)
> 𝐹 ෨𝐹 𝑃
> Decrypt
> &Decode
> Encrypted Input [෨𝑋 ]
> Fig. 1. Traditional private inference protects the complete model under FHE. Public-encoder offloading executes an independently public en-coder outside FHE and protects the task-specific predictor. The user still needs the predictor to obtain Y;Gecko is designed to limit additional model-side exposure caused by this partition.

nor output; this protection follows from the underlying FHE scheme.  

> •

The model-extraction adversary controls the user side and may choose valid raw inputs, submit their prescribed encodings, decrypt the returned results, and train a sur-rogate from the intended class-probability outputs. We assume it knows the protocol, predictor architecture, pub-lic backbone, projection, and all other public components, but not the private predictor parameters or server state. Because the setting is semi-honest, every submitted representation is assumed to be generated by the pre-scribed public encoder from a valid raw input. Chosen-latent queries, malformed ciphertexts, and other protocol deviations are outside scope. Likewise, the model-extraction adversary’s access to class probabilities is an intended API disclosure, not a breach. 

Security goal: avoiding additional model-side exposure. Any pre-diction API may be replicated through ordinary black-box queries. Our goal is to ensure that Gecko ’s offloaded public components do not create an easier feature-space route than that baseline without offloading. We assess this incremental risk under the specific attacks in Section 5.4. 

2.3 Risks of Naive Public-Encoder Offloading 

Exposing proprietary artifacts. One approach moves part of a proprietary model outside protection, exposing partial weights [25], [26], selected neurons [27], intermediate acti-vations [28], or activation statistics [29], [30]. These signals come from the private model and may directly aid surrogate training. Gecko instead offloads an independently available pretrained encoder (e.g., ResNet [31]) and protects the en-tire task-specific predictor. This public-resource assumption prevents direct disclosure of proprietary artifacts, but it does not by itself prevent the exposed representation from simplifying extraction. 

Feature-space shortcut. Prior public-encoder designs com-monly place a shallow private predictor on the final back-bone representation [8], [9]. As Figure 2 illustrates, this exact interface may expose a compact feature space that has already discarded much input variation and retained 3Encoder’s Deep                                      

> Features ෩𝑿
> Features Used for Prediction
> All Input Features
> 𝒀
> Shortcut: ෩𝑿 →𝒀
> Attacker ’s Search Space
> 𝑿 →𝒀
> Fig. 2. A feature-space shortcut. Naive final-layer offloading may expose a selective representation ˜X, giving an extraction adversary an addi-tional feature-to-output route beyond ordinary learning of X→Y.Client Server Client
> …
> 𝑋
> ෨𝑋
> HFG Layer
> Re -center ෨𝑋 ෨𝑌
> Public
> Encoder
> Output
> Mapping
> Proprietary
> Predictor
> benign
> malicious
> 𝑋 ෨𝑋 ෨𝑌 𝑌
> Softmax  𝑌 ෨𝑌
> Projector
> MI Estimator Inference under FHE One -to -one Mapping
> Backbone
> Predictor
> Fig. 3. Gecko workflow. Online inference follows X→frozen multi-level backbone and fixed projections →˜X→encryption →proprietary predictor under FHE →centered logits ˜Y→decryption and softmax
> →Y. HFG is private and fused into the predictor; the MI estimator runs offline during representation design.

prediction-relevant cues. An adversary can still ignore this interface and conduct an ordinary raw-input attack; the incremental concern is that reusing it may reduce the search to a simpler mapping from the exposed features to outputs. This is the feature-space shortcut .Section 3 characterizes the corresponding reduction in prediction-functional uncertainty, motivating the empirical extraction analysis. Section 5.5 then compares this naive interface with Gecko at similar representation dimensions. The design challenge of Gecko is therefore to preserve richer input information without adapting the public encoder to the task or making the encrypted predictor too costly. 

## 3 GECKO OVERVIEW AND DESIGN PRINCIPLES 

3.1 Workflow Overview 

Gecko combines three design choices. First, a frozen, task-agnostic backbone supplies features from multiple levels rather than only its selective final layer. Second, fixed Fastfood projections compress those public features, while Hierarchical Feature Gating (HFG) privately balances their contributions without adding FHE depth after fusion. Third, ideal information-theoretic criteria guide the public/private partition, and estimated mutual information provides an offline diagnostic of input-representation richness. 

End-to-end inference. Figure 3 separates the online work-flow into three stages:  

> •

Public encoding. The user applies the frozen multi-level backbone and fixed projections in plaintext to obtain ˜X. 

> •

Proprietary prediction. The user encrypts ˜X, and the server evaluates the task-specific predictor under FHE to pro-duce encrypted centered logits ˜Y . 

> •

Output reconstruction. The user decrypts ˜Y and applies the fixed softmax mapping to recover the intended probabili-ties Y .The public encoder contains no task-adapted parame-ters, whereas HFG and the predictor are learned for the service and remain private. The following formalization develops ideal conditions for this separation; Sections 5.4 and 5.5 separately evaluate whether the public components help the tested extraction attacks. 

3.2 Formal Setup 

We formalize the public component P as side information and characterize when observing it preserves uncertainty about prediction-relevant functionality. The resulting cri-teria guide Gecko ’s public/private partition, while the ex-traction experiments evaluate the corresponding behavior under concrete learning attacks. Let F be a random prediction functionality from task input X to output Y , and let P be public side information. Raw inputs are sampled independently of the deployed functionality and public component, X ⊥ (F, P ), and Y is determined by the prediction channel once (X, F ) are given. 

Gecko decomposes this workflow as X → ˜X → ˜Y → Y ,where ˜F denotes the protected mapping ˜X → ˜Y . Table 1 summarizes the notation.                           

> TABLE 1 Summary of Notation.
> Symbol Description
> X, Y Task input and output
> FEnd-to-end functionality X7→Y
> ˜X, ˜YInput representation and protected output
> ˜FProtected private component of functionality ˜X7→˜YPExposed public component or side information
> H(·), H (· | · )Entropy and conditional entropy
> I(·;·), I (·;· | · )Mutual and conditional mutual information
> A⊥B|CConditional independence
> ℓX, ℓ YNormalized input and output information losses

Definition 3.1 (Prediction-relevant functionality ). Let G be a random functionality with distribution μ over supp( G). For input U and output V , it induces 

Pr  

> μ

(v | u) = X

> g∈supp( G)

Pr( v | u, g )μ(g).

We call G prediction-relevant when the map μ 7 → Pr μ(· | ·) is injective. Functionally inert parameter differences that induce the same prediction behavior are therefore treated as equivalent. 

Lemma 3.2 (Relevance Equivalence ). Let G be prediction-relevant for input U and output V . Assume U ⊥ G, U ⊥ G | P ,and V ⊥ P | (U, G ). Then 

I(G; P ) = 0 ⇐⇒ I(V ; P | U ) = 0 .

Proof. For each p in the positive-probability support, the two input-independence assumptions and prediction-channel assumption give 

Pr( v | u, p ) = X

> g

Pr( v | u, g ) Pr( g | p),

Pr( v | u) = X

> g

Pr( v | u, g ) Pr( g).4

If I(G; P ) = 0 , the mixing distributions are equal, implying 

I(V ; P | U ) = 0 . Conversely, conditional independence of V and P given U makes the two induced prediction functions equal. Injectivity in Definition 3.1 then gives 

Pr( G | P = p) = Pr( G) and hence I(G; P ) = 0 .For (U, G, V ) = ( X, F, Y ), the assumptions follow from 

X ⊥ (F, P ) and the prediction channel. Application to the latent workflow requires the corresponding latent-input assumptions stated below. 

3.3 Public-Offloading Criteria 

We first exclude prediction-related artifacts from the public component, then characterize the complementary risk cre-ated by a selective latent interface. 

Definition 3.3 (Criterion 1: Prediction-Artifact Independence ).

P satisfies prediction-artifact independence if 

I( ˜Y ; P | ˜X) = 0 .

Under the following latent-input conditions, Criterion 1 also characterizes the dependence between P and the pro-tected functionality ˜F .

Corollary 3.4. Assume ˜F is prediction-relevant, ˜X ⊥ ˜F ,

˜X ⊥ ˜F | P , and ˜Y ⊥ P | ( ˜X, ˜F ). If Criterion 1 holds, then 

I( ˜F ; P ) = 0 .Proof. Apply Lemma 3.2 with (U, G, V ) = ( ˜X, ˜F , ˜Y ).These assumptions reflect Gecko ’s public-resource con-struction. Raw inputs are sampled independently of the deployed task-specific functionality, and the frozen back-bone and fixed projection are selected independently of that functionality. Applying this fixed public encoder to indepen-dently sampled inputs yields the latent-input independence conditions used above. 

Definition 3.5 (Feature-Space Shortcut ). For nonzero H(X)

and H(Y ), define the normalized information losses 

ℓX = 1 − I(X; ˜X)

H(X) = H(X | ˜X)

H(X) ,ℓY = 1 − I(Y ; ˜Y )

H(Y ) = H(Y | ˜Y )

H(Y ) .

The pair (ℓX , ℓ Y ) measures feature-space shortcut severity: larger values indicate that the latent interface preserves less uncertainty from the original input or output. A selective final-layer interface primarily creates a large input loss ℓX .Criterion 1 removes prediction-related artifacts from P ,while the feature-space shortcut captures the complemen-tary risk that a selective representation presents the adver-sary with a substantially reduced input space. 

Lemma 3.6 (Conditional Functionality Correspondence ). Sup-pose F and ˜F are conditionally functionally equivalent once P is fixed, meaning 

H(F | P, ˜F ) = 0 , H( ˜F | P, F ) = 0 .

Then 

H(F | P ) = H( ˜F | P ).

If Criterion 1 and the conditions of Corollary 3.4 hold, then 

H(F | P ) = H( ˜F ).

Proof. Expand H(F, ˜F | P ) in both orders. Conditional functional equivalence gives H(F | P ) = H(F, ˜F | P ) = 

H( ˜F | P ). Criterion 1 and Corollary 3.4 give I( ˜F ; P ) = 0 ,and hence H( ˜F | P ) = H( ˜F ).

Definition 3.7 (Criterion 2: Information Preservation ). Infor-mation preservation is satisfied for the task when ℓX =

ℓY = 0 , equivalently, 

I(X; ˜X) = H(X), I (Y ; ˜Y ) = H(Y ).

Assumption 3.8 (Lossless Representation–Functionality Consis-tency ). If Criterion 2 holds, then the latent and original function-alities have equal uncertainty: 

H( ˜F ) = H(F ).

This assumption captures the limiting behavior behind representation preservation. As the latent interface retains more input and output information, it distinguishes abroader range of prediction-relevant behaviors, so the un-certainty of ˜F is expected to approach that of F . Assump-tion 3.8 requires only the lossless endpoint of this intuition and leaves the rate of approach unrestricted. 

Theorem 3.9 (Ideal Public-Offloading Guarantee ). Assume the conditional functional-equivalence conditions of Lemma 3.6, the conditions of Corollary 3.4, Criteria 1–2, and Assumption 3.8. Then 

H(F | P ) = H(F ).

Proof. Lemma 3.6 and Criterion 1 give H(F | P ) = H( ˜F ).Criterion 2 and Assumption 3.8 give H( ˜F ) = H(F ). Com-bining the two equalities yields H(F | P ) = H(F ).The theorem places ideal public-encoder offloading at the same prediction-functional uncertainty as protecting the entire pipeline: exposing the public encoder leaves the original uncertainty H(F ) unchanged. For comparison with a traditional end-to-end trained model Fe2e , we model function families with comparable predictive capacity and task performance as having comparable functionality un-certainty, H(F ) ≈ H(Fe2e ). Prior results show that pre-dictors built from pretrained intermediate representations can match or approach conventional end-to-end fine-tuning across diverse transfer settings [32], [33], [34], supporting this reference choice. Thus, at the ideal limit, Gecko targets the functionality uncertainty of full-model protection while moving generic public computation outside FHE. 

Implications for Gecko . Criterion 1 calls for a frozen public backbone and fixed projection free of task-adapted artifacts. Criterion 2 calls for a broad multi-level input representation and a bijective centered-logit output mapping. The centered-logit mapping realizes ℓY = 0 exactly. The multi-level projected representation reduces ℓX , and estimated NPMI provides relative evidence of progress toward the lossless endpoint. Section 4 develops these choices and the offline mutual-information diagnostic. 5

## 4 GECKO METHOD 

Guided by the design principles in Section 3, we now instantiate Gecko ’s three workflow stages. 

4.1 Stage 1: Public encoding 

The first stage constructs a useful public representation without adapting the encoder to the server’s private task or data. Task adaptation would make the exposed component deployment-specific and undermine the public-resource as-sumption. A natural starting point is therefore an off-the-shelf pretrained backbone. Such models are publicly available, broadly reusable, and widely used as generic feature extrac-tors in transfer learning [35], [36]. We keep this backbone frozen. This choice is important not merely for convenience, but for security: any fine-tuning or task-specific adaptation would make the public encoder reflect the protected deploy-ment, causing the exposed component to carry information about the private task or data distribution. 

Hierarchical feature aggregation. One may worry that such a restriction makes the encoder too weak. In practice, how-ever, modern pretrained backbones already provide high-quality reusable representations. Transfer-learning studies have shown that useful predictive information is not limited to the final layer; intermediate activations can also pro-vide comparable or even stronger utility for downstream tasks [32], [33], [34]. This observation is particularly impor-tant: if we rely only on the deepest feature of the public backbone, we risk creating the feature-space shortcut dis-cussed in Section 2.3. To limit this shortcut, we aggregate multiple levels of the frozen backbone. Earlier layers retain local patterns and structure that deeper layers gradually discard, while later layers supply semantic features. Their combination exposes broader input information than the final representation alone. This richer representation introduces a second challenge: raw multi-level features are too large to use directly. Con-catenating several intermediate activation maps can pro-duce millions of values for a single input, which would substantially increase communication and may force the encrypted predictor to become large enough to cancel the efficiency gain of offloading. We address this problem by compressing each selected feature map before concatena-tion. For each intermediate representation Ei(X) from the 

i-th selected layer of the public backbone E, we apply a random projection and concatenate the projected features: 

˜X = Concat (M1E1(X), M 2E2(X), . . . , M LEL(X)) , (1) where L is the number of included layers and each Mi

projects the corresponding feature into a D-dimensional vector. The Johnson–Lindenstrauss result motivates random projection because a sufficiently large projected dimension preserves pairwise geometry with high probability [37]. Here D is an empirical richness–cost parameter: increasing it generally improves preservation but increases commu-nication and encrypted computation. We implement each fixed, public projection with the Fastfood transform [13], a 1 2 4 8 16 32 50 

10 

15 

Number of Included Layers MI (nats) NPMI MI Ideal 

0

0.5

1

> NPMI
> Fig. 4. The preserved information for CIFAR10 when including features from different numbers of MobileNetV3 layers.

structured approximation that avoids storing and multiply-ing by a dense random matrix. Therefore, the projection is a compression mechanism, not a learned defense. 

Estimating preserved information. After projection, an-other question remains: how do we assess whether the representation is becoming richer? There is no task-independent threshold that establishes the absence of a shortcut. We therefore use MI estimation [38] as a practi-cal assessment of information preservation (Definition 3.7). Specifically, we define the normalized preserved mutual information (NPMI) as 

NPMI = I(X; ˜X)

I(X; ˜Xlossless ) , (2) where ˜X is the exposed representation and ˜Xlossless is a reference encoding with I(X; ˜Xlossless ) = H(X). NPMI is estimated offline and interpreted relatively: a larger value indicates that the estimator attributes more retained in-put information to the representation. Reported values are capped at 1.0 relative to the reference estimate. A low estimate may prompt the designer to include more feature levels or increase D, subject to system cost. Figure 4 shows how the estimate changes as intermediate levels are in-cluded. MI estimation serves as an offline design diagnostic rather than an inference-time operation. Table 2 reports the estimated NPMI values. The headline system results use the 104k/88k configurations and yield the reported 0.4–2.2-second latency range. The larger 468k Caltech101 and 396k Organ configurations serve as offline richness diagnostics. Together, these settings illustrate how increasing representation richness selects a different operat-ing point in the richness–cost tradeoff.                                             

> TABLE 2 Estimated NPMI across latent dimensions.
> Task Pets MNIST EuroSAT CIFAR100 FSDD Caltech101 Dimension 104k 104k 104k 104k 104k 104k 208k 468k
> NPMI 0.95 1.0 1.0 0.99 0.96 0.73 0.90 0.97
> Task Blood Path. Breast Skin PN Organ Dimension 88k 88k 88k 88k 88k 88k 352k 396k
> NPMI 1.0 0.93 0.95 1.0 1.0 0.88 0.93 0.99

Implementation. We obtain the pretrained backbones from Timm [35]. For ResNet50v2, we select the outputs of every activation together with the stem, normalization, global-pooling, and classifier layers, yielding L = 52 feature groups. For MobileNetV3, we select all ReLU, Hardsigmoid, 6

1 10 20 30 40 50 

0

100 

200 

300 

Index of Included Layers 

> L-2 Norm

Original 

with HFG 

> Fig. 5. Representation norms across layers before and after applying the HFG layer.

and Hardswish outputs together with the stem, global-pooling, flatten, and classifier layers, yielding L = 50 and 

44 groups for the Large and Small variants. Each group is projected to D = 2 ,000 values in the main configurations, so D is the per-layer projection size and the total dimension is L × D: 104,000 for ResNet50v2, 100,000 for MobileNetV3-Large, and 88,000 for MobileNetV3-Small. We adjust the in-cluded levels or D offline using the relative NPMI estimate together with latency and communication costs. 

4.2 Stage 2: Encrypted proprietary inference 

After the public encoder produces the latent representation 

˜X, the server no longer needs to evaluate a large end-to-end network under FHE. Instead, it only evaluates a compact proprietary predictor over ˜X. We refer to this protected predictor as the latent model , since it operates in the representation space produced by the public encoder and outputs the latent result ˜Y for later reconstruction. Pretrained intermediate representations can support the latent model with a much smaller or even linear classi-fier [32], [33], [34], so the protected component need not relearn feature extraction. 

Hierarchical feature gating. This design still faces an im-portant training challenge. The latent representation ˜X is formed by concatenating projected activations from differ-ent backbone levels. These features may have substantially different magnitudes and statistical properties, as illustrated in Figure 5. If they are directly fed into the latent model, features with larger norms may dominate optimization even when they are not the most useful for the downstream task, while informative but smaller-scale features may be underused. This imbalance can make training unstable and may also increase overfitting, especially when the target task depends unevenly on different abstraction levels. To address this issue, we introduce a lightweight Hi-erarchical Feature Gating (HFG) layer at the input of the latent model. Let E′

> i

(X) = MiEi(X) denote the projected feature from the i-th selected layer, as defined in Equation 1. HFG rescales the contribution of each selected layer before concatenation: HFG ( ˜X) = Concat (w1E′

> 1

(X), . . . , w LE′

> L

(X)) , (3) where w1, . . . , w L are learned nonnegative gating weights inside the proprietary predictor. In the released implemen-tation, each gate is obtained independently from a trainable parameter qi:

wi = sigmoid( qi). (4) This mechanism gives the latent model an explicit way to balance features from different abstraction levels. Rather than forcing the following fully connected layer to com-pensate implicitly for scale differences, HFG rescales each feature group. The predictor can therefore emphasize useful levels and suppress noisy ones. Section 5.8 evaluates its effect on convergence and overfitting. HFG improves training without increasing the crypto-graphic cost of inference. Once training is finished, the learned gating operation is only a fixed linear rescaling of the input feature groups. Therefore, it can be absorbed into the first fully connected layer of the latent model: 

Wnew = Wold WHFG , (5) where WHFG is the matrix form of the learned gating trans-formation. After this absorption, encrypted inference does not need an additional FHE operation for HFG. 

FHE instantiation. The protected computation uses pure FHE and a single interaction round. Following prior work [8], we densely pack the representation into ciphertext slots. The headline one-layer predictor is linear after HFG fu-sion; for supported multilayer predictors, nonlinear layers use a degree-2 polynomial approximation of ReLU [39]. The lightweight fully connected computation is parallelized across output channels. 

4.3 Stage 3: Output mapping 

The intended API output is the class-probability vector, which the user is allowed to recover. Returning raw log-its would reveal one unnecessary degree of freedom: for any constant c, softmax( z) = softmax( z + c). The output interface therefore removes this common offset and reveals exactly the information needed to reconstruct the intended probabilities. 

Bijective logit mapping. To avoid this, we re-center the latent logits before returning them. Let logits denote the output of the latent model over C classes. We compute 

˜Yi = logits i − 1

C

> C

X

> j=1

logits j . (6) This fixed, parameter-free operation removes the softmax-invariant offset and leaves the relative logit differences. The centered logits are in one-to-one correspondence with the class-probability vector, i.e., Y = softmax ( ˜Y ) ⇔ ˜Y =log Y − 1

> C

PCj=1 log Yj . Thus the user intentionally recovers 

Y , but receives no additional absolute-logit offset. The same mapping is used for every query and does not change prediction correctness. Together, these stages move a task-agnostic , feature-rich public encoder outside encrypted computation while retain-ing only the compact proprietary predictor under FHE. 

## 5 EXPERIMENTS 

We evaluate Gecko ’s efficiency and utility across realis-tic tasks, then separately assess its incremental extraction risk under concrete component-reuse attacks. Our imple-mentation is available at https://github.com/CassiniHuy/ gecko-infer. 7

5.1 Experiment Setup 

FHE Scheme and Parameter Setting. We use the FHE li-brary Lattigo [40] with the CKKS scheme [41], the conjugate-invariant variant [42] with a polynomial degree of N =8192 , a total ciphertext modulus of approximately 2218 , and a plaintext scale of 230 . These parameters ensure at least 128-bit security [43]. The baseline approaches [21], [20], [5] use their original parameter settings under the same security level. 

Hardware Environment. All experiments are conducted on a Linux machine with dual 2.10 GHz CPUs and 128 GB of memory. We use a CPU-only environment for inference-time evaluation to ensure fair overhead comparisons, although GPUs could further accelerate the public encoder. Reported latency numbers are averaged over 16 test runs. 

Neural Network Architectures. ResNet50v2 [44] serves as the default public backbone in Gecko . We also evaluate MobileNetV3 [45] as a compact alternative designed for resource-constrained devices. This combination allows us to study both a strong, widely used pretrained backbone and a lightweight practical one. All pretrained models are obtained from the Timm library [35]. 

Tasks and Datasets. To evaluate Gecko in realistic settings, we use a diverse set of classification tasks spanning natural images, biomedical images, and audio spectrograms [14], [15], [16], [17], [18], [19], [2]. These tasks cover input reso-lutions up to 224 × 224 and up to 101 classes, and include privacy-sensitive biomedical applications. For each dataset, we follow the original train/test split when available. We fine-tune for 100 epochs with learning rates ranging from 0.0001 to 0.01 and report the final test accuracy. 

Metrics. We evaluate the following dimensions:  

> •

Accuracy : accuracy of the private inference service.  

> •

Accuracy (Baseline) : accuracy of a standard transfer-learning baseline that trains a classifier on the final layer of the same public backbone under the same settings.  

> •

Crypto. MACs : number of multiply-accumulate operations (MACs) performed under cryptographic protection.  

> •

Reduced MACs : number of protected MACs reduced by 

Gecko relative to the corresponding baseline for the same task, backbone, and input size.  

> •

Communication : total data exchanged between the client and server per inference, without pre-computation.  

> •

End-to-End Time : total latency per private inference, from the raw input to the final output, without precomputation. Our evaluation is designed to reflect realistic use cases rather than only small benchmark settings. Besides CIFAR-like standard datasets, we include higher-resolution image tasks such as Caltech101, as well as biomedical image appli-cations where private inference is especially meaningful. 

5.2 Performance Comparison to Baselines 

We begin with CIFAR10 because it is the most widely used benchmark in prior private-inference studies, which enables the most direct comparison with existing baselines. We then extend the evaluation to more realistic real-world tasks in the following subsections. 

Baseline Setup. This comparison measures how much en-crypted computation can be removed while protecting pro-prietary functionality. The end-to-end baselines protect a complete ResNet [31], including its generic feature extractor. In contrast, Gecko moves only an independently available, frozen backbone outside FHE; all task-specific parameters and the proprietary input–output mapping remain in the protected predictor. Thus, although Gecko cryptographically protects fewer operations, it protects the same type of pro-prietary functionality, maintains comparable task accuracy, and provides comparable extraction robustness under the evaluated attacks in Section 5.4. For smaller ResNets, we use an optimized pure-FHE baseline [21], which supports models up to ResNet20. For ResNet50 with 224 × 224 

inputs, we use the hybrid Cheetah [20] and Rhombus [5] results in their reported LAN setting. Gecko uses comparable ResNet18 and ResNet50v2 public backbones. Naive final-layer offloading can achieve similar or lower overhead, but it exposes the selective representation con-sumed by its private head. At matched representation di-mensions, our extraction experiments find this shortcut sub-stantially easier to imitate than Gecko for query budgets of 200–5,000 (Figure 7c). It therefore trades model-side robust-ness for efficiency and is evaluated in the extraction ablation rather than included as a performance baseline here.                                       

> TABLE 3 CIFAR10 efficiency comparison. Gecko moves only an independently public generic encoder outside FHE while protecting all task-specific proprietary functionality and maintaining comparable accuracy. Reduced MACs are measured against a complete network matching each Gecko backbone; the displayed columns report representative prior private-inference systems. Naive final-layer offloading offers similar efficiency but is substantially easier to extract, so we evaluate it separately in Section 5.5.
> Gecko End-to-End Baseline Crypto. Scheme Pure-FHE (CKKS) Pure-FHE [21] Hybrid [20] Hybrid [5] Architecture ResNet18 ResNet50v2 ResNet20 ResNet50 Accuracy 89.48% 93.51% 90.39% 93.62% 93.62% Crypto. MACs 38.01K 1.04M 41.32M 4.13G 4.13G Reduced MACs 37.22M 4.09G N/A N/A N/A Communication 4.45 MB 10.8 MB 3.15 MB 1,815 MB 1,152 MB End-to-End Time 0.33 s 0.99 s 644 s 188 s 145 s

Results. Table 3 shows that moving independently pub-lic computation outside FHE reduces cryptographic cost while maintaining comparable task accuracy. Crypto. MACs fall from 41.32M/4.13G in the end-to-end baselines to 38.01K/1.04M in the ResNet18/ResNet50v2 Gecko config-urations. Corresponding end-to-end latency is 0.33 s and 0.99 s, versus hundreds of seconds for the complete pro-tected networks. This advantage is complementary to lower-level cryptographic optimizations [5], [46]. 

5.3 Generalization Across Tasks and Backbones 

Image tasks with different backbones. To show that Gecko 

is not tied to a single benchmark or backbone, we evaluate it on a diverse set of image classification tasks with input resolutions up to 224 × 224 and up to 101 classes, includ-ing Pets [16], MNIST [17], EuroSAT [18], CIFAR100 [14], and Caltech101 [47]. We instantiate Gecko with two widely used public backbones: ResNet50v2 and MobileNetV3 [45]. MobileNetV3 is included to evaluate whether the public encoder can remain practical even in resource-constrained settings, since it contains only 5.48M parameters, substan-tially fewer than ResNet50v2’s 25.56M. 8

TABLE 4 Performance of Gecko on various tasks.                                                                            

> Task MNIST EuroSAT CIFAR100 Pets Caltech101 Resolution 28 64 32 224 224 Categories 10 10 100 37 101 ResNet50v2 as Backbone Accuracy 96.55% 95.03% 78.41% 90.19% 95.48% Accuracy (Baseline) 96.53% 94.85% 76.67% 90.92% 94.54% Crypto. MACs 1.04M 1.04M 10.00M 3.85M 10.50M Reduced MACs 79.16M 333.65M 83.42M 4.09G 4.09G Communication 10.8 MB 10.8 MB 10.8 MB 10.8 MB 10.8 MB End-to-End Time 0.98 s 0.97 s 2.17 s 1.28 s 2.20 s MobileNetV3 as Backbone Accuracy 97.27% 94.41% 76.08% 89.83% 95.02% Accuracy (Baseline) 97.85% 94.30% 75.61% 88.23% 95.04% Crypto. MACs 1.00M 1.00M 10.00M 3.70M 10.10M Reduced MACs 6.85M 20.09M 7.08M 215.36M 215.36M Communication 10.8 MB 10.8 MB 10.8 MB 10.8 MB 10.8 MB End-to-End Time 0.78 s 0.78 s 2.00 s 1.09 s 2.01 s

Results. Table 4 shows that Gecko consistently maintains accuracy close to the corresponding transfer-learning base-lines across all evaluated tasks, while greatly reducing the amount of protected computation. Across these image tasks, end-to-end private inference completes in 0.78–2.20 seconds with communication of at most 10.8 MB. The results also show that Gecko is robust to the backbone choice: while ResNet50v2 delivers the strongest default performance, the compact MobileNetV3 backbone remains highly competi-tive, indicating that Gecko does not rely on a single large public encoder to be effective. 

From image to audio. To further test whether Gecko is limited to vision backbones and tasks, we evaluate it on the FSDD audio task [19]. We consider two public encoders: an ImageNet-pretrained ResNet50v2 and Google’s AudioSet-pretrained YAMNet [48], [49], which uses a MobileNetV1 backbone with only 3.73M parameters. Gecko achieves 96.33% accuracy with 0.62 s latency using YAMNet, and still reaches 96.76% accuracy with 0.97 s latency even when the public encoder is an image-pretrained ResNet50v2. These results suggest that Gecko can work with readily available off-the-shelf encoders beyond a single modality. 

Biomedical case study. We next evaluate Gecko on six biomedical image processing tasks [2] covering different biomedical image types and modalities at resolution 128 ×

128 . This setting is particularly relevant because the in-puts are privacy-sensitive and realistic private inference is therefore especially valuable. To keep the public component lightweight, we use MobileNetV3-Small [35] as the public backbone, which has only 1.59M parameters, and use a single fully connected layer as the latent model. Table 5 shows that Gecko remains accurate and efficient in these biomedical settings. Across all six tasks, its accuracy stays within 1% of the corresponding baseline, while the encrypted computation remains below 1M Crypto. MACs and the total latency is at most 604 ms. These results show that Gecko generalizes across various realistic tasks and privacy-sensitive biomedical applications, while remaining effective with both strong and compact public backbones. 

TABLE 5 Performance of Gecko on biomedical tasks.                                                             

> Task Blood Organ Path. Breast Skin PN Modality Micro. CT Hist. US Derm. X-Ray Resolution 128 128 128 128 128 128 Categories 811 9272Accuracy 95.73% 76.28% 88.48% 87.18% 78.75% 87.66% Accuracy (Baseline) 94.94% 75.94% 88.30% 87.82% 78.75% 88.62% Crypto. MACs 704K 968K 792K 176K 616K 176K Reduced MACs 7.85M 7.85M 7.85M 7.85M 7.85M 7.85M Communication 9.2 MB 9.2 MB 9.2 MB 9.2 MB 9.2 MB 9.2 MB End-to-End Time 575 ms 604 ms 507 ms 413 ms 555 ms 429 ms

5.4 Security Evaluation: Extraction Risk Analysis 

We now ask whether Gecko ’s public components im-prove extraction over ordinary black-box access. This is an incremental-risk question, not a claim that Gecko eliminates model extraction. 

Attack setup. On Caltech101, the model-extraction ad-versary queries valid ImageNet inputs [50] and distills the returned class-probability vectors, following Knockoff Nets [11]. Every surrogate uses the predictor architecture of its corresponding target; Gecko and its component-reuse surrogates use the same one-layer private head. Surrogates train for 30 epochs under the same query budgets shown in Figure 6. We report surrogate accuracy on the true task labels because it measures attacker utility. 

(a) Comparison to the baseline model. This experiment asks whether Gecko itself becomes an easier extraction target than a standard baseline. We compare two target models: a baseline model trained by standard transfer learning with a ResNet50v2 backbone, whose task accuracy is 94.54%, and a Gecko target using the same ResNet50v2 backbone as the public encoder, whose task accuracy is 95.48%. For both targets, the attacker uses the same pretrained backbone architecture as the target model, initializes it with ImageNet-pretrained weights, and trains the whole surrogate model. This setting evaluates whether replacing the baseline model with Gecko increases the attacker’s extraction success under the same public-resource assumption. 

(b) Public-component reuse attacks. This experiment fixes 

Gecko as the target and progressively reuses its public com-ponents. The free-tuning attack initializes the same pretrained backbone architecture and fine-tunes the complete surro-gate. The backbone-reuse attack instead reuses and freezes 

Gecko ’s exposed backbone, training only the downstream predictor. The encoder-reuse attack reuses and freezes the complete public encoder, including the exact projection. These variants test whether direct reuse helps under the evaluated optimization strategies. 

Results. Figure 6a shows lower surrogate task accuracy against Gecko than against the transfer-learning target at every tested budget. At 6400 queries, the values are 85.71% and 93.11%, respectively, while Gecko itself has slightly higher target accuracy. The 85.71% result is substantial or-dinary extraction risk, not a security threshold; the relevant observation is that replacing the baseline target with Gecko 

did not increase attacker utility in this experiment. Figure 6b shows 85.71%, 83.38%, and 55.81% for free tuning, backbone reuse, and encoder reuse at 6,400 queries. Free tuning achieves the highest accuracy at every budget 9

100 400 1600 6400 

0

0.2

0.4

0.6

0.8

1

Query Budget 

> Attack Accuracy

Baseline as target 

Gecko as target 

(a) Comparison to baseline model 

100 400 1600 6400 

0

0.2

0.4

0.6

0.8

1

Query Budget 

Free tuning 

Backbone reuse 

Encoder reuse 

(b) Public-component reuse  

> Fig. 6. Surrogate task accuracy under the evaluated extraction settings. Target accuracies are 94.54% (baseline) and 95.48% ( Gecko ). (a) The same transfer-learning attack has lower accuracy against Gecko . (b) Backbone reuse freezes the public backbone; encoder reuse addition-ally freezes the exact projection. Reuse provides no consistent advan-tage over free end-to-end surrogate tuning and is less effective from 200 queries onward.

from 200 onward because it can optimize the complete surrogate, whereas the reuse variants freeze the exposed components. Across the tested budgets, component reuse therefore offers no consistent extraction advantage. 

5.5 Security Ablations for Public Offloading 

We next examine two risks highlighted by our formaliza-tion: exposing prediction-related artifacts through the public component, and creating a feature-space shortcut through an overly selective representation. We study both risks un-der practical model-extraction settings with query access to the target model and publicly available substitute data. 

Prediction-artifact independence ablation. We first exam-ine whether exposing additional prediction-related artifacts can help an attacker through two experiments:  

> •

Exp. 1 (Increasing Component Reuse) : We test whether the public encoder used by Gecko leaks exploitable informa-tion. The attacker builds the surrogate model under three settings with increasing levels of reuse: (1) same Gecko design , where the attacker follows the same public-encoder design but uses a different backbone and projection; (2) 

+same backbone , where the attacker further uses the same backbone as the target but reinitializes the projection parameters; and (3) +same projection , where the attacker also uses the same projection as the Gecko target, i.e., exactly the same public encoder exposed. If the exposed encoder contains exploitable information, increasing the overlap with the target encoder should improve extraction performance; otherwise, the results should remain similar across these settings.  

> •

Exp. 2 (Partial-weight exposure) : we construct an insecure baseline that exposes incomplete but informative model weights. Following prior work [27], we protect only a subset of neurons in the first two layers and expose the remaining model. The protected neurons are selected by importance under structured pruning [51]. Note that the exposed partial model is no longer independently usable for accurate inference. Figure 7 reports the results on CIFAR10 using STL10 [52] as the substitute query set. Figure 7a shows no consis-tent accuracy increase as the attacker reuses more of the public encoder, indicating that it provides little exploitable deployment-specific information in this setting. In contrast, Figure 7b shows that exposing informative model weights substantially helps extraction: at 5,000 queries, partial expo-sure raises attack accuracy by approximately 43–44 percent-age points over full protection. 

Feature richness ablation. We next examine whether a lack of feature richness leads to the feature-space shortcut prob-lem, which makes the proprietary model easier to imitate.  

> •

Exp. 3 (Naive representation) : we replace Gecko ’s multi-level latent representation with a naive deep representation built only from the exposed backbone’s final features, following prior encoder-splitting designs [8], [9]. The rep-resentation dimension is kept at the same level as that of 

Gecko , so the main difference is the amount of preserved input information rather than the raw feature size.  

> •

Exp. 4 (Varying feature richness) : we study whether richer public representations make the remaining private pre-dictor harder to extract. We vary the latent dimension to control how much input information is preserved in the 

Gecko public representation, and use the estimated input– representation MI as a proxy for feature richness. To avoid underfitting due to the increased feature dimension, we ensure every surrogate model has converged. Figure 7c is our most controlled shortcut comparison: at similar total representation dimensions (about 100k), the naive final-layer target yields 19–27 percentage points higher surrogate task accuracy for query budgets of 200– 5,000. This is consistent with the selective-interface risk. In Figure 7d, the largest tested representation has higher estimated MI and 26–29 percentage points lower surrogate accuracy than the smallest representation. 

5.6 Representation Design Ablation 

Figure 8 shows the effect of varying the total latent di-mension from 2,000 to 104,000 with the public backbone fixed. Accuracy is nearly stable from about 20,000 onward, whereas estimated MI rises from 4.01 to 11.78 nats. Latency and communication concurrently rise from about 0.3 s to 1.0 s and from 1.3 MB to 10.8 MB. This is distinct from the main configuration’s D = 2 ,000 per-layer projection size, which produces 104,000 total values for ResNet50v2. 

5.7 Client-side Overhead Analysis 

Figure 9 reports the client-side overhead introduced by 

Gecko . Since the output mapping is negligible, the main prac-tical cost is executing the public encoder. Under our CPU-only setting, client-side processing remains below 0.5 s, with memory usage ranging from roughly 940 MiB to 1,368 MiB across the evaluated latent dimensions. Thus, the client overhead remains practical while increasing in a predictable way with richer exposed representations. This overhead can be further reduced through GPU acceleration, lightweight public backbones such as MobileNet variants, and standard compression techniques such as quantization [53]. It can also be offloaded to the server by protocols that protect only user privacy for public encoding. 10 

100 400 1600 5000 

0.2

0.4

0.6

0.8

Query Budget 

> Attack Accuracy

Same Gecko design 

+Same backbone 

+Same projection 

(a) Exp. 1: Component Reuse 

100 400 1600 5000 

0.2

0.4

0.6

0.8

Query Budget 

> Attack Accuracy

100% 75% 

50% 25% 

(b) Exp.2: Partial-weight ex-posure 

100 400 1600 5000 

0.2

0.4

0.6

0.8

Query Budget 

> Attack Accuracy

Gecko Deep Rep. 

(c) Exp. 3: Naive representa-tion 

4 6 8 10 12 

0.6

0.8

MI 

|Q|=800 |Q|=3200 

|Q|=5000 NPMI 

0.2 

0.6 

1NPMI 

(d) Exp. 4: Feature Richness  

> Fig. 7. Extraction ablations. (a) Increasing public-component reuse pro-duces no consistent improvement. (b) Exposing more private-model information raises surrogate accuracy; percentages denote protected neurons. (c) At similar representation dimensions, naive final-layer of-floading is easier to imitate than Gecko . (d) Higher estimated represen-tation richness corresponds to lower attack accuracy.

5.8 HFG Ablation 

Figure 10 compares latent-model training with and with-out HFG while holding all other settings fixed. On CI-FAR10, HFG produces a smoother test-accuracy trajectory and raises the final test accuracy from 90.10% to 92.67%. The smaller Pets dataset exposes the overfitting effect more clearly: without HFG, training accuracy reaches 100% while test accuracy remains around 70%; with HFG, test accuracy exceeds 90%. These results show that balancing feature groups improves optimization stability and generalization. 

## 6 RELATED WORK 

Cryptographic Optimizations. Pure-FHE inference protects the complete model [3], including its architecture when circuit-private schemes are used [54], [55]. Prior optimiza-tions improve ciphertext packing and reduce rotations [8], [20], [21], [23], [5], or approximate nonlinear activations for arithmetic FHE schemes [56], [41], [57], [58], [59], [60]. Hybrid protocols reduce latency but require depth-dependent interaction and high communication [61], [62]; precomputation-based systems reduce online cost but retain substantial per-input work [63], [6], [64], [7]. Gecko changes the system partition and remains compatible with these optimizations. 

Heuristic Approaches. Other systems protect only a small predictor over pretrained features [8], [9], [10], selected neu-

0.2 0.6 1

·10 5

0.8

0.85 

0.9

0.95 

1

> Accuracy

(a) Accuracy 

0.2 0.6 1

·10 5

0

5

10 

15 

> MI (nats)

(b) Mutual Information 

0.2 0.6 1

·10 5

0

0.5

1

1.5

2

> Total Time (s)

(c) Time 

0.2 0.6 1

·10 5

0

5

10 

15 

> Commu. (MB)

(d) Communication 

> Fig. 8. Representation richness–cost tradeoff as total latent dimension increases. (a) Task accuracy, (b) estimated input–representation MI, (c) CPU end-to-end latency, and (d) communication. Accuracy stabilizes before estimated MI, while system cost grows with representation size.

0.2 0.6 1

·10 5

0

0.2

0.4

0.6

0.8

1

> Time (s)

(a) Time 

0.2 0.6 1

·10 5

800 

1,000 

1,200 

1,400 

> Mem. (MiB)

(b) Memory 

> Fig. 9. Client overhead of executing the public encoder locally at 32-bit precision as total latent dimension increases: (a) CPU time and (b) memory usage.

rons [27], or selected layers [65], [26]. Related acceleration techniques also expose model details without analyzing their leakage [28], [29], [30]. As Section 2.3 shows, assuming that an exposed partial network is unusable can weaken model-side security; Gecko instead makes the public/private split explicit and evaluates its extraction risk. 

## 7 DISCUSSION AND CONCLUSION 

Gecko demonstrates the efficiency potential of treating an independently public encoder differently from a propri-etary task-specific predictor. Its formalization identifies information-theoretic conditions that preserve uncertainty about proprietary functionality, and the extraction evalu-ation shows robustness comparable to ordinary black-box access when attackers reuse the exposed components. To-gether, these results support public-encoder offloading as a practical route to reducing encrypted computation while retaining model-side protection. The evaluated system covers CNN-based classification encoders across image and audio applications. Extending 

Gecko to transformer and generative models introduces richer output spaces, potentially larger private predictors, and new challenges in assessing representation richness. 11 

10 25 40 

0.4

0.6

0.8

1

> Accuracy

(a) CIFAR10 w.o. HFG 

10 25 40 

0.4

0.6

0.8

1

> Accuracy

train 

test 

(b) CIFAR10 with HFG 

10 25 40 

0

0.2

0.4

0.6

0.8

1

> Accuracy

(c) Pets w.o. HFG 

10 25 40 

0

0.2

0.4

0.6

0.8

1

> Accuracy

train 

test 

(d) Pets with HFG 

Fig. 10. HFG ablation on CIFAR10 and Pets over 50 training epochs. Dashed red curves show training accuracy and solid blue curves show test accuracy. HFG stabilizes convergence and reduces overfitting on the smaller Pets dataset. 

These settings provide an important direction for future work. More broadly, efficient private inference requires both cryptographic optimization and an explicit separation between independently public resources and proprietary functionality. 

## REFERENCES 

[1] J. Yang, R. Shi, and B. Ni, “Medmnist classification decathlon: A lightweight automl benchmark for medical image analysis,” in IEEE 18th International Symposium on Biomedical Imaging (ISBI) .IEEE, 2021, pp. 191–195. [2] J. Yang et al. , “Medmnist v2-a large-scale lightweight benchmark for 2d and 3d biomedical image classification,” Scientific Data ,vol. 10, no. 1, p. 41, 2023. [3] R. Gilad-Bachrach, N. Dowlin, K. Laine, K. Lauter, M. Naehrig, and J. Wernsing, “CryptoNets: Applying Neural Networks to En-crypted Data with High Throughput and Accuracy,” in Proceedings of The 33rd International Conference on Machine Learning . PMLR, Jun. 2016, pp. 201–210. [4] L. K. L. Ng and S. S. M. Chow, “SoK: Cryptographic Neural-Network Computation,” in 2023 IEEE Symposium on Security and Privacy (SP) . IEEE, May 2023, pp. 497–514. [5] J. He et al. , “Rhombus: Fast Homomorphic Matrix-Vector Multipli-cation for Secure Two-Party Inference,” in Proceedings of the 2024 on ACM SIGSAC Conference on Computer and Communications Security .ACM, 2024, pp. 2490–2504. [6] J. Feng, Y. Wu, H. Sun, S. Zhang, and D. Liu, “Panther: Practical secure two-party neural network inference,” Trans. Info. For. Sec. ,vol. 20, p. 1149–1162, Jan. 2025. [7] X. Liu et al. , “Antelope: Fast and Secure Neural Network Infer-ence,” IEEE Transactions on Dependable and Secure Computing , pp. 1–13, 2025. [8] A. Brutzkus, R. Gilad-Bachrach, and O. Elisha, “Low Latency Pri-vacy Preserving Inference,” in Proceedings of the 36th International Conference on Machine Learning . PMLR, May 2019, pp. 812–821. [9] W. Ruan, M. Xu, W. Fang, L. Wang, L. Wang, and W. Han, “Private, Efficient, and Accurate: Protecting Models Trained by Multi-party Learning with Differential Privacy,” in 2023 IEEE Symposium on Security and Privacy (SP) . IEEE, May 2023, pp. 1926–1943. [10] S. Xia et al. , “CryptPEFT: Efficient and Private Neural Network Inference via Parameter-Efficient Fine-Tuning,” in Proceedings 2026 Network and Distributed System Security Symposium , 2026. [11] T. Orekondy, B. Schiele, and M. Fritz, “Knockoff nets: Stealing functionality of black-box models,” in Proceedings of the IEEE/CVF conference on computer vision and pattern recognition . IEEE/CVF, 2019, pp. 4954–4963. [12] R. Shwartz-Ziv and N. Tishby, “Opening the Black Box of Deep Neural Networks via Information,” Apr. 2017. [13] Q. Le, T. Sarl ´ os, and A. Smola, “Fastfood: Approximating kernel expansions in loglinear time,” in Proceedings of the 30th International Conference on International Conference on Machine Learning - Volume 28 , ser. ICML’13. Atlanta, GA, USA: JMLR.org, Jun. 2013, pp. III–244–III–252. [14] A. Krizhevsky, “Learning multiple layers of features from tiny images,” 2009. [15] L. Deng, “The mnist database of handwritten digit images for machine learning research [best of the web],” IEEE signal processing magazine , vol. 29, no. 6, pp. 141–142, 2012. [16] O. M. Parkhi, A. Vedaldi, A. Zisserman, and C. Jawahar, “Cats and dogs,” in 2012 IEEE conference on computer vision and pattern recognition , IEEE. IEEE, 2012, pp. 3498–3505. [17] Y. LeCun, L. Bottou, Y. Bengio, and P. Haffner, “Gradient-based learning applied to document recognition,” Proceedings of the IEEE ,vol. 86, no. 11, pp. 2278–2324, 1998. [18] P. Helber, B. Bischke, A. Dengel, and D. Borth, “Eurosat: A novel dataset and deep learning benchmark for land use and land cover classification,” IEEE Journal of Selected Topics in Applied Earth Observations and Remote Sensing , vol. 12, no. 7, pp. 2217–2226, 2019. [19] Z. Jackson, C. Souza, J. Flaks, Y. Pan, H. Nicolas, and A. Thite, “Jakobovski/free-spoken-digit-dataset: V1.0.8,” Zenodo, 2018-08. [20] Z. Huang, W. jie Lu, C. Hong, and J. Ding, “Cheetah: Lean and fast secure Two-Party deep neural network inference,” in 31st USENIX Security Symposium (USENIX Security 22) . Boston, MA: USENIX Association, Aug. 2022, pp. 809–826. [21] D. Kim and C. Guyot, “Optimized Privacy-Preserving CNN Infer-ence With Fully Homomorphic Encryption,” IEEE Transactions on Information Forensics and Security , vol. 18, pp. 2175–2187, 2023. [22] P. Mishra, R. Lehmkuhl, A. Srinivasan, W. Zheng, and R. A. Popa, “Delphi: A Cryptographic Inference System for Neural Net-works,” in Proceedings of the 2020 Workshop on Privacy-Preserving Machine Learning in Practice , ser. PPMLP’20. New York, NY, USA: Association for Computing Machinery, Nov. 2020, pp. 27–30. [23] S. Singh, S. Singh, S. Gudaparthi, X. Fan, and R. Balasubramonian, “Hyena: Balancing Packing, Reuse, and Rotations for Encrypted Inference,” in 2024 IEEE Symposium on Security and Privacy (SP) .IEEE Computer Society, Feb. 2024, pp. 110–110. [24] Z. Yang, C. Niu, B. Wei, Z. Huang, C. Hong, and T. Wei, “RBOOT: Accelerating Homomorphic Neural Network Inference by Fusing ReLU within Bootstrapping,” 2025. [25] F. Mo et al. , “DarkneTZ: Towards model privacy at the edge using trusted execution environments,” in Proceedings of the 18th International Conference on Mobile Systems, Applications, and Services ,ser. MobiSys ’20. New York, NY, USA: Association for Computing Machinery, Jun. 2020, pp. 161–174. [26] T. Shen et al. , “ {SOTER }: Guarding Black-box Inference for Gen-eral Neural Networks at the Edge,” in 2022 USENIX Annual Technical Conference (USENIX ATC 22) . USENIX, 2022, pp. 723– 738. [27] L. Ren, Z. Liu, F. Li, K. Liang, Z. Li, and B. Luo, “PrivDNN: A Secure Multi-Party Computation Framework for Deep Learning using Partial DNN Encryption,” in Proceedings on Privacy Enhanc-ing Technologies , vol. 2024, Jul. 2024, pp. 477–494. [28] T. Chen et al. , “THE-X: Privacy-Preserving Transformer Inference with Homomorphic Encryption,” in Findings of the Association for Computational Linguistics: ACL 2022 , S. Muresan, P. Nakov, and A. Villavicencio, Eds. Dublin, Ireland: Association for Computa-tional Linguistics, May 2022, pp. 3510–3520. [29] G. Yan et al. , “Comet: Accelerating Private Inference for Large Language Model by Predicting Activation Sparsity,” in Proceedings of the {IEEE } Symposium on Security and Privacy . {IEEE }, 2025, pp. 2827–2845. [30] S. Zhu, K. Chen, Y. Zhao, and C. Wei, “Scope: Expanding client-side post-processing for efficient privacy-preserving model infer-ence,” in Proceedings of the 2025 ACM SIGSAC Conference on Com-puter and Communications Security , ser. CCS ’25. New York, NY, USA: Association for Computing Machinery, 2025, p. 4020–4034. [31] K. He, X. Zhang, S. Ren, and J. Sun, “Deep residual learning for image recognition,” in Proceedings of the IEEE conference on computer vision and pattern recognition , 2016, pp. 770–778. [32] U. Evci, V. Dumoulin, H. Larochelle, and M. C. Mozer, “Head2Toe: Utilizing Intermediate Representations for Better Transfer Learn-ing,” in Proceedings of the 39th International Conference on Machine Learning . PMLR, Jun. 2022, pp. 6009–6033. [33] C.-H. Tu, Z. Mai, and W.-L. Chao, “Visual Query Tuning: Towards Effective Usage of Intermediate Representations for Parameter and 12 

Memory Efficient Transfer Learning,” in 2023 IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , 2023, pp. 7725– 7735. [34] P. Wang et al. , “Understanding Deep Representation Learning via Layerwise Feature Compression and Discrimination,” Journal of Machine Learning Research , vol. 26, no. 220, pp. 1–61, 2025. [35] R. Wightman, “Pytorch image models,” https://github.com/ rwightman/pytorch-image-models, 2019. [36] F. Zhuang et al. , “A Comprehensive Survey on Transfer Learning,” 

Proceedings of the IEEE , vol. 109, no. 1, pp. 43–76, 2025. [37] E. Bingham and H. Mannila, “Random projection in dimensional-ity reduction: Applications to image and text data,” in Proceedings of the Seventh ACM SIGKDD International Conference on Knowledge Discovery and Data Mining , ser. KDD ’01. New York, NY, USA: Association for Computing Machinery, Aug. 2001, pp. 245–250. [38] J. Song and S. Ermon, “Understanding the Limitations of Varia-tional Mutual Information Estimators,” in Proceedings of the 8th In-ternational Conference on Learning Representations . OpenReview.net, 2020. [39] E. Hesamifard, H. Takabi, and M. Ghasemi, “CryptoDL: Deep Neural Networks over Encrypted Data,” Nov. 2017. [40] C. Mouchet, J.-P. Bossuat, J. Troncoso-Pastoriza, and J. Hubaux, “Lattigo: A Multiparty Homomorphic Encryption Library in Go,” in Proceedings of the 8th Workshop on Encrypted Computing and Applied Homomorphic Cryptography , 2020. [41] J. H. Cheon, A. Kim, M. Kim, and Y. Song, “Homomorphic Encryption for Arithmetic of Approximate Numbers,” in Advances in Cryptology – ASIACRYPT 2017 , T. Takagi and T. Peyrin, Eds. Springer International Publishing, 2017, pp. 409–437. [42] D. Kim and Y. Song, “Approximate Homomorphic Encryption over the Conjugate-Invariant Ring,” in Proceedings of the Infor-mation Security and Cryptology - {ICISC } 2018 - 21st International Conference , ser. Lecture Notes in Computer Science, vol. 11396. Springer, 2018, pp. 85–102. [43] M. Albrecht et al. , “Homomorphic Encryption Standard,” in Pro-tecting Privacy through Homomorphic Encryption , K. Lauter, W. Dai, and K. Laine, Eds. Cham: Springer International Publishing, 2021, pp. 31–62. [44] K. He, X. Zhang, S. Ren, and J. Sun, “Identity mappings in deep residual networks,” in Computer Vision–ECCV 2016: 14th European Conference, Amsterdam, The Netherlands, October 11–14, 2016, Pro-ceedings, Part IV 14 . Springer, 2016, pp. 630–645. [45] A. Howard et al. , “Searching for mobilenetv3,” in Proceedings of the IEEE/CVF international conference on computer vision , 2019, pp. 1314–1324. [46] J. H. Ju et al. , “NeuJeans: Private Neural Network Inference with Joint Optimization of Convolution and FHE Bootstrapping,” in 

Proceedings of the 2024 on ACM SIGSAC Conference on Computer and Communications Security , ser. CCS ’24. New York, NY, USA: Association for Computing Machinery, Dec. 2024, pp. 4361–4375. [47] L. Fei-Fei, R. Fergus, and P. Perona, “Learning generative visual models from few training examples: An incremental bayesian approach tested on 101 object categories,” in 2004 conference on computer vision and pattern recognition workshop . IEEE, 2004, pp. 178–178. [48] Tensorflow, “Yamnet,” Google, 2025. [49] J. F. Gemmeke et al. , “Audio Set: An ontology and human-labeled dataset for audio events,” in 2017 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP) , 2017, pp. 776–780. [50] O. Russakovsky et al. , “ImageNet Large Scale Visual Recognition Challenge,” International Journal of Computer Vision , vol. 115, no. 3, pp. 211–252, 2015. [51] S. Anwar, K. Hwang, and W. Sung, “Structured pruning of deep convolutional neural networks,” ACM Journal on Emerging Tech-nologies in Computing Systems (JETC) , vol. 13, no. 3, pp. 1–18, 2017. [52] A. Coates, A. Ng, and H. Lee, “An analysis of single-layer net-works in unsupervised feature learning,” in Proceedings of the Fourteenth International Conference on Artificial Intelligence and Statis-tics , ser. Proceedings of Machine Learning Research, G. Gordon, D. Dunson, and M. Dud´ ık, Eds., vol. 15. Fort Lauderdale, FL, USA: PMLR, 11–13 Apr 2011, pp. 215–223. [53] J. Wu, C. Leng, Y. Wang, Q. Hu, and J. Cheng, “Quantized convo-lutional neural networks for mobile devices,” in Proceedings of the IEEE conference on computer vision and pattern recognition . IEEE, 2016, pp. 4820–4828. [54] F. Bourse, R. Del Pino, M. Minelli, and H. Wee, “FHE Circuit Privacy Almost for Free,” in Advances in Cryptology – CRYPTO 2016 , M. Robshaw and J. Katz, Eds. Springer Berlin Heidelberg, 2016, vol. 9815, pp. 62–89. [55] C. Gentry, A Fully Homomorphic Encryption Scheme (Chapter 20) .Stanford University, 2009. [56] J. Fan and F. Vercauteren, “Somewhat Practical Fully Homomor-phic Encryption,” IACR Cryptol. ePrint Arch. , vol. 2012, p. 144, 2012. [57] I. Zimerman, M. Baruch, N. Drucker, G. Ezov, O. Soceanu, and L. Wolf, “Converting Transformers to Polynomial Form for Secure Inference Over Homomorphic Encryption,” in Proceedings of the 41st International Conference on Machine Learning . PMLR, Jul. 2024, pp. 62 803–62 814. [58] K. Nam, Y. Joo, S. Ha, and Y. Paek, “SLOTHE : Lazy Approxima-tion of Non-Arithmetic Neural Network Functions over Encrypted Data,” in 34th USENIX Security Symposium (USENIX Security 25) ,2025, pp. 3083–3102. [59] W. Ao and V. N. Boddeti, “AutoFHE: Automated adaption of CNNs for efficient evaluation over FHE,” in 33rd USENIX Security Symposium (USENIX Security 24) . Philadelphia, PA: USENIX Association, Aug. 2024, pp. 2173–2190. [60] A. Diaa et al. , “Fast and Private Inference of Deep Neural Net-works by Co-designing Activation Functions,” in Proceedings of the 33rd {USENIX } Security Symposium . {USENIX } Association, 2024, pp. 2191–2208. [61] T. Xu, L. Wu, R. Wang, and M. Li, “PrivCirNet: Efficient Private Inference via Block Circulant Transformation,” in Proceedings of the Advances in Neural Information Processing Systems 38: Annual Conference on Neural Information Processing Systems 2024 , 2024. [62] T. Xu et al. , “Breaking the Layer Barrier: Remodeling Private Transformer Inference with Hybrid CKKS and MPC,” in 34th USENIX Security Symposium (USENIX Security 25) . USENIX, 2025, pp. 2653–2672. [63] L. K. L. Ng and S. S. M. Chow, “ {GForce }: {GPU-Friendly }

Oblivious and Rapid Neural Network Inference,” in 30th USENIX Security Symposium (USENIX Security 21) , 2021, pp. 2147–2164. [64] Y. Fu et al. , “Swift: Fast secure neural network inference with fully homomorphic encryption,” Trans. Info. For. Sec. , vol. 20, p. 2793–2806, Jan. 2025. [65] Z. Zhang et al. , “No Privacy Left Outside: On the (In-)Security of TEE-Shielded DNN Partition for On-Device ML,” in 2024 IEEE Symposium on Security and Privacy (SP) . IEEE, May 2024, pp. 3327–3345.