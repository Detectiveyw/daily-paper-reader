---
title: "MOSAIC: Masked Outsourcing of Secure AI Computations"
title_zh: MOSAIC：安全 AI 计算的掩码外包
authors: "James Hsin-yu Chiang, Sheila Zingg, Kari Kostiainen, Srdjan Capkun"
date: 2026-07-31
pdf: "https://arxiv.org/pdf/2607.29221v1"
tags: ["query:fss-llm"]
score: 9.0
evidence: 大型Transformer推理的安全外包
tldr: MOSAIC 提出了一种新型矩阵乘法掩码协议，旨在将 AI 计算从弱客户端安全外包给强大的非信服务器。通过引入少量噪声并放宽精确度要求，该方法在保证安全性的同时实现了极低的客户端开销，支持 70B 参数的大型 Transformer 模型推理。其核心技术包括基于 LWE/LPN 的掩码机制和控制误差增长的随机 Hadamard 旋转，在保持模型性能的同时显著提升了计算效率。
selection_source: fresh_fetch
motivation: 解决在客户端资源有限且需保护模型与数据隐私的情况下，如何高效地将大规模 AI 推理任务外包给不可信服务器的问题。
method: 提出一种基于 LWE 和 LPN 假设的矩阵乘法掩码协议，并利用随机 Hadamard 旋转技术来抑制多层 Transformer 中的误差累积。
result: 在 70B 模型上的实验表明，MOSAIC 的困惑度与主流量化方法相当，且在 HumanEval 任务上达到了与全精度推理一致的性能。
conclusion: MOSAIC 为现代数据中心的大规模机密 AI 计算提供了一条可行路径，通过最小化可信计算基（TCB）实现了高效且安全的计算外包。
---

## 摘要
我们解决了在客户端持有输入和模型，且服务器不得获知两者的情况下，如何安全高效地将 AI 计算从受信任但计算能力较弱的客户端外包给不受信任但功能强大的服务器的挑战。我们提出了 MOSAIC，其核心是一种新型的矩阵乘法掩码协议，该协议可扩展到比先前工作大得多的矩阵，从而实现了对现代工作负载（如大型 Transformer 推理）的安全外包。通过在乘法结果中引入少量噪声并由此放宽正确性要求，MOSAIC 实现了最优的渐近客户端开销，且实际运行时间比先前工作快几个数量级。其安全性可归约为判定性 LWE 和 LPN 假设。由于这种噪声会在 Transformer 的多个层中累积，一个关键的技术挑战是限制误差增长；MOSAIC 通过一种基于随机 Hadamard 旋转的误差缩放机制解决了这一问题。在大型 70B Transformer 模型上，MOSAIC 的困惑度与流行的量化方法相当，甚至在 HumanEval 基准测试上达到了全精度 BF16 推理的水平。最后，我们展示了一个端到端实现，说明了像 MOSAIC 这样的思路如何为现代数据中心的大规模机密 AI 提供一条可行路径。非机密推理已经跨阶段（预填充/解码）、层和时间进行分布式处理，以最大限度地利用异构硬件，并使用类 RDMA 网络在节点间移动激活值、缓存的 KV 值和权重。MOSAIC 通过保持较小的可信计算基（TCB）并将大部分 AI 计算外包给不受信任的加速器，实现了机密计算的可扩展性。

## Abstract
We address the challenge of securely and efficiently outsourcing AI computations from a trusted but computationally weak client to an untrusted but powerful server, in the setting where the client holds both the input and the model, and the server must learn neither. We present MOSAIC, whose core is a novel matrix-multiplication masking protocol that scales to far larger matrices than prior work, enabling the safe outsourcing of modern workloads such as large transformer inference. By introducing small amounts of noise to the multiplication result and thereby relaxing correctness, MOSAIC achieves optimal asymptotic client overhead and concrete runtimes orders of magnitude faster than prior work. Its security reduces to the decisional LWE and LPN assumptions.   Because this noise accumulates across the many layers of a transformer, a key technical challenge is bounding error growth; MOSAIC addresses this with an error-scaling mechanism based on random Hadamard rotations. On large 70B transformer models, MOSAIC's perplexity is comparable to popular quantization approaches and even matches full-precision BF16 inference on HumanEval.   Finally, we present an end-to-end implementation showing how ideas like MOSAIC can promise a path towards large-scale confidential AI in modern data centers. Non-confidential inference is already distributed across phase (prefill/decode), layer, and time to maximize utilization of heterogeneous hardware, using RDMA-like networking to move activations, cached KV values, and weights across nodes. MOSAIC enables scaling of confidential compute by keeping the trusted computing base (TCB) small and outsourcing the bulk of the AI computation to untrusted accelerators.

---

## 论文详细总结（自动生成）

### MOSAIC: 安全 AI 计算的掩码外包 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）规模突破千亿参数，计算资源受限的客户端（如边缘设备或小型私有云）难以独立运行推理任务。现有的外包方案存在以下痛点：
*   **隐私风险**：直接外包会泄露敏感的输入数据和昂贵的模型权重。
*   **扩展性差**：传统的加密计算（如 FHE、MPC）在处理 70B 规模的模型时，计算开销或通信延迟高得无法接受。
*   **精度崩溃**：在多层 Transformer 架构中，固定点运算和噪声累积会导致模型精度迅速下降。

**MOSAIC** 旨在解决如何在保护模型和数据隐私的前提下，将大规模 AI 推理（如 70B 参数模型）高效外包给不可信的强大服务器（如云端 GPU 池）。

#### 2. 方法论：核心思想与关键技术
MOSAIC 的核心是一个**基于掩码的近似矩阵乘法协议**，其安全性建立在 **LWE（学习带噪声）**和 **LPN（学习奇偶校验带噪声）**这两个后量子密码学假设之上。

*   **矩阵掩码机制**：
    *   客户端将权重矩阵 $W$ 掩码为 $W' = W + LM + E_w$，将输入 $X$ 掩码为 $X' = X + NR + E_x$。其中 $L, M, N, R$ 是低秩均匀随机矩阵，$E$ 是小范数高斯噪声。
    *   服务器计算加密后的乘积 $Y' = W'X'$。
    *   客户端利用预计算的低秩项，以 $O((m+n)l)$ 的极低开销（远低于矩阵乘法的 $O(mnl)$）移除掩码，恢复出 $Y \approx WX$。
*   **递归掩码优化**：通过嵌套 LPN 实例，将稠密掩码替换为稀疏掩码，进一步降低客户端的在线计算负担。
*   **随机 Hadamard 旋转（Random Hadamard Rotation）**：
    *   这是解决误差累积的关键。LLM 的激活值中存在极少数“离群值”（Outliers），它们在量化和加噪过程中会放大误差。
    *   MOSAIC 在计算前对权重和激活值进行随机旋转，使能量均匀分布在所有维度上，从而将噪声方差降低了约 $n/\log n$ 倍，确保误差在 80 多层 Transformer 中非破坏性累积。

#### 3. 实验设计
*   **测试模型**：Qwen2.5-32B/72B、LLaMA-3-70B、DeepSeek-R1-Distill-LLaMA-70B。
*   **数据集/场景**：
    *   **精度评估**：WikiText-2（评估困惑度 PPL）、HumanEval（评估代码生成能力 pass@1）。
    *   **性能评估**：数据中心场景（RDMA/NVLink 连接）和远程办公场景（WAN 网络）。
*   **Benchmark 与对比方法**：
    *   **精度对比**：全精度 BF16（基准）、INT8 量化（LLM.int8()）、4-bit NF4 量化。
    *   **性能对比**：先前最先进的矩阵外包协议（如 [6] Trapdoor Matrices）。

#### 4. 资源与算力
*   **硬件配置**：实验使用了 **NVIDIA H200 GPU (141GB VRAM)**。
*   **部署拓扑**：1 个受信任的 GPU 作为客户端（TCB），3 个不受信任的 GPU 作为计算服务器。
*   **算力细节**：由于当前 GPU Tensor Core 不原生支持 32-bit 整数运算，作者通过 **8-bit Tensor Core 模拟 32-bit 整数乘法**（每个 32-bit 运算拆分为 10 个 8-bit 运算）。
*   **训练/推理时长**：70B 模型的预填充（Prefill）在 NVLink 环境下约需数秒，解码（Decode）速度约为 0.5-0.6 秒/ token。

#### 5. 实验数量与充分性
*   **实验覆盖面**：作者针对不同规模（32B 到 72B）和不同量化敏感度（LLaMA-3 对量化极敏感，Qwen 较鲁棒）的模型进行了广泛测试。
*   **消融实验**：详细测试了不同噪声水平（$\sigma$ 从 0 到 2.0）对模型精度的影响，并给出了 140-bit 安全等级下的参数建议。
*   **客观性**：实验不仅关注理论复杂度，还提供了端到端的墙钟时间（Wall-clock time），并考虑了网络延迟（从 NVLink 到 WAN）的影响，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **精度接近无损**：在 140-bit 安全参数下，MOSAIC 在 70B 模型上的表现优于 4-bit 量化，甚至在 HumanEval 上能匹配全精度 BF16 的性能。
*   **高效外包**：客户端的开销仅占总运行时间的 3.3% - 8%，实现了真正的“弱客户端”外包。
*   **通信可行性**：在现代数据中心（NVLink/RDMA）环境下，层与层之间的通信不再是瓶颈，通信开销仅占总延迟的 4%-16%。
*   **硬件限制**：目前的性能瓶颈在于 GPU 缺乏原生 32-bit 整数 Tensor Core 支持，导致相比非加密推理有 3x-11x 的减速。

#### 7. 优点
*   **扩展性突破**：是首个能实际运行 70B 参数模型且保持高精度的安全外包协议。
*   **理论与实践结合**：不仅有严格的 LWE/LPN 安全证明，还解决了 LLM 特有的离群值导致的误差爆炸问题。
*   **渐近最优**：客户端复杂度达到了理论最优的 $O((m+n)l)$。

#### 8. 不足与局限
*   **硬件依赖性**：高度依赖高效的整数运算。在缺乏原生 INT32 支持的硬件上，模拟开销较大。
*   **通信频率**：采用逐层外包模式，虽然在数据中心可行，但在高延迟的广域网（WAN）下进行自回归解码（逐 token 通信）仍然非常缓慢。
*   **模型架构限制**：目前主要针对线性层外包，非线性层（如 Softmax、RMSNorm）仍需客户端处理，这在一定程度上限制了外包的彻底性。
*   **特定模型敏感度**：对于极度敏感的模型（如 DeepSeek 蒸馏版），在高噪声水平下仍会出现精度下降。

（完）
