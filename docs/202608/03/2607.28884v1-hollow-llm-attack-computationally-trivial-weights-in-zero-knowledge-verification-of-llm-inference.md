---
title: "Hollow-LLM Attack: Computationally Trivial Weights in Zero-Knowledge Verification of LLM Inference"
title_zh: Hollow-LLM 攻击：大语言模型推理零知识验证中计算代价极低的权重
authors: "Chen Gong, Beijie Liu, Mengyuan Li"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.28884v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 大模型推理的零知识验证及其安全漏洞
tldr: 本研究揭示了零知识（ZK）大语言模型推理验证中的一个关键漏洞，即“Hollow-LLM攻击”。虽然ZK证明能确保推理过程符合公开架构和私有权重，但攻击者可以通过嵌入具有特殊代数结构的“幽灵权重”，在保持架构不变的情况下大幅降低实际计算量。这种攻击使得服务商能以小模型成本生成有效证明，却虚报大模型规模。研究强调，现有的推理正确性证明并不等同于大模型计算量的保证，亟需新的保护机制。
selection_source: fresh_fetch
motivation: 现有的零知识大模型推理验证仅能保证计算过程的代数正确性，却无法约束服务商实际投入的计算量，导致虚报模型规模的风险。
method: 提出Hollow-LLM攻击，通过在标准Transformer块中嵌入可折叠计算的“幽灵权重”，使证明者能在满足验证电路的同时显著减少计算开销。
result: 实验表明，攻击者可以在不损失输出质量且通过相同验证电路的前提下，以极低的计算成本提供虚假的大模型推理服务。
conclusion: 证明推理的正确性并不代表证明了大规模模型的实际执行，必须引入额外的机制来将验证正确性与可验证的计算工作量绑定。
---

## 摘要
随着大语言模型（LLM）规模的增长且主要由远程平台提供服务，验证推理执行的忠实性变得至关重要（即确保提供商确实执行了宣称的模型和计算工作量，而非篡改或缩减后的变体）。零知识（ZK）LLM 推理提供了一种极具吸引力的方法。它承诺了公开可验证性，并通过证明输出与在承诺的私有权重下执行公开架构的一致性，提供了单次实例的等式正确性保证。然而，我们证明了它并未约束生成输出所投入的计算努力。在本文中，我们形式化了这一被忽视的努力差距，并引入了 Hollow-LLM 攻击。在这种攻击中，不诚实的提供商保留了声明的架构和参数数量，但嵌入了“幽灵权重”（ghost weights），其代数结构使有效计算发生坍缩。这些见证值满足验证电路并生成有效的证明，尽管作为证明者的不诚实模型所有者所执行的计算量仅相当于一个比声明的公开架构小得多的模型。这创造了一种有利可图的均衡，即提供商以小模型的成本交付可证明正确的输出，同时虚报模型规模。据此，我们刻画了可与标准 Transformer 块组合的特定幽灵权重族，并证明此类“虚空”部署在相同的验证电路下，能在零质量损失的情况下大幅降低服务成本。这些发现强调了正确推理的证明并不等同于大模型执行的证明，并表明需要额外的保护措施来将正确性与可验证的计算工作绑定。

## Abstract
As large language models (LLMs) grow in scale and are predominantly served from remote platforms, verifying faithful inference execution becomes critical (i.e., ensuring that a provider actually executes the advertised model and computational workload rather than a tampered or downsized variant). Zero-knowledge (ZK) LLM inference offers an appealing approach. It promises public verifiability and delivers per-instance guarantees of equational correctness by proving that an output is consistent with executing a public architecture under committed, private weights. Though, we show that it does not bind the effort expended to produce the output. In this paper, we formalize this overlooked effort gap and introduce the Hollow-LLM Attack, in which a dishonest provider retains the declared architecture and parameter count but embeds ghost weights whose algebraic structure collapses effective computation. These witnesses satisfy the verification circuit and yield valid proofs, even though the dishonest model owner, who serves as the prover, performs computation commensurate with a much smaller model than the declared public architecture. This creates a profitable equilibrium in which providers deliver provably correct outputs at small-model cost while overclaiming model size. Accordingly, we characterize concrete families of ghost weights that compose with standard transformer blocks and show that such hollow deployments substantially reduce serving cost with zero quality loss under the same verification circuit. These findings underscore that proof of correct inference is not proof of large-model execution and necessitate additional protections to bind correctness to verifiable computational work.

---

## 论文详细总结（自动生成）

这篇论文对当前零知识大语言模型（zkLLM）推理验证体系提出了挑战，以下是详细的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着LLM云端服务的普及，用户需要验证服务商是否“忠实”地执行了宣称的模型（例如：是否偷偷换成了更小、更便宜的模型）。零知识证明（ZKP）被认为是解决这一信任问题的良方，因为它能在不泄露私有权重的前提下证明输出的正确性。
*   **核心问题**：论文发现现有的zkML验证存在一个**“努力差距”（Effort Gap）**。ZKP只能证明输出符合代数等式（即输出与声明的架构和承诺的权重一致），但无法证明服务商在生成该输出时实际投入了多少计算量。
*   **整体含义**：论文提出了 **Hollow-LLM（虚空大模型）攻击**，证明不诚实的服务商可以构造特殊的“幽灵权重”，使得一个庞大的声明模型在实际运行时仅消耗极小模型的计算资源，却能生成完全合规的零知识证明。

### 2. 论文提出的方法论
核心思想是利用Transformer架构的代数不变性，构造**无需训练**的幽灵权重（Ghost Weights），将小模型（Inner Model）嵌入到大模型（Outer Model）中。
*   **关键技术细节**：
    *   **攻击 A：深度虚构（增加层数）**：利用残差结构的特性，将多余的层设置为“恒等映射层”。通过将注意力权重和全连接层（FFN）权重设为零，使得 $x_{l+1} = x_l + 0 = x_l$。在验证电路中这些层依然存在，但实际推理时可以跳过。
    *   **攻击 B：宽度虚构（增加维度）**：利用复制不变性（Replication Invariance）。将隐藏层维度扩大 $m$ 倍，通过块对角矩阵构造权重，使 $m$ 个副本并行运行且互不干扰。
    *   **输出对齐**：为了解决维度扩大导致的 Logits 缩放问题（影响 Softmax 结果），论文设计了特殊的解码器权重处理方案（如在 Tied Decoder 模式下引入 $1/m$ 的缩放因子），确保虚空大模型的输出与原始小模型完全一致。
*   **算法流程**：服务商首先承诺一组精心构造的幽灵权重；推理时仅运行核心小模型以节省成本；审计时，利用缓存的中间值快速构造符合大模型架构的见证值（Witness），生成合规证明。

### 3. 实验设计
*   **场景与框架**：基于 `zkGPT` 框架，针对 GPT-2 架构的 Transformer 进行验证。
*   **模型配置**：
    *   **内部模型 (IM1)**：6层，512维度（实际运行的模型）。
    *   **外部模型 (A/B/A+B)**：通过攻击手段虚构出的模型，如12层、1024维度。
    *   **基准对比 (Benchmark)**：对比了“诚实运行的大模型 (IM2)”（12层，1024维度）。
*   **对比指标**：推理延迟（Prefill/Decode）、ZKP 门电路数量（Gates）、证明生成时间（Prover Time）、验证时间（Verify Time）。

### 4. 资源与算力
*   **硬件环境**：实验在一台运行 Ubuntu 24.04 的本地工作站上完成。
*   **核心配置**：Intel Core Ultra 7 265F CPU (20核)。
*   **算力说明**：论文**未明确提到使用 GPU** 进行证明生成，主要展示了在 CPU 上的运行效率。由于 zkGPT 本身支持多线程 CPU 证明，实验数据反映了 CPU 环境下的开销。

### 5. 实验数量与充分性
*   **实验规模**：针对深度扩展（Attack A）、宽度扩展（Attack B）以及两者组合（A+B）分别进行了实验。
*   **充分性评价**：
    *   实验涵盖了 Transformer 的核心组件（LayerNorm, MHA, FFN, Positional Encoding）。
    *   **客观性**：通过对比 IM1、虚空模型和 IM2，清晰地展示了“推理成本随 IM1 保持不变”而“证明成本随声明规模增长”的非对称性。
    *   **局限性**：实验主要在较小规模的模型（如 GPT-2 级别）上验证概念，对于百亿级（10B+）参数模型的实际部署开销仅做了理论推导。

### 6. 论文的主要结论与发现
*   **计算非对称性**：虚空模型在推理时的 FLOPs 和延迟仅为小模型水平，但其生成的 ZK 证明在数学上与大模型完全等价。
*   **证明不等于工作量**：现有的 zkML 协议只能保证“等式正确性”，不能作为“计算量证明（Proof of Computation）”。
*   **经济激励**：服务商有强烈的动机虚报模型规模，以获取更高的品牌溢价或服务收费，同时通过虚空部署降低运营成本。

### 7. 优点
*   **视角独特**：首次系统性地指出了 zkML 领域中“正确性证明”与“工作量绑定”之间的脱节。
*   **方案优雅**：提出的幽灵权重构造方法是代数上的精确解，无需重新训练模型，且能保证输出完全一致（零质量损失）。
*   **兼容性强**：详细讨论了不同位置编码（RoPE, Sinusoidal）对攻击的影响，证明了该攻击对现代主流 LLM 架构的普适性。

### 8. 不足与局限
*   **检测风险未深究**：虽然证明无法区分，但论文提到极端的虚构可能导致模型行为异常（如质量不匹配），但未给出具体的检测阈值或统计学审计方法。
*   **动机假设**：部分评审认为，如果用户只看输出质量而不看参数量，服务商虚报规模的动力可能不足（尽管论文通过市场调研反驳了这一点）。
*   **防御机制较薄弱**：论文提出的对策（如硬件 TEE 结合、权重稀疏性证明）多为方向性建议，缺乏具体实现的性能评估。

（完）
