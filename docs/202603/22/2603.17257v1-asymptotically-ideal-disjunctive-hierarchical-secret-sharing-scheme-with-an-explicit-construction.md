---
title: Asymptotically ideal Disjunctive Hierarchical Secret Sharing Scheme with an Explicit Construction
title_zh: 具有显式构造的渐近理想析取分级秘密共享方案
authors: "Jian Ding, Cheng Wang, Haifeng Yu, Hongju Li, Cheng Shu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17257v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 析取分级秘密共享方案的显式构造
tldr: 本文针对析取分级秘密共享（DHSS）中现有方案份额过大或难以兼顾安全与渐近理想性的问题，提出了一种显式构造方法。该方案通过结合多项式、线性齐次递推关系和单向函数，实现了在计算安全前提下的渐近理想特性。其核心贡献在于显著降低了参与者的份额大小，并确保分发者在多项式时间内完成操作，为复杂分级环境下的秘密共享提供了高效且实用的解决方案。
selection_source: fresh_fetch
motivation: 解决现有析取分级秘密共享方案在份额大小、安全性和渐近理想性之间难以平衡的问题。
method: 利用多项式、多个线性齐次递推关系和单向函数，提出了一种显式的渐近理想DHSS构造方法。
result: 成功构建了计算安全且份额较小的渐近理想方案，且分发者计算复杂度为多项式时间。
conclusion: 该研究为实现高效且具有小份额特性的渐近理想分级秘密共享方案提供了有效的显式构造手段。
---

## 摘要
析取分级秘密共享（DHSS）方案是一种秘密共享方案，其中所有参与者的集合被划分为不相交的子集。每个不相交的子集被称为一个层级，不同层级具有不同的信任程度和阈值。如果来自给定层级的协作参与者数量未达到其阈值，则差额可以由更高层级的参与者进行补偿。目前已提出了许多理想的 DHSS 方案，但它们通常面临份额尺寸过大的问题。相反，现有的非理想 DHSS 方案虽然实现了较小的份额尺寸，但无法同时满足安全性和渐近理想性。在这项工作中，我们利用多项式、多个线性齐次递推关系和单向函数，提出了一种渐近理想 DHSS 方案的显式构造。尽管我们的方案具有计算安全性且包含许多公开值，但它具有较小的份额尺寸，且分发者仅需多项式时间。

## Abstract
Disjunctive Hierarchical Secret Sharing (DHSS) scheme is a secret sharing scheme in which the set of all participants is partitioned into disjoint subsets. Each disjoint subset is said to be a level, and different levels have different degrees of trust and different thresholds. If the number of cooperating participants from a given level falls to meet its threshold, the shortfall can be compensated by participants from higher levels. Many ideal DHSS schemes have been proposed, but they often suffer from big share sizes. Conversely, existing non-ideal DHSS schemes achieve small share sizes, yet they fail to be both secure and asymptotically ideal simultaneously. In this work, we present an explicit construct of an asymptotically ideal DHSS scheme by using a polynomial, multiple linear homogeneous recurrence relations and one-way functions. Although our scheme has computational security and many public values, it has a small share size and the dealer is required polynomial time.

---

## 论文详细总结（自动生成）

这是一份关于论文《Asymptotically ideal Disjunctive Hierarchical Secret Sharing Scheme with an Explicit Construction》（具有显式构造的渐近理想析取分级秘密共享方案）的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在析取分级秘密共享（DHSS）中实现**显式构造**、**小份额尺寸（Share Size）**、**计算安全性**与**渐近理想性**的统一。
*   **研究背景**：在 DHSS 方案中，参与者被分为不同层级，高层级参与者可以补偿低层级参与者的名额空缺。
    *   **现有方案缺陷**：早期的理想方案（如 Brickell, Tassa 方案）虽然安全，但往往份额尺寸过大，或者需要分发者进行复杂的非奇异性检查（非显式构造）。
    *   **安全性漏洞**：一些基于中国剩余定理（CRT）或多项式的显式方案（如 Ghodosi 等人的 GPN 方案）被证明是不安全的，存在秘密被非法获取或无法正确重建的风险。

### 2. 论文提出的方法论
该论文提出了一种结合代数构造与密码学原语的新型 DHSS 方案。
*   **核心思想**：利用多项式处理最低层级的阈值要求，同时利用**线性齐次递推（LHR）关系**和**单向函数**建立高层级对低层级的补偿机制。
*   **关键技术细节**：
    1.  **份额生成**：分发者将秘密 $s$ 嵌入到一个随机多项式 $f(x)$ 的常数项中。前 $m-1$ 层的参与者获得随机选取的份额，而最后一层参与者获得多项式在特定点的值。
    2.  **LHR 关系构建**：为每个层级构建特定的 LHR 关系，其初始值由单向函数处理后的份额组成。
    3.  **公开值发布**：分发者发布一系列公开值（如 $r_\ell$ 和 $I_i^{(\ell)}$），这些值允许授权子集通过 LHR 关系的通项公式或 Lagrange 插值法恢复秘密，但不会泄露份额信息。
*   **算法流程**：
    *   **分发阶段**：选择大素数 $p$、多项式、单向函数及 LHR 辅助方程，计算并分发私有份额，同时公布辅助计算的公开值。
    *   **重建阶段**：根据参与者所属的层级 $\ell$，若满足阈值条件，则利用公开值和私有份额，通过 LHR 通项恢复或多项式插值恢复秘密 $s$。

### 3. 实验设计与对比
由于本文属于理论密码学研究，其“实验”主要体现为**理论性能分析**和**安全性证明**，而非大规模数据集测试。
*   **对比对象（Benchmark）**：
    *   **理想方案**：Brickell (1989)、Tassa (2007)、Chen (2022) 等。
    *   **非理想/显式方案**：Ghodosi (1998)、Harn (2014)、Ersoy (2016)、Yang (2024) 等。
*   **对比维度**：是否显式构造、信息率（是否理想）、安全性类型（无条件 vs 计算安全）、份额尺寸。
*   **安全性攻击演示**：论文在附录中专门针对 Ghodosi 等人的 GPN 方案设计了两个反例，证明了该方案在特定参数下无法保证秘密的唯一性或隐私性。

### 4. 资源与算力
*   **算力说明**：论文中**未提及**使用 GPU 或高性能计算集群。
*   **计算复杂度**：作者强调该方案的分发者操作在**多项式时间**内即可完成，这意味着它在普通商用计算机上即可高效运行，无需特殊算力支持。

### 5. 实验数量与充分性
*   **理论证明**：论文提供了详尽的数学证明，涵盖了**正确性（Correctness）**、**渐近完美性（Asymptotic Perfectness）**和**渐近理想性（Asymptotically Ideality）**。
*   **反例分析**：通过 2 组具体的数值反例（针对 $n=7$ 的场景）成功攻击了前人方案的正确性和隐私性，证明了提出新方案的必要性。
*   **充分性评价**：对于密码学理论论文而言，其逻辑推导和针对性反例足以支撑其结论，实验（证明）是充分且客观的。

### 6. 主要结论与发现
*   **渐近理想性**：随着秘密空间（素数 $p$）的增大，方案的信息率趋近于 1，且非授权子集获取的信息趋近于 0。
*   **份额优化**：方案的份额尺寸仅为 $\log_2 p$ 位，优于许多现有的理想 DHSS 方案。
*   **安全性**：该方案在计算安全模型下是稳健的，解决了以往显式构造方案中常见的安全漏洞。

### 7. 优点
*   **高效性**：份额尺寸小，且分发者无需进行复杂的矩阵非奇异性检查，计算开销低。
*   **显式构造**：给出了明确的数学步骤，易于在实际系统中编程实现。
*   **安全性修复**：通过引入单向函数和改进 LHR 构造，克服了前人方案中方程组解不唯一的问题。

### 8. 不足与局限
*   **公开值较多**：为了实现小份额和显式构造，方案需要发布较多的公开值（Public Values），这增加了系统的存储负担和通信开销。
*   **计算安全限制**：与 Brickell 等人的无条件安全方案不同，本方案依赖于单向函数的计算假设，若未来单向函数被破解，安全性将受威胁。
*   **应用限制**：主要适用于对份额尺寸敏感但对公开信息量不敏感的分级管理场景。

（完）
