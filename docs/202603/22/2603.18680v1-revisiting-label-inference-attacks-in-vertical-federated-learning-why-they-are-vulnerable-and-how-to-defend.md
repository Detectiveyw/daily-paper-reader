---
title: "Revisiting Label Inference Attacks in Vertical Federated Learning: Why They Are Vulnerable and How to Defend"
title_zh: 重新审视纵向联邦学习中的标签推理攻击：其脆弱性原因及防御方法
authors: "Yige Liu, Dexuan Xu, Zimai Guo, Yongzhi Cao, Hanpin Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18680v1"
tags: ["query:ppml"]
score: 8.0
evidence: 纵向联邦学习中的标签推理攻击
tldr: 本研究深入探讨了纵向联邦学习中的标签推理攻击（LIA）。通过互信息分析，作者首次发现了“模型补偿”现象，证明底层模型主要提取特征而非标签信息，现有攻击的成功本质上依赖于特征与标签的分布对齐。基于此洞察，研究提出了一种零开销的层调整防御方法，通过增加顶层模型的层数比例，在不损失性能的前提下显著提升了对标签隐私的保护能力。
selection_source: fresh_fetch
motivation: 旨在揭示纵向联邦学习中标签推理攻击的内在机制及其脆弱性，并寻求更高效的防御方案。
method: 通过互信息理论分析模型各层与标签的相关性，并提出将切割层前移以增加顶层模型占比的防御策略。
result: 实验表明，该方法在五种数据集和多种架构下均能有效抵御攻击，并能与其他防御技术产生协同效应。
conclusion: 标签推理攻击的成功高度依赖特征分布对齐，通过优化模型层级划分可实现低成本且强力的隐私防御。
---

## 摘要
纵向联邦学习（VFL）允许拥有顶层模型的主动方与拥有底层模型的多个被动方进行协作。在这种场景下，仅持有特征的被动方可能会尝试推理主动方的私有标签，使得标签推理攻击（LIAs）成为一项重大威胁。先前的 LIA 研究声称，训练良好的底层模型可以有效地表征标签。然而，我们证明了这一观点具有误导性，并揭示了现有 LIA 的脆弱性。通过利用互信息，我们首次观察到了 VFL 中的“模型补偿”现象。我们从理论上证明，在 VFL 中，层输出与标签之间的互信息随层深度的增加而增加，这表明底层模型主要提取特征信息，而顶层模型负责标签映射。基于这一见解，我们引入了任务重新分配，以展示现有 LIA 的成功实际上源于特征与标签之间的分布对齐。当这种对齐被破坏时，LIA 的性能会急剧下降，甚至完全失效。此外，我们还研究了这一见解对防御的启示。我们提出了一种基于层调整的零开销防御技术。在五个数据集和五种代表性模型架构上的广泛实验表明，将切割层前移以增加顶层模型在整个模型中的比例，不仅提高了对 LIA 的抵抗力，还增强了其他防御措施的效果。

## Abstract
Vertical federated learning (VFL) allows an active party with a top model, and multiple passive parties with bottom models to collaborate. In this scenario, passive parties possessing only features may attempt to infer active party's private labels, making label inference attacks (LIAs) a significant threat. Previous LIA studies have claimed that well-trained bottom models can effectively represent labels. However, we demonstrate that this view is misleading and exposes the vulnerability of existing LIAs. By leveraging mutual information, we present the first observation of the "model compensation" phenomenon in VFL. We theoretically prove that, in VFL, the mutual information between layer outputs and labels increases with layer depth, indicating that bottom models primarily extract feature information while the top model handles label mapping. Building on this insight, we introduce task reassignment to show that the success of existing LIAs actually stems from the distribution alignment between features and labels. When this alignment is disrupted, the performance of LIAs declines sharply or even fails entirely. Furthermore, the implications of this insight for defenses are also investigated. We propose a zero-overhead defense technique based on layer adjustment. Extensive experiments across five datasets and five representative model architectures indicate that shifting cut layers forward to increase the proportion of top model layers in the entire model not only improves resistance to LIAs but also enhances other defenses.

---

## 论文详细总结（自动生成）

以下是对论文《Revisiting Label Inference Attacks in Vertical Federated Learning: Why They Are Vulnerable and How to Defend》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在纵向联邦学习（VFL）中，多个被动方（持有特征）与一个主动方（持有标签）协作训练。由于被动方无法接触标签，标签推理攻击（LIA）成为隐私威胁。
*   **核心质疑**：现有 LIA 研究普遍基于一个直觉假设——训练良好的底层模型（Bottom Model）能够建立特征嵌入与标签之间的强映射。
*   **研究动机**：作者质疑这一假设的普遍性，旨在通过信息论工具揭示 VFL 中底层模型与顶层模型（Top Model）的真实功能分工，并据此提出更有效的防御机制。

### 2. 方法论：核心思想与关键技术
*   **互信息（MI）量化分析**：利用互信息量化模型各层输出与标签之间的相关性。
*   **“模型补偿”现象（Model Compensation）**：首次观察并证明，在 VFL 中，底层模型主要负责特征提取，而标签映射功能主要由顶层模型承担。随着参与方增多，底层模型的标签表征能力进一步减弱，迫使顶层模型进行“补偿”。
*   **理论证明**：通过将 VFL 建模为分块马尔可夫链（Lumped Markov Chains），从理论上证明了层输出与标签之间的互信息 $I(Layer; Label)$ 随层深度的增加而单调递增。
*   **任务重新分配（Task Reassignment）**：设计实验通过改变标签分类逻辑（如将数字分类改为奇偶分类），打破特征与原始标签的自然分布对齐，从而验证现有攻击的脆弱性。
*   **防御技术：切割层前移（Moving Cut Layer Forward）**：核心思想是增加顶层模型的层数比例，减少底层模型层数。这能显著削弱底层模型输出（Embedding）中的标签信息。

### 3. 实验设计
*   **数据集**：涵盖 5 个数据集，包括基准数据集（MNIST, CIFAR-10, CINIC-10）和现实场景数据集（SVHN 街景数字, GTSRB 交通标志）。
*   **模型架构**：针对不同数据集采用了 MLP、AlexNet、ResNet、LeNet 和通用 CNN。
*   **对比方法**：
    *   **攻击方法**：对比了基于聚类的 LIA（Cluster-based）和基于模型补全的 LIA（Model Completion）。
    *   **防御方法**：对比了差分隐私（DP）、梯度裁剪、梯度压缩、混淆自动编码器（CAE）和随机标签扩展（RLE）。
*   **场景设置**：从 2 方扩展到多方（最多 16 个被动方）的纵向联邦场景。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。实验描述侧重于模型架构的层数调整和超参数设置。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨越 5 种数据集和 5 种代表性架构，验证了结论的普适性。
    *   进行了多方参与（1, 2, 4, 8, 16 方）的对比实验。
    *   针对防御效果，进行了不同网络宽度（因子 0.5 到 4.0）和深度（10 到 18 层）的鲁棒性测试。
*   **充分性评价**：实验设计非常充分且严谨。通过任务重新分配实验有力地反驳了现有攻击的前提假设，并通过消融实验验证了防御手段在不同架构下的稳定性，具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **功能分化**：底层模型主要学习特征结构，顶层模型学习标签映射。
*   **攻击本质**：现有 LIA 的成功很大程度上是由于特征分布与标签分布的“天然对齐”带来的错觉，而非底层模型真正学到了标签。
*   **防御增益**：将切割层仅前移 1 层即可显著降低攻击准确率；前移 3 层可使攻击效果降至随机猜测水平。
*   **性能提升**：增加顶层模型占比不仅能防御攻击，还能轻微提升主任务预测准确率（MTA），因为顶层模型能更早地聚合全局特征。

### 7. 优点
*   **理论深刻**：首次提出并证明了 VFL 中的“模型补偿”现象，填补了 VFL 内部表征学习机制的研究空白。
*   **零开销防御**：提出的防御方法只需调整模型切割位置，不引入额外的计算、通信开销或噪声，极具实用价值。
*   **协同效应**：证明了层调整技术可以显著增强现有其他防御手段（如 DP 或 CAE）的效果。

### 8. 不足与局限
*   **特征泄露风险**：作者也指出，过度减少底层模型层数（切割层太靠前）可能会导致 Embedding 保留过多原始特征信息，从而增加**特征推理攻击（FIA）**的风险。
*   **任务类型限制**：实验主要集中在图像分类任务，对于回归任务或自然语言处理（NLP）等其他领域的适用性尚待进一步验证。
*   **极端 Non-IID 情况**：未深入探讨当各被动方特征分布极度不均匀时，模型补偿现象是否会发生质变。

（完）
