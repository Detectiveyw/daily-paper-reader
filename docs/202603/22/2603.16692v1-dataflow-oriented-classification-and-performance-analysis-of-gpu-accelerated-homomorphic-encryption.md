---
title: Dataflow-Oriented Classification and Performance Analysis of GPU-Accelerated Homomorphic Encryption
title_zh: 面向数据流的 GPU 加速同态加密分类与性能分析
authors: "Ai Nozaki, Takuya Kojima, Hideki Takase, Hiroshi Nakamura"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16692v1"
tags: ["query:ppml"]
score: 8.0
evidence: GPU加速的全同态加密
tldr: 本研究针对全同态加密（FHE）中CKKS方案的GPU加速问题，指出参数配置（如乘法深度和安全要求）对内存占用及性能有重大影响。以往研究多采用统一优化手段，而本文通过数据流视角对现有优化策略进行分类，并开展了深入的定性与定量分析。研究发现，最优GPU优化策略高度依赖于CKKS参数，不同策略间的性能差异可达1.98倍，且选择标准随GPU架构而异。
selection_source: fresh_fetch
motivation: 现有GPU加速CKKS的研究往往忽略了不同参数配置对内存占用和性能优化的显著影响。
method: 提出一种基于数据流的分类方法，对现有的GPU优化策略进行定性和定量分析。
result: 实验表明，即使在相同架构下，最优优化策略也会随CKKS参数变化而改变，性能差异最高可达1.98倍。
conclusion: 选择GPU加速策略时必须考虑CKKS参数和硬件架构的匹配，以实现最佳性能。
---

## 摘要
全同态加密 (FHE) 能够对加密数据进行安全计算，但其计算开销仍是实际部署的主要障碍。为了减轻这一开销，许多研究探索了针对 CKKS 方案的 GPU 加速，该方案广泛用于近似算术。在 CKKS 中，通过平衡乘法深度、安全需求和性能，为每个工作负载配置 CKKS 参数。这些参数显著影响密文大小，从而决定了内存占用如何适应 GPU 内存层级。然而，先前的研究通常统一应用其提出的优化方法，而未考虑 CKKS 参数配置的差异。在这项工作中，我们证明了 CKKS 的最优 GPU 优化策略取决于 CKKS 参数配置。我们首先根据影响内存占用的数据流的两个方面对先前的优化进行分类，然后进行定性和定量性能分析。我们的分析表明，即使在相同的 GPU 架构上，最优策略也会随 CKKS 参数而变化，策略之间的性能差异高达 1.98 倍，且选择合适策略的标准在不同 GPU 架构之间也存在差异。

## Abstract
Fully Homomorphic Encryption (FHE) enables secure computation over encrypted data, but its computational cost remains a major obstacle to practical deployment. To mitigate this overhead, many studies have explored GPU acceleration for the CKKS scheme, which is widely used for approximate arithmetic. In CKKS, CKKS parameters are configured for each workload by balancing multiplicative depth, security requirements, and performance. These parameters significantly affect ciphertext size, thereby determining how the memory footprint fits within the GPU memory hierarchy. Nevertheless, prior studies typically apply their proposed optimization methods uniformly, without considering differences in CKKS parameter configurations. In this work, we demonstrate that the optimal GPU optimization strategy for CKKS depends on the CKKS parameter configuration. We first classify prior optimizations by two aspects of dataflows which affect memory footprint and then conduct both qualitative and quantitative performance analyses. Our analysis shows that even on the same GPU architecture, the optimal strategy varies with CKKS parameters with performance differences of up to 1.98 $\times$ between strategies, and that the criteria for selecting an appropriate strategy differ across GPU architectures.

---

## 论文详细总结（自动生成）

这篇论文对 GPU 加速的 CKKS 同态加密方案进行了深入的性能分析，重点探讨了加密参数与硬件架构对优化策略选择的影响。以下是详细的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在 GPU 加速 CKKS（一种支持近似计算的同态加密方案）时，是否存在一种“万能”的优化策略？
*   **研究动机**：
    *   CKKS 的计算开销极高（比明文慢 $10^4$ 到 $10^5$ 倍），KeySwitch 操作占据了约 70% 的执行时间。
    *   CKKS 参数（如多项式阶数 $N$、乘法深度 $L$、分解数 $dnum$）会显著改变密文的内存占用（Memory Footprint）。
    *   现有研究（如 WarpDrive, Cheddar, Neo 等）通常统一应用某种优化手段，忽略了参数变化导致内存占用与 GPU L2 缓存容量之间的动态关系。
    *   论文旨在证明：**最优 GPU 优化策略高度依赖于具体的 CKKS 参数配置和 GPU 硬件架构。**

### 2. 方法论：数据流分类与性能建模
论文通过两个数据流维度对现有的 GPU 优化策略进行了分类，并引入了定性分析模型：
*   **两个分类维度**：
    1.  **Digit 级并行度**：
        *   **DigitSerial (DS)**：顺序执行 KeySwitch 中的 $dnum$ 个独立子计算。
        *   **DigitParallel (DP)**：在单个内核中并行执行这些子计算，增加并行度但增大内存占用。
    2.  **输出粒度**：
        *   **OutputBulk (OB)**：一次性计算所有层级的输出。
        *   **OutputChunked (OC)**：将输出划分为多个分块（Chunks）增量计算，减少单次内核执行的内存占用。
*   **四种策略组合**：
    *   **DSOB**：基准方案。
    *   **DPOB**：高并行方案（如 WarpDrive），适合小参数。
    *   **DSOC**：内存感知方案（如 MAD），适合大参数或小缓存硬件。
    *   **DPOC**：本文提出的折中方案，结合了并行与分块。
*   **性能建模 (GCoM)**：利用 GPU 性能建模框架分析执行停顿（Stalls），包括数据依赖停顿、结构停顿、NoC/DRAM 争用停顿以及内核启动开销。

### 3. 实验设计
*   **测试场景**：同态乘法（HMUL），重点分析其中的 KeySwitch 过程。
*   **CKKS 参数空间**：
    *   $N \in \{2^{14}, 2^{15}, 2^{16}, 2^{17}\}$
    *   $L \in \{10, 30, 50\}$
    *   $dnum \in \{2, 4, 6, 8\}$
*   **对比方法**：对比了上述四种数据流策略（DSOB, DPOB, DSOC, DPOC），并针对 OC 策略测试了不同的分块数（Chunks 2-10）。
*   **Benchmark**：测量绝对执行时间、内核执行时间分解、L1/L2 缓存命中率及各类 Stall 占比。

### 4. 资源与算力
论文在四种不同架构和定位的 NVIDIA GPU 上进行了详尽测试：
*   **RTX 6000 Ada** (Lovelace 架构, 96MB L2)
*   **RTX 4090** (Lovelace 架构, 72MB L2)
*   **A100** (Ampere 架构, 40MB L2, 高 HBM 带宽)
*   **RTX 2080 Ti** (Turing 架构, 5.5MB L2)
*   **软件环境**：使用 NVIDIA Nsight Compute 进行性能剖析。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种 GPU 架构、数十组 CKKS 参数组合，每组实验重复 100 次取平均值。
*   **充分性**：实验设计非常充分。它不仅测量了端到端时间，还深入到了缓存命中率和微架构层面的指令停顿分析，能够合理解释为什么某种策略在特定条件下胜出。
*   **客观性**：通过跨代 GPU 对比，排除了单一硬件特性导致的偏差，结论具有普适性。

### 6. 主要结论与发现
*   **策略随参数漂移**：在 RTX 6000/4090 上，随着参数增大（内存占用增加），最优策略从 DPOB 转向 DPOC，最后转向 DSOC。
*   **性能差异巨大**：不同策略在相同参数下的性能差距最高可达 **1.98 倍**。
*   **架构决定标准**：
    *   **A100**：由于其极高的 DRAM 带宽和较低的访问延迟，DPOB 在大多数情况下表现最好，因为它能有效隐藏延迟。
    *   **RTX 2080 Ti**：由于 L2 缓存极小，几乎所有策略都会导致缓存失效，因此减少内核启动开销的 DPOB 往往占优。
*   **L2 缓存是关键**：当内存占用超过 L2 缓存容量的约 2 倍时，性能会因 DRAM 争用而剧烈下降，此时必须切换到 OC（分块）策略。

### 7. 优点
*   **视角独特**：首次系统性地将 CKKS 算法参数与 GPU 存储层级结构联系起来，填补了现有研究的空白。
*   **分类清晰**：提出的二维分类法（Digit 并行度 vs 输出粒度）简洁明了，涵盖了现有及潜在的优化方向。
*   **指导性强**：为 FHE 编译器和加速库提供了理论依据，支持根据运行时参数动态选择最优策略。

### 8. 不足与局限
*   **单卡限制**：研究仅限于单 GPU 环境，未探讨多 GPU 并行下的数据流优化。
*   **算子覆盖**：虽然 KeySwitch 是瓶颈，但未详细讨论旋转（Rotation）或自举（Bootstrapping）等其他复杂操作在不同参数下的表现。
*   **自动化程度**：论文指出了策略选择的重要性，但尚未提供一个自动化的启发式算法或模型来实时预测最优策略。

（完）
