---
title: Communication-Efficient and Robust Multi-Modal Federated Learning via Latent-Space Consensus
title_zh: 基于潜在空间共识的通信高效且鲁棒的多模态联邦学习
authors: "Mohamed Badi, Chaouki Ben Issaid, Mehdi Bennis"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19067v1"
tags: ["query:ppml"]
score: 8.0
evidence: 隐私保护多模态联邦学习
tldr: 针对多模态联邦学习中客户端模态异构、架构差异及通信开销大的挑战，本文提出CoMFed框架。该框架通过可学习的投影矩阵生成压缩的潜空间表示，并利用潜空间正则化器实现跨客户端的特征对齐。实验证明，CoMFed在人体活动识别任务中实现了高精度、低开销且对异常值具有鲁棒性，为分布式多模态学习提供了高效的对齐方案。
selection_source: fresh_fetch
motivation: 解决多模态联邦学习中由于客户端模态异构和模型架构差异导致的特征空间对齐困难及高通信成本问题。
method: 提出CoMFed框架，利用可学习投影矩阵生成压缩潜表示，并通过潜空间正则化器促进跨客户端的模态一致性。
result: 在人体活动识别基准测试中，该方法以极低的通信开销实现了具有竞争力的分类准确率和良好的鲁棒性。
conclusion: CoMFed为异构多模态联邦学习提供了一种通信高效、鲁棒且能有效处理模型异构性的特征对齐解决方案。
---

## 摘要
联邦学习 (FL) 能够在不共享原始数据的情况下，在分布式设备之间实现协作模型训练，但将联邦学习应用于多模态场景会带来重大挑战。客户端通常拥有异构的模态和模型架构，这使得在保护隐私和最小化通信成本的同时，难以高效地对齐特征空间。为了解决这一问题，我们引入了 CoMFed，这是一个通信高效的多模态联邦学习框架，它使用可学习的投影矩阵来生成压缩的潜在表示。潜在空间正则化项在客户端之间对齐这些表示，从而提高了跨模态一致性和对离群值的鲁棒性。在人类活动识别基准测试上的实验表明，CoMFed 以极小的开销实现了具有竞争力的准确率。

## Abstract
Federated learning (FL) enables collaborative model training across distributed devices without sharing raw data, but applying FL to multi-modal settings introduces significant challenges. Clients typically possess heterogeneous modalities and model architectures, making it difficult to align feature spaces efficiently while preserving privacy and minimizing communication costs. To address this, we introduce CoMFed, a Communication-Efficient Multi-Modal Federated Learning framework that uses learnable projection matrices to generate compressed latent representations. A latent-space regularizer aligns these representations across clients, improving cross-modal consistency and robustness to outliers. Experiments on human activity recognition benchmarks show that CoMFed achieves competitive accuracy with minimal overhead.

---

## 论文详细总结（自动生成）

以下是对论文《Communication-Efficient and Robust Multi-Modal Federated Learning via Latent-Space Consensus》（基于潜在空间共识的通信高效且鲁棒的多模态联邦学习）的结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：在现实的边缘计算场景中，联邦学习（FL）面临**多模态异构性**的严峻挑战。不同客户端拥有的传感器模态不同（如有的仅有图像，有的仅有音频）、模型架构各异，且通信带宽有限。
*   **研究动机**：
    *   **架构不兼容**：传统的 FedAvg 要求模型架构一致以进行参数平均，但在多模态下，客户端模型参数维度和语义完全不同，无法直接聚合。
    *   **通信瓶颈**：交换大型多模态模型的梯度或参数开销巨大。
    *   **数据依赖性**：现有的异构 FL 方法（如 FedMD）往往依赖共享的公共数据集进行知识蒸馏，但在隐私敏感场景下，公共数据难以获取。
    *   **鲁棒性需求**：分布式环境中存在离群值或拜占庭攻击（恶意客户端），需要增强系统的防御能力。

### 2. 论文提出的方法论：CoMFed 框架
*   **核心思想**：放弃参数聚合，转而通过**潜在空间（Latent Space）**进行协作。每个客户端学习一个私有的投影矩阵，将异构的本地特征映射到一个统一的、低维的共享潜在空间中，通过对齐类别级别的潜在统计量来实现知识共享。
*   **关键技术细节**：
    *   **可学习投影矩阵 ($P_i$)**：每个客户端 $i$ 拥有一个线性投影矩阵，将本地模型提取的高维特征 $v_i$ 映射为低维潜在表示 $u_i = P_i v_i$。
    *   **类别级潜在统计量**：客户端不发送原始特征，而是计算并发送每个类别的平均潜在表示（Class-wise mean latent representations），极大地压缩了通信量。
    *   **潜在空间正则化器**：在本地损失函数中加入正则项，惩罚本地潜在表示与邻居（或服务器）共识表示之间的偏差。
    *   **鲁棒共识机制**：引入**几何中位数（Geometric Median）**代替算术平均值来计算共识目标，从而抵御拜占庭攻击和噪声干扰。
    *   **交替优化流程**：在每一轮中，客户端先更新本地模型参数 $w_i$，然后交换潜在表示，最后更新投影矩阵 $P_i$。

### 3. 实验设计
*   **数据集与场景**：
    *   **USC-HAD**：人体活动识别数据集，包含 14 个客户端。模拟模态异构：部分客户端仅有加速度计，部分仅有陀螺仪，部分两者皆有。
    *   **DeepSense**：毫米波（mmWave）与激光雷达（LiDAR）阻塞预测数据集。模拟强模态差异和高异构性场景。
*   **Benchmark（基准方法）**：
    *   **Harmony (UniFL)**：按模态组合划分训练子集的联邦学习方法。
    *   **FedMD**：基于公共数据集蒸馏的异构联邦学习。
    *   **FedIoT**：利用无监督自编码器和公共数据集的方法。
    *   **Centralized**：理想化的中心化训练（性能上限）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：提到学习率为 $10^{-3}$，总迭代次数为 250 次，CoMFed 的投影维度 $d$ 在实验中进行了 8 到 64 的消融测试。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个真实世界的多模态数据集上进行了验证。
    *   进行了**鲁棒性实验**：模拟了不同数量（$\gamma=1, 2$）的拜占庭客户端攻击。
    *   进行了**消融实验**：探讨了投影维度 $d$ 对准确率与通信成本的影响。
    *   进行了**通信效率对比**：量化了达到目标准确率所需的累积通信数据量（KB/MB/GB 级别）。
*   **充分性评价**：实验设计较为全面，涵盖了准确率、通信效率、鲁棒性和超参数敏感性。对比实验考虑了当前主流的异构 FL 方案，具有较强的说服力。

### 6. 主要结论与发现
*   **高准确率**：CoMFed 在两个数据集上的平均测试准确率均优于 Harmony、FedMD 和 FedIoT，且更接近中心化训练的性能。
*   **极高通信效率**：相比于传输模型参数的方法（如 Harmony），CoMFed 的通信开销降低了数个数量级（从 GB 级降至 KB/MB 级）。
*   **强鲁棒性**：采用几何中位数的 CoMFed 变体在面对拜占庭攻击时，性能下降远小于传统平均法。
*   **架构无关性**：证明了该框架可以有效连接使用 CNN、GRU 等不同架构的客户端。

### 7. 优点与亮点
*   **通信极简**：仅交换类别级别的低维向量，非常适合带宽受限的边缘设备。
*   **无需公共数据**：解决了许多异构 FL 方法依赖共享数据集的痛点，提升了实用性。
*   **灵活性高**：支持任意通信拓扑（去中心化图结构或中心化星型结构），且对客户端本地模型架构无限制。
*   **鲁棒性设计**：将几何中位数引入潜在空间对齐，增强了系统在真实复杂环境下的生存能力。

### 8. 不足与局限
*   **标签空间假设**：该方法假设所有客户端共享相同的标签空间（Label Set），在标签异构（Label Heterogeneity）严重的场景下可能失效。
*   **隐私风险**：虽然不共享原始数据，但类别平均潜在表示仍可能存在被逆向工程攻击的风险（论文提到未来可结合差分隐私 DP 解决）。
*   **投影矩阵初始化**：实验中投影矩阵是随机初始化的，在极端非独立同分布（Non-IID）情况下，初始阶段的对齐可能会较慢。
*   **实验覆盖**：未在大规模视觉-语言模型（VLM）等超大规模多模态任务上进行验证，主要集中在传感器数据。

（完）
