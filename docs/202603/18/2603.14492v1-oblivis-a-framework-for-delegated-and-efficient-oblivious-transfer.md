---
title: "Oblivis: A Framework for Delegated and Efficient Oblivious Transfer"
title_zh: Oblivis：一种用于委托且高效的不经意传输框架
authors: "Aydin Abadi, Yvo Desmedt"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14492v1"
tags: ["query:ppml"]
score: 9.0
evidence: 委派且高效的不经意传输 (OT)
tldr: 针对云平台和边缘设备中薄客户端的安全检索需求，本文提出 Oblivis 框架。该框架解决了传统不经意传输（OT）协议不支持委派查询且对客户端计算要求高的问题。Oblivis 包含委派查询 OT、多接收者 OT 及高效的 Supersonic OT 等组件，能在保护查询隐私的同时实现极高的处理速度。实验表明，其核心协议在资源受限的硬件上表现优异，显著提升了 OT 的实用性。
selection_source: fresh_fetch
motivation: 现有的 OT 协议难以满足薄客户端在云端环境下进行委派查询和高效隐私检索的需求。
method: 构建了包含委派查询机制和 Supersonic OT 协议的模块化框架，支持信息论安全且响应大小恒定。
result: Supersonic OT 较现有高效协议提速 92 倍以上，且在树莓派等受限设备上实现了毫秒级响应。
conclusion: Oblivis 为分布式和边缘计算环境提供了一种高效、可扩展且支持委派的隐私保护数据检索方案。
---

## 摘要
随着数据库部署向云平台和边缘设备转移，瘦客户端需要安全地检索敏感记录，且不向介导访问的代理泄露其查询意图或元数据。不经意传输（OT）是隐私检索的核心工具，但现有的 OT 方案假设客户端与数据库之间存在直接交互，且缺乏对委托查询或轻量级客户端的支持。我们提出了 Oblivis，这是一个由新型 OT 协议组成的模块化框架，能够实现委托且保护隐私的查询执行。Oblivis 允许客户端在没有直接访问权限的情况下检索数据库记录，防止向数据库和代理泄露信息，并在设计时考虑了实际效率。其组件包括：(1) 委托查询 OT，允许查询生成的安全外包；(2) 用于合并的云托管数据库的多接收者 OT；(3) 一个生成适用于瘦客户端的常数大小响应的编译器；以及 (4) Supersonic OT，一种基于代理、信息论安全且高效的 2 选 1 OT。这些协议在基于模拟的范式下得到了正式定义，并在不合谋假设下被证明是安全的。我们实现并对 Supersonic OT 进行了实证评估。在 200 到 100,000 次调用中，它比高效的 2 选 1 OT 至少实现了 92 倍的加速，比标准的 OT 扩展实现了 2.6 倍至 106 倍的加速。我们的实现进一步表明，Supersonic OT 即使在受限硬件上也能保持高效，例如，在树莓派 4 上完成一次端到端传输仅需 1.36 毫秒。

## Abstract
As database deployments shift toward cloud platforms and edge devices, thin clients need to securely retrieve sensitive records without leaking their query intent or metadata to the proxies that mediate access. Oblivious Transfer (OT) is a core tool for private retrieval, yet existing OTs assume direct client-database interaction and lack support for delegated querying or lightweight clients. We present Oblivis, a modular framework of new OT protocols that enable delegated, privacy-preserving query execution. Oblivis allows clients to retrieve database records without direct access, protects against leakage to both databases and proxies, and is designed with practical efficiency in mind. Its components include: (1) Delegated-Query OT, which permits secure outsourcing of query generation; (2) Multi-Receiver OT for merged, cloud-hosted databases; (3) a compiler producing constant-size responses suitable for thin clients; and (4) Supersonic OT, a proxy-based, informationtheoretic, and highly efficient 1-out-of-2 OT. The protocols are formally defined and proven secure in the simulation-based paradigm, under non-colluding assumption. We implement and empirically evaluate Supersonic OT. It achieves at least a 92x speedup over a highly efficient 1-out-of-2 OT, and a 2.6x-106x speedup over a standard OT extension across 200-100,000 invocations. Our implementation further shows that Supersonic OT remains efficient even on constrained hardware, e.g., it completes an end-to-end transfer in 1.36 ms on a Raspberry Pi 4.

---

## 论文详细总结（自动生成）

以下是对论文《Oblivis: A Framework for Delegated and Efficient Oblivious Transfer》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的不经意传输（OT）协议假设客户端与数据库之间是直接交互的，且客户端需承担主要的计算压力。但在现代云环境和边缘计算中，客户端往往是“薄客户端”（资源受限），且通常通过中介代理（如Cloudflare、金融顾问）访问数据。
*   **研究动机**：
    *   **缺乏委派支持**：现有OT不支持将查询生成的重任安全地外包给代理。
    *   **隐私泄露风险**：中介代理可能观察到查询意图或元数据。
    *   **性能瓶颈**：在移动设备或自主设备上运行复杂的公钥加密协议效率低下。
    *   **多租户环境**：云端合并数据库会泄露数据库总体大小或访问模式等敏感元数据。

### 2. 论文提出的方法论：核心思想与技术细节
论文提出了 **Oblivis** 框架，包含以下核心组件：
*   **DQ-OT (Delegated-Query OT)**：
    *   **核心思想**：利用两个非合谋代理（$P_1, P_2$）协助生成查询。
    *   **流程**：客户端将选择索引 $s$ 进行秘密共享（Secret Sharing）得到 $s_1, s_2$，并发送给两个代理。代理通过顺序计算和排序，生成结构上等同于标准 Naor-Pinkas OT 的查询，而代理本身无法获知真实的 $s$。
*   **DUQ-OT (Delegate-Unknown-Query OT)**：
    *   **核心思想**：支持“隐藏查询”，即接收者在不知道具体索引的情况下检索记录（适用于访问控制场景）。通过在消息中加入随机结构（如随机数 $r_3$），让接收者尝试解密所有响应并匹配结构。
*   **多接收者变体 (DQ-MR-OT / DUQ-MR-OT)**：
    *   针对云端合并数据库设计，确保接收者不了解数据库总规模，发送者不了解哪个接收者查询了哪条记录。
*   **常数大小响应编译器**：
    *   利用**加法同态加密**（如 Paillier），将 $n$ 个响应压缩为一个，使客户端下载开销从 $O(n)$ 降至 $O(1)$。
*   **Supersonic OT**：
    *   **关键技术**：基于代理的、信息论安全的 2 选 1 OT。它引入了**受控交换（Controlled Swap）**机制，利用简单的 XOR 操作和随机置换实现极速传输，不依赖复杂的公钥运算。

### 3. 实验设计
*   **实验场景**：评估协议在不同调用次数（1 到 100,000 次）下的端到端延迟和各阶段耗时。
*   **Benchmark（基准方法）**：
    *   **Simplest OT** (Chou & Orlandi)：目前最高效的基础 OT 协议之一。
    *   **IKNP OT Extension** (Ishai et al.)：标准的 OT 扩展协议。
    *   其他传统协议如 Naor-Pinkas OT。
*   **硬件平台**：
    *   高性能设备：MacBook Pro (Apple M3 Pro CPU, 36GB RAM)。
    *   受限硬件：树莓派 4 (Raspberry Pi 4, 4GB RAM)。

### 4. 资源与算力
*   **算力说明**：该研究主要涉及密码学协议的 CPU 实现，**未提及使用 GPU**。
*   **软件环境**：使用 C++ 实现，依赖 GMP 大数运算库。
*   **运行时间**：Supersonic OT 在 MacBook 上单次调用仅需 0.53ms，在树莓派上需 1.36ms。

### 5. 实验数量与充分性
*   **实验规模**：每组实验重复 50 次并取平均值，涵盖了从单次调用到 10 万次大规模调用的不同量级。
*   **充分性评价**：实验设计较为充分。作者不仅对比了基础协议，还对比了工业界常用的 OT 扩展技术，并跨平台（笔记本 vs 树莓派）验证了协议在薄客户端上的可行性。对比维度包括运行时间、加速比和功能特性，具有较强的客观性。

### 6. 论文的主要结论与发现
*   **极高的效率**：Supersonic OT 比 Simplest OT 快至少 **92 倍**；在小规模调用（200次）时比 IKNP 扩展快 **106 倍**，在大规模调用（10万次）时仍保持 **2.6 倍** 的优势。
*   **薄客户端友好**：在树莓派上实现毫秒级响应，证明了该框架非常适合边缘计算和物联网设备。
*   **安全性提升**：Supersonic OT 实现了**信息论安全**，具备抗量子攻击的潜力，且不依赖噪声信道或复杂的初始化设置。
*   **委派可行性**：证明了可以在不牺牲隐私的前提下，将 OT 的计算压力转移给非合谋的第三方代理。

### 7. 优点
*   **模块化设计**：框架组件可根据具体应用场景（如是否隐藏查询、是否多接收者）灵活组合。
*   **通信优化**：通过编译器实现了常数级的响应下载量，极大减轻了薄客户端的带宽负担。
*   **实用性强**：不依赖昂贵的预处理或海量的本地存储，易于在现有云基础设施中部署。

### 8. 不足与局限
*   **非合谋假设**：协议的安全性高度依赖于两个代理（或代理与发送者）之间**不进行合谋**。如果代理串通，客户端的查询隐私将荡然无存。
*   **查询类型限制**：目前主要针对 1-out-of-2 或 1-out-of-n 的简单检索，对于更复杂的范围查询或谓词查询支持有限。
*   **代理依赖**：虽然减轻了客户端负担，但引入了对第三方代理可用性的依赖，增加了系统架构的复杂性。

（完）
