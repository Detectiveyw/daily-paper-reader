---
title: "FedLECC: Cluster- and Loss-Guided Client Selection for Federated Learning under Non-IID Data"
title_zh: FedLECC：非独立同分布数据下联邦学习中基于聚类和损失引导的客户端选择
authors: "Daniel M. Jimenez-Gutierrez, Giovanni Giunta, Mehrdad Hassanzadeh, Aris Anagnostopoulos, Ioannis Chatzigiannakis, Andrea Vitaletti"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08911v1"
tags: ["query:ppml"]
score: 8.0
evidence: 联邦学习客户端选择策略
tldr: 联邦学习在处理非独立同分布（Non-IID）数据时面临收敛慢和通信受限的挑战。本文提出FedLECC，一种轻量级的客户端选择策略。该方法通过标签分布相似性对客户端进行聚类，并优先选择局部损失较高的聚类和客户端，从而在每轮训练中挑选出既具代表性又包含丰富信息的子集。实验证明，FedLECC显著提升了模型精度并大幅降低了通信开销。
selection_source: fresh_fetch
motivation: 针对跨设备联邦学习中非独立同分布数据导致的模型质量下降及通信受限问题，需要更智能的客户端选择机制。
method: 提出FedLECC策略，结合基于标签分布的客户端聚类与基于局部损失的优先级排序来筛选参与训练的客户端。
result: "在严重标签偏移场景下，该方法将测试准确率提升了高达12%，同时减少了约22%的通信轮数和50%的总通信开销。"
conclusion: 通过结合聚类感知和损失引导的客户端选择，可以有效提升云边协同系统中联邦学习的效率与可扩展性。
---

## 摘要
联邦学习（FL）通过允许在不集中数据的情况下进行协作模型训练，实现了跨云边环境的分布式人工智能（AI）。在跨设备部署中，联邦学习系统面临严格的通信和参与限制，以及严重的非独立同分布（non-IID）数据问题，这会降低收敛速度和模型质量。由于每轮训练只能有一部分设备（即客户端）参与，智能客户端选择成为了一个关键的系统挑战。本文提出了 FedLECC（增强聚类选择的联邦学习），这是一种针对跨设备联邦学习的轻量级、聚类感知且损失引导的客户端选择策略。FedLECC 根据标签分布相似性对客户端进行分组，并优先选择具有较高本地损失的聚类和客户端，从而能够选出一组规模虽小但信息丰富且具有多样性的客户端。在严重标签偏斜情况下的实验结果表明，与强基准方案相比，FedLECC 将测试准确率提高了多达 12%，同时减少了约 22% 的通信轮数，并将总体通信开销降低了多达 50%。这些结果证明，明智的客户端选择能够提高云边系统中联邦学习工作负载的效率和可扩展性。

## Abstract
Federated Learning (FL) enables distributed Artificial Intelligence (AI) across cloud-edge environments by allowing collaborative model training without centralizing data. In cross-device deployments, FL systems face strict communication and participation constraints, as well as strong non-independent and identically distributed (non-IID) data that degrades convergence and model quality. Since only a subset of devices (a.k.a clients) can participate per training round, intelligent client selection becomes a key systems challenge. This paper proposes FedLECC (Federated Learning with Enhanced Cluster Choice), a lightweight, cluster-aware, and loss-guided client selection strategy for cross-device FL. FedLECC groups clients by label-distribution similarity and prioritizes clusters and clients with higher local loss, enabling the selection of a small yet informative and diverse set of clients. Experimental results under severe label skew show that FedLECC improves test accuracy by up to 12%, while reducing communication rounds by approximately 22% and overall communication overhead by up to 50% compared to strong baselines. These results demonstrate that informed client selection improves the efficiency and scalability of FL workloads in cloud-edge systems.

---

## 论文详细总结（自动生成）

### 论文总结：FedLECC — 非独立同分布数据下联邦学习的聚类与损失引导客户端选择策略

#### 1. 研究动机与核心问题
*   **背景**：在云边协同（Cloud-Edge）环境下，联邦学习（FL）面临两大挑战：一是**通信受限**，带宽和设备能耗限制了每轮参与训练的客户端数量；二是**数据异构性（Non-IID）**，尤其是标签偏移（Label Skew），会导致模型收敛缓慢、精度下降及客户端漂移。
*   **核心问题**：如何在每轮训练中，从大量异构客户端中挑选出一个**规模极小但既具多样性（Diversity）又具信息量（Informativeness）**的客户端子集，以在降低通信开销的同时提升模型性能。

#### 2. 论文提出的方法论：FedLECC
FedLECC（Federated Learning with Enhanced Cluster Choice）采用三阶段的轻量级选择机制：
*   **非独立同分布量化**：客户端向服务端发送其本地数据的标签分布直方图。服务端利用 **Hellinger 距离（HD）** 计算客户端间的相似性。这种方式比传输特征分布更轻量且保护隐私。
*   **客户端聚类**：基于 HD 距离矩阵，使用 **OPTICS 算法** 对客户端进行聚类。OPTICS 的优势在于不需要预设聚类数量，能自动识别具有相似数据分布的设备组。
*   **聚类与客户端选择算法**：
    1.  **计算损失**：客户端在本地计算当前全局模型的经验损失（Loss）并上传。
    2.  **聚类优先级**：服务端计算每个聚类的平均损失，优先选择平均损失最高的 $J$ 个聚类（代表模型在该数据分布上表现最差）。
    3.  **客户端筛选**：在选中的每个聚类内部，优先选择局部损失最高的 $z$ 个客户端。
*   **核心思想**：通过聚类确保**多样性**（覆盖不同的数据分布），通过损失引导确保**信息量**（关注模型尚未掌握的难点数据）。

#### 3. 实验设计
*   **数据集**：MNIST 和 Fashion-MNIST (FMNIST)。
*   **场景设置**：使用 Dirichlet 分布进行数据划分，模拟极高程度的 Non-IID 场景（Hellinger 距离约 0.9）。客户端总数 $K$ 设置为 100、250 或 300。
*   **对比方法（Benchmark）**：
    *   **正则化类**：FedAvg, FedProx, FedNova, FedDyn。
    *   **选择策略类**：HACCS, FedCLS, FedCor, POC（Power-of-Choice）。
*   **评估指标**：测试准确率、达到目标精度所需的通信轮数、总体通信开销（MB）。

#### 4. 资源与算力
*   **模型架构**：使用多层感知机（MLP），包含两个隐藏层（每层 200 个神经元）。
*   **超参数**：学习率 0.005，Batch Size 64，共训练 150 轮。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。由于实验基于 MNIST 等小型数据集，推测计算需求较低。

#### 5. 实验数量与充分性
*   **实验规模**：针对两个数据集在不同客户端规模（100/250/300）下进行了多次实验，每组实验均运行 5 个随机种子并取平均值。
*   **充分性评价**：实验对比了 8 种主流基准算法，覆盖了从经典的 FedAvg 到最新的相关性选择策略（FedCor），对比较为全面。
*   **客观性**：在极高 Non-IID 环境下进行测试，能够客观反映算法在极端异构场景下的鲁棒性。

#### 6. 主要结论与发现
*   **精度提升**：在严重标签偏移下，FedLECC 的测试准确率比强基准方法提升了高达 **12%**。
*   **收敛加速**：达到相同精度所需的通信轮数减少了约 **22%**。
*   **通信效率**：通过限制参与客户端数量并精准选择，总体通信开销降低了多达 **50%**。
*   **稳定性**：结合聚类和损失引导的方法比单一使用损失引导（如 POC）在 Non-IID 场景下更稳定，有效缓解了客户端漂移。

#### 7. 优点
*   **双重引导机制**：巧妙结合了“聚类（保证覆盖面）”与“损失（保证学习效率）”，解决了纯损失引导可能导致的过拟合特定难点数据的问题。
*   **系统友好性**：算法逻辑简单，标签直方图的传输开销极小，适合资源受限的边缘设备。
*   **即插即用**：不改变本地训练逻辑或聚合规则，易于集成到现有的 FL 框架中。

#### 8. 不足与局限
*   **数据集复杂度较低**：实验仅限于 MNIST 和 FMNIST 等简单图像数据集，未在 CIFAR-100 或 ImageNet 等更复杂的视觉任务或 NLP 任务上验证。
*   **超参数敏感性**：算法性能依赖于选定聚类数 $J$ 和客户端数 $m$ 的配置，论文提到这需要根据具体工作负载进行调整，缺乏自动化的参数自适应机制。
*   **隐私风险**：虽然标签直方图比原始数据安全，但在某些极端敏感场景下，标签分布本身可能泄露客户端的隐私信息（论文提到未来可结合差分隐私解决）。

（完）
