---
title: A Disguise-and-Squeeze PIR Scheme for the MDS-TPIR Setting and Beyond
title_zh: 一种适用于 MDS-TPIR 场景及其扩展的伪装与压缩 PIR 方案
authors: "Rui Sun, Ran Tao, Jingke Xu, Yiwei Zhang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10769v1"
tags: ["query:ppml"]
score: 10.0
evidence: 从MDS编码数据库中进行隐私信息检索 (PIR)
tldr: 本文研究了在具有共谋服务器的MDS编码数据库中的私有信息检索（MDS-TPIR）问题。针对现有容量猜想被推翻的现状，提出了一种基于“伪装与压缩”框架的新型方案。该方案将Sun-Jafar反例推广至更一般的参数设置，在特定条件下超越了现有最优检索率，并证明了在K=2时可达到线性容量。此外，该方案具有更小的有限域规模和更强的模型适应性，为多文件检索及循环共谋等场景提供了有效支持。
selection_source: fresh_fetch
motivation: 旨在解决MDS编码数据库中存在共谋服务器时的私有信息检索效率问题，并挑战和完善现有的容量猜想。
method: 提出了一种名为“伪装与压缩”（disguise-and-squeeze）的新型PIR方案框架，用于构造高效的检索策略。
result: 在特定参数设置下取得了优于现有技术的检索率，实现了K=2时的线性容量，并降低了实现所需的有限域规模。
conclusion: 该研究不仅提升了MDS-TPIR的检索效率，还通过构造更多反例深化了对该领域容量极限的理论认知。
---

## 摘要
我们研究了从具有合谋服务器的 MDS 编码数据库中进行私有信息检索（PIR）的问题，即 MDS-TPIR。在 MDS-TPIR 场景下，$M$ 个文件存储在 $N$ 台服务器上，每个文件均使用 $(N,K)$-MDS 码独立存储。用户希望检索其中一个文件，且不向任何最多包含 $T$ 台合谋服务器的集合泄露目标文件的索引。研究 PIR 方案的核心问题是最大化 PIR 率，即目标文件大小

## Abstract
We consider the problem of private information retrieval (PIR) from MDS coded databases with colluding servers, i.e., MDS-TPIR. In the MDS-TPIR setting, $M$ files are stored across $N$ servers, where each file is stored independently using an $(N,K)$-MDS code. A user wants to retrieve one file without disclosing the index of the desired file to any set of up to $T$ colluding servers. The general problem in studying PIR schemes is to maximize the PIR rate, defined as the ratio of the size of the desired file to the size of the total download. Freij-Hollanti et al. proposed a conjecture of the MDS-TPIR capacity (the maximum achievable PIR rate), which was later disproved by Sun and Jafar by a counterexample with $(M,N,T,K)=(2,4,2,2)$.   In this paper, we propose a new MDS-TPIR scheme based on a disguise-and-squeeze approach. The features of our scheme include the following.   Our scheme generalizes the Sun-Jafar counterexample to $(M,N,T,K)=(2,N,2,K)$ with $N\geq K+2$ for an arbitrary $(N,K)$-MDS coded system, providing more counterexamples to the conjecture by Freij-Hollanti et al.   For $(M,N,T,K)=(2,N,2,K)$ and a GRS (generalized Reed-Solomon codes) coded system, our scheme has rate $\frac{N^2-N}{N^2+KN-2K}$, beating the state-of-the-art results. We further show that this rate achieves the linear MDS-TPIR capacity when $K=2$.   Our scheme features a significantly smaller field size for implementation and the adaptiveness to generalized PIR models such as multi-file MDS-TPIR and MDS-PIR against cyclically adjacent colluding servers.   Lastly, we provide an $ε$-error MDS-TPIR scheme for $T\geq 3$ based on the disguise-and-squeeze framework.

---

## 论文详细总结（自动生成）

### 论文总结：一种适用于 MDS-TPIR 场景及其扩展的伪装与压缩 PIR 方案

#### 1. 核心问题与整体含义（研究动机和背景）
本文探讨了**分布式存储系统中的隐私信息检索（PIR）**问题，特别是针对使用 **MDS（最大距离可分）码**存储且存在**服务器合谋（Colluding Servers）**的场景（简称 **MDS-TPIR**）。
*   **研究动机**：此前学术界对 MDS-TPIR 的容量（最大检索率）存在一个著名的猜想（FGHK 猜想），但该猜想在 2017 年被 Sun 和 Jafar 通过一个特例 $(2, 4, 2, 2)$ 推翻。
*   **核心问题**：如何构建一种通用的方案，在更广泛的参数设置下超越现有检索率，并进一步逼近或确定 MDS-TPIR 的真实容量。

#### 2. 方法论：核心思想与关键技术
论文提出了一种名为**“伪装与压缩”（Disguise-and-Squeeze）**的新型框架：
*   **伪装阶段（Disguising Phase）**：
    *   **核心思想**：通过精心设计的查询向量空间，使得任何 $T$ 台合谋服务器无法通过查询请求分辨出用户想要的是目标文件还是非目标文件。
    *   **技术细节**：利用矩阵 $H$（基于 Lemma III.2 构造）和随机置换（Permutations），确保目标文件和非目标文件的查询集在统计上对合谋服务器是等价的。
*   **压缩阶段（Squeezing Phase）**：
    *   **核心思想**：利用 MDS 编码带来的冗余性，将非目标文件的查询结果进行“挤压”。
    *   **技术细节**：服务器在返回响应前，根据预设的组合策略（Combination Strategy）对查询到的符号进行线性组合。通过减少非目标文件符号的下载量，腾出空间给目标文件，从而提高 PIR 率。
    *   **进阶优化**：针对 **GRS（广义里德-所罗门）码**，利用 **Schur 积（Schur Product）** 理论进一步挖掘冗余，获得了更高的检索率。对于 $T \ge 3$ 的复杂场景，引入了**外积（Exterior Products）**工具来构造查询空间。

#### 3. 实验设计与对比
本论文属于**信息论与编码理论**研究，其“实验”主要体现为数学证明和针对特定参数场景的数值对比：
*   **对比场景**：主要针对 $(M, N, T, K) = (2, N, 2, K)$ 及其扩展模型（多文件检索、循环相邻合谋等）。
*   **Benchmark（基准）**：
    *   **FGHK 猜想值**：此前被认为可能是容量上限的理论值。
    *   **Sun-Jafar 方案**：此前的反例方案（仅限于特定参数）。
    *   **SOTA 方案**：如 D'Oliveira 等人的 One-shot PIR 方案和 Holzbaur 等人的线性方案。
*   **对比指标**：PIR 率（Rate），即检索到的目标文件大小与总下载量之比。

#### 4. 资源与算力
*   **算力说明**：本文为**纯理论推导与数学证明论文**，不涉及深度学习训练或大规模数值模拟。
*   **实现细节**：文中提到的方案实现主要涉及有限域（Finite Field）上的矩阵运算。作者特别强调其方案在**较小的有限域规模**下即可实现（如 $F_3, F_5, F_7$ 等），这显著降低了实际部署时的计算开销。

#### 5. 实验数量与充分性
*   **覆盖范围**：论文不仅给出了 $T=2$ 时的通用闭式解，还探讨了 $T \ge 3$ 的情况、多文件检索（Multi-file PIR）以及受限合谋模式（如循环相邻合谋）。
*   **充分性评价**：作者通过严谨的代数证明确保了方案的**正确性（Correctness）**和**隐私性（Privacy）**。通过对不同参数组合（如 $N \le 2K$ 和 $N > 2K$）的分类讨论，证明了方案在多种 MDS 编码设置下的优越性。实验（理论推导）覆盖了从特例到通项的完整逻辑，论证充分。

#### 6. 主要结论与发现
*   **突破猜想**：在 $(2, N, 2, K)$ 且 $N \ge K+2$ 的条件下，新方案的 PIR 率全面超越了 FGHK 猜想值。
*   **确定容量**：证明了当 $K=2$ 时，该方案在 GRS 编码系统下达到了**线性 MDS-TPIR 容量**。
*   **检索率提升**：对于 GRS 编码系统，给出了检索率公式 $R = \frac{N^2-N}{N^2+KN-2K}$，优于目前所有已知结果。
*   **扩展性**：成功构造了 $T \ge 3$ 时的 $\epsilon$-误差 PIR 方案，并确定了循环相邻合谋模式下的确切容量。

#### 7. 优点（亮点）
*   **通用性强**：将 Sun-Jafar 的单一反例推广到了任意 $N, K$ 参数。
*   **实现友好**：相比前人方案，所需的有限域规模大幅减小，降低了存储和计算复杂度。
*   **理论深度**：巧妙结合了 Schur 积和外积等代数工具，解决了高阶合谋（$T \ge 3$）下的查询空间构造难题。

#### 8. 不足与局限
*   **误差风险**：对于 $T \ge 3$ 的场景，目前方案是 **$\epsilon$-误差** 的（即存在极小概率检索错误），未能实现完全的零误差（Zero-error）。
*   **文件数量限制**：虽然方案可以扩展到 $M > 2$ 个文件，但在单文件检索且 $M$ 较大时，其检索率优势可能不再明显。
*   **确定性组合策略**：在 $T \ge 3$ 时，难以预设确定性的服务器组合矩阵，目前依赖随机化方法。

（完）
