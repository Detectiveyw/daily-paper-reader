---
title: "Differentially Private Secure Multiplication: Beyond Two Multiplicands"
title_zh: 差分隐私安全乘法：超越两个乘数
authors: "Haoyang Hu, Viveck R. Cadambe"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08944v1"
tags: ["query:ppml"]
score: 9.0
evidence: 分布式系统中的差分隐私安全乘法
tldr: 本研究探讨了分布式计算中差分隐私（DP）安全乘法问题，旨在解决多个私有输入乘积计算中的隐私与准确性权衡。针对 N 个节点协作计算 M 个输入且面临最多 T 个节点合谋的情况，本文提出了一种基于编码多项式和分层噪声注入的通用框架。该方法通过系统性抵消低阶噪声项，显著提升了估计精度，并刻画了不同节点规模下的最优隐私-准确性权衡，填补了多乘数场景下的研究空白。
selection_source: fresh_fetch
motivation: 现有的差分隐私安全乘法研究主要局限于两个乘数，难以直接扩展到多个乘数同时保证隐私与精度的复杂场景。
method: 提出一种基于编码多项式与分层噪声注入的计算框架，通过系统性抵消低阶噪声项来提高乘积的估计精度。
result: 在不同节点规模配置下，分别刻画了最优的隐私-准确性权衡曲线，并推导出高隐私状态下的渐近紧致界限。
conclusion: 该研究为多乘数分布式安全计算提供了理论支撑和优化方案，有效提升了差分隐私保护下的计算效能。
---

## 摘要
我们研究了分布式计算系统中的差分隐私（DP）安全乘法问题，重点关注无法同时实现完美隐私和完美准确性的情形。具体而言，N个节点协作计算M个私有输入的乘积，同时保证针对至多T个节点合谋的epsilon-DP。先前的研究已经刻画了两个乘数乘法的基本隐私-准确性权衡。在本文中，我们将这些结果扩展到计算任意数量M个乘数之积的更一般设置。我们提出了一种基于精心设计的编码多项式结合分层噪声注入的安全乘法框架。所提出的构造推广了现有方案，并能够系统地消除低阶噪声项，从而提高估计准确性。我们探索了两种情形：(M-1)T+1 <= N <= MT 以及 N = T+1。对于 (M-1)T+1 <= N <= MT，我们刻画了最优的隐私-准确性权衡。当 N = T+1 时，我们推导出了在强隐私机制下渐近紧致的非平凡可实现性界限和逆界限。

## Abstract
We study the problem of differentially private (DP) secure multiplication in distributed computing systems, focusing on regimes where perfect privacy and perfect accuracy cannot be simultaneously achieved. Specifically, N nodes collaboratively compute the product of M private inputs while guaranteeing epsilon-DP against any collusion of up to T nodes. Prior work has characterized the fundamental privacy-accuracy trade-off for the multiplication of two multiplicands. In this paper, we extend these results to the more general setting of computing the product of an arbitrary number M of multiplicands. We propose a secure multiplication framework based on carefully designed encoding polynomials combined with layered noise injection. The proposed construction generalizes existing schemes and enables the systematic cancellation of lower-order noise terms, leading to improved estimation accuracy. We explore two regimes: (M-1)T+1 <= N <= MT and N = T+1. For (M-1)T+1 <= N <= MT, we characterize the optimal privacy--accuracy trade-off. When N = T+1, we derive nontrivial achievability and converse bounds that are asymptotically tight in the high-privacy regime.

---

## 论文详细总结（自动生成）

这篇论文《Differentially Private Secure Multiplication: Beyond Two Multiplicands》深入探讨了分布式计算环境下，如何在保证差分隐私（DP）的前提下，高效地计算多个私有输入的乘积。以下是对该论文的结构化总结：

### 1. 核心问题与背景（研究动机）
*   **核心问题**：在分布式系统中，$N$ 个节点协作计算 $M$ 个私有变量 $A_1, A_2, \dots, A_M$ 的乘积。目标是在存在至多 $T$ 个节点合谋的情况下，满足 $\epsilon$-差分隐私（$\epsilon$-DP），同时尽可能提高计算结果的准确性（最小化均方误差 LMSE）。
*   **研究背景**：传统的安全多方计算（MPC）协议（如 BGW）为了实现“完美隐私”和“完美准确”，通常需要大量的通信轮数或极高的节点冗余（单轮协议需 $N \ge MT+1$）。
*   **研究动机**：现有的差分隐私安全乘法研究大多局限于 $M=2$（两个乘数）。随着机器学习等应用中非线性计算需求的增加，亟需一种能处理 $M > 2$ 且在节点资源受限（$N < MT+1$）时仍能提供最优隐私-准确性权衡的方案。

### 2. 方法论：核心思想与技术细节
论文提出了一种基于**编码多项式（Encoding Polynomials）**与**分层噪声注入（Layered Noise Injection）**的通用框架：
*   **核心思想**：利用多项式编码将私有输入与精心设计的噪声结合，通过节点间的协作计算，使解码器能够利用线性组合抵消低阶噪声项，仅保留目标乘积和不可避免的高阶微小噪声。
*   **关键技术**：
    1.  **分层噪声构造**：为每个输入 $A_i$ 构造多项式 $p_i(x)$。噪声分为三层：第一层是满足 $\epsilon$-DP 的最优阶梯噪声（Staircase Mechanism）；第二层是用于模拟秘密共享的辅助噪声；第三层是与第一层相关的缩放噪声。
    2.  **多项式乘积与解码**：每个节点计算其本地数据的乘积 $\tilde{V}^{(j)} = \prod p_i(x_j)$。解码器收集所有节点的输出，利用范德蒙德矩阵（Vandermonde Matrix）的性质，通过线性解码函数恢复出目标乘积的估计值。
    3.  **几何解释**：论文为 $M=2$ 提供了直观的几何解释（矩形面积分解），并将其推广到 $M > 2$ 的高维张量空间，证明了误差主要来源于各变量估计误差的乘积。

### 3. 实验设计与对比
*   **场景设定**：模拟了 $N=5, M=3, T=2$ 的分布式计算场景，即 5 个节点计算 3 个数的乘积，容忍 2 个节点合谋。
*   **Benchmark（基准方案）**：
    1.  **复值 Shamir 秘密共享（Complex-valued Shamir SS）**：基于现有文献中将秘密共享应用于实数域的方案。
    2.  **独立加性噪声（Independent Noise）**：在每个节点简单添加独立的差分隐私噪声。
*   **对比指标**：隐私参数 $\epsilon$ 与线性均方误差（LMSE）之间的权衡曲线。

### 4. 资源与算力
*   **算力说明**：该论文属于**理论信息论与编码**领域，主要侧重于数学证明、闭式解推导和渐近分析。
*   **资源消耗**：文中未提及具体的 GPU 或大规模集群算力，实验部分主要是基于数值模拟（如使用 MATLAB 或 Python 进行曲线绘制），不涉及深度学习训练。

### 5. 实验数量与充分性
*   **实验规模**：论文主要通过数值图表展示了在特定参数（如 $N=5, M=3, T=2$）下的性能对比。
*   **充分性评价**：对于理论型论文，其实验（数值模拟）的主要目的是验证理论界限（Achievability 和 Converse）的正确性。论文通过严谨的数学证明涵盖了 $(M-1)T+1 \le N \le MT$ 和 $N=T+1$ 两种关键机制，实验结果与理论推导高度吻合，论证充分且逻辑自洽。

### 6. 主要结论与发现
*   **最优权衡刻画**：在 $(M-1)T+1 \le N \le MT$ 机制下，论文给出了最优的隐私-准确性权衡公式。结果显示，LMSE 随乘数 $M$ 的增加呈指数级变化，具体形式为 $\eta^M / (1 + SNR^*(\epsilon))^M$。
*   **节点效率**：证明了使用 $N = (M-1)T+1$ 个节点即可达到该机制下的性能极限，增加更多节点（只要不超过 $MT$）并不能进一步提升精度。
*   **高隐私极限**：在极简冗余场景（$N=T+1$）下，虽然存在理论间隙，但在高隐私需求（$\epsilon \to 0$）时，所提方案的界限是渐近紧致的。

### 7. 优点
*   **理论突破**：打破了以往研究仅限于两个乘数的局限，为任意数量乘数的安全乘法提供了通用的信息论框架。
*   **资源高效**：相比传统 MPC，该方案在单轮通信内实现了更少的节点需求，显著降低了基础设施开销。
*   **构造精巧**：通过分层噪声设计，巧妙地解决了实数域上差分隐私与代数编码结构不兼容的问题。

### 8. 不足与局限
*   **应用限制**：目前的研究假设输入变量 $A_i$ 是相互独立的，在某些实际场景（如变量间存在强相关性）下，隐私泄露的分析会更加复杂。
*   **理论与实践的鸿沟**：分析基于实数域和渐近极限（$\zeta \to 0$），在有限精度计算或固定参数设置下，可能会遇到数值稳定性问题。
*   **未闭合的间隙**：在 $N=T+1$ 且 $N < M$ 的场景下，可实现界限与逆界限之间仍存在一定间隙，尚未达成完全的最优性证明。

（完）
