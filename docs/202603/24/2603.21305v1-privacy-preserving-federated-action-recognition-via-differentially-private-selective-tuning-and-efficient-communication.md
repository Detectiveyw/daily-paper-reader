---
title: Privacy-Preserving Federated Action Recognition via Differentially Private Selective Tuning and Efficient Communication
title_zh: 通过差分隐私选择性微调与高效通信实现的隐私保护联邦动作识别
authors: "Idris Zakariyya, Pai Chet Ng, Kaushik Bhargav Sivangi, S. Mohammad Sheikholeslami, Konstantinos N. Plataniotis, Fani Deligianni"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21305v1"
tags: ["query:ppml"]
score: 8.0
evidence: 用于视频动作识别的差分隐私联邦学习
tldr: "联邦视频动作识别面临隐私泄露和高通信开销的挑战。本文提出FedDP-STECAR框架，通过差分隐私（DP）选择性地微调和扰动一小部分任务相关层，在保护隐私的同时保持视频特征的时间连贯性。该方法仅传输微调层，使通信量减少99%以上。在UCF-101数据集上的实验表明，该框架在严格隐私保护下显著提升了准确率，并大幅加快了训练速度，实现了可扩展且隐私保护的动作识别。"
selection_source: fresh_fetch
motivation: 针对联邦视频动作识别中梯度交换导致的隐私泄露以及高维模型带来的巨大通信开销问题。
method: 提出FedDP-STECAR框架，利用差分隐私技术仅对少量关键层进行选择性微调和扰动，并仅传输这些层。
result: "在UCF-101数据集上，该方法在严格隐私条件下准确率提升高达70.2%，通信量减少99%以上，训练速度提升48%。"
conclusion: 该研究证明了通过选择性微调和差分隐私结合，可以在极低通信成本下实现高效且高隐私强度的联邦视频动作识别。
---

## 摘要
联邦视频动作识别能够在不共享原始视频数据的情况下进行协作模型训练，但仍面临两个关键挑战：模型暴露和通信开销。客户端与服务器之间交换的梯度可能会泄露私有的运动模式，而高维视频网络的全模型同步会导致巨大的带宽和通信成本。为了解决这些问题，我们提出了“面向动作识别的差分隐私选择性微调与高效通信联邦学习”（FedDP-STECAR）。我们的 FedDP-STECAR 框架在差分隐私（DP）下，仅选择性地微调和扰动一小部分与任务相关的层，在减少信息泄露面的同时，保持了视频特征的时间相干性。通过在聚合过程中仅传输微调后的层，与全模型更新相比，通信流量减少了 99% 以上。在 UCF-101 数据集上使用 MViT-B-16x4 Transformer 进行的实验表明，FedDP-STECAR 在中心化设置下的严格隐私保护（ε=0.65）下，准确率提高了高达 70.2%；在联邦设置下，训练速度提高了 48%，准确率达到 73.1%，从而实现了可扩展且隐私保护的视频动作识别。代码可在 https://github.com/izakariyya/mvit-federated-videodp 获取。

## Abstract
Federated video action recognition enables collaborative model training without sharing raw video data, yet remains vulnerable to two key challenges: \textit{model exposure} and \textit{communication overhead}. Gradients exchanged between clients and the server can leak private motion patterns, while full-model synchronization of high-dimensional video networks causes significant bandwidth and communication costs. To address these issues, we propose \textit{Federated Differential Privacy with Selective Tuning and Efficient Communication for Action Recognition}, namely \textit{FedDP-STECAR}. Our \textit{FedDP-STECAR} framework selectively fine-tunes and perturbs only a small subset of task-relevant layers under Differential Privacy (DP), reducing the surface of information leakage while preserving temporal coherence in video features. By transmitting only the tuned layers during aggregation, communication traffic is reduced by over 99\% compared to full-model updates. Experiments on the UCF-101 dataset using the MViT-B-16x4 transformer show that \textit{FedDP-STECAR} achieves up to \textbf{70.2\% higher accuracy} under strict privacy ($ε=0.65$) in centralized settings and \textbf{48\% faster training} with \textbf{73.1\% accuracy} in federated setups, enabling scalable and privacy-preserving video action recognition. Code available at https://github.com/izakariyya/mvit-federated-videodp

---

## 论文详细总结（自动生成）

以下是对论文《Privacy-Preserving Federated Action Recognition via Differentially Private Selective Tuning and Efficient Communication》的结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：在联邦学习（FL）框架下进行视频动作识别时，面临**模型暴露（隐私泄露）**和**通信开销巨大**两大挑战。
    *   **隐私泄露**：客户端与服务器交换的梯度可能被恶意推断出原始视频的运动模式或身份信息。
    *   **通信瓶颈**：视频识别模型（如 Transformer）参数量巨大，全模型同步会消耗极高的带宽和电量。
*   **研究动机**：现有的差分隐私（DP）方法在严格隐私预算下往往导致准确率大幅下降，且未充分考虑视频模型的高维特性和通信效率。本文旨在寻找隐私、准确率与通信成本之间的最优平衡。

### 2. 论文提出的方法论：FedDP-STECAR
核心思想是**FedDP-STECAR**框架，通过“选择性微调”和“高效通信”来优化联邦视频学习。
*   **选择性微调（Selective Tuning）**：
    *   将模型参数分为冻结层（$\theta_f$）和可训练层（$\theta_t$）。仅对任务相关的关键层（如分类头和最后的 Transformer 阶段）进行更新。
    *   **关键点**：DP 噪声仅注入到这部分子集中，减少了噪声传播对模型整体特征表示的破坏，保留了视频的时间相干性。
*   **差分隐私与顶层采样（TLS）**：
    *   使用 **DP-SGD** 算法，对每样本梯度进行裁剪（Clipping）并添加高斯噪声。
    *   引入 **Top-Level Sampling (TLS)**：采用无放回采样（Sampling without replacement），在每个本地 Epoch 中确保样本只使用一次，从而实现隐私增强（Privacy Amplification）。
*   **高效通信聚合**：
    *   **聚合无关性**：支持 FedAvg 和 FedNova 等多种联邦聚合算法。
    *   **通信压缩**：客户端仅上传被微调的层（$\theta_t$）的梯度掩码，大幅降低传输数据量。

### 3. 实验设计
*   **数据集**：使用 **UCF-101** 动作识别数据集（包含 101 类动作，约 1.3 万个视频）。
*   **模型架构**：采用 **MViT-B-16x4**（多尺度视觉 Transformer），预训练于 Kinetics-400。
*   **Benchmark 与对比方法**：
    *   **对比策略**：全量微调（Full Fine-tuning, FT） vs. 选择性微调（Selective Tuning, Sel）。
    *   **隐私配置**：带/不带 DP-SGD，带/不带 TLS 采样。
    *   **联邦算法**：对比了 FedAvg 和 FedNova。
    *   **基准线**：中心化训练结果作为性能上限参考。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练时长**：论文提供了运行时间（Runtime）对比。例如，在联邦设置下，全量微调需要约 339 分钟，而 FedDP-STECAR 仅需约 179 分钟（缩短了约 48%）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **中心化实验**：测试了不同隐私预算 $\epsilon$（从 0.65 到 2.0）下的准确率。
    *   **联邦实验**：测试了不同客户端数量（2, 5, 10, 20）和通信轮数（10, 20）的扩展性。
    *   **消融实验**：验证了 TLS 采样对隐私和准确率的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从单机到分布式的性能验证，并对比了不同的联邦优化器，证明了方法的通用性。但在超大规模客户端（如成百上千个节点）上的表现尚未验证。

### 6. 论文的主要结论与发现
*   **隐私与效用平衡**：在极严苛的隐私预算（$\epsilon = 0.65$）下，选择性微调比全量微调的准确率高出 **70.2%**。
*   **通信效率极大化**：通过仅传输微调层，通信流量减少了 **99.8%**（从 1456MB 降至 3.1MB），通信延迟降低了约 470 倍。
*   **联邦性能**：在 $\epsilon = 1.33$ 的联邦设置下，达到了 **73.1%** 的准确率，且在增加客户端数量时性能保持稳定（仅 1-2% 的波动）。
*   **TLS 作用**：TLS 在中心化设置下能显著提升严格隐私下的模型效用，但在联邦设置下提升较小。

### 7. 优点
*   **极高的通信效率**：针对视频模型参数量大的痛点，通过选择性更新实现了近乎百倍的通信压缩。
*   **隐私保护针对性强**：将 DP 噪声限制在特定层，有效解决了 DP 导致视频特征时间维度混乱的问题。
*   **算法兼容性好**：不绑定特定的联邦聚合算法，具有较强的工程落地潜力。

### 8. 不足与局限
*   **数据集单一**：仅在 UCF-101 上进行了验证，缺乏在更复杂、更大规模视频数据集（如 Kinetics-700 或 Something-Something）上的泛化性测试。
*   **非 IID 讨论不足**：论文未深入探讨数据异构性（Non-IID）对选择性微调策略的具体影响。
*   **客户端规模有限**：实验最高仅模拟了 20 个客户端，对于真实世界中数以千计的边缘设备场景，其收敛性有待观察。

（完）
