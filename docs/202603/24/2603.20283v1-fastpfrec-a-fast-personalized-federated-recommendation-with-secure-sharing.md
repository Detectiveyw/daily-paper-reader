---
title: "FastPFRec: A Fast Personalized Federated Recommendation with Secure Sharing"
title_zh: FastPFRec：一种具有安全共享机制的快速个性化联邦推荐框架
authors: "Zhenxing Yan, Jidong Yuan, Yongqi Sun, Haiyang Liu, Zhihui Gao"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.20283v1"
tags: ["query:ppml"]
score: 8.0
evidence: 具有安全共享的联邦推荐系统
tldr: 针对基于图神经网络的联邦推荐系统在收敛速度和隐私泄露方面的挑战，本文提出FastPFRec框架。该框架通过高效的本地更新策略加速模型收敛，并引入隐私感知参数共享机制来降低泄露风险。在四个真实数据集上的实验表明，FastPFRec在显著缩短训练时间和减少训练轮数的同时，提升了推荐准确率，为可扩展的联邦推荐提供了高效且安全的解决方案。
selection_source: fresh_fetch
motivation: 现有的基于图神经网络的联邦推荐系统面临图数据收敛缓慢以及协作过程中存在隐私泄露风险的问题。
method: 提出了一种结合高效本地更新策略与隐私感知参数共享机制的个性化联邦推荐框架。
result: "在多个真实数据集上，该方法相比基准模型减少了32%的训练轮数和34.1%的时间，同时准确率提升了8.1%。"
conclusion: FastPFRec成功平衡了联邦推荐系统的训练效率、预测精度与数据安全性，为大规模联邦推荐提供了高效的解决方案。
---

## 摘要
基于图神经网络（GNN）的联邦推荐系统在保护数据隐私的同时，能有效捕捉用户与物品之间的关系。然而，现有方法在图数据上往往面临收敛速度慢以及协作过程中的隐私泄露风险。为了应对这些挑战，我们提出了 FastPFRec（具有安全共享机制的快速个性化联邦推荐），这是一个同时增强训练效率和数据安全性的新颖框架。FastPFRec 通过高效的本地更新策略加速模型收敛，并引入了一种隐私感知的参数共享机制以降低泄露风险。在四个真实数据集（Yelp、Kindle、Gowalla-100k 和 Gowalla-1m）上的实验表明，与现有基准方法相比，FastPFRec 的训练轮数减少了 32.0%，训练时间缩短了 34.1%，准确率提高了 8.1%。这些结果证明，FastPFRec 为可扩展的联邦推荐提供了一种高效且隐私保护的解决方案。

## Abstract
Graph neural network (GNN)-based federated recommendation systems effectively capture user-item relationships while preserving data privacy. However, existing methods often face slow convergence on graph data and privacy leakage risks during collaboration. To address these challenges, we propose FastPFRec (Fast Personalized Federated Recommendation with Secure Sharing), a novel framework that enhances both training efficiency and data security. FastPFRec accelerates model convergence through an efficient local update strategy and introduces a privacy-aware parameter sharing mechanism to mitigate leakage risks. Experiments on four real-world datasets (Yelp, Kindle, Gowalla-100k, and Gowalla-1m) show that FastPFRec achieves 32.0% fewer training rounds, 34.1% shorter training time, and 8.1% higher accuracy compared with existing baselines. These results demonstrate that FastPFRec provides an efficient and privacy-preserving solution for scalable federated recommendation.

---

## 论文详细总结（自动生成）

以下是对论文《FastPFRec: A Fast Personalized Federated Recommendation with Secure Sharing》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**基于图神经网络（GNN）的联邦推荐系统**。虽然 GNN 能有效捕捉用户-物品的复杂关系，且联邦学习（FL）能保护原始数据隐私，但两者结合面临三大挑战：
*   **收敛缓慢：** 在非独立同分布（Non-IID）的图数据上，模型训练轮数多、通信开销大。
*   **隐私泄露风险：** 尽管不共享原始数据，但交换的梯度或参数仍可能通过推理攻击泄露用户信息。
*   **系统脆弱性：** 标准的两层（客户端-服务器）架构难以抵御恶意客户端的噪声注入或服务器端的源追踪攻击。

### 2. 方法论：核心思想与关键技术
FastPFRec 提出了一个**三层联邦架构**（客户端、信任节点、服务器），核心思想是通过非对称更新加速收敛，并通过多层机制保障安全。
*   **FastGNN 更新策略：** 提出“非对称更新”机制。由于用户偏好动态变化而物品特征相对稳定，FastGNN 每轮更新用户嵌入，但每隔 $H \times 10$ 轮才更新一次物品嵌入。这显著降低了计算复杂度并缓解了 GNN 的过平滑问题。
*   **三层安全架构：** 引入**信任节点（Trusted Nodes）**作为中间层。它们负责初步聚合参数并执行基于中位数绝对偏差（MAD）的**异常检测**，从而解耦客户端与服务器，防止源追踪。
*   **隐私保护组合拳：** 
    *   **图扰动：** 在本地随机添加虚假交互边，模糊真实行为。
    *   **本地差分隐私（LDP）：** 在上传参数前添加拉普拉斯噪声。
    *   **安全聚合：** 在信任节点进行中间聚合，增强匿名性。

### 3. 实验设计
*   **数据集：** 使用了四个真实世界数据集：Yelp、Kindle、Gowalla-100k 和 Gowalla-1m（涵盖了从 10 万到 100 万级的交互规模）。
*   **基准方法（Benchmark）：**
    *   **集中式模型：** MF, LightGCN, SGL, XSimGCL。
    *   **联邦推荐模型：** FedMF, FedNCF, FedGNN, PerFedRec, PerFedRec++（SOTA）。
*   **评价指标：** Hit Rate (HR@10) 和 NDCG@10。

### 4. 资源与算力
*   **硬件配置：** 使用了一台配备 **NVIDIA Tesla V100 GPU (32GB RAM)** 的工作站，辅以 1TB SSD。
*   **软件环境：** Ubuntu 20.04, PyTorch 2.2.2, CUDA 11.8。
*   **训练效率：** 论文明确提到 FastPFRec 相比基准模型缩短了 **34.1% 的训练时间**。例如在 Gowalla-1m 上，基准模型需超过 5 小时，而 FastPFRec 在 100 轮内即可收敛。

### 5. 实验数量与充分性
*   **实验规模：** 针对 4 个数据集进行了全面对比，每组实验重复 5 次以确保统计显著性（通过 Wilcoxon 符号秩检验）。
*   **消融实验：** 极其充分，分别验证了 FastGNN 结构、信任节点、异常检测、LDP 噪声以及更新频率对性能的影响。
*   **鲁棒性测试：** 模拟了恶意攻击（30% 恶意客户端注入噪声）和基础设施故障（30% 信任节点失效），验证了系统的弹性。
*   **客观性：** 实验涵盖了不同稀疏度和规模的数据，对比了最新的 SOTA 模型，实验设计客观且公平。

### 6. 主要结论与发现
*   **性能卓越：** FastPFRec 在准确率上比现有最强联邦基准提升了 **8.1%**。
*   **极速收敛：** 训练轮数减少了 **32.0%**。在大型数据集 Gowalla-1m 上，FastPFRec 仅需约 80 轮即可达到最优性能，而基准模型需 300-500 轮。
*   **高安全性：** 信任节点架构能检测并隔离 **94.8%** 的恶意攻击行为。
*   **隐私-效用平衡：** 引入 LDP 仅导致约 4.3% 的性能下降，但在隐私保护下依然优于无隐私保护的基准模型。

### 7. 优点
*   **效率创新：** 通过“稀疏更新物品嵌入”的策略，巧妙地解决了 GNN 在联邦场景下计算量大且易过平滑的痛点。
*   **架构稳健：** 三层架构不仅提升了安全性，还通过中间层减轻了中央服务器的计算压力。
*   **实用性强：** 实验证明该方法在极度稀疏（0.04% 密度）的大规模数据集上依然表现稳健。

### 8. 不足与局限
*   **信任假设：** 系统依赖于“诚实但好奇”的信任节点，在完全去中心化（无可靠边缘服务器）的场景下难以部署。
*   **攻击模型局限：** 虽然对噪声注入攻击防御效果好，但尚未深入探讨针对更高级的“成员推理攻击”或“模型反转攻击”的防御表现。
*   **超参数依赖：** 物品更新频率（$h=10$）是一个经验值，在不同动态特性的数据集上可能需要重新调优。

（完）
