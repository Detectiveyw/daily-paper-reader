---
title: Secure Linear Alignment of Large Language Models
title_zh: 大语言模型的安全线性对齐
authors: "Matt Gorbett, Suman Jana"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18908v2"
tags: ["query:ppml"]
score: 9.0
evidence: 使用同态加密实现语言模型间跨孤岛推理的隐私保护框架
tldr: 本研究利用大语言模型表示空间的收敛性，提出了一种隐私保护的跨模型推理框架。该框架通过在公共数据集上学习线性仿射变换来对齐独立训练的模型，并利用同态加密技术保护推理时的客户端查询。实验表明，该方法在保持高安全性的同时实现了亚秒级延迟，不仅在分类和离群检测任务中表现出色，还首次实现了跨模型的文本生成，为隐私受限环境下的模型协作开辟了新路径。
selection_source: fresh_fetch
motivation: 针对隐私和安全限制导致无法直接共享数据或模型的问题，探索利用模型表示收敛性实现跨模型协作的可能性。
method: 结合线性仿射变换与同态加密技术，仅对对齐和分类操作进行加密，以平衡推理效率与数据安全性。
result: 实验证明该框架在多种任务上性能损失极小，且推理延迟达到亚秒级，并成功实现了跨独立模型的文本生成。
conclusion: 线性对齐是实现安全、高效跨模型推理的有效手段，证明了独立训练模型间存在深层的表示兼容性。
---

## 摘要
尽管在训练目标、架构和数据模态上存在差异，但语言模型似乎越来越多地学习到相似的表示。这种独立训练模型之间新兴的兼容性，为跨模型对齐下游目标提供了新的机遇。此外，它还开启了新的潜在应用领域，例如由于安全、隐私或竞争约束而禁止直接共享数据或模型的场景。在这项工作中，我们提出了一个隐私保护框架，利用表示收敛来实现独立语言模型之间的跨孤岛推理。该框架在共享的公开数据集上学习仿射变换，并应用同态加密在推理过程中保护客户端查询。通过仅对线性对齐和分类操作进行加密，该方法在保持强安全保证的同时，实现了亚秒级的推理延迟。我们通过对表示收敛的实证研究来支持该框架，在研究中我们学习了独立模型最终隐藏状态之间的线性变换。我们在嵌入分类和分布外检测上评估了这些跨模型映射，观察到不同模型对之间的性能下降极小。此外，我们首次展示了线性对齐有时能够实现跨独立训练模型的文本生成。

## Abstract
Language models increasingly appear to learn similar representations, despite differences in training objectives, architectures, and data modalities. This emerging compatibility between independently trained models introduces new opportunities for cross-model alignment to downstream objectives. Moreover, it unlocks new potential application domains, such as settings where security, privacy, or competitive constraints prohibit direct data or model sharing. In this work, we propose a privacy-preserving framework that exploits representational convergence to enable cross-silo inference between independent language models. The framework learns an affine transformation over a shared public dataset and applies homomorphic encryption to protect client queries during inference. By encrypting only the linear alignment and classification operations, the method achieves sub-second inference latency while maintaining strong security guarantees. We support this framework with an empirical investigation into representational convergence, in which we learn linear transformations between the final hidden states of independent models. We evaluate these cross-model mappings on embedding classification and out-of-distribution detection, observing minimal performance degradation across model pairs. Additionally, we show for the first time that linear alignment sometimes enables text generation across independently trained models.

---

## 论文详细总结（自动生成）

以下是对论文《Secure Linear Alignment of Large Language Models》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何在不直接共享原始数据或模型参数的前提下，实现独立训练的大语言模型（LLM）之间的协作与推理？
*   **研究背景**：
    *   **表示收敛性**：近期研究（如柏拉图表示假设）发现，尽管架构和数据不同，大规模模型往往会学习到相似的潜在表示空间。
    *   **隐私与安全约束**：在医疗（HIPAA）、金融或跨境数据传输（GDPR）场景下，直接共享模型或数据受到严格法律和竞争限制。
    *   **现有方案不足**：现有的隐私保护推理（如全模型同态加密）计算开销巨大，延迟通常以分钟计，无法满足实际应用需求。

### 2. 方法论：HELIX 框架
*   **核心思想**：利用模型表示的线性相似性，通过一个简单的**线性仿射变换（Affine Map）**对齐两个模型的特征空间，并结合**同态加密（HE）**保护推理过程中的查询隐私。
*   **关键技术细节**：
    *   **线性对齐**：在共享的公共数据集（如 Wikipedia）上，提取源模型 $g_B$ 和目标模型 $g_A$ 的最终隐藏层表示，通过岭回归（Ridge Regression）学习变换矩阵 $W$ 和偏置 $b$。
    *   **隐私保护协议**：
        1.  **训练阶段**：客户端加密其嵌入向量发送给服务端，服务端在加密状态下计算互协方差矩阵并返回，客户端解密后本地求解对齐参数。
        2.  **推理阶段**：客户端在本地对查询进行线性对齐，随后使用 **CKKS 同态加密方案**加密对齐后的向量并发送。服务端在加密状态下应用其线性分类头，返回加密预测结果。
*   **算法流程**：仅对计算开销极小的线性对齐和分类操作进行加密，而将复杂的 Transformer 计算保留在客户端本地（明文执行）。

### 3. 实验设计
*   **数据集与场景**：
    *   **下游任务**：SST-2（情感分析）、TREC（问题分类）、AG News（新闻分类）、MNLI（自然语言推理）、DBpedia。
    *   **文本生成**：使用 MMLU（多任务理解）和 Alpaca（指令遵循）评估跨模型生成能力。
    *   **公共数据**：使用 Wikipedia 和 IMDB 作为对齐用的公共数据集。
*   **Benchmark（基准）**：
    *   原生目标模型在完整数据上训练的线性分类器性能。
    *   在极少量（64/128个）样本上训练的独立分类器。
*   **对比方法**：对比了多种现有的隐私保护推理系统（如 BOLT, Nimbus, MPCFormer, Nexus 等）在延迟和准确率上的表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100 数量）或总训练时长。
*   **实现细节**：提到使用 `TenSEAL` 库实现 CKKS 同态加密，实验主要在 CPU 环境下评估加密操作的延迟。
*   **效率数据**：强调了推理延迟为**亚秒级（<1s）**，通信开销小于 **1MB/样本**，这远低于全模型加密方案。

### 5. 实验数量与充分性
*   **实验规模**：
    *   分析了 **34 对模型组合**，涵盖了从 270M 到 32B 参数的模型（包括 Llama 3, Qwen 2.5, Mistral, Gemma, Gemini API, OpenAI API 等）。
    *   使用了 CKA（中心化内核对齐）和 SVCCA 两种数学指标系统性地验证了表示相似性。
*   **充分性评价**：实验设计较为全面，不仅涵盖了传统的判别式任务（分类、OOD 检测），还深入探讨了生成式任务。引入了 **LLM-as-a-Judge（GPT-5 & Claude 4）** 和 **双盲人类评估**，增强了结果的可信度和客观性。

### 6. 主要结论与发现
*   **线性对齐的有效性**：独立训练的模型之间存在显著的线性结构共享，简单的线性映射即可在分类任务中保持极高的精度（性能下降极小）。
*   **跨模型生成**：首次证明线性对齐可以支持跨模型的文本生成。例如，可以用 Qwen 的编码器配合 Llama 的输出头生成连贯文本。
*   **成功的关键因素**：
    1.  **分词器（Tokenizer）兼容性**：词表重叠度（Jaccard Index）和精确标记匹配率是预测生成成功的核心指标（相关性 $r=0.898$）。
    2.  **模型规模**：源模型需达到一定规模（通常 >4B）才能提供足够的表示能力进行有效对齐。
*   **隐私与效率平衡**：HELIX 在提供 128 位安全强度的同时，将推理速度提升了数个数量级。

### 7. 优点与亮点
*   **极高的实用性**：通过“本地特征提取 + 云端加密线性推理”的模式，解决了同态加密在 LLM 领域难以落地的延迟瓶颈。
*   **跨厂商协作**：为不同厂商（如 OpenAI 和 Anthropic）之间的模型互操作提供了一种无需泄露核心权重的技术路径。
*   **理论与实证结合**：不仅提出了框架，还对 LLM 表示空间的几何特性进行了深入的实证分析。

### 8. 不足与局限
*   **分词器束缚**：该方法高度依赖分词器的兼容性。对于词表差异巨大的模型（如 Gemma 系列与其他模型），对齐效果显著变差。
*   **模型规模门槛**：实验显示小模型（<2B）难以通过线性映射实现高质量的生成对齐。
*   **安全性假设**：虽然保护了查询隐私，但对齐矩阵 $W$ 存储在客户端，可能存在针对服务端模型结构的成员推理攻击风险（尽管文中对此做了初步防御分析）。
*   **任务范围**：目前主要验证了线性分类头，对于更复杂的非线性下游任务（如复杂的推理链）是否依然适用尚待研究。

（完）
