---
title: Low-pass Personalized Subgraph Federated Recommendation
title_zh: 低通个性化子图联邦推荐
authors: "Wooseok Sim, Hogun Park"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20338v1"
tags: ["query:ppml"]
score: 8.0
evidence: 使用图傅里叶变换的隐私保护联邦推荐系统
tldr: 联邦推荐系统面临子图结构不平衡的挑战，即不同客户端的用户-项目子图在规模和连接性上存在巨大差异，导致模型表示失准。本文提出LPSFed框架，利用图傅里叶变换和低通频谱滤波提取稳定的低频结构信号，并通过中性结构锚点引导个性化参数更新。此外，引入局部流行度偏差感知边际来纠正推荐偏差。实验证明该方法在五个真实数据集上均取得了优异的准确性和鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决联邦推荐中由于客户端子图规模和连接性差异导致的结构不平衡及表示对齐困难问题。
method: 利用低通频谱滤波提取稳定的结构信号，并结合结构锚点引导的个性化更新与局部流行度偏差修正机制。
result: 在五个真实世界数据集上的实验验证了LPSFed在提升推荐准确性和增强模型鲁棒性方面的优越性。
conclusion: 通过结合频谱分析与个性化偏差纠正，LPSFed有效克服了联邦学习中的子图失衡挑战，为构建鲁棒的个性化联邦推荐系统提供了新方案。
---

## 摘要
联邦推荐系统 (FRS) 通过在客户端特定的用户-项目子图上训练去中心化模型，且不共享原始数据，从而保护隐私。然而，FRS 面临着一个独特的挑战：子图结构不平衡。子图规模（用户/项目数量）和连通性（项目度）的剧烈变化导致客户端表示失准，使得训练一个既能尊重每个客户端独特结构特征又具有鲁棒性的模型变得具有挑战性。为了解决这一问题，我们提出了一种低通个性化子图联邦推荐系统 (LPSFed)。LPSFed 利用图傅里叶变换和低通谱滤波来提取低频结构信号，这些信号在不同规模和度的子图中保持稳定，从而在与中性结构锚点的相似性引导下，实现鲁棒的个性化参数更新。此外，我们利用一种局部流行度偏差感知的边际（margin），捕捉每个子图内部的项目度不平衡，并将其整合到个性化偏差修正项中，以减轻推荐偏差。在理论分析的支持下，并在五个真实世界数据集上进行了验证，LPSFed 实现了卓越的推荐准确性并增强了模型的鲁棒性。

## Abstract
Federated Recommender Systems (FRS) preserve privacy by training decentralized models on client-specific user-item subgraphs without sharing raw data. However, FRS faces a unique challenge: subgraph structural imbalance, where drastic variations in subgraph scale (user/item counts) and connectivity (item degree) misalign client representations, making it challenging to train a robust model that respects each client's unique structural characteristics. To address this, we propose a Low-pass Personalized Subgraph Federated recommender system (LPSFed). LPSFed leverages graph Fourier transforms and low-pass spectral filtering to extract low-frequency structural signals that remain stable across subgraphs of varying size and degree, allowing robust personalized parameter updates guided by similarity to a neutral structural anchor. Additionally, we leverage a localized popularity bias-aware margin that captures item-degree imbalance within each subgraph and incorporates it into a personalized bias correction term to mitigate recommendation bias. Supported by theoretical analysis and validated on five real-world datasets, LPSFed achieves superior recommendation accuracy and enhances model robustness.

---

## 论文详细总结（自动生成）

以下是对论文《Low-pass Personalized Subgraph Federated Recommendation》（低通个性化子图联邦推荐）的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：联邦推荐系统（FRS）在保护隐私的同时，面临严重的**子图结构不平衡（Subgraph Structural Imbalance）**挑战。
*   **研究背景**：不同客户端拥有的用户-项目交互子图在规模（节点数）和连通性（节点度）上存在巨大差异。传统的空间图神经网络（如 LightGCN）对局部拓扑高度敏感，导致不同客户端训练出的模型表示空间失准，难以进行有效的全局聚合。
*   **次要问题**：结构失衡还会加剧**局部流行度偏差**。在稠密子图中，热门项目会掩盖长尾项目；在稀疏子图中，模型则被迫依赖极少数高频交互，导致训练不稳定。

### 2. 核心方法论 (LPSFed)
LPSFed 框架由三个关键阶段组成，旨在通过频谱分析实现鲁棒的个性化对齐：
*   **低通频谱滤波（Stage 1）**：客户端使用低通图卷积网络（LGCN）。通过图傅里叶变换（GFT）提取低频结构信号，这些信号代表了图的核心连通模式，对噪声和规模变化更具鲁棒性。
*   **结构相似度计算（Stage 2）**：
    *   服务器生成一个中性的**结构锚点（Reference Graph）**（基于 GNMK 随机图模型）。
    *   客户端计算其本地低通卷积核与服务器锚点核之间的 **KL 散度**，得到结构相似度评分 $\rho_c$。
*   **个性化聚合与分发（Stage 3）**：服务器根据相似度评分对客户端参数进行加权聚合。相似度高的客户端在更新中占更高权重，从而缓解结构异构带来的负面影响。
*   **局部流行度偏差感知边际（Bias-aware Margin）**：引入一种自适应边际项，结合局部项目度分布和全局偏差上下文，通过偏差感知对比损失（BC-loss）来抑制热门项目的过度推荐，保护长尾项目。

### 3. 实验设计
*   **数据集**：使用了 5 个主流真实世界数据集：Amazon-Book, Gowalla, Movielens-1M, Yelp2018, Tmall-Buy。
*   **Benchmark 与对比方法**：
    *   **基础联邦学习**：FedAvg。
    *   **矩阵分解类**：FedMF, F2MF。
    *   **个性化联邦类**：PFedRec, FedRAP, FedPUB。
    *   **图神经网络类**：FedPerGNN, FedHGNN, FedSSP（最新的频谱联邦学习方法）。
*   **评估指标**：Recall@20 和 NDCG@20。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及具体的 GPU 型号和数量**。
*   **效率数据**：提供了详细的训练耗时和内存占用。例如，在 Amazon-Book 数据集上，LPSFed 每轮训练约 19 秒，总训练时间约 39 分钟，内存占用为 6.2GB。
*   **优化技术**：利用 **Lanczos 求解器**在预处理阶段计算特征向量，避免了训练过程中的高额计算开销，提高了在大规模稀疏图上的扩展性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个数据集上进行了全量对比实验。
    *   针对 15 个客户端进行了结构分组实验（大而稠密、中等平衡、小而稀疏）。
    *   进行了详尽的**消融实验**（验证 LGCN、偏差修正、个性化模块的必要性）。
    *   进行了**超参数敏感性分析**（如切断频率 $\Phi$、边际强度 $\gamma$ 等）。
*   **客观性**：实验通过多次运行取平均值，并对比了从传统 MF 到最新频谱 GNN 的各类方法，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **性能领先**：LPSFed 在所有数据集上均优于现有基准，NDCG 提升最高达 24.2%（Amazon-Book）。
*   **鲁棒性强**：在子图结构极度不平衡的情况下，LPSFed 能够保持稳定的推荐质量，尤其是在稀疏客户端上表现优异。
*   **偏差抑制**：通过 Jaccard 指数分析证明，该方法有效打破了流行度偏差的反馈循环，提升了长尾项目的曝光率。
*   **理论支撑**：论文证明了低通滤波能作为频谱正则化项，稳定跨图的相似度测量。

### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了算法，还提供了严谨的数学证明（如特征值分布收敛性）。
*   **解决结构异构**：创新性地引入“中性结构锚点”概念，为联邦学习中的非独立同分布（Non-IID）图数据对齐提供了新思路。
*   **计算效率高**：通过预计算特征向量，解决了 GNN 在联邦环境下通信和计算开销大的痛点。

### 8. 不足与局限
*   **预处理依赖**：虽然训练快，但预处理阶段的特征分解（Eigendecomposition）对于超大规模图（节点数千万级以上）仍可能存在内存瓶颈。
*   **超参数敏感**：低通滤波的切断频率 $\Phi$ 需要根据具体数据集手动调整，缺乏自动化的参数选择机制。
*   **隐私边界**：虽然不共享原始数据，但共享结构相似度评分和标量偏差信息在极端攻击模型下的隐私泄露风险（尽管极小）未做深入探讨。

（完）
