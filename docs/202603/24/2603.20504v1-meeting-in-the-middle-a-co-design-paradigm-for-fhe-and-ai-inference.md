---
title: "Meeting in the Middle: A Co-Design Paradigm for FHE and AI Inference"
title_zh: 中途相会：全同态加密与 AI 推理的协同设计范式
authors: "Bernardo Magri, Benjamin Marsh, Paul Gebheim"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20504v1"
tags: ["query:ppml"]
score: 8.0
evidence: 针对FHE和AI推理的协同设计，解决双侧隐私问题
tldr: 本文针对云端AI推理中的双向隐私保护难题，探讨了全同态加密（FHE）因高昂计算成本难以应用的问题。作者提出了一种“中途相遇”的协同设计范式，主张通过针对推理电路的静态结构定制FHE方案，同时约束AI模型架构以减少核心同态开销。该研究为实现高效的隐私保护推理指明了优化方向，是结合密码学与深度学习架构的重要尝试。
selection_source: fresh_fetch
motivation: 现代云端推理面临用户输入隐私与服务商模型权重保护的双重挑战，而现有的全同态加密方案计算成本过高。
method: 提出一种协同设计范式，通过为推理电路定制FHE编译器并约束AI架构来降低同态计算的主要成本。
result: 确立了“中途相遇”的研究议程，并针对FHE方案优化和AI推理架构约束提出了具体的优化目标。
conclusion: 协同设计是解决FHE在现代AI推理中性能瓶颈、实现实用化隐私保护计算的必经之路。
---

## 摘要
现代云端推理产生了一个双向隐私问题：用户向服务提供商泄露敏感输入，而提供商必须在可能存在泄露风险的执行环境中运行专有的模型权重。全同态加密 (FHE) 提供了密码学保障，但对于现代架构而言，其开销仍然高得令人望而却步。我们认为，取得进展需要进行协同设计，即针对推理电路的静态结构专门化 FHE 方案/编译器，同时约束推理架构以减少主要的同态成本驱动因素。我们概述了一个“中途相会”的议程，并确定了两个维度上的具体优化目标。

## Abstract
Modern cloud inference creates a two sided privacy problem where users reveal sensitive inputs to providers, while providers must execute proprietary model weights inside potentially leaky execution environments. Fully homomorphic encryption (FHE) offers cryptographic guarantees but remains prohibitively expensive for modern architectures. We argue that progress requires co-design where specializing FHE schemes/compilers for the static structure of inference circuits, while simultaneously constraining inference architectures to reduce dominant homomorphic cost drivers. We outline a meet in the middle agenda and concrete optimization targets on both axes.

---

## 论文详细总结（自动生成）

以下是对论文《Meeting in the Middle: A Co-Design Paradigm for FHE and AI Inference》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在云端部署大型神经网络模型时，存在严重的隐私冲突。用户希望保护输入数据的隐私，而模型提供商希望保护其专有的模型权重。
*   **核心问题**：目前的解决方案（如 TEE 硬件可信执行环境）存在侧信道攻击风险且需信任制造商；而全同态加密（FHE）虽然提供密码学保障，但计算开销极大，处理现代 Transformer 模型时效率极低。
*   **整体含义**：本文提出了一种“中途相会”（Meeting in the Middle）的协同设计范式。其核心观点是：不应将 FHE 视为通用的计算工具，而应利用 AI 推理任务高度可预测、结构固定的特性，同时对加密方案和模型架构进行相互定制，以实现实用的隐私保护推理。

### 2. 论文提出的方法论
作者从两个互补的维度提出了协同设计框架：

*   **维度一：为推理定制 FHE 方案**
    *   **利用静态特性**：由于推理没有数据依赖的分支，电路拓扑固定，可以进行离线电路级优化。
    *   **参数与编码优化**：根据量化感知训练（QAT）确定的动态范围，定制明文模数、缩放策略和编码方式。
    *   **SIMD 布局对齐**：设计特定的打包策略，使密文槽位与模型的矩阵乘法结构对齐，从而大幅减少昂贵的密码学旋转（Rotation）和密钥交换操作。
    *   **静态自举（Bootstrapping）调度**：预先计算并测量噪声积累点，制定全局最优的静态自举计划，最小化自举次数。

*   **维度二：为 FHE 约束 AI 推理架构**
    *   **FHE 感知型神经架构搜索（NAS）**：将乘法深度和旋转次数作为 NAS 的主要优化目标，倾向于选择“浅而宽”的层。
    *   **多项式替代**：用低次多项式替换 ReLU 或 GeLU 等非线性激活函数。
    *   **近似计算**：针对 Softmax 等复杂函数开发实验性近似方案。
    *   **结构化稀疏**：强制执行与 SIMD 打包模式一致的块结构稀疏模式，以便在同态评估中直接跳过操作。

### 3. 实验设计
*   **性质**：本文主要是一篇**立场论文（Position Paper）**，侧重于提出研究议程，而非大规模实验验证。
*   **微基准测试（Microbenchmark）**：作者针对 **Softmax 函数** 衍生出了一种在有限输入范围内的实验性近似方案。
*   **对比对象**：将该近似方案的同态评估速度与在相同 CPU 上运行的、未经优化的标量明文 Rust 实现进行了对比。
*   **评估指标**：观察到的误差率（低于 0.5%）和执行时间。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **硬件环境**：微基准测试提到了在“相同 CPU”上运行，并明确指出明文对比组未使用 SIMD/AVX 指令集，也未使用 GPU 加速。

### 5. 实验数量与充分性
*   **实验数量**：极少。仅展示了一个关于 Softmax 近似函数的微基准测试作为概念验证（Proof of Concept）。
*   **充分性评价**：作为立场论文，其目的在于论证方向的可行性，而非提供详尽的性能数据。因此，实验并不充分，无法代表完整模型在 FHE 下的实际表现。
*   **客观性**：对比实验存在一定的局限性，因为它对比的是未优化的标量明文代码，这可能放大了 FHE 方案的性能优势。

### 6. 论文的主要结论与发现
*   **协同设计的必要性**：单纯依靠优化 FHE 算法或单纯优化 AI 模型都无法解决性能瓶颈，必须两者深度结合。
*   **初步可行性**：通过对 Softmax 的近似处理，证明了在特定算子级别，同态计算可以达到甚至超过某些明文实现的效率（在特定约束下）。
*   **研究路径**：确立了通过量化感知训练（QAT）和 FHE 感知 NAS 来构建“FHE 友好型”模型的研究路径。

### 7. 优点
*   **视角独特**：打破了将 FHE 视为“黑盒”底层技术的传统思维，提出了密码学与神经网络架构的深度融合。
*   **针对性强**：精准识别了 FHE 在 AI 推理中的核心痛点（如旋转开销、非线性函数处理、噪声管理）。
*   **前瞻性**：为未来隐私保护计算（PPML）的标准化和硬件加速提供了理论框架。

### 8. 不足与局限
*   **实验覆盖不足**：缺乏端到端的大型模型（如完整的 Transformer 或 ResNet）实验数据。
*   **精度损失风险**：大量使用多项式近似和量化可能会导致模型在复杂任务上的准确率大幅下降，文中对此讨论较少。
*   **对比基准偏差**：微基准测试中与“未优化标量 Rust 代码”对比的参考价值有限，实际应用中明文推理通常会使用高度优化的 SIMD/GPU 库。
*   **工程复杂度**：协同设计要求开发者同时精通密码学和深度学习，实施门槛极高。

（完）
