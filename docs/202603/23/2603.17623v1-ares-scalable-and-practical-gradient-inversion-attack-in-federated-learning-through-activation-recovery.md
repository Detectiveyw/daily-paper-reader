---
title: "ARES: Scalable and Practical Gradient Inversion Attack in Federated Learning through Activation Recovery"
title_zh: ARES：通过激活恢复实现联邦学习中可扩展且实用的梯度反演攻击
authors: "Zirui Gong, Leo Yu Zhang, Yanjun Zhang, Viet Vo, Tianqing Zhu, Shirui Pan, Cong Wang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17623v1"
tags: ["query:ppml"]
score: 8.0
evidence: 联邦学习中的隐私保护机器学习与梯度逆向攻击
tldr: 联邦学习通过共享模型更新保护隐私，但面临梯度反向攻击（GIA）的威胁。本文提出ARES攻击，这是一种无需修改模型架构即可从大批量训练数据中恢复原始样本的主动攻击方法。ARES将激活恢复建模为稀疏恢复任务，利用广义Lasso求解，并结合印记法处理多样本。实验证明其在大批量和现实场景下具有极高的重构保真度，揭示了中间激活值的严重隐私风险。
selection_source: fresh_fetch
motivation: 现有的主动梯度反向攻击通常需要修改模型架构，限制了其在实际联邦学习场景中的应用。
method: 将激活恢复建模为带噪声的稀疏恢复问题，利用广义Lasso求解，并结合印记法实现多样本的解耦与重构。
result: 在CNN和MLP架构及多种数据集上的实验表明，ARES在大批量设置下的重构效果显著优于现有方法。
conclusion: ARES证明了中间激活值在联邦学习中存在被低估的隐私风险，强调了开发更强防御机制的紧迫性。
---

## 摘要
联邦学习（FL）通过共享模型更新而非原始数据来实现协作式模型训练，旨在保护用户隐私。然而，最近的研究表明，这些共享的更新可能会通过梯度反演攻击（GIA）无意中泄露敏感的训练数据。其中，主动式 GIA 特别强大，即使在大批量（batch size）情况下也能实现单个样本的高保真重构。尽管如此，现有方法通常需要修改模型架构，这限制了它们的实际适用性。在这项工作中，我们通过引入基于稀疏反演的激活恢复（ARES）攻击来填补这一空白。ARES 是一种主动式 GIA，旨在从大训练批量中重构训练样本，且无需修改架构。具体而言，我们将恢复问题建模为带噪声的稀疏恢复任务，并使用广义最小绝对收缩和选择算子（Lasso）进行求解。为了将攻击扩展到多样本恢复，ARES 结合了烙印（imprint）方法来解耦激活，从而实现可扩展的逐样本重构。我们进一步确定了预期恢复率，并推导了重构误差的上界，为 ARES 攻击提供了理论保证。在 CNN 和 MLP 上的广泛实验表明，ARES 在不同数据集上均实现了高保真重构，在大批量和真实的联邦学习设置下显著优于先前的 GIA。我们的结果强调，中间激活在联邦学习中构成了严重且被低估的隐私风险，凸显了对更强防御手段的迫切需求。

## Abstract
Federated Learning (FL) enables collaborative model training by sharing model updates instead of raw data, aiming to protect user privacy. However, recent studies reveal that these shared updates can inadvertently leak sensitive training data through gradient inversion attacks (GIAs). Among them, active GIAs are particularly powerful, enabling high-fidelity reconstruction of individual samples even under large batch sizes. Nevertheless, existing approaches often require architectural modifications, which limit their practical applicability. In this work, we bridge this gap by introducing the Activation REcovery via Sparse inversion (ARES) attack, an active GIA designed to reconstruct training samples from large training batches without requiring architectural modifications. Specifically, we formulate the recovery problem as a noisy sparse recovery task and solve it using the generalized Least Absolute Shrinkage and Selection Operator (Lasso). To extend the attack to multi-sample recovery, ARES incorporates the imprint method to disentangle activations, enabling scalable per-sample reconstruction. We further establish the expected recovery rate and derive an upper bound on the reconstruction error, providing theoretical guarantees for the ARES attack. Extensive experiments on CNNs and MLPs demonstrate that ARES achieves high-fidelity reconstruction across diverse datasets, significantly outperforming prior GIAs under large batch sizes and realistic FL settings. Our results highlight that intermediate activations pose a serious and underestimated privacy risk in FL, underscoring the urgent need for stronger defenses.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ARES**（Activation REcovery via Sparse inversion）的新型梯度反演攻击方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：联邦学习（FL）通过只共享梯度而非原始数据来保护隐私。然而，梯度反演攻击（GIA）证明了从梯度中恢复原始数据是可能的。
*   **核心痛点**：
    *   **被动攻击局限性**：在训练批量（Batch Size）较大（如 >64）时，重构效果极差。
    *   **现有主动攻击局限性**：虽然主动攻击（如 RtF, LOKI）能处理大批量，但通常需要**修改模型架构**（例如在模型前端插入自定义的全连接层），这在实际应用中容易被检测或拒绝。
*   **研究动机**：开发一种既能处理大批量数据，又**无需修改模型架构**（保持架构完整性）的实用主动攻击方法。

### 2. 方法论：核心思想与技术细节
ARES 将攻击分为**准备阶段**和**推理阶段**，核心思想是将“激活值到样本”的重构建模为一个**带噪声的稀疏恢复问题**。

*   **准备阶段（恶意参数初始化）**：
    *   **卷积层处理**：利用“直通（Direct-pass）”初始化，使输入信号能尽可能无损地传递到深层的全连接（FC）层。
    *   **全连接层处理（印记法 Imprint）**：精心设计 FC 层的权重和偏置，利用偏置作为切割点，将不同的输入样本分配到不同的神经元“桶”中，实现样本解耦。
*   **推理阶段（两步恢复法）**：
    1.  **激活恢复（Activation Recovery）**：利用线性层泄露原理，从 FC 层的梯度中解析出输入到该层的中间激活值 $h$。
    2.  **稀疏反演（Sparse Inversion）**：
        *   **非线性松弛**：将 ReLU 等非线性激活函数近似为带噪声的缩放线性映射。
        *   **稀疏性利用**：利用数据（图像、音频、文本）在特定域（如 DCT 变换域）的稀疏性，将问题转化为 $h \approx \mu A \alpha + \mu b$，其中 $\alpha$ 是稀疏系数。
        *   **Lasso 求解**：使用广义 Lasso 算法（$\ell_1$ 范数优化）寻找最符合观测激活值的稀疏解，从而重构原始样本。
*   **理论保证**：利用受限等距性质（RIP）推导了重构误差的上界，证明了攻击的稳定性。

### 3. 实验设计
*   **数据集**：涵盖了图像（MNIST, CIFAR-10, ImageNet, HAM10000, Lung-Colon Cancer）、文本（Wikitext）和音频（AudioMNIST）多种模态。
*   **模型架构**：4 层 CNN 和 4 层 MLP。
*   **对比基准（Baselines）**：
    *   **被动攻击**：iDLG, InvertingGrad, GradInversion, FedLeak。
    *   **主动攻击**：Fishing, RtF (Robbing the Fed), Trap Weight, LOKI, Scale-MIA。
*   **评估指标**：PSNR（峰值信噪比）、MSE、LPIPS（感知相似度）、恢复率（Recovery Rate）以及文本专用的 BLEU/ROUGE。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：作者提供了 GitHub 开源代码，并提到使用了 CVXPY 框架进行凸优化求解。通常此类基于优化的重构攻击在单个现代 GPU（如 RTX 3090/4090）上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。
*   **维度覆盖**：
    *   测试了不同批量大小（从 32 到 256）。
    *   跨模态验证（图像、文本、音频）。
    *   **防御鲁棒性测试**：针对差分隐私（DP）、梯度量化、梯度稀疏化、数据增强（ATS）和安全聚合（Secure Aggregation）均做了对比实验。
    *   **复杂场景**：涵盖了非独立同分布（Non-IID）数据、FedAvg 算法以及异步联邦学习场景。
*   **公平性**：对比了近年来最先进的（SOTA）主动和被动攻击，实验设置符合当前学术界主流标准。

### 6. 主要结论与发现
*   **重构精度极高**：在相同批量下，ARES 的 PSNR 显著高于现有方法（最高提升达 7 倍），能实现近乎完美的像素级重构。
*   **架构完整性**：证明了无需修改模型结构，仅通过操纵权重即可实现深度泄露。
*   **防御抗性**：现有的轻量级防御（如低强度的 DP 或量化）难以阻止 ARES，只有在极大牺牲模型效用的情况下，防御才有效。
*   **隐私风险低估**：揭示了神经网络中间层的激活值包含了极其丰富的原始数据信息，这一风险在以往的 FL 安全研究中被低估了。

### 7. 优点（亮点）
*   **实用性强**：不改变模型结构，使其比以往的主动攻击更具隐蔽性。
*   **数学严谨**：引入压缩感知理论和 Lasso 优化，并给出了误差上界的理论证明。
*   **通用性**：不仅限于图像，还成功扩展到了文本和音频领域，证明了该攻击逻辑的普适性。

### 8. 不足与局限
*   **恶意服务器假设**：攻击前提是服务器必须是恶意的且能控制全局模型参数，这在某些强安全假设的 FL 场景下可能受限。
*   **特定层依赖**：攻击高度依赖于模型中存在“线性+激活”结构的层（如 FC 层），对于某些完全不含此类结构的特殊架构可能需要调整。
*   **计算开销**：虽然比迭代梯度匹配快，但对于超大规模参数模型，求解 Lasso 问题的计算成本可能会增加。

（完）
