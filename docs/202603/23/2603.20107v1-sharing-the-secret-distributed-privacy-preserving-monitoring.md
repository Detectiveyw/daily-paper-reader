---
title: "Sharing The Secret: Distributed Privacy-Preserving Monitoring"
title_zh: 共享秘密：分布式隐私保护监控
authors: "Mahyar Karimi, K. S. Thejaswini, Roderick Bloem, Thomas A. Henzinger"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20107v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 使用高效秘密共享方案的分布式监控
tldr: 针对传统运行时验证中隐私保护监控计算开销大的问题，本文提出了一种分布式隐私保护监控架构。通过将监控器分布在多个参与方之间并利用高效的秘密共享方案，该方法避免了沉重的加密原语。研究引入了支持持续监控和内部状态演化的协议，在保证隐私的同时显著提升了实时应用的扩展性。
selection_source: fresh_fetch
motivation: 传统的单体监控器在执行隐私保护任务时因依赖重型加密原语而导致计算开销过大，难以满足实时应用需求。
method: 提出一种基于秘密共享的分布式监控架构，支持在多个参与方之间进行持续的状态演化和重复评估。
result: 通过 MP-SPDZ 框架实现的实验表明，该协议在保持强隐私保证的前提下，其扩展性显著优于现有的加密监控方案。
conclusion: 分布式秘密共享架构是实现高效、可扩展且具有隐私保护能力的实时系统监控的有效途径。
---

## 摘要
在传统的运行时验证中，系统通常由一个单体监控器进行观测。在此类设置中实施隐私保护的计算开开销巨大，因为它需要繁重的密码学原语。因此，隐私保护监控对于实时应用而言仍然是不切实际的。在这项工作中，我们通过将监控器分布在多个参与方（其中至少一个是诚实的）之间，解决了这一可扩展性挑战。这种架构允许使用高效的秘密共享方案，而非计算密集型的密码学方法，在保持强隐私保证的同时大幅降低了开销。虽然现有的秘密共享方法通常局限于不维护内部状态的一次性执行，但我们引入了一种专为持续监控量身定制的协议，该协议支持对不断演变的内部状态（对系统和监控实体保密）进行重复评估。我们使用 MP-SPDZ 框架实现了我们的方法。实验表明，在这些架构假设下，我们的协议比现有的替代方案具有显著更高的可扩展性。

## Abstract
In traditional runtime verification, a system is typically observed by a monolithic monitor. Enforcing privacy in such settings is computationally expensive, as it necessitates heavy cryptographic primitives. Therefore, privacy-preserving monitoring remains impractical for real-time applications. In this work, we address this scalability challenge by distributing the monitor across multiple parties -- at least one of which is honest. This architecture enables the use of efficient secret-sharing schemes instead of computationally intensive cryptography, dramatically reducing over-head while maintaining strong privacy guarantees. While existing secret-sharing approaches are typically limited to one-shot executions which do not maintain an internal state, we introduce a protocol tailored for continuous monitoring that supports repeated evaluations over an evolving internal state (kept secret from the system and the monitoring entities). We implement our approach using the MP-SPDZ framework. Our experiments demonstrate that, under these architectural assumptions, our protocol is significantly more scalable than existing alternatives.

---

## 论文详细总结（自动生成）

以下是对论文《Sharing The Secret: Distributed Privacy-Preserving Monitoring》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在现代软件验证（如医疗、银行、合规性审计）中，存在双向隐私需求：系统（System）不希望泄露敏感运行数据，监控器（Monitor）不希望泄露其验证逻辑或规范（Specification）。
*   **核心挑战**：现有的隐私保护运行时验证（RV）方案主要依赖全同态加密（FHE）或混淆电路（GC）。这些技术计算开销极大（单步验证往往需数分钟），导致隐私保护监控在实时应用中几乎不可行。
*   **整体含义**：本文旨在通过改变监控架构，将单体监控器转变为分布式多方架构，利用高效的**秘密共享（Secret Sharing）**技术替代重型加密原语，从而在保证隐私的前提下实现量级上的性能提升。

### 2. 方法论
*   **核心思想**：将监控器职能分散到多个参与方（Monitor Parties），假设其中至少一个是诚实的（诚实大多数假设）。通过多方安全计算（MPC）协议，在不还原原始数据的情况下对加密份额进行计算。
*   **关键技术细节**：
    *   **抽象共享系统**：定义了一套通用的秘密共享接口，支持算术共享（适用于加法/乘法）和布尔共享（适用于逻辑运算）。
    *   **份额转换（Share Conversion）**：这是本文的高效核心。引入了“位分解（Bit Decomposition）”等协议，允许在算术份额（高效求和）和布尔份额（高效比较）之间无缝切换，而无需重建秘密。
    *   **持续状态监控协议**：不同于传统的一次性MPC任务，该协议支持维护一个持久且加密的内部状态 $[[\mu_t]]$。在每个时间步 $t$：
        1. 系统将输出 $\sigma_t$ 拆分为份额分发给各监控方。
        2. 监控方共同计算下一状态 $[[\mu_{t+1}]]$ 和违规标志 $[[\phi]]$。
        3. 仅重构（公开）违规标志位，其余信息保持加密。
*   **算法流程**：采用基于寄存器的抽象机模型，将高级规范编译为包含 ADD、MUL、LT（小于比较）、EQ（等于）等指令的序列，其中 LT 和 EQ 内部触发份额转换协议。

### 3. 实验设计
*   **实验场景**：
    1.  **访问控制系统 (ACS)**：监控大楼内两类员工的人数差，扩展至 10 到 1000 个门。
    2.  **分布式锁管理**：验证锁的申请与释放序列，扩展至 100 到 1000 个锁。
    3.  **血糖监控**：基于信号时序逻辑（STL）的生理数据监控。
    4.  **总统座驾地理围栏 (Geofencing)**：在高维空间（最高 1024 维）内监控物体是否超出动态半径，涉及复杂的非线性算术。
*   **Benchmark 与对比方法**：
    *   对比了 Henzinger 等人（2025）基于**混淆电路 (GC)** 的方案。
    *   对比了 Banno 等人（2022）基于**全同态加密 (FHE)** 的方案。
*   **实现框架**：使用 **MP-SPDZ** 框架，采用 Shamir 秘密共享协议。

### 4. 资源与算力
*   **算力说明**：文中未详细列出具体的 CPU/GPU 型号及具体训练时长。
*   **环境配置**：实验在 3 个监控参与方的设定下运行，使用 128 位域（Field），通信采用 TLS 1.2 加密。由于是基于秘密共享的 MPC，性能瓶颈主要在于网络通信带宽和延迟，而非纯粹的算力。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 个不同的应用场景进行了多组扩展性实验，涵盖了从简单逻辑到复杂高维算术的多种负载。
*   **充分性评价**：实验设计较为充分。通过改变系统规模（如门和锁的数量、空间维度）展示了协议的线性扩展性。对比实验直接针对当前最先进的（SOTA）加密监控方案，客观地展示了性能差距。

### 6. 主要结论与发现
*   **性能飞跃**：相比于基于 GC 或 FHE 的方案，本文方法实现了 **2 到 3 个数量级** 的加速。例如，在 ACS 场景下比 GC 方案快 100-250 倍。
*   **实时性**：大多数场景的单步监控延迟控制在 **0.1 秒以内**，真正达到了实时监控的要求。
*   **安全性**：在半诚实模型下提供了信息论级别的隐私保证（若使用 Shamir 共享），且支持无限轮次的重复执行而不泄露状态。

### 7. 优点
*   **架构创新**：通过引入分布式信任架构，巧妙地绕过了单体加密监控的性能瓶颈。
*   **混合协议优化**：灵活切换算术和布尔表示，解决了复杂时序逻辑中非线性运算（如比较、距离计算）的效率问题。
*   **状态持久化**：成功将 MPC 应用于长程、有状态的运行时验证任务。

### 8. 不足与局限
*   **架构假设限制**：依赖“诚实大多数”假设。如果监控方之间发生合谋（Collusion），隐私保护将失效。
*   **大规模系统状态处理**：如果系统拥有海量内部状态（如巨型数据库），共享整个状态仍不现实，文中提到未来需结合私有信息检索（PIR）技术。
*   **工具集成度**：目前仍是基于 MP-SPDZ 的原型实现，尚未与主流的运行时验证工具（如 MonPoly、DejaVu）深度集成。
*   **主动攻击防御**：主要针对半诚实对手，若要防御恶意（Active）对手，需引入 MAC 认证等技术，这会带来额外的性能开销。

（完）
