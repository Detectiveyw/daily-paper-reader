---
title: "SecDTD: Dynamic Token Drop for Secure Transformers Inference"
title_zh: SecDTD：面向安全 Transformer 推理的动态 Token 丢弃
authors: "Yifei Cai, Zhuoran Li, Yizhou Feng, Qiao Zhang, Hongyi Wu, Danella Zhao, Chunsheng Xin"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13670v1"
tags: ["query:fss-llm"]
score: 10.0
evidence: 密文场景下安全 Transformer 推理的动态令牌丢弃
tldr: 针对 Transformer 安全推理中计算和通信开销巨大的问题，本文提出 SecDTD 动态 Token 丢弃方案。该方案通过将丢弃阶段提前，并引入 Softmax 无关的评分方法 MCN 以及高效的遗忘中值选择协议 OMSel，在保证隐私和精度的前提下，显著降低了安全计算成本，在 GLUE 任务上实现了 4.47 倍的端到端推理加速。
selection_source: fresh_fetch
motivation: 现有的 Token 丢弃方法在密文环境下由于计算成本分布差异而效果不佳，且安全推理框架普遍面临极高的性能瓶颈。
method: 提出了 SecDTD 方案，包含一种无需 Softmax 的评分方法 MCN 以支持早期丢弃，以及一个比排序更快的遗忘中值选择协议 OMSel。
result: 在 8 个 GLUE 数据集上的实验表明，SecDTD 在不损失精度的情况下实现了 4.47 倍的端到端推理加速，且 OMSel 协议比现有方法快 16.9 倍。
conclusion: SecDTD 证明了通过针对性优化 Token 丢弃策略和底层安全协议，可以大幅提升 Transformer 安全推理的实用性。
---

## 摘要
基于 Transformer 的 AI 的快速普及是由 ChatGPT 等易于获取的模型推动的，这些模型为开发者和企业提供基于 API 的服务。然而，随着这些在线推理服务越来越多地处理敏感输入，隐私问题已成为一个重大挑战。为了解决这一问题，研究者提出了安全推理框架，但其高昂的计算和通信开销往往限制了实际部署。在明文设置下，Token 丢弃是降低推理成本的有效技术；然而，我们的分析表明，由于安全计算中截然不同的成本分布，将此类方法直接应用于密文场景并非最优。我们提出了 SecDTD，一种专为安全 Transformer 推理定制的动态 Token 丢弃方案。SecDTD 通过将丢弃操作提前到推理的早期阶段来改进 Token 丢弃，从而有效降低了 Softmax 等关键组件的成本。为支持这一点，我们引入了两种核心技术。以最大值为中心的归一化（Max-Centric Normalization, MCN）：一种新颖的、独立于 Softmax 的评分方法，能够以极小的开销实现早期 Token 丢弃并改进归一化，在不损失准确性的情况下支持更激进的丢弃。OMSel：一种更快的、不经意的中位数选择协议，可安全地识别重要性得分的中位数以支持 Token 丢弃。与现有的基于排序的方法相比，OMSel 在保持安全性、不经意性和随机性的同时，实现了 16.9 倍的加速。我们在 BOLT 和 BumbleBee 框架下，通过在各种网络设置下的 8 个 GLUE 数据集上进行的 48 组实验对 SecDTD 进行了评估。SecDTD 在不降低准确性的情况下，实现了 4.47 倍的端到端推理加速。

## Abstract
The rapid adoption of Transformer-based AI has been driven by accessible models such as ChatGPT, which provide API-based services for developers and businesses. However, as these online inference services increasingly handle sensitive inputs, privacy concerns have emerged as a significant challenge. To address this, secure inference frameworks have been proposed, but their high computational and communication overhead often limit practical deployment. In plaintext settings, token drop is an effective technique for reducing inference cost; however, our analysis reveals that directly applying such methods to ciphertext scenarios is suboptimal due to distinct cost distributions in secure computation. We propose SecDTD, a dynamic token drop scheme tailored for secure Transformer inference. SecDTD advances token drop by shifting the dropping to earlier inference stages, effectively reducing the cost of key components such as Softmax. To support this, we introduce two core techniques. Max-Centric Normalization (MCN): A novel, Softmax-independent scoring method that enables early token drop with minimal overhead and improved normalization, supporting more aggressive dropping without accuracy loss. OMSel: A faster, oblivious median selection protocol that securely identifies the median of importance scores to support token drop. Compared to existing sorting-based methods, OMSel achieves a 16.9$\times$ speedup while maintaining security, obliviousness and randomness. We evaluate SecDTD through 48 experiments across eight GLUE datasets under various network settings using the BOLT and BumbleBee frameworks. SecDTD achieves 4.47 times end-to-end inference acceleration without degradation in accuracy.

---

## 论文详细总结（自动生成）

## 论文总结：SecDTD - 面向安全 Transformer 推理的动态 Token 丢弃

### 1. 核心问题与研究动机
*   **背景**：随着 Transformer 模型（如 BERT、GPT）在医疗、金融等敏感领域的广泛应用，如何在保护用户隐私（输入数据）和模型参数的前提下进行高效推理成为关键。
*   **核心痛点**：现有的安全推理框架（基于 MPC 和 HE）面临巨大的计算和通信开销。虽然明文环境下“Token 丢弃”（Token Drop）能有效减速，但直接迁移至密文环境效果不佳。
*   **研究动机**：
    *   **成本分布差异**：明文推理中 Softmax 开销可忽略，而密文推理中 Softmax 等非线性操作占比极高。
    *   **丢弃阶段滞后**：现有安全丢弃方法依赖 Softmax 输出，导致 Softmax 本身无法享受加速。
    *   **决策开销大**：现有的安全中值选择（用于决定丢弃哪些 Token）依赖昂贵的排序算法（如 Bitonic Sort），效率低下。

### 2. 方法论
SecDTD 提出了一种专为密文环境设计的动态 Token 丢弃方案，核心思想是将丢弃操作提前至 Softmax 之前。
*   **MCN (Max-Centric Normalization) 评分方法**：
    *   **核心思想**：设计一种独立于 Softmax 的评分机制。
    *   **技术细节**：利用 MPC 中已计算的最大值（Max），通过公式 $MCN(x)_{ij} = (x_{ij} - \max_i) / \max_i^n$ 进行归一化。
    *   **优势**：避免了昂贵的指数运算，且比简单的中间值评分更具鲁棒性，支持在不损失精度的情况下进行更激进的丢弃。
*   **OMSel (Oblivious Median Selection) 协议**：
    *   **核心思想**：一种快速且“不经意”（Oblivious）的中值选择算法，用于执行“丢弃得分较低的一半 Token”策略。
    *   **技术细节**：基于 QuickSelect 思想，但采用逻辑掩码（Pseudo-partitioning）而非物理移动数据，确保访问模式不泄露信息。引入随机 Pivot（初始轮）和均值 Pivot（后续轮）来加速收敛。
*   **Pre-Softmax Gain 策略**：通过 MCN 和 OMSel，在计算 $V$ 矩阵投影和 Softmax 之前就完成丢弃，使后续所有线性与非线性层均获益。

### 3. 实验设计
*   **数据集**：GLUE 基准测试中的 8 个任务：CoLA, SST-2, MRPC, STS-B, QNLI, QQP, MNLI, RTE。
*   **Benchmark 框架**：BOLT 和 BumbleBee（代表了当前最先进的安全 Transformer 推理框架）。
*   **对比方法**：
    *   **Baseline**：无 Token 丢弃的原始安全推理。
    *   **W.E. (Word Elimination)**：现有的基于排序的安全丢弃方法。
    *   **CipherPrune**：基于静态阈值的安全剪枝方法。
*   **网络场景**：模拟了 LAN（高带宽低延迟）、WAN（中等环境）和 Mobile（低带宽网络）三种真实场景。

### 4. 资源与算力
*   **硬件环境**：使用 AMD 3995 64核 CPU 进行测试。
*   **算力说明**：论文未提及 GPU 使用，这符合 MPC/HE 安全推理通常在 CPU 上执行加密原语的特性。
*   **线程配置**：在 BOLT 框架下使用 2 线程以确保测量准确性；在 BumbleBee 下利用了其多线程优化。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **48 组** 端到端实验，涵盖了 8 个数据集在 3 种网络环境下的表现。
*   **充分性分析**：
    *   **多维度验证**：涵盖了不同输入长度（64 到 256）和不同任务类型（分类、回归、问答）。
    *   **消融与对比**：对比了 MCN 与 W.E. 在极端丢弃比例下的精度稳定性，并对比了 OMSel 与 Bitonic Sort 的计算复杂度。
    *   **框架兼容性**：在两个不同的底层加密框架上均验证了有效性，实验设计全面、客观。

### 6. 主要结论与发现
*   **端到端加速**：SecDTD 在不损失精度的情况下，实现了高达 **4.47 倍** 的推理加速。
*   **协议效率提升**：提出的 OMSel 协议比传统的 Bitonic Sort 快 **16.9 倍**。
*   **Pre-Softmax 优势**：相比于现有方法，SecDTD 在长序列和差网络环境下优势更明显，最高可比现有丢弃方法再提速 206%。
*   **安全性增强**：通过固定丢弃比例和引入随机性，有效防止了通过丢弃模式（指纹）推测输入信息的侧信道攻击。

### 7. 优点
*   **洞察深刻**：准确识别了密文环境下成本分布的特殊性，通过“阶段前移”打破了性能瓶颈。
*   **算法高效**：MCN 评分极低开销且稳定，OMSel 解决了 MPC 中“选择问题”比“排序问题”更适合推理的痛点。
*   **易于集成**：作为通用插件，可直接应用于现有的安全推理框架，且支持免微调部署。

### 8. 不足与局限
*   **模型局限**：实验主要集中在 BERT（Encoder 架构），虽然文中讨论了 GPT 等 Decoder 架构的适用性，但未给出具体的实验数据。
*   **策略灵活性**：采用“减半丢弃”虽然符合 HE 槽位（Slot）的幂次特性，但在需要极精细控制丢弃比例的场景下可能略显僵化。
*   **微调依赖性**：虽然支持免微调，但对于某些极难任务，若要保持高精度下的超高加速比，仍需 5 个 Epoch 左右的轻量级微调。

（完）
