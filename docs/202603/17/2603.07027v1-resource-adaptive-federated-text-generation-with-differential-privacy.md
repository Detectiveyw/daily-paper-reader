---
title: Resource-Adaptive Federated Text Generation with Differential Privacy
title_zh: 具有差分隐私的资源自适应联邦文本生成
authors: "Jiayi Wang, John Gounley, Heidi Hanson"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07027v1"
tags: ["query:ppml"]
score: 8.0
evidence: 具有差分隐私的资源自适应联邦学习
tldr: 针对跨机构联邦学习中计算资源异构和隐私保护的挑战，本文提出一种资源自适应的差分隐私（DP）文本生成框架。该框架允许强算力客户端进行DP微调，而弱算力客户端通过轻量级DP投票机制贡献数据特征。通过控制代码确保合成数据与全局分布一致，在保护隐私的同时解决了数据倾斜问题，显著提升了合成数据的质量和下游任务的鲁棒性。
selection_source: carryover_cache
motivation: 传统的联邦微调因客户端计算资源不均常排除弱算力节点，导致数据偏斜并放大了差分隐私噪声对模型性能的负面影响。
method: 提出一种双阶段框架，结合强客户端的DP联邦微调与弱客户端的轻量级DP投票机制，并利用控制代码引导合成文本的语义对齐。
result: 实验证明该框架在差分隐私和资源异构环境下，显著改善了合成数据的分布一致性，并增强了下游任务的稳健性。
conclusion: 该研究通过灵活的资源自适应参与机制，有效整合了异构客户端的贡献，为隐私保护下的联邦文本生成提供了高效方案。
---

## 摘要
在跨机构联邦学习（FL）中，由于隐私法规的限制，敏感文本数据集仍局限于本地组织，这使得针对每个下游任务的重复训练既耗费通信资源又对隐私保护有极高要求。一个极具前景的替代方案是生成近似全局分布且可跨任务重用的差分隐私（DP）合成数据集。然而，预训练大语言模型（LLMs）在领域偏移下往往表现不佳，且联邦微调受到计算异构性的阻碍：只有资源丰富的客户端才能更新模型，而性能较弱的客户端则被排除在外，这放大了数据偏斜和差分隐私噪声的负面影响。我们提出了一种能够适应客户端能力的灵活参与框架。强客户端执行差分隐私联邦微调，而弱客户端则通过一种轻量级的差分隐私投票机制来优化合成文本。为了确保合成数据能够反映全局数据集，我们应用了代表每个客户端数据比例的控制代码（如标签、主题、元数据），并将投票限制在语义一致的子集中。这种两阶段方法对弱客户端仅需一轮通信，并整合了所有参与者的贡献。实验表明，我们的框架在差分隐私和异构性条件下提高了分布对齐度和下游鲁棒性。

## Abstract
In cross-silo federated learning (FL), sensitive text datasets remain confined to local organizations due to privacy regulations, making repeated training for each downstream task both communication-intensive and privacy-demanding. A promising alternative is to generate differentially private (DP) synthetic datasets that approximate the global distribution and can be reused across tasks. However, pretrained large language models (LLMs) often fail under domain shift, and federated finetuning is hindered by computational heterogeneity: only resource-rich clients can update the model, while weaker clients are excluded, amplifying data skew and the adverse effects of DP noise. We propose a flexible participation framework that adapts to client capacities. Strong clients perform DP federated finetuning, while weak clients contribute through a lightweight DP voting mechanism that refines synthetic text. To ensure the synthetic data mirrors the global dataset, we apply control codes (e.g., labels, topics, metadata) that represent each client's data proportions and constrain voting to semantically coherent subsets. This two-phase approach requires only a single round of communication for weak clients and integrates contributions from all participants. Experiments show that our framework improves distribution alignment and downstream robustness under DP and heterogeneity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Resource-Adaptive Federated Text Generation with Differential Privacy》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在跨机构（Cross-silo）联邦学习中，如何利用分布在不同机构、且计算资源极度不均（异构）的敏感文本数据，生成高质量且具备差分隐私（DP）保证的合成数据集。
*   **研究动机**：
    *   **隐私与通信压力**：直接在联邦环境下为每个下游任务重复训练模型开销巨大，生成可重用的合成数据是更好的选择。
    *   **计算资源异构**：大语言模型（LLM）微调需要高算力，导致只有“强客户端”能参与，而“弱客户端”被排除，这加剧了数据偏斜（Data Skew）。
    *   **DP噪声负面影响**：在参与度低且数据异构的情况下，DP-SGD 引入的噪声会严重破坏模型收敛和生成质量。

### 2. 方法论
论文提出了一种**两阶段资源自适应框架**，结合了模型微调与非训练式的精炼机制：
*   **核心思想**：让强客户端负责“学习通用模式”（微调），让弱客户端负责“校准分布”（投票），通过控制代码（Control Codes）进行语义对齐。
*   **关键技术细节**：
    1.  **控制代码（Control Codes）**：利用标签、主题或元数据作为控制代码，将文本划分为语义子集，用于指导生成比例并约束投票范围。
    2.  **阶段一：DP 联邦微调**：仅由强客户端（$C_s$）参与，使用 DP-SGD 算法对预训练模型（如 GPT-2）进行微调，捕获领域特定知识。
    3.  **阶段二：DP 投票精炼（Refinement）**：
        *   **分析与生成**：所有客户端上传受 DP 保护的控制代码分布（Profile），服务器据此生成初始合成文本。
        *   **轻量级投票**：弱客户端（$C_r$）无需反向传播，仅需通过句子嵌入（Sentence Transformer）计算本地数据与合成数据的相似度，为候选样本投票。
        *   **DP 聚合与重采样**：服务器聚合带噪声的投票，对合成样本进行重采样，使最终数据集更贴近全局分布。

### 3. 实验设计
*   **数据集**：
    *   **Yelp Reviews**：用于商业分类和评分预测任务。
    *   **PubMed Abstracts**：用于医学领域适配和命名实体识别（NER）任务。
*   **场景设置**：涵盖 IID（独立同分布）和 Non-IID（非独立同分布，模拟数据偏斜）设置。
*   **Benchmark 与对比方法**：
    *   **Pretrained**：直接使用预训练模型进行零样本（Zero-shot）生成。
    *   **$\epsilon = \infty$**：无隐私保护的联邦微调基准。
    *   **DP Finetuning (No Refine)**：仅进行 DP 微调但不进行投票精炼。
    *   **对比维度**：不同强客户端占比（1% - 40%）、不同隐私预算（$\epsilon=4, 8$）。

### 4. 资源与算力
*   **硬件环境**：使用了 NVIDIA DGX 系统，配备 **8 台 H100 GPU (80GB)**，2 台 AMD EPYC 9654 CPU 和 2TB 系统内存。
*   **模型规模**：实验涉及 GPT-2、GPT-2-large 以及 **LLaMA-7B**（结合 LoRA 技术）。
*   **训练时长**：文中未明确给出总训练时长，但提到微调轮数（如 GPT-2 为 200 轮，GPT-2-large 为 50 轮）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量实验，包括：
    *   不同参与率（1% 到 40% 的强客户端占比）的性能演变。
    *   IID 与多种 Non-IID 偏斜策略下的鲁棒性测试。
    *   消融实验：针对采样率 $r$、投票邻居数 $K$、生成温度等超参数进行了测试。
    *   模型扩展性：验证了从小型 GPT-2 到大型 LLaMA-7B 的适用性。
*   **充分性评价**：实验设计非常充分且客观。通过对比“预训练模型”和“无精炼的 DP 微调”，清晰地证明了投票机制在弥补算力短缺和抵消 DP 噪声方面的有效性。

### 6. 主要结论与发现
*   **精炼机制至关重要**：即使只有 1% 的强客户端参与微调，通过弱客户端的 DP 投票精炼，合成数据的质量也能显著超过纯微调模型，甚至接近参与率更高的非隐私模型。
*   **缓解 DP 噪声**：投票机制能有效过滤掉 DP-SGD 产生的低质量样本，显著提升下游任务的准确率和 F1 分数。
*   **解决异构性**：在 Non-IID 场景下，投票机制能纠正因强客户端数据偏斜导致的全局模型偏差。
*   **效率优势**：弱客户端仅需一轮通信且无需计算梯度，极大地降低了参与门槛。

### 7. 优点
*   **实用性强**：直接针对跨机构 FL 中最现实的“算力贫富差距”问题提出方案。
*   **隐私与效用平衡**：在严格的样本级 DP 保证下，依然维持了较高的合成数据可用性。
*   **框架灵活**：投票机制是插件式的，可以与 LoRA 等参数高效微调技术完美结合。

### 8. 不足与局限
*   **控制代码依赖**：该方法假设控制代码（如标签）是预定义的且非私有的，对于完全无标签或主题极其模糊的文本数据，效果可能受限。
*   **弱客户端仍需推理能力**：虽然无需微调，但弱客户端仍需运行句子嵌入模型（Sentence Transformer）进行 KNN 检索，这对于极低端的设备（如 IoT 传感器）可能仍有压力。
*   **语义对齐风险**：投票基于向量相似度，可能无法完全捕捉复杂的逻辑一致性或事实准确性。

（完）
