---
title: Privacy-Preserving Federated Fraud Detection in Payment Transactions with NVIDIA FLARE
title_zh: 基于 NVIDIA FLARE 的支付交易隐私保护联邦欺诈检测
authors: "Holger R. Roth, Sarthak Tickoo, Mayank Kumar, Isaac Yang, Andrew Liu, Amit Varshney, Sayani Kundu, Iustina Vintila, Peter Madsgaard, Juraj Milcak, Chester Chen, Yan Cheng, Andrew Feng, Jeff Savio, Vikram Singh, Craig Stancill, Gloria Wan, Evan Powell, Anwar Ul Haq, Sudhir Upadhyay, Jisoo Lee"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13617v1"
tags: ["query:ppml"]
score: 9.0
evidence: 用于欺诈检测的隐私保护联邦学习
tldr: 本研究针对金融欺诈检测中隐私监管限制数据共享的问题，利用NVIDIA FLARE框架提出了一种联邦学习方案。通过在异构金融机构间进行协作训练，该方法在保护数据主权的同时，显著提升了欺诈检测性能。实验表明，联邦模型在F1分数上远超本地模型，接近中心化训练水平，并具备良好的收敛性、可解释性及隐私保护能力，为受监管金融环境下的协作建模提供了可行路径。
selection_source: fresh_fetch
motivation: 传统的中心化欺诈检测系统受限于隐私法规和数据主权，难以整合多方数据应对日益增长的金融欺诈风险。
method: 基于NVIDIA FLARE框架，采用深度神经网络和FedAvg算法，在模拟的异构金融机构间进行联邦异常检测训练。
result: 联邦模型的平均F1分数达到0.903，远高于本地模型的0.643，且在10个通信轮次内即可快速收敛。
conclusion: 联邦学习能在满足严格隐私要求的前提下，实现接近中心化训练的欺诈检测性能，具有极高的工业应用价值。
---

## 摘要
欺诈相关的经济损失持续上升，而监管、隐私和数据主权约束日益限制了集中式欺诈检测系统的可行性。联邦学习 (FL) 已成为一种极具前景的范式，能够在不共享原始交易数据的情况下实现跨机构的协作模型训练。然而，其在现实、非独立同分布 (non-IID) 的金融数据分布下的实际有效性仍未得到充分验证。在这项工作中，我们提出了一项面向行业的、多机构的概念验证研究，使用 NVIDIA FLARE 框架评估支付交易的联邦异常检测。我们模拟了一个由异构金融机构组成的现实联邦，每个机构观察到不同的欺诈类型，并在严格的数据隔离下运行。使用通过联邦平均 (FedAvg) 训练的深度神经网络，我们证明了联邦模型的平均 F1 分数达到 0.903——显著优于本地训练的模型 (0.643)，并接近集中式训练的性能 (0.925)，同时保留了完整的数据主权。我们进一步分析了收敛行为，结果显示在 10 个联邦通信轮次内即可实现强大的性能，突显了联邦学习在延迟和成本敏感的金融环境中的运营可行性。为了支持在受监管环境中的部署，我们使用基于 Shapley 的特征归因评估了模型的可解释性，并确认联邦模型依赖于语义一致且与领域相关的决策信号。最后，我们通过 DP-SGD 引入了样本级差分隐私，并展示了良好的隐私-效用权衡。

## Abstract
Fraud-related financial losses continue to rise, while regulatory, privacy, and data-sovereignty constraints increasingly limit the feasibility of centralized fraud detection systems. Federated Learning (FL) has emerged as a promising paradigm for enabling collaborative model training across institutions without sharing raw transaction data. Yet, its practical effectiveness under realistic, non-IID financial data distributions remains insufficiently validated. In this work, we present a multi-institution, industry-oriented proof-of-concept study evaluating federated anomaly detection for payment transactions using the NVIDIA FLARE framework. We simulate a realistic federation of heterogeneous financial institutions, each observing distinct fraud typologies and operating under strict data isolation. Using a deep neural network trained via federated averaging (FedAvg), we demonstrate that federated models achieve a mean F1-score of 0.903 - substantially outperforming locally trained models (0.643) and closely approaching centralized training performance (0.925), while preserving full data sovereignty. We further analyze convergence behavior, showing that strong performance is achieved within 10 federated communication rounds, highlighting the operational viability of FL in latency- and cost-sensitive financial environments. To support deployment in regulated settings, we evaluate model interpretability using Shapley-based feature attribution and confirm that federated models rely on semantically coherent, domain-relevant decision signals. Finally, we incorporate sample-level differential privacy via DP-SGD and demonstrate favorable privacy-utility trade-offs...

---

## 论文详细总结（自动生成）

### 论文总结：基于 NVIDIA FLARE 的支付交易隐私保护联邦欺诈检测

#### 1. 论文的核心问题与整体含义
*   **研究背景**：金融欺诈造成的损失巨大（美国商户每 1 美元的欺诈活动平均损失达 4.61 美元）。传统的欺诈检测依赖中心化数据聚合，但在现代监管（如 GDPR）、数据主权和机构风险偏好的约束下，跨机构共享原始交易数据变得愈发困难。
*   **核心问题**：联邦学习（FL）被视为解决数据孤岛的方案，但在金融领域，数据通常具有高度不平衡、非独立同分布（non-IID）以及机构特定分布的特征。论文旨在验证 FL 在这种现实且严苛的金融场景下，是否能实现接近中心化训练的性能，同时满足隐私和可解释性要求。

#### 2. 论文提出的方法论
*   **核心框架**：采用 **NVIDIA FLARE (NVFlare)** 联邦计算框架，构建了一个多方协作的深度学习系统。
*   **关键技术细节**：
    *   **模型架构**：使用三层全连接深度神经网络（DNN），神经元数量分别为 64、32、16。选择 DNN 而非树模型是因为其梯度空间更易于进行联邦聚合。
    *   **聚合算法**：主要采用 **FedAvg**（联邦平均），并对比了 **FedProx**（增加近端项防止漂移）和 **FedOpt**（服务器端优化）。
    *   **损失函数**：使用 **FocalLoss** 处理极端的类别不平衡（欺诈率仅约 0.5%）。
    *   **隐私增强**：集成 **DP-SGD**（通过 Opacus 库实现样本级差分隐私），通过高斯机制和梯度裁剪提供形式化隐私保证。
    *   **可解释性**：利用 **Shapley 值（Captum GradientShap）** 进行特征归因分析，确保模型决策符合金融监管逻辑。

#### 3. 实验设计
*   **数据集与场景**：开发了一个合成交易生成器，模拟 5 家异构金融机构（Site A-E）。
    *   **Non-IID 设置**：每家银行观察到的欺诈类型分布不同。欺诈类型包括：1. 地理位置异常；2. 新开户大额交易；3. 沉睡账户激活；4. 异常高频交易。
*   **Benchmark（基准）**：
    *   **Centralized（中心化）**：所有数据聚合在一起训练（性能上限）。
    *   **Local-only（仅本地）**：各银行仅使用自有数据训练（性能下限）。
*   **对比方法**：对比了 FedAvg、FedProx、FedOpt 三种联邦聚合算法，以及不同隐私预算（$\epsilon = 1.0, 10.0, 50.0$）下的 DP-FedAvg。

#### 4. 资源与算力
*   **硬件平台**：实验在 **NVIDIA DGX Cloud** 上运行。
*   **算力配置**：
    *   **服务器端**：1 个 NVFlare 实例（仅 CPU）。
    *   **客户端**：5 个站点，每个站点配备专用的 **NVIDIA H200 GPU** 实例。
*   **训练时长**：论文未明确给出总时长，但提到模型在 3-5 个联邦轮次内即可稳定，通常在 10-20 轮内完成训练，显示出极高的通信效率。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从性能基准测试、收敛性分析、混淆矩阵评估到差分隐私权衡以及特征归因分析的多维度实验。
*   **充分性评价**：实验设计较为充分。通过模拟 5 个站点的异构分布，验证了 FL 在处理“未见过的欺诈类型”时的泛化能力（Cross-Domain Learning）。对比了多种 FL 算法，并对隐私保护下的性能损失进行了量化，实验流程客观且具有工业参考价值。

#### 6. 主要结论与发现
*   **性能卓越**：联邦模型（FedAvg）的平均 F1 分数达到 **0.903**，远超本地训练的 **0.643**（提升 40%），且非常接近中心化训练的 **0.925**。
*   **跨域泛化**：FL 显著提升了银行对本地未曾出现的欺诈类型的检测能力（例如，仅有类型 1 数据的银行在联邦后对类型 2 的检测 F1 > 0.94）。
*   **快速收敛**：模型在 10 轮通信内即可达到高性能，适合对延迟敏感的金融环境。
*   **隐私-效用权衡**：在 $\epsilon = 10.0$ 的隐私预算下，F1 分数仅轻微下降至 0.879，证明了在受监管环境下部署带隐私保证的 FL 是可行的。

#### 7. 优点
*   **工业实用性强**：基于 NVIDIA FLARE 生产级框架，展示了从数据生成、模型训练到监控（MLFlow）和解释的全流程。
*   **可解释性验证**：通过 Shapley 值证明了联邦模型学习到的特征与欺诈规则一致，满足金融监管（如 SR 11-7）的要求。
*   **隐私保护完备**：结合了 FL 的架构隔离和 DP-SGD 的数学保证，提供了多层级的隐私防护。

#### 8. 不足与局限
*   **数据真实性限制**：实验完全基于合成数据。虽然模拟了统计异构性，但仍无法完全替代真实世界中复杂的对抗性攻击、概念漂移和标签模糊问题。
*   **参与者假设**：研究假设所有参与者都是“诚实的”，未评估恶意节点进行梯度污染或模型投毒攻击的鲁棒性。
*   **规模限制**：实验仅在 5 个站点上进行，更大规模（如数十家银行）的通信瓶颈和异构性挑战尚待进一步研究。

（完）
