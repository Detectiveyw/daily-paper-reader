---
title: Domain-Skewed Federated Learning with Feature Decoupling and Calibration
title_zh: 基于特征解耦与校准的领域偏斜联邦学习
authors: "Huan Wang, Jun Shen, Jun Yan, Guansong Pang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14238v1"
tags: ["query:ppml"]
score: 9.0
evidence: 具有特征解耦的隐私保护联邦学习
tldr: 针对联邦学习中的领域偏移问题，本文提出了一种名为 F2DC 的特征解耦与校准框架。该方法通过领域特征解耦器（DFD）将本地特征分为领域鲁棒和领域相关特征，并利用领域特征校正器（DFC）对偏差特征进行校准，以提取更多类别相关信息。最后通过领域感知聚合提升全局模型的一致性，显著增强了模型在多领域场景下的泛化能力。
selection_source: fresh_fetch
motivation: 联邦学习中的领域偏移会导致本地模型特征坍缩至低维子空间，限制了全局模型的泛化性能。
method: 提出 F2DC 框架，利用解耦器分离领域鲁棒与相关特征，并通过校正器对领域相关特征进行校准以捕获类别信号。
result: 在三个主流多领域数据集上的实验证明了 F2DC 及其核心模块在提升模型性能方面的有效性。
conclusion: 通过特征解耦与校准，可以有效缓解联邦学习中的领域偏移问题，实现更一致且具泛化性的跨领域特征表示。
---

## 摘要
联邦学习（FL）允许分布式客户端以隐私保护的方式协作训练全局模型。然而，一个主要的挑战是领域偏斜（domain skew），即来自不同领域的客户端数据可能会阻碍聚合后的全局模型学习到一致的表示空间，从而导致在多个领域中的泛化能力较差。在本文中，我们认为领域偏斜体现在每个客户端的领域特定偏置特征中，导致本地模型的表示坍缩到一个狭窄的低维子空间中。随后，我们提出了联邦特征解耦与校准（$F^2$DC），通过校准领域特定的偏置特征来释放有价值的类别相关信息，从而实现跨领域更一致的表示。$F^2$DC 首先引入了一个新颖的组件——领域特征解耦器（DFD），用于确定每个特征单元的鲁棒性，从而将本地特征划分为领域鲁棒特征和领域相关特征。进一步提出了领域特征修正器（DFC），通过显式链接判别信号来校准这些领域相关特征，捕捉补充领域鲁棒特征的额外类别相关线索。最后，对本地模型进行领域感知聚合，以促进客户端之间的共识。在三个流行的多领域数据集上的实验结果证明了所提 $F^2$DC 的有效性及其两个模块的贡献。代码可在 https://github.com/mala-lab/F2DC 获取。

## Abstract
Federated learning (FL) allows distributed clients to collaboratively train a global model in a privacy-preserving manner. However, one major challenge is domain skew, where clients' data originating from diverse domains may hinder the aggregated global model from learning a consistent representation space, resulting in poor generalizable ability in multiple domains. In this paper, we argue that the domain skew is reflected in the domain-specific biased features of each client, causing the local model's representations to collapse into a narrow low-dimensional subspace. We then propose Federated Feature Decoupling and Calibration ($F^2$DC), which liberates valuable class-relevant information by calibrating the domain-specific biased features, enabling more consistent representations across domains. A novel component, Domain Feature Decoupler (DFD), is first introduced in $F^2$DC to determine the robustness of each feature unit, thereby separating the local features into domain-robust features and domain-related features. A Domain Feature Corrector (DFC) is further proposed to calibrate these domain-related features by explicitly linking discriminative signals, capturing additional class-relevant clues that complement the domain-robust features. Finally, a domain-aware aggregation of the local models is performed to promote consensus among clients. Empirical results on three popular multi-domain datasets demonstrate the effectiveness of the proposed $F^2$DC and the contributions of its two modules. Code is available at https://github.com/mala-lab/F2DC.

---

## 论文详细总结（自动生成）

### 论文总结：基于特征解耦与校准的领域偏斜联邦学习 ($F^2DC$)

#### 1. 核心问题与整体含义（研究动机和背景）
本文针对联邦学习（FL）中的**领域偏斜（Domain Skew）**问题展开研究。在现实场景中，不同客户端的数据往往来自不同领域（如不同天气下的驾驶图像、不同风格的艺术画），尽管类别分布相似，但特征分布差异巨大。
*   **核心痛点**：传统的 FL 方法在处理此类数据时，本地模型容易过度拟合特定领域的偏置特征，导致特征表示坍缩到狭窄的低维子空间（Dimensional Collapse），使得全局模型在跨领域泛化时表现极差。
*   **研究动机**：现有的领域偏斜 FL 方法倾向于“剔除”领域特定特征，但作者认为这些偏置特征中往往纠缠着有价值的类别线索（例如：素描风格中的“轮廓”既是领域特征也是类别特征）。因此，本文的目标不是简单删除偏置，而是通过**解耦与校准**来提取更多有用的判别信息。

#### 2. 论文提出的方法论：$F^2DC$ 框架
$F^2DC$ 主要由三个核心模块组成，旨在实现特征的精细化处理和更公平的全局聚合：
*   **领域特征解耦器 (DFD)**：
    *   利用一个两层 CNN 架构生成归因图，并通过 **Gumbel Concrete 分布**生成伪二进制掩码（Mask）。
    *   将本地特征图分解为**领域鲁棒特征**（跨领域一致的信号）和**领域相关特征**（包含领域偏置和潜在类别线索）。
    *   通过分离性损失和判别性损失进行优化，确保两类特征有效分离。
*   **领域特征校正器 (DFC)**：
    *   针对解耦出的“领域相关特征”，使用残差学习机制进行校准。
    *   通过显式注入类别标签信号，迫使模型从偏置特征中提取被误分类或忽略的类别相关线索，生成“校准特征”。
*   **领域感知聚合 (Domain-Aware Aggregation)**：
    *   在服务器端，不再采用简单的加权平均（FedAvg）。
    *   引入**领域差异度 ($d_k$)** 指标，衡量本地领域分布与理想统一分布的距离。
    *   综合考虑数据量和领域差异，为每个客户端分配聚合权重，以促进全局共识并提升公平性。

#### 3. 实验设计
*   **数据集**：
    1.  **Digits**：包含 MNIST, USPS, SVHN, SYN 四个领域。
    2.  **Office-Caltech**：包含 Caltech, Amazon, Webcam, DSLR 四个领域。
    3.  **PACS**：包含 Photo, Art-Painting, Cartoon, Sketch 四个领域。
*   **Benchmark 与对比方法**：
    *   对比了 9 种 SOTA 方法，包括基础方法（FedAvg, FedProx）、对比学习方法（MOON, FedRCL）、原型学习方法（FPL, FedTGP, FedSA）以及专门针对领域偏斜的方法（FedHEAL, FDSE）。
*   **评价指标**：Top-1 准确率、跨领域平均准确率 (AVG) 和标准差 (STD，用于衡量公平性/一致性)。

#### 4. 资源与算力
*   **模型架构**：统一使用 **ResNet-10** 作为骨干网络。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量。
*   **效率数据**：论文提供了效率对比表，显示 $F^2DC$ 在 PACS 数据集上每轮训练时间约为 180.67s，FLOPs 略高于 FedAvg，但**通信开销（Upload Size）与 FedAvg 完全一致**，证明了其在实际部署中的可行性。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集上进行了详尽对比，每组实验均运行 3 次取平均值。
*   **消融实验**：非常充分。分别验证了 DFD、DFC 和领域感知聚合模块的独立贡献；分析了不同特征（鲁棒特征 vs 校准特征）对性能的影响。
*   **超参数敏感性**：对损失权重 ($\lambda_1, \lambda_2$)、平滑参数 ($\sigma$)、温度系数 ($\tau$) 以及聚合权重参数 ($\alpha, \beta$) 均做了详细的参数搜索实验。
*   **可视化分析**：提供了 T-SNE 特征空间可视化和 Grad-CAM 激活图可视化，直观展示了方法在解决特征坍缩和捕获判别线索方面的优势。

#### 6. 主要结论与发现
*   **性能领先**：$F^2DC$ 在所有数据集上均显著优于现有 SOTA 方法，尤其在 PACS 数据集上，平均准确率比 FedAvg 提升了约 10%。
*   **泛化与公平**：通过降低 STD 值，证明了该方法能使全局模型在不同领域间表现更均衡。
*   **模块化优势**：实验证明 DFD 和 DFC 模块可以作为插件集成到其他 FL 算法（如 FPL, FedHEAL）中，并带来稳定的性能提升。

#### 7. 优点
*   **视角独特**：不同于以往“消除偏置”的思路，本文提出的“校准并利用偏置”更具信息利用率。
*   **通信友好**：在提升性能的同时，没有增加客户端与服务器之间的通信负担。
*   **解释性强**：通过可视化手段清晰地展示了模型关注点的变化，证明了特征解耦的有效性。

#### 8. 不足与局限
*   **领域先验依赖**：在计算领域感知聚合权重时，需要预知领域总数 $Q$ 以及客户端所属领域的先验信息，在完全盲测或领域标签缺失的场景下可能受限。
*   **任务单一性**：实验主要集中在图像分类任务，尚未在目标检测、语义分割或自然语言处理（NLP）等更复杂的跨领域任务中验证。
*   **计算开销**：虽然通信量没变，但本地训练时增加了 DFD 和 DFC 的前向与反向传播计算量。

（完）
