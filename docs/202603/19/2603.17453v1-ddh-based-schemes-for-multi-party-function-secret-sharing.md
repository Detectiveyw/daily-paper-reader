---
title: DDH-based schemes for multi-party Function Secret Sharing
title_zh: 基于 DDH 的多方函数秘密共享方案
authors: "Marc Damie, Florian Hahn, Andreas Peter, Jan Ramon"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17453v1"
tags: ["query:fss-llm"]
score: 10.0
evidence: 基于DDH的多方分布式点函数(DPF)FSS方案
tldr: 本研究针对多方函数秘密共享（FSS）中分布式点函数（DPF）密钥规模过大的问题，提出了一种基于DDH假设的新技术。该方法将诚实大多数场景下的密钥大小从O(√N)降低至O(∛N)，在实际应用中比现有方案缩小了10倍，并成功扩展至比较函数，显著提升了多方FSS的效率。
selection_source: fresh_fetch
motivation: 现有的多方分布式点函数方案在密钥规模上存在瓶颈，难以满足大规模应用的需求。
method: 提出一种基于DDH（判定性Diffie-Hellman）假设的优化技术，用于构建更高效的多方FSS方案。
result: 在诚实大多数模型下实现了O(∛N)的密钥规模，且在实际测试中比最先进方案的密钥缩小了多达10倍。
conclusion: 该技术显著提升了多方FSS的效率，并能有效支持点函数及比较函数，具有广泛的应用前景。
---

## 摘要
函数秘密共享 (FSS) 方案能够高效地共享秘密函数。专门针对点函数（Point Functions）的方案被称为分布式点函数 (DPF)，由于其在隐私信息检索、匿名通信和机器学习等领域的广泛应用，它们是 FSS 研究的核心。虽然两方 DPF 受益于具有对数级密钥大小的方案，但多方 DPF 的进展有限：密钥大小为 $O(\sqrt{N})$（其中 $N$ 为函数定义域大小）和/或在密钥大小中存在指数因子。我们提出了一种基于 DDH 的技术，可以减小现有多方方案的密钥大小。特别地，我们构建了一个密钥大小为 $O(\sqrt[3]{N})$ 的诚实大多数（honest-majority）DPF。我们的基准测试表明，在实际问题规模下，其密钥大小比现有最先进的方案小多达 10 倍。最后，我们将该技术扩展到支持比较函数的方案中。

## Abstract
Function Secret Sharing (FSS) schemes enable sharing efficiently secret functions. Schemes dedicated to point functions, referred to as Distributed Point Functions (DPFs), are the center of FSS literature thanks to their numerous applications including private information retrieval, anonymous communications, and machine learning. While two-party DPFs benefit from schemes with logarithmic key sizes, multi-party DPFs have seen limited advancements: $O(\sqrt{N})$ key sizes (with $N$, the function domain size) and/or exponential factors in the key size.   We propose a DDH-based technique reducing the key size of existing multi-party schemes. In particular, we build an honest-majority DPF with $O(\sqrt[3]{N})$ key size. Our benchmark highlights key sizes up to $10\times$ smaller (on realistic problem sizes) than state-of-the-art schemes. Finally, we extend our technique to schemes supporting comparison functions.

---

## 论文详细总结（自动生成）

以下是对论文《DDH-based schemes for multi-party Function Secret Sharing》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在多方（$p > 2$）场景下，如何降低**函数秘密共享（FSS）**，特别是**分布式点函数（DPF）**和**分布式比较函数（DCF）**的密钥规模。
*   **研究背景**：FSS 允许将函数 $f$ 拆分为多个密钥，使得各方在不泄露函数信息的情况下计算 $f(x)$ 的份额。虽然两方 DPF 已实现对数级密钥规模 $O(\log N)$，但现有的多方方案要么密钥过大（$O(\sqrt{N})$），要么在处理大素数域输出时存在指数级增长的因子（$q^{p-1}$），导致其在隐私统计、匿名通信等实际应用中效率低下。
*   **研究动机**：在不引入未经充分验证的新硬性假设的前提下，寻找一种介于 $O(\sqrt{N})$ 和 $O(\log N)$ 之间的折中方案，消除指数级规模开销。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用**判定性 Diffie-Hellman (DDH)** 假设，将函数定义域 $N$ 映射为一个三维或特定的二维网格结构（大小约为 $\sqrt[3]{N}^2 \times \sqrt[3]{N}$），结合现有的信息论 DPF 方案作为“子方案”来处理行索引，并利用 DDH 组元素作为“修正点”来处理列索引。
*   **关键技术细节**：
    1.  **网格化表示**：将输入 $x$ 表示为坐标 $(\gamma, \delta)$。
    2.  **子方案嵌入**：使用一个密钥规模为 $O(\sqrt{M})$ 的信息论 DPF 方案（如 Bunn 等人的方案）来共享两个秘密值 $r$ 和 $1$，用于定位目标行 $\gamma^*$。
    3.  **DDH 修正点**：在 Gen 算法中，为每一列生成一对修正点 $(g_\delta, h_\delta)$。对于非目标列，这些点在评估时会相互抵消；对于目标列 $\delta^*$，则会通过 DDH 组运算暴露出秘密值 $\beta$ 的编码。
    4.  **秘密编码**：提供了两种编码方式：**基于点的编码**（支持有限压缩，适用于匿名广播）和**基于指数的编码**（支持完全压缩，但要求秘密值较小以便计算离散对数，适用于隐私统计）。
*   **算法流程**：Gen 算法生成包含子 DPF 密钥和修正点列表的密钥；Eval 算法通过群幂运算和乘法合成结果份额；Decode 算法通过群运算还原秘密值。

### 3. 实验设计与 Benchmark
*   **实验场景**：对比不同方案在不同定义域大小 $N$、参与方数量 $p$ 以及输出域模数 $q$ 下的总密钥大小。
*   **Benchmark（对比方法）**：
    *   **PRG-based (Boyle et al. 2015)**：具有指数级开销的经典方案。
    *   **DDH-based (Corrigan-Gibbs et al. 2015 / Kumar et al. 2024)**：现有的 $O(\sqrt{N})$ 方案。
    *   **Info-Th. (Bunn et al. 2022)**：诚实大多数模型下的信息论方案。
    *   **Trivial Scheme**：直接共享函数真值表的平凡方案（作为基准线）。
*   **实现细节**：使用 **P-256 椭圆曲线** 实现 DDH 循环群。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **原因**：该研究属于密码学协议优化，核心指标是**通信开销（密钥大小）**而非计算时间或模型训练。实验主要通过数学计算和模拟生成密钥大小数据，不涉及深度学习中的大规模算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了定义域 $N$ 从 $10^2$ 到 $10^{10}$ 的变化。
    *   测试了参与方数量 $p$ 从 3 到 10 的变化。
    *   针对不同类型的模数（素数、合数、阶乘数）分析了现有方案的扩展性。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅对比了最先进的学术方案，还对比了“平凡方案”，揭示了某些 PRG 方案在实际参数下甚至不如直接共享真值表的发现。通过多维度的曲线图清晰展示了 $O(\sqrt[3]{N})$ 相对于 $O(\sqrt{N})$ 的渐进优势。

### 6. 主要结论与发现
*   **密钥规模优化**：成功将诚实大多数模型下的多方 DPF 密钥规模从 $O(\sqrt{N})$ 降低至 **$O(\sqrt[3]{N})$**。
*   **实际性能**：在 $N=10^9$ 等现实问题规模下，密钥大小比现有最先进方案**缩小了约 10 倍**。
*   **消除指数因子**：证明了基于 DDH 的方法可以完全避免 PRG 方案在非 $F_2$ 域上的指数级规模爆炸问题。
*   **DCF 扩展**：该技术可无缝扩展至比较函数（DCF），填补了多方高效 DCF 方案的空白。

### 7. 优点
*   **效率显著提升**：在保持标准安全假设的前提下，大幅压缩了多方 FSS 的通信成本。
*   **假设稳健**：仅依赖于广泛研究的 **DDH 假设**，相比于同时期依赖新硬性假设（如矩阵不变性）的对数级方案，更具工业落地潜力。
*   **实用性强**：详细讨论了秘密编码方式及其对“份额可压缩性”的影响，直接指导了在隐私直方图和匿名通信中的应用。

### 8. 不足与局限
*   **安全模型限制**：仅适用于**诚实大多数（Honest Majority）**场景（即恶意节点少于总数的一半），在不诚实大多数场景下表现受限。
*   **无法递归**：由于该方案输出的是 DDH 群中的乘法份额，无法像某些 PRG 方案那样通过递归调用进一步降低到对数级规模。
*   **计算开销**：相比于仅使用对称加密（PRG）的方案，基于椭圆曲线的 DDH 运算在 Eval 阶段的计算开销相对较高。

（完）
