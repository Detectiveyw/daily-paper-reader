---
title: "QuantFL: Sustainable Federated Learning for Edge IoT via Pre-Trained Model Quantisation"
title_zh: QuantFL：通过预训练模型量化实现边缘物联网的可持续联邦学习
authors: "Charuka Herath, Yogachandran Rahulamathavan, Varuna De Silva, Sangarapillai Lambotharan"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17507v1"
tags: ["query:ppml"]
score: 9.0
evidence: 面向边缘物联网设备的隐私保护联邦学习
tldr: 针对物联网设备在联邦学习中因频繁上行传输导致的高能耗和碳足迹问题，本文提出 QuantFL 框架。该框架利用预训练模型初始化来支持激进且轻量级的量化方案，通过桶量化技术减少通信开销，无需复杂的误差反馈。实验表明，QuantFL 在保持高精度的同时，显著降低了通信比特数，为受限的物联网网络提供了一种可持续的绿色训练方案。
selection_source: fresh_fetch
motivation: 联邦学习在物联网应用中面临高昂的通信能耗和碳排放挑战，而预训练模型在降低微调能耗方面的潜力尚未被充分挖掘。
method: 提出 QuantFL 框架，利用预训练初始化带来的参数更新集中性，采用内存高效的桶量化实现激进压缩，且无需复杂的误差反馈机制。
result: "在 MNIST 和 CIFAR-100 上，QuantFL 减少了约 40% 的总通信量（上行链路减少可达 80% 以上），且在严格带宽限制下精度优于未压缩基准。"
conclusion: QuantFL 为电池受限的物联网设备提供了一种实用且环保的联邦学习方案，实现了通信效率与模型性能的平衡。
---

## 摘要
联邦学习 (FL) 能够在物联网 (IoT) 设备上实现隐私保护的智能，但由于频繁上行传输的高能耗，会产生显著的碳足迹。虽然预训练模型在边缘设备上越来越普及，但其在降低微调能耗方面的潜力仍未得到充分探索。在这项工作中，我们提出了 QuantFL，这是一个可持续的联邦学习框架，它利用预训练初始化来实现激进且计算轻量级的量化。我们证明了预训练能自然地集中更新统计数据，使我们能够

## Abstract
Federated Learning (FL) enables privacy-preserving intelligence on Internet of Things (IoT) devices but incurs a significant carbon footprint due to the high energy cost of frequent uplink transmission. While pre-trained models are increasingly available on edge devices, their potential to reduce the energy overhead of fine-tuning remains underexplored. In this work, we propose QuantFL, a sustainable FL framework that leverages pre-trained initialisation to enable aggressive, computationally lightweight quantisation. We demonstrate that pre-training naturally concentrates update statistics, allowing us to use memory-efficient bucket quantisation without the energy-intensive overhead of complex error-feedback mechanisms. On MNIST and CIFAR-100, QuantFL reduces total communication by 40\% ($\simeq40\%$ total-bit reduction with full-precision downlink; $\geq80\%$ on uplink or when downlink is quantised) while matching or exceeding uncompressed baselines under strict bandwidth budgets; BU attains 89.00\% (MNIST) and 66.89\% (CIFAR-100) test accuracy with orders of magnitude fewer bits. We also account for uplink and downlink costs and provide ablations on quantisation levels and initialisation. QuantFL delivers a practical, "green" recipe for scalable training on battery-constrained IoT networks.

---

## 论文详细总结（自动生成）

### QuantFL：通过预训练模型量化实现边缘物联网的可持续联邦学习

#### 1. 核心问题与整体含义
联邦学习（FL）虽然保护了隐私，但其频繁的模型更新传输产生了巨大的通信开销，导致边缘物联网（IoT）设备能耗高、碳足迹大。论文的核心动机是利用边缘设备上日益普及的**预训练模型**来优化通信效率。研究发现，预训练初始化能使模型更新（$\Delta w$）的统计分布更加集中（范围更小、方差更低），从而允许使用极其激进且计算开销极低的量化方案，而无需复杂的误差反馈（Error Feedback）机制，实现“绿色”且可持续的分布式训练。

#### 2. 方法论
QuantFL 框架结合了**预训练初始化**与**层级桶标量量化（Bucketed Scalar Quantisation）**：
*   **核心思想**：预训练使模型处于较好的参数空间，局部更新量级小且分布集中。利用这一特性，将高维更新信号划分为若干“桶”，仅传输桶索引。
*   **关键技术细节**：
    *   **BU（Bucket-Uniform）**：等宽划分桶边界，对异构数据（Non-IID）具有较强的鲁棒性。
    *   **BQ（Bucket-Quantile）**：基于经验分布的等质量划分，适应峰值分布。
    *   **量化流程**：客户端计算更新 $\Delta w$ $\rightarrow$ 按层进行桶量化 $\rightarrow$ 上行传输索引（及偶尔刷新的边界信息） $\rightarrow$ 服务器通过中点解码（Mid-point Decoding）并聚合。
    *   **代码本刷新**：边界信息（Codebook）不随每轮传输，而是每隔 $T$ 轮刷新一次，分摊了通信成本。

#### 3. 实验设计
*   **数据集**：MNIST（简单 CNN 架构）和 CIFAR-100（复杂的 ResNet-18 架构）。
*   **场景设置**：模拟 100 个用户，每轮随机选择 10 个客户端，每个客户端进行 2 个本地 Epoch。
*   **基准方法（Benchmarks）**：
    *   **FedAvg (NQ)**：全精度非量化基准。
    *   **QSGD**：随机量化基准。
    *   **NP (Not-Pre-trained)**：从零开始训练的消融对照组。
*   **对比维度**：测试精度、训练损失、通信比特数（上行+下行）、数据异构性（Dirichlet $\alpha$ 扫描）。

#### 4. 资源与算力
*   **硬件环境**：使用了一台高性能计算服务器，配置为 **NVIDIA RTX 6000 GPU (48GB VRAM)** 和 **Intel Core i9-10980 处理器**。
*   **训练时长**：文中未明确说明具体的训练总时长（小时/天），但详细列出了通信轮数和本地迭代次数。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单到复杂的两种数据集，并针对量化等级（$L=64, 128$）、初始化方式（预训练 vs 从零开始）以及数据异构性（$\alpha=1.0, 0.5, 0.1$）进行了多组消融实验。
*   **充分性评价**：实验设计较为全面，不仅关注精度，还详细计算了上行和下行的通信成本。通过 Dirichlet 分布模拟 Non-IID 场景，验证了方法在现实物联网环境中的稳定性，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **通信大幅减少**：QuantFL 在保持或超过全精度基准精度的情况下，减少了约 **40% 的总通信量**。若仅计算上行链路，比特数减少可达 **80% 以上**（从 32 位降至约 6 位）。
*   **预训练的红利**：预训练初始化显著提升了量化后的模型性能，BU 方案在 CIFAR-100 上达到了 66.89% 的精度，远超非预训练版本。
*   **BU 优于 BQ**：在处理重尾分布或高度异构的数据时，等宽桶量化（BU）比等质量量化（BQ）和 QSGD 更稳健。
*   **无需误差反馈**：预训练自然降低了更新方差，使得简单的量化即可达到高性能，节省了 IoT 设备存储残差误差的内存开销。

#### 7. 优点
*   **部署简单**：采用标量桶量化，计算逻辑极简，非常适合算力受限的 IoT 芯片。
*   **能效比高**：通过大幅削减上行传输（IoT 设备最耗电的部分），显著延长了电池寿命。
*   **鲁棒性强**：在 Non-IID 数据下表现优异，解决了许多压缩算法在异构环境下精度崩塌的问题。

#### 8. 不足与局限
*   **任务覆盖面**：目前仅在图像分类任务上进行了验证，尚未在自然语言处理（NLP）或大规模基础模型（Foundation Models）上测试。
*   **下行链路假设**：部分实验假设下行链路为全精度，虽然文中提到了下行量化，但主要收益仍集中在上行。
*   **动态调整**：量化等级 $L$ 和刷新周期 $T$ 目前是静态设置的，缺乏根据网络状况自动调整的自适应机制。

（完）
