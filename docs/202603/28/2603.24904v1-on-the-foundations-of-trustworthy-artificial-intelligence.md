---
title: On the Foundations of Trustworthy Artificial Intelligence
title_zh: 论可信人工智能的基础
authors: TJ Dunham
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24904v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 可验证推理与跨架构确定性的整数引擎
tldr: 本文提出“确定性命题”，认为平台确定性推理是实现可信AI的充要条件。研究通过引入“信任熵”量化非确定性成本，指出IEEE 754浮点运算导致了验证难题。为此，作者开发了一个纯整数推理引擎，实现了跨ARM和x86架构的位级一致性。实验证明，该系统在多地节点和链上验证中表现出零误差，成功将AI信任问题转化为可验证的算术一致性问题。
selection_source: fresh_fetch
motivation: 现有AI推理因浮点运算的非确定性导致跨平台结果不一致，使得AI系统的安全性、公平性等可信属性难以被有效验证。
method: 提出确定性理论并构建了一个9.9万行Rust代码的纯整数推理引擎，通过消除浮点运算实现跨硬件架构的位级一致性输出。
result: 在包含67亿参数模型的跨架构测试中实现了零哈希不匹配，并通过全球分布式节点的链上存证验证了推理结果的绝对一致性。
conclusion: 平台确定性是可信AI的基石，通过算术层面的确定性可将复杂的AI验证问题简化为高效的O(1)哈希比对。
---

## 摘要
我们证明了平台确定性推理是实现可信人工智能的充分必要条件。我们将其形式化为“确定性命题”（Determinism Thesis），并引入“信任熵”（trust entropy）来量化非确定性的成本，证明了验证失败概率精确等于 1 - 2^{-H_T}。我们证明了“确定性-验证坍缩”（Determinism-Verification Collapse）：在确定性条件下，验证仅需 O(1) 次哈希比较；若不具备确定性，验证者将面临一个难以处理的成员资格问题。IEEE 754 浮点运算从根本上违反了确定性要求。我们通过构建一个纯整数推理引擎解决了这一问题，该引擎在 ARM 和 x86 架构上实现了位级一致的输出。在针对参数量高达 67 亿的模型进行的 82 次跨架构测试中，我们观察到哈希不匹配次数为零。四个地理分布的节点产生了完全相同的输出，并通过 356 笔链上证明交易进行了验证。人工智能系统的每一项主要信任属性（公平性、鲁棒性、隐私性、安全性、对齐性）都以平台确定性为前提。我们的系统由部署在三大洲的 9.9 万行 Rust 代码组成，它确立了人工智能的信任本质上是一个算术问题。

## Abstract
We prove that platform-deterministic inference is necessary and sufficient for   trustworthy AI. We formalize this as the Determinism Thesis and introduce trust   entropy to quantify the cost of non-determinism, proving that verification failure   probability equals 1 - 2^{-H_T} exactly. We prove a Determinism-Verification   Collapse: verification under determinism requires O(1) hash comparison; without it,   the verifier faces an intractable membership problem. IEEE 754 floating-point   arithmetic fundamentally violates the determinism requirement. We resolve this by   constructing a pure integer inference engine that achieves bitwise identical output   across ARM and x86. In 82 cross-architecture tests on models up to 6.7B parameters,   we observe zero hash mismatches. Four geographically distributed nodes produce   identical outputs, verified by 356 on-chain attestation transactions. Every major   trust property of AI systems (fairness, robustness, privacy, safety, alignment)   presupposes platform determinism. Our system, 99,000 lines of Rust deployed across   three continents, establishes that AI trust is a question of arithmetic.

---

## 论文详细总结（自动生成）

这是一份关于论文《On the Foundations of Trustworthy Artificial Intelligence》（论可信人工智能的基础）的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：论文指出，现代 AI 推理在不同硬件平台上具有**非确定性**。由于 IEEE 754 浮点运算不满足结合律（即 $(a+b)+c \neq a+(b+c)$），相同的模型在不同处理器（如 ARM 与 x86）上会产生微小的舍入差异。
*   **研究动机**：这些位级差异在深度神经网络中会随层数指数级放大，导致最终输出的 Token 序列完全不同。这种不可预测性使得 AI 的**可验证性、可复现性、可审计性和可认证性**在法律和技术层面失效。
*   **整体含义**：作者认为，AI 信任问题本质上不是对齐或解释性问题，而是底层的**算术一致性**问题。

### 2. 方法论：核心思想与关键技术
*   **确定性命题（Determinism Thesis）**：形式化证明了平台确定性是实现可信 AI 的充分必要条件。
*   **信任熵（Trust Entropy）**：引入基于 Rényi 碰撞熵的度量 $H_T$，证明验证失败概率精确等于 $1 - 2^{-H_T}$。
*   **确定性-验证坍缩（Determinism-Verification Collapse）**：证明了在确定性计算下，验证复杂度可从处理组合爆炸的成员资格问题坍缩为 $O(1)$ 的哈希比对。
*   **ARC 引擎技术细节**：
    *   **纯整数算术**：放弃浮点运算，采用 Q16 定点数表示法（16位小数部分）。
    *   **整数化组件**：实现了整数版 RMSNorm（利用牛顿-拉夫逊法求平方根倒数）、整数版 RoPE（旋转位置嵌入）和整数版 SiLU 激活函数（基于查找表和线性插值）。
    *   **KV 缓存精度**：为了防止注意力机制失效，KV 缓存保持全量 Q16 精度而非 INT8。
*   **加密验证层**：集成 Circle STARKs 证明系统，为矩阵乘法提供简洁的链上证明。

### 3. 实验设计
*   **数据集与模型**：使用了 Llama-2-7B（67亿参数）和 TinyLlama-1.1B。
*   **测试场景**：
    1.  **跨架构一致性**：对比 Apple M2 Ultra (ARM/NEON) 与 Vultr 云服务器 (x86/AVX2)。
    2.  **多节点共识**：在洛杉矶、阿姆斯特丹、伦敦、新加坡四个地理分布节点运行 DAG 共识协议。
*   **Benchmark 与对比方法**：
    *   对比了基于浮点运算的 `candle` 推理后端。
    *   测量了 WikiText-2 上的困惑度（Perplexity）以评估量化带来的精度损失。
    *   对比了 EZKL、Modulus 等现有可验证推理系统。

### 4. 资源与算力
*   **硬件设备**：
    *   **ARM 端**：Apple M2 Ultra（24核 CPU，76核 GPU，64GB 统一内存）。
    *   **x86 端**：Vultr 云实例（2 vCPU，8GB RAM）。
*   **算力表现**：
    *   ARC 引擎在 GPU 上达到 76 ms/token，在 CPU 上为 139 ms/token。
    *   **效率提升**：整数引擎比优化后的浮点后端快 1.26 倍（CPU）至 2.3 倍（GPU），因为它规避了浮点数反常值处理和舍入管理开销。
*   **训练/运行时间**：STARK 证明生成时间随层维度线性增长，1024x1024 维度的证明耗时约 1.36 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 **82 次跨架构测试**，涵盖 8 到 1024 个 Token 的生成长度，结果为零哈希不匹配。
    *   在链上记录了 **356 笔推理证明交易**。
    *   生成了 **60 组 Circle STARK 证明**，覆盖从 1B 到 70B 参数规模的层维度。
*   **充分性评价**：实验设计非常严谨。通过“浮点数发散”作为对照组，证明了非确定性是算术层面的必然，而 ARC 引擎的成功则验证了整数路径的可行性。实验跨越了三大洲和两种主流指令集，具有很强的说服力。

### 6. 主要结论与发现
*   **确定性是基石**：证明了公平性、鲁棒性等所有高层信任属性都依赖于底层的平台确定性。
*   **整数推理可行且高效**：纯整数推理不仅能实现位级一致性，且在性能上优于传统的浮点推理。
*   **验证成本极低**：在确定性前提下，任何第三方只需通过一次重执行和哈希比对即可完成验证，无需复杂的交互式证明。

### 7. 优点（亮点）
*   **理论与工程结合**：不仅提出了严密的数学定理（如定理 16 的验证坍缩），还交付了 9.9 万行 Rust 代码的工业级实现。
*   **硬件无关性**：成功解决了困扰高性能计算几十年的跨平台浮点数不一致问题。
*   **性能逆袭**：打破了“为了安全/确定性必须牺牲性能”的固有印象，整数引擎反而更快。

### 8. 不足与局限
*   **精度损失（Perplexity 风险）**：实验显示 INT8 量化导致 WikiText-2 的困惑度显著上升（从 5.47 升至 144）。虽然 Token 生成在逻辑上依然连贯，但在处理极度敏感的任务时，INT8 的精度可能不足。
*   **模型覆盖有限**：目前主要针对 Llama 类架构（Transformer 变体），对于其他非线性算子较多的模型（如某些 CNN 或 RNN 变体）尚未验证。
*   **STARK 覆盖范围**：目前的 STARK 证明仅覆盖了密集的矩阵乘法层，尚未实现全算子的端到端零知识证明。

（完）
