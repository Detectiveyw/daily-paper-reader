---
title: "NANOZK: Layerwise Zero-Knowledge Proofs for Verifiable Large Language Model Inference"
title_zh: NANOZK：用于可验证大语言模型推理的分层零知识证明
authors: Zhaohui Geoffrey Wang
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.18046v1"
tags: ["query:fss-llm"]
score: 9.0
evidence: 用于可验证大语言模型推理的零知识证明
tldr: 针对闭源大模型API缺乏计算真实性保障的问题，本文提出NANOZK，一种用于可验证LLM推理的分层零知识证明系统。该系统利用Transformer的分层特性，将推理分解为独立层并生成常数大小的证明，支持并行化处理。通过查找表近似非算术操作并引入Fisher信息引导验证，NANOZK在保持模型精度的同时，显著降低了证明大小和生成时间，为确保模型推理的真实性提供了高效的密码学方案。
selection_source: fresh_fetch
motivation: 解决用户在使用闭源LLM API时无法验证服务商是否真实使用了声明的模型或是否进行了过度量化等欺诈行为。
method: 采用分层证明框架将Transformer推理分解为独立计算块，并结合查找表近似非算术运算及Fisher信息引导的验证策略。
result: 相比现有系统EZKL，NANOZK在d=128时证明大小缩小了70倍，证明速度提升了5.7倍，且实现了零精度损失。
conclusion: NANOZK通过高效的分层零知识证明技术，为大规模语言模型的可验证推理提供了一种兼具扩展性、高性能和安全性的可行方案。
---

## 摘要
当用户查询专有大语言模型（LLM）API 时，收到的输出缺乏密码学保证，无法确认服务商是否确实使用了声称的模型。服务提供商可能会替换为更廉价的模型、应用激进的量化或返回缓存的响应，而支付高昂费用以获取前沿能力的用户对此无法察觉。我们提出了 METHOD，一个使 LLM 推理可验证的零知识证明系统：用户可以通过密码学方式确认输出对应于特定模型的计算。我们的方法利用了 Transformer 推理可自然分解为独立层计算的特性，实现了一个分层证明框架，其中每一层生成的证明大小恒定，且与模型宽度无关。这种分解规避了整体式方法面临的可扩展性障碍，并支持并行证明。我们为非算术操作（softmax、GELU、LayerNorm）开发了查找表近似方法，实现了零可测量准确率损失，并针对全量层证明不切实际的场景引入了费雪信息引导的验证。在维度高达 d=128 的 Transformer 模型上，METHOD 生成的层证明大小恒定为 5.5KB（2.1KB 注意力 + 3.5KB MLP），验证时间为 24 毫秒。与 EZKL 相比，METHOD 在 d=128 时实现了证明体积缩小 70 倍，证明速度提升 5.7 倍，同时保持了形式化的可靠性保证（epsilon < 1e-37）。查找表近似精确地保留了模型困惑度，实现了无质量损失的验证。

## Abstract
When users query proprietary LLM APIs, they receive outputs with no cryptographic assurance that the claimed model was actually used. Service providers could substitute cheaper models, apply aggressive quantization, or return cached responses - all undetectable by users paying premium prices for frontier capabilities. We present METHOD, a zero-knowledge proof system that makes LLM inference verifiable: users can cryptographically confirm that outputs correspond to the computation of a specific model.   Our approach exploits the fact that transformer inference naturally decomposes into independent layer computations, enabling a layerwise proof framework where each layer generates a constant-size proof regardless of model width. This decomposition sidesteps the scalability barrier facing monolithic approaches and enables parallel proving. We develop lookup table approximations for non-arithmetic operations (softmax, GELU, LayerNorm) that introduce zero measurable accuracy loss, and introduce Fisher information-guided verification for scenarios where proving all layers is impractical.   On transformer models up to d=128, METHOD generates constant-size layer proofs of 5.5KB (2.1KB attention + 3.5KB MLP) with 24 ms verification time. Compared to EZKL, METHOD achieves 70x smaller proofs and 5.7x faster proving time at d=128, while maintaining formal soundness guarantees (epsilon < 1e-37). Lookup approximations preserve model perplexity exactly, enabling verification without quality compromise.

---

## 论文详细总结（自动生成）

### NANOZK：用于可验证大语言模型推理的分层零知识证明

#### 1. 核心问题与整体含义（研究动机和背景）
随着“大模型即服务（LLM-as-a-Service）”的兴起，用户与服务商之间存在严重的**信任不对称**。用户支付高昂费用使用前沿模型（如 GPT-4），但服务商出于成本考虑，可能在后台偷偷替换为廉价模型、应用激进的量化技术或返回缓存结果，而用户在技术上无法察觉。
论文提出了 **NANOZK**，旨在通过**零知识证明（ZKP）**技术为 LLM 推理提供密码学层面的真实性保证。其核心目标是让用户在不获取模型权重（保护知识产权）的前提下，能够验证输出确实是由声称的特定模型生成的。

#### 2. 方法论：核心思想与关键技术
NANOZK 的核心思想是将庞大的 Transformer 模型推理过程进行**分层分解（Layerwise Decomposition）**，主要包含以下技术细节：
*   **分层证明框架与承诺链**：利用 Transformer 的顺序结构，将每一层的计算独立生成证明（$\pi_\ell$）。通过 SHA-256 哈希建立“承诺链”，确保前一层的输出严格等于后一层的输入，防止攻击者拼凑不同模型的中间层证明。
*   **常数大小证明**：基于 Halo2 IPA 协议，实现每层证明大小恒定（约 6.9KB），不随模型隐藏层维度（宽度）增加而膨胀。
*   **ZK 友好型查找表近似**：针对 Softmax、GELU 和 LayerNorm 等非算术运算，采用 16 位精度的查找表（Lookup Tables）代替复杂的多项式近似。这种方法在 ZK 电路中效率极高，且能保持极高的数值精度。
*   **Fisher 信息引导的验证优先级**：在算力受限无法验证所有层时，利用费雪信息（Fisher Information）量化各层对输出的影响力，优先验证“高影响力”层，以实现效率与安全性的平衡。

#### 3. 实验设计
*   **模型对象**：涵盖了 GPT-2 (Small/Medium)、TinyLLaMA-1.1B 以及用于扩展性分析的 Phi-2 (2.7B)。
*   **数据集**：使用 WikiText-2 评估模型在应用 ZK 近似后的困惑度（Perplexity）变化。
*   **Benchmark 与对比方法**：
    *   **EZKL**：目前最广泛使用的 ZKML（零知识机器学习）工具包。
    *   **指标**：证明生成时间（Proving Time）、证明大小（Proof Size）、验证时间（Verification Time）以及模型精度损失（$\Delta$PPL）。

#### 4. 资源与算力
*   **硬件环境**：实验主要在单台 **Intel Xeon CPU @ 2.4GHz** 上运行，配备 **64GB RAM**。
*   **软件实现**：使用 Rust 语言开发，基于 Halo2 IPA 后端。
*   **算力说明**：论文强调了其 CPU 友好性。对于 GPT-2 Small，单层证明约需 6.3 秒，全模型（12层）在并行模式下约需 3.2 分钟。文中未提及使用大规模 GPU 集群，显示了该方案对中等算力设备的适配性。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **性能对比**：在不同维度的 MLP 层上对比了 NANOZK 与 EZKL 的速度和内存消耗。
*   **精度验证**：在三个不同规模的模型（124M 到 1.1B 参数）上测试了查找表近似对困惑度的影响。
*   **消融/策略实验**：对比了 Fisher 引导、随机选择和均匀选择三种层验证策略的覆盖率。
*   **充分性评价**：实验设计较为全面，涵盖了从微观算子到宏观模型性能的评估，且对比了主流基准工具，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **高性能**：相比 EZKL，NANOZK 实现了平均 **52 倍**的加速，在大型 MLP 模型上加速比高达 **228 倍**。
*   **零精度损失**：16 位查找表近似在 WikiText-2 上的困惑度变化为 **0.00%**，证明了验证过程不会牺牲模型质量。
*   **极小证明体积**：每层证明仅 6.9KB，全模型证明远小于同类研究（如 zkGPT）。
*   **Fisher 策略有效性**：Fisher 引导验证在 50% 的预算下能捕捉到 65-86% 的模型敏感度，显著优于随机选择。

#### 7. 优点与亮点
*   **扩展性强**：分层设计巧妙规避了整体式证明面临的内存爆炸问题，使证明大规模模型成为可能。
*   **并行化友好**：各层证明可独立生成，极大缩短了端到端的挂钟时间。
*   **实用性高**：23ms 的验证时间足以支持实时应用，且无需昂贵的 GPU 即可运行。
*   **理论完备**：提供了形式化的组合可靠性（Compositional Soundness）证明，确保了分层拼接后的安全性。

#### 8. 不足与局限
*   **证明延迟**：尽管已有大幅提升，但证明生成时间（分钟级）仍远慢于原生推理（秒级），对于极低延迟要求的场景仍有挑战。
*   **Fisher 策略的局限**：Fisher 引导验证提供的是概率性保证而非绝对的密码学完整性，极端攻击者可能专门针对未验证的低 Fisher 层进行微小篡改。
*   **硬件适配**：目前主要针对 CPU 优化，尚未充分展示在 GPU 加速下的性能潜力（尽管论文提到这是未来方向）。

（完）
