---
title: Probabilistic Federated Learning on Uncertain and Heterogeneous Data with Model Personalization
title_zh: 针对不确定且异构数据并结合模型个性化的概率联邦学习
authors: "Ratun Rahman, Dinh C. Nguyen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18083v1"
tags: ["query:ppml"]
score: 9.0
evidence: 针对异构数据的个性化概率联邦学习
tldr: 针对联邦学习中数据不确定性和异构性导致的训练退化问题，本文提出 Meta-BayFL 框架。该方法结合了贝叶斯神经网络（BNN）与元学习，通过在隐藏层引入不确定性建模并利用自适应学习率进行个性化更新，有效提升了在非独立同分布（non-IID）及噪声数据下的模型鲁棒性。实验证明其在多个数据集上显著优于现有主流个性化联邦学习方法，并兼顾了计算与通信效率。
selection_source: fresh_fetch
motivation: 传统联邦学习难以应对客户端数据的异构性与不确定性，且现有概率方法在联邦场景下的运行开销与部署可行性研究不足。
method: 提出 Meta-BayFL 框架，利用 BNN 建模隐藏层不确定性，并结合元学习与自适应学习率实现个性化更新与稳健的全局聚合。
result: "在 CIFAR-10/100 和 Tiny-ImageNet 实验中，Meta-BayFL 的测试准确率比 pFedMe、Ditto 等现有方法最高提升了 7.42%。"
conclusion: Meta-BayFL 通过融合概率建模与个性化元学习，为处理异构且不确定数据的联邦学习提供了一种高精度且具备理论收敛保证的方案。
---

## 摘要
传统的联邦学习（FL）框架往往由于本地客户端之间的数据不确定性和异构性而面临训练退化的问题。贝叶斯神经网络（BNN）等概率方法可以通过显式建模不确定性来缓解这一问题，但它们会引入额外的运行时间、延迟和带宽开销，这在联邦设置中鲜有研究。为了应对这些挑战，我们提出了 Meta-BayFL，这是一种将元学习与 BNN 相结合的个性化概率联邦学习方法，旨在提高在不确定和异构数据下的训练效果。该框架具有三个主要特征：（1）基于 BNN 的客户端模型在隐藏层中引入了不确定性，以稳定在小型和噪声数据集上的训练；（2）具有自适应学习率的元学习实现了个性化更新，增强了非独立同分布（non-IID）条件下的本地训练；（3）统一的概率化和个性化设计提高了全局模型聚合的鲁棒性。我们提供了理论收敛性分析，并刻画了全局模型在通信轮次中的上界。此外，我们评估了计算成本（运行时间、延迟和通信），并讨论了在边缘节点和物联网（IoT）系统等资源受限设备上部署的可行性。在 CIFAR-10、CIFAR-100 和 Tiny-ImageNet 上的广泛实验表明，Meta-BayFL 始终优于最先进的方法，包括标准和个性化联邦学习方法（如 pFedMe、Ditto、FedFomo），测试准确率最高提升了 7.42%。

## Abstract
Conventional federated learning (FL) frameworks often suffer from training degradation due to data uncertainty and heterogeneity across local clients. Probabilistic approaches such as Bayesian neural networks (BNNs) can mitigate this issue by explicitly modeling uncertainty, but they introduce additional runtime, latency, and bandwidth overhead that has rarely been studied in federated settings. To address these challenges, we propose Meta-BayFL, a personalized probabilistic FL method that combines meta-learning with BNNs to improve training under uncertain and heterogeneous data. The framework is characterized by three main features: (1) BNN-based client models incorporate uncertainty across hidden layers to stabilize training on small and noisy datasets, (2) meta-learning with adaptive learning rates enables personalized updates that enhance local training under non-IID conditions, and (3) a unified probabilistic and personalized design improves the robustness of global model aggregation. We provide a theoretical convergence analysis and characterize the upper bound of the global model over communication rounds. In addition, we evaluate computational costs (runtime, latency, and communication) and discuss the feasibility of deployment on resource-constrained devices such as edge nodes and IoT systems. Extensive experiments on CIFAR-10, CIFAR-100, and Tiny-ImageNet show that Meta-BayFL consistently outperforms state-of-the-art methods, including both standard and personalized FL approaches (e.g., pFedMe, Ditto, FedFomo), with up to 7.42\% higher test accuracy.

---

## 论文详细总结（自动生成）

### 针对不确定且异构数据并结合模型个性化的概率联邦学习 (Meta-BayFL) 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的联邦学习（FL）场景中，数据往往存在两大挑战：
*   **数据不确定性（Uncertain Data）：** 客户端数据可能包含大量噪声、样本量极小或信息过时，导致全局模型训练不稳定、难以收敛。
*   **数据异构性（Heterogeneous Data）：** 客户端之间的特征空间和标签分布通常是非独立同分布的（Non-IID），单一的全局模型难以适配所有客户端的个性化需求。
虽然贝叶斯神经网络（BNN）能通过建模参数分布来处理不确定性，但在联邦学习框架下，如何结合个性化技术并控制其带来的计算/通信开销仍是一个亟待解决的难题。

#### 2. 论文提出的方法论：Meta-BayFL 框架
论文提出了 **Meta-BayFL**，其核心思想是将**贝叶斯神经网络（BNN）**与**元学习（Meta-learning）**相结合：
*   **概率建模（BNN）：** 客户端不再学习确定的权重，而是学习权重的概率分布（均值 $\mu$ 和方差 $\sigma$）。通过最大化证据下界（ELBO）来优化模型，使模型在面对噪声和小样本数据时更具鲁棒性。
*   **元学习个性化：** 客户端在本地训练前，先利用一小部分本地数据作为验证集，尝试多种候选学习率。通过比较不同学习率下的损失值，筛选出最适合当前本地数据分布的“最优学习率”（$\eta_{best}$），从而实现自适应的个性化更新。
*   **算法流程：** 
    1. 服务器下发全局模型参数分布。
    2. 客户端接收后，基于元学习策略在本地寻找最优超参数。
    3. 客户端利用 BNN 结构进行本地训练，更新变分分布参数。
    4. 服务器对各客户端上传的分布参数进行加权聚合（Federated Averaging）。

#### 3. 实验设计
*   **数据集：** CIFAR-10、CIFAR-100 和 Tiny-ImageNet。
*   **Non-IID 场景：** 采用了“Step”（阶梯分布）和“Dirichlet”（狄利克雷分布，$\alpha=0.1$）两种异构划分方式。
*   **Benchmark 与对比方法：**
    *   **基础方法：** 传统 CNN、FedAvg。
    *   **概率/个性化方法：** BNN、pFedMe、Ditto、FedFomo、pFedBayes、pFedBL、Fedmask 等。
*   **环境模拟：** 引入了不同比例的数据缺失（10% 到 100%）和不同强度的噪声干扰（$\epsilon$ 从 0 到 0.1）。

#### 4. 资源与算力
*   **算力说明：** 论文**未明确说明**服务器端训练所使用的具体 GPU 型号（如 A100 或 RTX 3090）及数量。
*   **边缘部署测试：** 论文特别提到了在 **Raspberry Pi（树莓派）** 上进行了 CPU-only 的部署实验，以评估该方法在资源受限设备上的运行时间（每轮约 27-37 秒）和延迟。

#### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度评估：** 涵盖了准确率对比、收敛性曲线、噪声鲁棒性测试、小样本性能测试。
*   **消融实验：** 对比了全 BNN 架构与 MC Dropout（蒙特卡洛丢弃法）的性能差异。
*   **扩展性测试：** 验证了客户端数量（5 到 20 个）增加对模型性能的影响。
*   **跨域验证：** 测试了当服务器端拥有不同领域（Out-of-domain）无标签数据时对模型的影响。

#### 6. 主要结论与发现
*   **性能领先：** Meta-BayFL 在所有测试数据集上均优于现有 SOTA 方法，在 Tiny-ImageNet 上比第二名（Fedmask）准确率高出 **7.42%**。
*   **鲁棒性极强：** 在数据量仅为 10% 且噪声水平极高（$\epsilon=0.1$）的极端环境下，Meta-BayFL 的表现远超 FedAvg，证明了概率建模在处理不确定性方面的优势。
*   **元学习有效性：** 实验证明，通过元学习动态调整学习率比固定学习率的 BNN 具有更快的收敛速度和更高的最终精度。

#### 7. 优点（亮点）
*   **理论支撑：** 提供了详尽的收敛性分析，推导了全局模型在通信轮次中的误差上界。
*   **协同效应：** 成功将 BNN 的不确定性处理能力与元学习的个性化适配能力融合，解决了 FL 中的双重挑战。
*   **现实可行性：** 考虑了边缘设备的部署成本，通过树莓派实验证明了该算法在 IoT 环境下的潜在应用价值。

#### 8. 不足与局限
*   **通信开销：** 由于需要传输 BNN 的均值和方差两个参数，其每轮通信的数据量大约是传统 FL 的 **2 倍**。
*   **计算复杂度：** 客户端在本地需要进行元学习（尝试多个学习率）和概率推理，计算负担比普通 CNN 显著增加，可能导致电池受限设备的能耗过快。
*   **超参数敏感性：** 虽然元学习能自动选择学习率，但元学习本身的候选池设置及本地验证集的划分比例仍需人工干预。

（完）
