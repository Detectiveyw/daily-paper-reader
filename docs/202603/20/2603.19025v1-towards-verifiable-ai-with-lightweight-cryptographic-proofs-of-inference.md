---
title: Towards Verifiable AI with Lightweight Cryptographic Proofs of Inference
title_zh: 迈向具有轻量级推理密码学证明的可验证人工智能
authors: "Pranay Anchuri, Matteo Campanelli, Paul Cesaretti, Rosario Gennaro, Tushar M. Jois, Hasan S. Kayman, Tugce Ozdemir"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19025v1"
tags: ["query:fss-llm"]
score: 9.0
evidence: 使用轻量级密码学证明的大模型可验证推理
tldr: 针对云端大模型推理缺乏可验证性的问题，本文提出一种基于轻量级密码学证明的框架。该方法利用神经网络的统计特性，通过采样推理执行轨迹而非生成完整证明，将验证时间从分钟级缩短至毫秒级。实验证明该方案能有效检测对抗性欺诈，在保证效率的同时提供了实用的安全保障，适用于大规模部署和审计场景。
selection_source: fresh_fetch
motivation: 现有的密码学证明系统在验证大模型推理时计算开销巨大，无法满足实时云服务的性能需求。
method: 利用默克尔树对推理执行轨迹进行向量承诺，并结合神经网络的统计特性进行随机路径采样验证。
result: 该方法将证明生成时间从分钟级缩短至毫秒级，且在ResNet-18和Llama-2等模型上成功抵御了多种对抗性欺诈策略。
conclusion: 通过在健全性与效率之间取得平衡，该研究为大规模可验证AI推理提供了一种极具实用价值的高性能解决方案。
---

## 摘要
当大型 AI 模型作为云端服务部署时，客户端无法保证响应是正确的，或者是由预期的模型生成的。对于大型模型，在本地重新运行推理是不可行的，而现有的密码学证明系统虽然提供了强大的正确性保证，但引入了极高的证明者开销（例如，对于十亿参数模型，每次查询需要数百秒）。我们提出了一种验证框架和协议，通过基于神经网络统计特性的轻量级采样方法取代了完整的密码学证明。我们形式化了在何种条件下可以利用功能不同的模型之间的轨迹分离（trace separation）来论证可验证推理协议的安全性。证明者通过基于 Merkle 树的向量承诺对推理的执行轨迹进行承诺，并仅公开从输出到输入的随机采样路径上的少量条目。这产生了一种以可靠性（soundness）换取效率的协议，这种权衡非常适合审计、重复查询会放大检测概率的大规模部署场景，以及证明者具有理性动机且在被检测到违规时面临惩罚的场景。与最先进的密码学证明系统相比，我们的方法将证明时间缩短了几个数量级，从分钟级降至毫秒级，而证明文件的大小仅适度增加。在 ResNet-18 分类器和 Llama-2-7B 上的实验证实，通用架构具备我们协议所需的统计特性，且自然的对抗策略（梯度下降重构、逆变换、logit 交换）无法生成逃避检测的轨迹。此外，我们还提出了一种仲裁委托模型（refereed delegation model）下的协议，其中两个相互竞争的服务器能够在对数轮次内实现正确输出的识别。

## Abstract
When large AI models are deployed as cloud-based services, clients have no guarantee that responses are correct or were produced by the intended model. Rerunning inference locally is infeasible for large models, and existing cryptographic proof systems -- while providing strong correctness guarantees -- introduce prohibitive prover overhead (e.g., hundreds of seconds per query for billion-parameter models). We present a verification framework and protocol that replaces full cryptographic proofs with a lightweight, sampling-based approach grounded in statistical properties of neural networks. We formalize the conditions under which trace separation between functionally dissimilar models can be leveraged to argue the security of verifiable inference protocols. The prover commits to the execution trace of inference via Merkle-tree-based vector commitments and opens only a small number of entries along randomly sampled paths from output to input. This yields a protocol that trades soundness for efficiency, a tradeoff well-suited to auditing, large-scale deployment settings where repeated queries amplify detection probability, and scenarios with rationally incentivized provers who face penalties upon detection. Our approach reduces proving times by several orders of magnitude compared to state-of-the-art cryptographic proof systems, going from the order of minutes to the order of milliseconds, with moderately larger proofs. Experiments on ResNet-18 classifiers and Llama-2-7B confirm that common architectures exhibit the statistical properties our protocol requires, and that natural adversarial strategies (gradient-descent reconstruction, inverse transforms, logit swapping) fail to produce traces that evade detection. We additionally present a protocol in the refereed delegation model, where two competing servers enable correct output identification in a logarithmic number of rounds.

---

## 论文详细总结（自动生成）

以下是对论文《Towards Verifiable AI with Lightweight Cryptographic Proofs of Inference》（迈向具有轻量级推理密码学证明的可验证人工智能）的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：当大型 AI 模型（如 LLM）部署在云端时，用户无法确定返回的结果是否由指定的模型正确生成。
*   **研究背景**：
    *   **本地运行不可行**：大模型对计算资源要求极高，普通用户无法本地复现推理过程。
    *   **现有密码学证明（如 SNARKs）开销巨大**：为数十亿参数的模型生成正确性证明通常需要数百秒甚至数十分钟（如 zkLLM 需 15 分钟），无法满足实时推理的需求。
*   **研究目标**：开发一种极轻量级的验证框架，在保证一定安全性的前提下，将验证开销降低几个数量级。

### 2. 方法论：核心思想与技术细节
*   **核心思想：轨迹分离（Trace Separation）**：
    基于 AI 领域的“表示相似性”研究，论文提出：如果两个模型的功能不同（输出不同），那么它们在处理相同输入时的内部激活轨迹（Activation Trace）也必然存在显著差异。
*   **关键技术：随机路径采样验证（RandPathTest）**：
    1.  **承诺阶段**：证明者（服务器）使用基于 Merkle 树的**向量承诺（Vector Commitment）**对推理过程中的所有激活值（轨迹）进行承诺。
    2.  **挑战阶段**：验证者（用户）随机选择一条从输出层回溯到输入层的路径。
    3.  **验证阶段**：证明者仅公开该路径上涉及的少量神经元激活值及其对应的模型权重。验证者检查局部一致性：即当前层神经元的激活值是否确实由前一层神经元的激活值与正确权重的加权和（经过激活函数）计算得出。
*   **算法流程**：
    *   验证者从输出节点开始，每层随机采样一个节点，并检查其所有输入连接的正确性。
    *   通过这种“抽查”方式，只要轨迹中存在不一致，随机路径就有极高概率捕捉到错误。
*   **扩展模型**：还提出了一种**仲裁委托模型（Refereed Model）**，通过两个竞争服务器之间的对数级轮次二分查找来定位错误。

### 3. 实验设计与对比
*   **实验场景与数据集**：
    *   **图像分类**：使用 ResNet-18 架构，在 Animals-10 和 Stanford Dogs 数据集上进行实验（如对比“猫狗分类”与“狗松鼠分类”模型的内部差异）。
    *   **大语言模型**：使用 Llama-2-7B，对比原始模型与微调后的聊天版本（Llama-2-7b-chat）。
*   **对抗性策略（Benchmark）**：
    *   **梯度下降重构**：攻击者试图通过优化激活值来伪造符合权重但输出错误的轨迹。
    *   **逆变换攻击**：利用矩阵伪逆试图反推激活值。
    *   **Logit 交换攻击**：直接篡改输出层的 Logit 值并尝试修复中间层。
*   **对比方法**：主要对比了最先进的 SNARK 方案 **zkLLM**。

### 4. 资源与算力
*   **硬件配置**：AMD Ryzen 9 9900X CPU, 32GB RAM, **NVIDIA GeForce RTX 3090 GPU**。
*   **算力表现**：
    *   **证明生成时间**：对于 Llama-2-7B，本方案的轨迹承诺仅需 **5.8 毫秒**，而 zkLLM 需要 **388.3 秒**。
    *   **验证时间**：约 **12.44 毫秒**。
    *   **证明大小**：约 **3.4 MB**（包含权重和激活值的 Merkle 证明），而 zkLLM 约为 183 KB。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LLM 攻击测试中，评估了超过 **350 万条路径**。
    *   针对分类器进行了多维度测试，包括不同任务、相同任务不同子集、以及细粒度品种分类。
*   **充分性评价**：实验设计较为全面，不仅验证了正常情况下的“模型分离度”，还深入探讨了多种潜在的对抗攻击手段。通过大规模采样证明了随机路径检测的统计有效性，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能飞跃**：证明生成速度比现有技术快了约 **50,000 倍**，实现了从“分钟级”到“毫秒级”的跨越。
*   **统计特性稳健**：实验证实，即使是极其相似的模型（如同一模型的不同微调版本），其内部激活轨迹也存在足够的差异供采样检测。
*   **防御有效**：常见的对抗性重构策略（如梯度下降）在复杂的非线性网络面前难以生成能够完美欺骗随机路径检测的伪造轨迹。

### 7. 优点与亮点
*   **极高性能**：几乎消除了证明者的计算负担，使得可验证 AI 推理在商业大规模部署中变得实用。
*   **理论创新**：首次将 AI 领域的表示相似性理论与密码学证明相结合，提出了“轨迹分离”这一安全假设。
*   **灵活性**：通过调整采样路径的数量，可以在安全性（Soundness）和效率之间进行灵活权衡。

### 8. 不足与局限
*   **证明文件较大**：由于需要传输 Merkle 路径和原始激活值/权重，证明大小（MB 级）显著高于 SNARK 方案（KB 级）。
*   **非完全健全性（Approximate Soundness）**：该方案提供的是概率性的安全保证，而非 SNARK 那样的确定性保证。
*   **全恶意攻击挑战**：虽然能有效检测“换模型”欺诈，但对于能够针对特定路径进行极端优化的“全恶意”攻击者，单路径检测的检测概率上限受限于网络宽度（1/N）。
*   **隐私问题**：目前的协议会泄露部分激活值和权重，若需保护模型隐私，还需额外引入零知识证明技术（作者将其列为未来工作）。

（完）
