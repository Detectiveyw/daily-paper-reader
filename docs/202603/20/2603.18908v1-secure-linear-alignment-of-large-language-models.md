---
title: Secure Linear Alignment of Large Language Models
title_zh: 大语言模型的安全线性对齐
authors: "Matt Gorbett, Suman Jana"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18908v1"
tags: ["query:fss-llm"]
score: 9.0
evidence: 大语言模型的隐私保护跨孤岛推理
tldr: 本研究利用大语言模型表示空间的收敛性，提出了一种隐私保护的跨模型推理框架。该框架通过在公共数据集上学习线性仿射变换来对齐独立训练的模型，并利用同态加密技术保护推理时的客户端查询。实验表明，该方法在保持高安全性的同时实现了亚秒级延迟，不仅在分类和离群检测任务中表现出色，还首次实现了跨模型的文本生成，为隐私受限环境下的模型协作开辟了新路径。
selection_source: fresh_fetch
motivation: 针对隐私和安全限制导致无法直接共享数据或模型的问题，探索利用模型表示收敛性实现跨模型协作的可能性。
method: 结合线性仿射变换与同态加密技术，仅对对齐和分类操作进行加密，以平衡推理效率与数据安全性。
result: 实验证明该框架在多种任务上性能损失极小，且推理延迟达到亚秒级，并成功实现了跨独立模型的文本生成。
conclusion: 线性对齐是实现安全、高效跨模型推理的有效手段，证明了独立训练模型间存在深层的表示兼容性。
---

## 摘要
尽管在训练目标、架构和数据模态上存在差异，但语言模型似乎越来越多地学习到相似的表示。这种独立训练模型之间新兴的兼容性，为跨模型对齐下游目标提供了新的机遇。此外，它还开启了新的潜在应用领域，例如在安全性、隐私性或竞争约束禁止直接共享数据或模型的场景中。在这项工作中，我们提出了一个隐私保护框架，利用表示收敛（representational convergence）来实现独立语言模型之间的跨孤岛推理（cross-silo inference）。该框架在共享的公开数据集上学习仿射变换，并应用同态加密（homomorphic encryption）来保护推理过程中的客户端查询。通过仅对线性对齐和分类操作进行加密，该方法在保持强大安全保障的同时，实现了亚秒级的推理延迟。我们通过对表示收敛的实证研究来支持这一框架，其中我们学习了独立模型最终隐藏状态之间的线性变换。我们在嵌入分类和分布外（out-of-distribution）检测上评估了这些跨模型映射，观察到模型对之间的性能下降极小。此外，我们首次展示了线性对齐有时能够实现跨独立训练模型的文本生成。

## Abstract
Language models increasingly appear to learn similar representations, despite differences in training objectives, architectures, and data modalities. This emerging compatibility between independently trained models introduces new opportunities for cross-model alignment to downstream objectives. Moreover, it unlocks new potential application domains, such as settings where security, privacy, or competitive constraints prohibit direct data or model sharing. In this work, we propose a privacy-preserving framework that exploits representational convergence to enable cross-silo inference between independent language models. The framework learns an affine transformation over a shared public dataset and applies homomorphic encryption to protect client queries during inference. By encrypting only the linear alignment and classification operations, the method achieves sub-second inference latency while maintaining strong security guarantees. We support this framework with an empirical investigation into representational convergence, in which we learn linear transformations between the final hidden states of independent models. We evaluate these cross-model mappings on embedding classification and out-of-distribution detection, observing minimal performance degradation across model pairs. Additionally, we show for the first time that linear alignment sometimes enables text generation across independently trained models.

---

## 论文详细总结（自动生成）

### 论文总结：大语言模型的安全线性对齐（HELIX）

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着大语言模型（LLM）规模的扩大，不同架构和训练数据下的模型在表示空间上呈现出收敛趋势（即“柏拉图表示假设”）。然而，在实际应用中，由于隐私保护、商业竞争或法律监管（如GDPR、HIPAA），直接共享模型权重或原始数据往往并不可行。
*   **核心问题**：如何在不泄露客户端私有数据/模型参数，也不泄露服务端分类器参数的前提下，实现独立模型之间的协同推理？
*   **整体含义**：本文提出了 **HELIX**（Homomorphically Encrypted Linear Inference across models）框架，利用模型间的表示相似性，通过极轻量级的线性对齐和同态加密技术，实现了跨机构、跨模型的隐私保护推理。

#### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用线性仿射变换（Affine Transformation）将源模型（客户端）的嵌入向量映射到目标模型（服务端）的特征空间，并仅对该线性对齐和后续的线性分类操作进行同态加密。
*   **关键技术流程**：
    1.  **训练阶段（安全对齐）**：
        *   双方利用共享的公开数据集（$D_{pub}$）提取嵌入向量。
        *   客户端加密其嵌入向量并发送给服务端。
        *   服务端在加密状态下计算跨模型协方差矩阵，客户端解密后利用岭回归（Ridge Regression）在本地求解对齐矩阵 $W^*$。
    2.  **推理阶段（隐私推理）**：
        *   客户端在本地对查询向量应用对齐映射，随后进行同态加密（使用 CKKS 方案）。
        *   服务端在加密向量上执行线性分类运算，并将加密结果返回。
        *   客户端解密获得最终预测结果。
*   **算法细节**：采用 CKKS 同态加密方案，支持实数运算；对齐过程基于最小二乘法，通过增加正则化项防止过拟合。

#### 3. 实验设计
*   **实验场景**：
    *   **下游任务分类**：在 AG News、DBpedia、MNLI、SST-2、TREC 等数据集上评估分类准确率。
    *   **分布外（OOD）检测**：测试对齐后的模型是否能保留原模型的置信度结构。
    *   **跨模型文本生成**：尝试将一个模型的隐藏状态映射到另一个模型的输出头（LM Head）进行解码。
*   **对比基准（Benchmark）**：
    *   **Baseline**：目标模型在原始特征空间上的原生性能。
    *   **Few-shot Baseline**：在少量样本上独立训练的分类器。
    *   **对比方法**：与 BOLT、Nimbus、Nexus 等全模型加密推理系统进行效率对比。
*   **涉及模型**：涵盖 OpenAI (text-embedding-3-small)、Google (Gemini)、Cohere、Mistral、Qwen、Llama 3 等主流闭源 API 和开源模型。

#### 4. 资源与算力
*   **算力说明**：论文未详细列出训练对齐矩阵所用的 GPU 总量，但指出对齐过程仅涉及线性代数运算（如岭回归），计算开销极低。
*   **推理性能**：同态加密部分的评估是在 **CPU** 上完成的（使用 TenSEAL 库）。实验结果显示，HELIX 的端到端延迟在 **亚秒级**（<1s），远快于需要加密整个 Transformer 层的现有方法（通常需数十秒甚至数分钟）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对文本生成，评估了 **34 组** 不同的模型配对。
    *   针对分类任务，在 6 个主流数据集上进行了详尽测试，并重复实验 3 次以确保结果稳定性。
    *   包含消融实验，探讨了公开数据量、领域内样本量对对齐效果的影响。
*   **充分性评价**：实验设计较为全面，不仅涵盖了判别式任务（分类、OOD），还挑战了生成式任务，并引入了“LLM-as-a-Judge”和人工评估，客观性较强。

#### 6. 主要结论与发现
*   **线性对齐的有效性**：简单的线性映射足以在不同厂商的模型间转移知识，分类性能损失极小。
*   **文本生成的关键因素**：
    *   **分词器兼容性**：分词器的重合度（Jaccard 指数）与生成质量高度相关（$r=0.822$）。
    *   **模型规模**：源模型规模若低于 4B 参数，跨模型生成效果会大幅下降。
*   **隐私与效率平衡**：HELIX 在提供 128 位安全强度的同时，通信量（<1MB/样本）和延迟均比全模型加密方案降低了 1-2 个数量级。

#### 7. 优点
*   **极高的推理效率**：通过只加密线性层而非整个 Transformer 架构，突破了同态加密在 LLM 推理中的性能瓶颈。
*   **跨厂商互操作性**：证明了即使是完全独立训练的闭源模型（如 OpenAI 与 Gemini），也存在可以被线性利用的共享特征空间。
*   **安全性设计**：在半诚实模型下提供了坚实的隐私保障，且通过成员推理攻击测试证明了对齐矩阵的泄露风险极低。

#### 8. 不足与局限
*   **分词器依赖**：文本生成任务高度依赖分词器的重合度，对于分词差异巨大的模型（如 Gemma 与其他模型），对齐效果较差。
*   **线性头限制**：目前仅适用于线性分类头，对于复杂的非线性下游任务，简单的线性对齐可能不足。
*   **威胁模型限制**：主要针对半诚实（Honest-but-curious）模型，尚未深入探讨恶意攻击者通过自适应查询进行模型逆向工程的防御。
*   **生成质量损失**：虽然实现了跨模型生成，但相比原生模型仍有 30%-40% 的质量下降。

（完）
