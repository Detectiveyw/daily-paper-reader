---
title: "Bodhi VLM: Privacy-Alignment Modeling for Hierarchical Visual Representations in Vision Backbones and VLM Encoders via Bottom-Up and Top-Down Feature Search"
title_zh: Bodhi VLM：通过自底向上与自顶向下特征搜索实现视觉骨干网络与 VLM 编码器中层级视觉表示的隐私对齐建模
authors: "Bo Ma, Wei Qi Yan, Jinsong Wu"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13728v2"
tags: ["query:ppml"]
score: 8.0
evidence: VLM中分层神经表示的隐私对齐建模
tldr: 本研究提出Bodhi VLM框架，旨在解决视觉骨干网络和多模态大模型（VLM）中分层视觉表示的隐私对齐建模问题。该框架通过自底向上（BUA）和自顶向下（TDA）策略定位多尺度特征中的敏感区域，并利用期望最大化隐私评估（EMPA）模块生成可解释的预算对齐信号。实验验证了其在目标检测器和VLM视觉编码器上的有效性，为隐私保护学习系统提供了一种可学习且可解释的建模视角。
selection_source: fresh_fetch
motivation: 现有的隐私保护系统在分层视觉表示中注入噪声，但缺乏一种可解释且通用的方法来建模这些扰动与隐私预算之间的对齐关系。
method: 提出Bodhi VLM框架，结合聚类技术、BUA/TDA特征搜索策略以及EMPA评估模块，实现对分层神经表示的隐私对齐建模。
result: 在YOLO、CLIP、LLaVA等多种模型上的实验表明，BUA和TDA策略表现出一致的偏差趋势，且EMPA能提供稳定的对齐信号。
conclusion: 该工作为分层表示的隐私对齐提供了一个可学习且可解释的建模视角，超越了传统的后期审计方法。
---

## 摘要
保护隐私的学习系统通常在层级视觉表示中注入噪声；一个核心挑战是如何以一种可解释且适用于视觉骨干网络和视觉语言模型（VLM）的方式，对这些扰动如何与声明的隐私预算对齐进行建模。我们提出了 Bodhi VLM，这是一个针对层级神经表示的隐私对齐建模框架：它 (1) 通过基于 NCP 和 MDAV 的聚类将敏感概念与逐层分组联系起来；(2) 在多尺度表示（如特征金字塔或视觉编码器层）上使用自底向上 (BUA) 和自顶向下 (TDA) 策略定位敏感特征区域；(3) 使用期望最大化隐私评估 (EMPA) 模块，通过将拟合的敏感特征分布与评估者指定的参考分布（如尺度为 c/ε 的拉普拉斯或高斯分布）进行比较，生成可解释的预算对齐信号。输出是相对于参考分布的，并非正式的差分隐私估计器。我们在层级特征结构上形式化了 BUA/TDA，并在目标检测器（YOLO、PPDPTS、DETR）以及 VLM 的视觉编码器（CLIP、LLaVA、BLIP）上验证了该框架。BUA 和 TDA 产生了相当的偏差趋势；在报告的设置下，EMPA 提供了稳定的对齐信号。我们与通用的差异基准（卡方、K-L、MMD）以及任务相关的基准（MomentReg、NoiseMLE、Wass-1）进行了比较。结果以多个种子的均值±标准差形式报告，置信区间见补充材料。这项工作为隐私对齐的层级表示提供了一个可学习、可解释的建模视角，而不仅仅是事后审计。源代码：https://github.com/mabo1215/bodhi-vlm.git

## Abstract
Learning systems that preserve privacy often inject noise into hierarchical visual representations; a central challenge is to \emph{model} how such perturbations align with a declared privacy budget in a way that is interpretable and applicable across vision backbones and vision--language models (VLMs). We propose \emph{Bodhi VLM}, a \emph{privacy-alignment modeling} framework for \emph{hierarchical neural representations}: it (1) links sensitive concepts to layer-wise grouping via NCP and MDAV-based clustering; (2) locates sensitive feature regions using bottom-up (BUA) and top-down (TDA) strategies over multi-scale representations (e.g., feature pyramids or vision-encoder layers); and (3) uses an Expectation-Maximization Privacy Assessment (EMPA) module to produce an interpretable \emph{budget-alignment signal} by comparing the fitted sensitive-feature distribution to an evaluator-specified reference (e.g., Laplace or Gaussian with scale $c/ε$). The output is reference-relative and is \emph{not} a formal differential-privacy estimator. We formalize BUA/TDA over hierarchical feature structures and validate the framework on object detectors (YOLO, PPDPTS, DETR) and on the \emph{visual encoders} of VLMs (CLIP, LLaVA, BLIP). BUA and TDA yield comparable deviation trends; EMPA provides a stable alignment signal under the reported setups. We compare with generic discrepancy baselines (Chi-square, K-L, MMD) and with task-relevant baselines (MomentReg, NoiseMLE, Wass-1). Results are reported as mean$\pm$std over multiple seeds with confidence intervals in the supplementary materials. This work contributes a learnable, interpretable modeling perspective for privacy-aligned hierarchical representations rather than a post hoc audit only. Source code: \href{https://github.com/mabo1215/bodhi-vlm.git}{Bodhi-VLM GitHub repository}

---

## 论文详细总结（自动生成）

以下是对论文《Bodhi VLM: Privacy-Alignment Modeling for Hierarchical Visual Representations in Vision Backbones and VLM Encoders via Bottom-Up and Top-Down Feature Search》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在隐私保护的学习系统中，通常会向层级视觉表示（如特征金字塔或视觉编码器层）注入噪声。然而，如何建模并解释这些层级扰动与声明的隐私预算（$\epsilon$）之间的对齐关系，是一个极具挑战性的问题。
*   **研究动机**：现有的隐私审计方法多为事后（post hoc）分析，缺乏一种能够跨视觉骨干网络和视觉语言模型（VLM）通用、且具备可解释性的层级神经表示建模框架。
*   **整体含义**：Bodhi VLM 并非为了提供正式的差分隐私（DP）证明，而是提供一个可学习、可解释的视角，用于评估观察到的隐私噪声是否与预设的预算参考分布相一致。

### 2. 论文提出的方法论
Bodhi VLM 框架由三个核心组件构成：
*   **敏感特征定位策略**：
    *   **自底向上策略 (BUA)**：从底层开始，利用 **NCP (归一化确定性惩罚)** 和 **MDAV (微聚合)** 聚类技术，逐层将特征划分为敏感组（$G^{(s)}_i$）和非敏感组（$G^{(n)}_i$）。
    *   **自顶向下策略 (TDA)**：从高层语义出发向下遍历，通过跨层链接和集合交集操作，将高层敏感语义追溯并细化到低层特征。
*   **核心算法/公式**：
    *   **NCP 惩罚**：用于衡量组内泛化程度，通过计算特征维度范围的加权和来评估聚类质量。
    *   **EMPA (期望最大化隐私评估)**：这是该框架的关键。它使用高斯混合模型 (GMM) 拟合敏感特征分布，并通过 EM 算法迭代更新参数。
    *   **预算对齐信号 (BAS)**：通过计算拟合分布参数与评估者指定的参考分布（如拉普拉斯分布）之间的 $L_2$ 距离，输出一个参考相对的差异得分。

### 3. 实验设计
*   **数据集**：
    *   **MOT20**：用于多目标检测场景。
    *   **COCO**：用于通用目标检测和 VLM 视觉编码器评估。
    *   **合成层级特征**：用于验证协议的受控实验。
*   **评估对象 (Backbones)**：
    *   **检测器**：YOLOv4, PPDPTS, DETR。
    *   **VLM 视觉编码器**：CLIP (ViT-B/32), LLaVA-1.5 (7B), BLIP (ResNet-50)。
*   **对比基准 (Benchmarks)**：
    *   **通用分布度量**：卡方检验 (Chi-square)、K-L 散度、最大均值差异 (MMD)。
    *   **任务相关基准**：MomentReg (矩回归)、NoiseMLE (最大似然估计)、Wass-1 (一阶沃瑟斯坦距离)。
    *   **VLM 隐私方法对比**：ViP, DP-Cap, DP-MTV, VisShield（定性对比范式）。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 **NVIDIA GPU** 进行实验，并基于 PyTorch 框架实现。
*   **缺失信息**：文中**未明确说明**具体的 GPU 型号（如 A100 或 V100）、具体的 GPU 数量以及模型训练/评估的总时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同模型（检测器与 VLM）进行了多组实验。
    *   包含针对 BUA、TDA 和 EMPA 组件的**消融实验**。
    *   使用了 5 个不同的随机种子进行重复实验，并报告了均值和标准差。
*   **充分性与客观性**：实验覆盖了从合成数据到真实复杂模型（如 LLaVA）的跨度。通过引入 95% 置信区间（见补充材料）和多种统计基准对比，实验设计较为严谨、客观。

### 6. 论文的主要结论与发现
*   **策略一致性**：BUA 和 TDA 在定位敏感特征和反映偏差趋势上表现相当，BUA 对噪声水平略微更敏感。
*   **EMPA 的稳定性**：在多种检测器和 VLM 架构下，EMPA 能够提供稳定的预算对齐信号，优于传统的 PSNR 或 SIFT 等度量指标。
*   **结构化优势**：相比于随机划分特征，基于 BUA/TDA 的结构化分组能产生更具解释性和稳定性的隐私偏差评估。

### 7. 优点
*   **架构无关性**：无需修改底层模型架构即可应用于 ViT 或 CNN 等多种视觉骨干网络。
*   **可解释性强**：通过层级搜索和分布拟合，将抽象的隐私预算转化为直观的特征层级偏差信号。
*   **多模态潜力**：成功将隐私评估扩展到了当前主流的视觉语言模型（VLM）领域。

### 8. 不足与局限
*   **参考依赖性**：评估结果是“参考相对”的，如果评估者指定的参考分布与实际部署的机制不符，得分将包含模型失配误差。
*   **模态局限**：目前仅验证了 VLM 的**视觉塔 (Vision Tower)** 部分，尚未涵盖全路径的跨模态融合和语言解码阶段。
*   **敏感集定义**：敏感概念（如“人”、“车”）仍依赖于人工定义或预设规则，缺乏自动化的敏感性发现机制。
*   **非正式证明**：该框架输出的是经验性的对齐信号，不能替代正式的差分隐私数学证明。

（完）
