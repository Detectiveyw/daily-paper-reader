---
title: "FedPBS: Proximal-Balanced Scaling Federated Learning Model for Robust Personalized Training for Non-IID Data"
title_zh: FedPBS：针对非独立同分布（Non-IID）数据的鲁棒个性化训练的近端平衡缩放联邦学习模型
authors: "Eman M. AbouNassara, Amr Elshalla, Sameh Abdulah"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13909v1"
tags: ["query:ppml"]
score: 8.0
evidence: 保护本地数据隐私且鲁棒的联邦学习算法
tldr: 联邦学习在保护隐私的同时面临数据异构和客户端参与不均的挑战。本文提出FedPBS算法，融合了FedBS的动态批大小缩放与FedProx的近端项修正。该方法根据客户端资源动态调整批大小，并对小批次客户端实施近端校正以减少模型分歧。实验证明，FedPBS在CIFAR-10等数据集的非独立同分布场景下，比现有主流方法具有更优的性能和更稳定的收敛性。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中因统计异构性和客户端参与度不均导致的模型收敛困难及性能下降问题。
method: 结合动态批大小调整与近端修正技术，通过平衡客户端贡献并稳定局部更新来减少与全局模型的偏差。
result: 在CIFAR-10和UCI-HAR等数据集的极端非独立同分布实验中，FedPBS的性能一致优于FedBS、MOON和FedProx等基准方法。
conclusion: FedPBS通过创新的缩放与修正机制，在高度异构的联邦学习环境中实现了鲁棒的性能提升和稳定的收敛。
---

## 摘要
联邦学习（FL）使一组分布式客户端能够在保护本地数据隐私的同时共同训练机器学习模型，这使其在医疗保健、金融、移动出行和智慧城市系统等应用中极具吸引力。然而，联邦学习面临着若干挑战，包括统计异构性和客户端参与不均衡，这些挑战可能会降低收敛速度和模型质量。在这项工作中，我们提出了 FedPBS，这是一种结合了 FedBS 和 FedProx 互补思想的联邦学习算法，旨在解决这些挑战。FedPBS 根据客户端资源动态调整批量大小，以支持平衡且可扩展的参与，并有选择地对小批量客户端应用近端修正，以稳定本地更新并减少与全局模型的偏差。在 CIFAR-10 和 UCI-HAR 等基准数据集上，在高度非独立同分布（non-IID）设置下的实验表明，FedPBS 始终优于包括 FedBS、FedGA、MOON 和 FedProx 在内的最先进方法。结果表明，在极端数据异构性下，该方法具有鲁棒的性能提升，平滑的损失曲线表明在各种联邦环境中均具有稳定的收敛性。在严重的非独立同分布条件下，FedPBS 在 UCI-HAR 和 CIFAR-10 数据集上始终优于最先进的联邦学习基准模型，同时保持了稳定可靠的收敛。

## Abstract
Federated learning (FL) enables a set of distributed clients to jointly train machine learning models while preserving their local data privacy, making it attractive for applications in healthcare, finance, mobility, and smart-city systems. However, FL faces several challenges, including statistical heterogeneity and uneven client participation, which can degrade convergence and model quality. In this work, we propose FedPBS, an FL algorithm that couples complementary ideas from FedBS and FedProx to address these challenges. FedPBS dynamically adapts batch sizes to client resources to support balanced and scalable participation, and selectively applies a proximal correction to small-batch clients to stabilize local updates and reduce divergence from the global model. Experiments on benchmarking datasets such as CIFAR-10 and UCI-HAR under highly non-IID settings demonstrate that FedPBS consistently outperforms state-of-the-art methods, including FedBS, FedGA, MOON, and FedProx. The results demonstrate robust performance gains under extreme data heterogeneity, with smooth loss curves indicating stable convergence across diverse federated environments. FedPBS consistently outperforms state-of-the-art federated learning baselines on UCI-HAR and CIFAR-10 under severe non-IID conditions while maintaining stable and reliable convergence.

---

## 论文详细总结（自动生成）

这是一份关于论文《FedPBS: Proximal-Balanced Scaling Federated Learning Model for Robust Personalized Training for Non-IID Data》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：联邦学习（FL）在实际应用中面临两大核心挑战：**统计异构性**（数据非独立同分布，Non-IID）和**系统异构性**（客户端计算资源和网络稳定性不一）。
*   **核心问题**：传统的 FedAvg 在 Non-IID 数据下容易产生“客户端漂移”，导致收敛缓慢或模型性能下降。现有的改进方案如 FedProx（引入近端项）虽然增加了稳定性，但对所有客户端统一施加正则化会拖慢优化速度；而 FedBS（基于批次缩放）虽然能识别不稳定客户端，但缺乏对这些客户端更新偏差的补偿机制。
*   **整体含义**：本文提出 **FedPBS** 框架，旨在通过“选择性正则化”和“动态批次缩放”的结合，在保证训练效率的同时，增强模型在极端异构环境下的鲁棒性和个性化能力。

### 2. 论文提出的方法论：FedPBS
FedPBS 耦合了 FedBS 的批次感知缩放和 FedProx 的近端修正思想，核心流程如下：
*   **客户端筛选与评估**：每个客户端计算其局部梯度的方差（Variance）和批次大小（Batch Size）。
*   **稳定更新重加权（SUR）**：服务器根据方差和重要性因子对客户端进行排名，识别出“脆弱客户端”（高方差或小批次）和“稳定客户端”。
*   **选择性近端修正（核心创新）**：
    *   对于**稳定客户端**：执行标准的局部随机梯度下降（SGD），以保持优化速度。
    *   对于**脆弱客户端**：在局部目标函数中引入**近端项（Proximal Term）**，限制局部更新偏离全局模型的程度，从而稳定训练。
*   **软聚合技术（SAT）**：服务器在聚合时，不是简单平均，而是根据客户端的方差动态调整权重，降低不稳定更新对全局模型的影响。

### 3. 实验设计
*   **数据集**：
    *   **UCI-HAR**：人类活动识别数据集（传感器数据），具有天然的个体差异性。
    *   **CIFAR-10**：图像分类基准数据集，通过 Dirichlet 分布人工构建 Non-IID 场景。
*   **基准方法（Benchmarks）**：对比了 FedAvg、FedProx、FedBS、FedGA（贪婪聚合）和 MOON（对比学习联邦）。
*   **场景设置**：通过调整 Dirichlet 分布的浓度参数 $\alpha$（从 0.01 到 100）来模拟从“极端异构”到“趋于同分布”的各种环境。

### 4. 资源与算力
*   **算力说明**：文中提到实验是在**单台 GPU** 上进行的，通过多进程环境模拟 $Q$ 个客户端的并行训练。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号（如 RTX 3090 或 A100）、具体的训练总时长以及内存消耗等硬件细节。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对两个数据集分别进行了多组对比实验。
    *   在 UCI-HAR 上测试了 9 种不同的 $\alpha$ 值，涵盖了极广的异构梯度。
    *   提供了详细的准确率对比表和损失函数收敛曲线图。
*   **充分性评价**：实验设计较为充分，通过改变 $\alpha$ 值展示了算法在不同异构程度下的表现。对比的方法涵盖了近年来主流的正则化、聚合优化和对比学习类 FL 算法，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：在极端 Non-IID 场景下（如 $\alpha=0.2$），FedPBS 在 UCI-HAR 上达到 94.0% 的准确率，显著高于 FedProx (91.8%) 和 FedBS (87.0%)。
*   **收敛稳定性**：FedPBS 的损失曲线比其他方法更平滑、单调递减，没有出现明显的震荡或过拟合现象，证明了选择性近端修正的有效性。
*   **鲁棒性**：随着数据异构程度增加，FedPBS 与其他基准方法的性能差距进一步拉大，显示出其在恶劣数据环境下的强健性。

### 7. 优点与亮点
*   **混合策略的灵活性**：不同于 FedProx 的“一刀切”正则化，FedPBS 只针对不稳定的客户端进行修正，平衡了“全局一致性”与“局部优化速度”。
*   **资源感知**：将批次大小作为筛选条件之一，考虑了客户端的系统异构性（计算能力差异）。
*   **实验结果显著**：在高度倾斜的数据分布下，准确率提升明显，且收敛过程非常稳健。

### 8. 不足与局限
*   **超参数敏感性**：算法引入了多个阈值（如方差阈值 $V_{th}$、批次阈值 $B_{th}$ 和近端系数 $\mu$），文中未深入讨论这些超参数在不同任务中的自适应调整方法。
*   **理论证明缺失**：论文主要侧重于实证研究，缺乏关于该混合策略收敛边界的严格数学证明。
*   **通信开销讨论不足**：虽然提到提高了效率，但未详细量化在引入方差计算和选择性修正后，客户端与服务器之间的额外通信负担或计算延迟。
*   **硬件细节模糊**：缺乏具体的算力资源描述，不利于其他研究者进行复现和性能对标。

（完）
