---
title: "SFedHIFI: Fire Rate-Based Heterogeneous Information Fusion for Spiking Federated Learning"
title_zh: SFedHIFI：基于脉冲发放率的脉冲联邦学习异构信息融合
authors: "Ran Tao, Qiugang Zhan, Shantian Yang, Xiurui Xie, Qi Tian, Guisong Liu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14956v1"
tags: ["query:ppml"]
score: 8.0
evidence: 异构脉冲联邦学习框架
tldr: 针对脉冲联邦学习（SFL）中模型同质化要求限制资源受限客户端参与的问题，本文提出SFedHIFI框架。该框架利用基于脉冲发放率的异构信息融合，通过通道级矩阵分解为不同客户端部署自适应规模的SNN模型，并实现跨尺度的模型聚合。实验证明，该方法在保持高能效的同时，有效提升了异构SFL系统的性能，优于现有基准。
selection_source: fresh_fetch
motivation: 现有的脉冲联邦学习要求模型同质，导致计算资源有限的客户端无法参与，难以应对现实中的系统异构性。
method: 提出SFedHIFI框架，采用通道级矩阵分解部署自适应规模模型，并利用异构信息融合模块实现不同宽度模型间的跨尺度聚合。
result: 在三个公共数据集上的实验表明，SFedHIFI性能优于所有基准方法，且相比人工神经网络（ANN）联邦学习显著降低了能耗。
conclusion: SFedHIFI成功实现了高效的异构脉冲联邦学习，在资源受限环境下展现了极佳的灵活性和能效优势。
---

## 摘要
脉冲联邦学习（SFL）因脉冲神经网络（SNNs）的高能效性而得到了广泛研究。然而，现有的 SFL 方法要求模型同质性，并假设所有客户端都拥有充足的计算资源，导致一些资源受限的客户端被排除在外。为了解决现实场景中普遍存在的系统异构性，构建允许客户端根据其本地资源自适应部署不同规模模型的异构 SFL 系统至关重要。为此，我们提出了 SFedHIFI，这是一种具有基于脉冲发放率的异构信息融合的新型脉冲联邦学习框架。具体而言，SFedHIFI 采用通道级矩阵分解，在异构资源客户端上部署具有自适应复杂度的 SNN 模型。在此基础上，所提出的异构信息融合模块实现了不同宽度模型之间的跨尺度聚合，从而增强了对多样化本地知识的利用。在三个公开基准数据集上的广泛实验表明，SFedHIFI 能够有效实现异构 SFL，且性能始终优于所有三种基准方法。与基于人工神经网络（ANN）的联邦学习相比，它在仅损失极小准确性的情况下实现了显著的节能。

## Abstract
Spiking Federated Learning (SFL) has been widely studied with the energy efficiency of Spiking Neural Networks (SNNs). However, existing SFL methods require model homogeneity and assume all clients have sufficient computational resources, resulting in the exclusion of some resource-constrained clients. To address the prevalent system heterogeneity in real-world scenarios, enabling heterogeneous SFL systems that allow clients to adaptively deploy models of different scales based on their local resources is crucial. To this end, we introduce SFedHIFI, a novel Spiking Federated Learning framework with Fire Rate-Based Heterogeneous Information Fusion. Specifically, SFedHIFI employs channel-wise matrix decomposition to deploy SNN models of adaptive complexity on clients with heterogeneous resources. Building on this, the proposed heterogeneous information fusion module enables cross-scale aggregation among models of different widths, thereby enhancing the utilization of diverse local knowledge. Extensive experiments on three public benchmarks demonstrate that SFedHIFI can effectively enable heterogeneous SFL, consistently outperforming all three baseline methods. Compared with ANN-based FL, it achieves significant energy savings with only a marginal trade-off in accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：SFedHIFI —— 基于脉冲发放率的异构脉冲联邦学习信息融合

#### 1. 核心问题与研究动机
*   **研究背景**：脉冲联邦学习（SFL）结合了脉冲神经网络（SNN）的高能效和联邦学习（FL）的隐私保护特性，非常适合资源受限的边缘设备。
*   **核心问题**：现有的 SFL 方法通常要求所有客户端使用**同质化（相同架构）**的模型。然而，现实中的边缘设备计算资源差异巨大（系统异构性），强制统一模型会导致资源贫瘠的设备无法参与，或者资源丰富的设备算力浪费。
*   **挑战**：直接对 SNN 进行剪枝以适应异构设备会导致严重的**信息丢失**，因为 SNN 的脉冲表示具有天然的稀疏性和二值性，简单的参数聚合难以捕捉跨尺度的知识。

#### 2. 方法论：SFedHIFI 框架
SFedHIFI 提出了一种支持异构模型宽度的 SFL 框架，核心包含以下技术：
*   **通道级矩阵分解（Channel-wise Matrix Decomposition）**：
    *   将卷积层权重 $W$ 分解为统一的**共享基矩阵 $B$**（代表全局通道空间）和客户端特定的**尺度因子矩阵 $M$**（编码异构通道维度）。
    *   $B$ 矩阵在所有客户端间聚合以捕获通用知识，$M$ 矩阵则根据模型规模进行分类聚合。
*   **基于脉冲发放率的异构信息融合（HIFI 模块）**：
    *   **识别关键层**：利用 SNN 的生物特性——**脉冲发放率（Firing Rate）**来衡量各层的信息密度。发放率最高的层被认为含有最丰富的特征。
    *   **跨尺度融合**：对识别出的高信息密度层，利用 **Tucker 分解**将不同尺度的 $M$ 矩阵进一步解构为核心张量和因子矩阵，通过聚合核心张量实现跨尺度的知识共享。
*   **本地优化目标**：采用时间高效训练（TET）损失，并引入**正交正则化项**约束基矩阵 $B$，以增强其知识表示能力和全局泛化性。

#### 3. 实验设计
*   **数据集**：Fashion-MNIST、CIFAR-10、CIFAR-100。
*   **数据分布场景**：包含 IID（独立同分布）和 Non-IID（非独立同分布，使用 Dirichlet 分布 $\alpha=0.5, 0.3$ 模拟）。
*   **模型规模**：设置了四种模型宽度尺度：0.25×、0.5×、0.75×、1.0×。
*   **对比基准（Benchmark）**：
    *   **FedAVG、FedProx、FedNova**：这些经典算法被适配到 SFL 环境中，但在异构设定下只能在相同规模的模型间进行聚合。
    *   **ANN 对比**：与基于人工神经网络（ANN）的异构 FL 进行性能与能效对比。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 `Spikejelly` 框架，SNN 采用 Spiking-ResNet18 架构，时间步 $T=10$。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个数据集上进行了全方位的基准测试。
    *   针对 HIFI 模块进行了消融实验（对比“无融合”、“固定层融合”、“随机层融合”）。
    *   进行了详细的能效分析实验。
*   **充分性与客观性**：实验覆盖了从简单到复杂的任务，并考虑了不同程度的数据异构性。通过消融实验证明了“脉冲发放率”作为筛选指标的科学性，对比实验遵循了联邦学习中常见的随机选择客户端机制，整体设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先**：SFedHIFI 在所有数据集和模型尺度上均优于基准方法。在复杂的 CIFAR-100 Non-IID 场景下，准确率提升最高达 19%。
*   **跨尺度增益**：通过 HIFI 模块，小规模模型能够从大规模模型中获取知识，显著缓解了因剪枝带来的性能下降。
*   **能效优势**：与同等规模的 ANN 相比，SFedHIFI 在 Fashion-MNIST 上实现了约 **43.74% 的能耗降低**，而准确率损失不到 1%。
*   **发放率相关性**：实验证实了脉冲发放率能有效反映 SNN 层的特征重要性，基于此的融合比随机或固定层融合更有效。

#### 7. 优点
*   **生物启发式创新**：巧妙利用 SNN 特有的“脉冲发放率”来指导异构参数融合，解决了 SNN 稀疏性导致的信息丢失问题。
*   **灵活性高**：允许客户端根据自身算力动态选择模型宽度，提高了 SFL 在真实边缘环境中的实用性。
*   **能效平衡**：在保持 SNN 低功耗优势的同时，通过矩阵分解技术弥补了异构带来的精度损失。

#### 8. 不足与局限
*   **异构维度单一**：目前主要关注**模型宽度**（通道数）的异构，未深入探讨模型深度（层数）或异构网络架构（如不同算子）的情况。
*   **计算开销**：在服务端进行 Tucker 分解和重构会增加额外的计算负担，论文未详细量化服务器端的开销。
*   **超参数敏感性**：矩阵分解中的秩（Rank）选择以及正交正则化的平衡因子 $\lambda$ 可能需要针对不同任务进行精细调优。

（完）
