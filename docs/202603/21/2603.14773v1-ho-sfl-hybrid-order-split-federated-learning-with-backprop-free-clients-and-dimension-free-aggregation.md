---
title: "HO-SFL: Hybrid-Order Split Federated Learning with Backprop-Free Clients and Dimension-Free Aggregation"
title_zh: HO-SFL：具有无反向传播客户端和无维度聚合的混合阶拆分联邦学习
authors: "Qiyuan Chen, Xian Wu, Yi Wang, Xianhao Chen"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14773v1"
tags: ["query:ppml"]
score: 8.0
evidence: 针对大模型的混合阶拆分联邦学习
tldr: 针对边缘设备微调大模型时反向传播（BP）内存消耗大的问题，本文提出混合阶拆分联邦学习（HO-SFL）。该方法通过拉格朗日框架解耦优化过程，使服务端执行一阶更新而客户端执行零阶优化，从而免除客户端BP并降低通信开销。理论与实验证明，HO-SFL在显著降低内存和通信成本的同时，实现了与一阶方法相当的收敛速度。
selection_source: fresh_fetch
motivation: 边缘设备在联邦学习中面临反向传播带来的巨大内存压力，而现有的零阶优化方法虽然省内存但收敛速度极慢。
method: 提出HO-SFL框架，利用拉格朗日公式将优化解耦，让服务端进行精确的一阶更新，客户端进行内存高效的零阶优化及维度无关的聚合。
result: 实验表明HO-SFL在视觉和语言任务上均达到了与一阶基准相当的收敛速度，并大幅减少了通信量和客户端内存占用。
conclusion: HO-SFL成功解决了零阶优化在联邦学习中的收敛瓶颈，为资源受限边缘设备上的大模型微调提供了一种高效且低开销的方案。
---

## 摘要
在边缘设备上微调大模型受到联邦学习和拆分学习等标准框架中高内存消耗的反向传播（BP）的严重阻碍。虽然用零阶优化替代反向传播可以显著减少内存占用，但通常会导致收敛速度大幅下降。为了解决这一困境，我们提出了混合阶拆分联邦学习（HO-SFL）。通过在拉格朗日框架内重新表述拆分学习过程，HO-SFL 解耦了优化景观：服务器执行精确的一阶更新（即反向传播），而客户端则进行内存高效的零阶优化。这种混合设计不仅消除了客户端对反向传播的需求，还实现了无维度模型聚合，从而大幅降低了通信成本。至关重要的是，我们提供了理论收敛性分析，证明了 HO-SFL 缓解了零阶优化中与维度相关的收敛减速问题，实现了与一阶方法相当的收敛速度。在视觉和语言模态任务上的广泛实验验证了 HO-SFL 在显著降低通信成本和客户端内存占用的同时，实现了与一阶基准相当的收敛速度。

## Abstract
Fine-tuning large models on edge devices is severely hindered by the memory-intensive backpropagation (BP) in standard frameworks like federated learning and split learning. While substituting BP with zeroth-order optimization can significantly reduce memory footprints, it typically suffers from prohibitively degraded convergence speed. To resolve this dilemma, we propose Hybrid-Order Split Federated Learning (HO-SFL). By reformulating the split learning process within a Lagrangian framework, HO-SFL decouples the optimization landscape: The server performs precise first-order updates (i.e., BP), whereas clients conduct memory-efficient zeroth-order optimization. This hybrid design not only eliminates the need for client-side BP but also enables dimension-free model aggregation, drastically lowering communication costs. Crucially, we provide a theoretical convergence analysis, demonstrating that HO-SFL mitigates the dimension-dependent convergence slowdown of zeroth-order optimization, achieving a convergence rate comparable to first-order methods. Extensive experiments on tasks across vision and language modalities validate that HO-SFL achieves convergence speeds comparable to first-order baselines while significantly reducing communication costs and client memory footprints.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **HO-SFL**（混合阶拆分联邦学习）的新型框架，旨在解决在资源受限的边缘设备上微调大语言模型（LLM）时面临的内存瓶颈和通信开销问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在边缘设备上微调大模型（如LLM）具有保护隐私和个性化适配的优势，但面临两大挑战：
    1.  **内存瓶颈**：标准的一阶优化（基于反向传播 BP）需要存储中间层激活值，其内存需求远超边缘硬件能力。
    2.  **收敛困境**：现有的零阶优化（ZO）虽然省内存（仅需前向传播），但在高维参数空间中收敛速度极慢，且通信开销依然巨大。
*   **核心问题**：能否在实现“无反向传播（BP-free）”客户端训练的同时，避免零阶优化在高维空间下的收敛减速？

### 2. 论文提出的方法论：HO-SFL 框架
*   **核心思想**：利用**拉格朗日框架（Lagrangian Framework）**将拆分学习的优化目标解耦。服务器端执行精确的一阶更新（BP），而客户端执行内存高效的零阶更新（ZO）。
*   **关键技术细节**：
    *   **优化解耦**：将客户端输出与服务器输入的对齐视为等式约束，通过拉格朗日乘子（即激活值的梯度 $\lambda$）引导客户端优化。
    *   **混合阶更新**：服务器计算损失函数对输入激活值的梯度并传回客户端；客户端利用该梯度定义局部代理目标，通过多次带扰动的前向传播（ZO）来估计更新方向。
    *   **无维度聚合（Dimension-Free Aggregation）**：利用共享随机种子生成扰动向量，客户端只需上传少量的标量投影值（Scalar Projections）而非高维模型参数，极大地降低了上行通信带宽。
    *   **延迟隐藏**：客户端的多次零阶扰动计算可以与服务器的 BP 过程及通信过程并行重叠，从而降低端到端延迟。

### 3. 实验设计
*   **数据集与场景**：
    *   **视觉任务**：CIFAR-10 和 CIFAR-100，涵盖 IID（独立同分布）和 Non-IID（非独立同分布，Dirichlet $\alpha=1$）设置。
    *   **语言任务**：GLUE 基准测试中的 SST-2、RTE、WSC 任务。
*   **模型（Benchmark）**：ResNet-18（视觉）；OPT-125M、Gemma-3-270M、LLaMA-3.2-1B（语言，配合 LoRA 微调）。
*   **对比方法（Baselines）**：
    *   **一阶方法**：标准 SFL、SplitLoRA、FSL-SAGE。
    *   **零阶方法**：ZO-SFL、MU-SplitFed。

### 4. 资源与算力
*   **服务器端**：使用了云级 **NVIDIA A100 GPU**（312 TFLOPS FP16）。
*   **客户端（模拟）**：模拟了具有 **2.0 TFLOPS (FP16)** 算力的边缘 AI 加速器（类似于 NVIDIA Jetson Orin Nano 或旗舰手机 NPU）。
*   **网络环境**：模拟了标准 5G 环境（上行 30 Mbps，下行 200 Mbps，RTT 30ms）。
*   **训练时长**：文中未明确给出总训练小时数，但设定了统一的终止标准（视觉任务 160k 样本，语言任务 80k 样本）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了视觉和语言两大模态，使用了从 125M 到 1B 参数量的多个模型。
*   **消融实验**：针对客户端模型深度（切分点）、扰动次数 $P$、平滑参数 $\mu$ 进行了详细的消融研究。
*   **充分性与公平性**：实验考虑了 Non-IID 数据分布，并使用“已处理样本数”而非“通信轮数”作为基准指标，确保了不同通信频率方法之间比较的公平性。实验重复了 10 个随机种子以保证结果的统计显著性。

### 6. 论文的主要结论与发现
*   **收敛速度**：HO-SFL 实现了与一阶方法（SFL/SplitLoRA）相当的收敛速度，显著优于纯零阶方法。
*   **理论证明**：证明了其收敛率受限于客户端维度 $d_c$ 而非全模型维度 $d$，即 $O(\sqrt{d_c/PT})$，有效缓解了维度灾难。
*   **资源效率**：
    *   **内存**：客户端内存占用降至“仅推理”水平（如 LLaMA-3.2-1B 从 8.78GB 降至 4.14GB）。
    *   **通信**：通过无维度聚合，通信开销比标准 SFL 降低了约 40%-80%。

### 7. 优点
*   **理论与实践结合**：通过拉格朗日对偶理论为混合阶优化提供了坚实的数学基础。
*   **极高的实用性**：解决了边缘设备微调大模型最核心的内存痛点，且不以牺牲收敛速度为代价。
*   **通信高效**：标量聚合机制使得该方法非常适合非对称带宽（上行慢、下行快）的无线网络。

### 8. 不足与局限
*   **计算量换内存**：虽然省了内存，但客户端需要进行多次（$P$ 次）前向传播来估计梯度，这增加了客户端的计算负载（尽管文中提到可以通过延迟隐藏来缓解）。
*   **超参数敏感性**：平滑参数 $\mu$ 的选择存在权衡，过大会引入偏差，过小会受限于机器浮点精度。
*   **切分点限制**：如果客户端保留的层数过多（$d_c$ 过大），零阶优化的方差仍会增加，导致性能下降。

（完）
