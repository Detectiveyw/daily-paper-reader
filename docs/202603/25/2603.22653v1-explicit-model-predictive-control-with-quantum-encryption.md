---
title: Explicit Model Predictive Control with Quantum Encryption
title_zh: 具有量子加密的显式模型预测控制
authors: "Yingjie Mi, Zihao Ren, Lei Wang, Daniel E. Quevedo, Guodong Shi"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22653v1"
tags: ["query:ppml"]
score: 8.0
evidence: 基于量子加密的多方安全计算云端控制
tldr: 本文研究了云架构下受限离散线性系统的量子加密显式模型预测控制（MPC）。通过离线求解有限时域二次MPC获得分段仿射控制器，并利用基于Bell对和量子密钥分发（QKD）生成的共享量子密钥，对传感器与执行器间的在线控制评估进行加密。该方案实现了轻量级的加密协议，在保证控制动作精确恢复的同时，显著降低了在线计算复杂度，并提升了系统数据的安全性。
selection_source: fresh_fetch
motivation: 旨在解决云端控制系统中受限线性系统的隐私保护问题，同时降低加密带来的在线计算负担。
method: 利用显式MPC的离线分段仿射解，结合基于量子纠缠和QKD生成的共享密钥对在线控制信号进行加密。
result: 实现了控制动作的精确恢复，且数值结果显示其在线计算复杂度优于传统的经典加密MPC方案。
conclusion: 该研究证明了量子加密在显式MPC架构下能有效平衡控制性能、计算效率与通信安全性。
---

## 摘要
本文研究了基于云架构的受限离散时间线性系统的量子加密显式模型预测控制（MPC）。通过离线求解有限时域二次 MPC 问题，获得分段仿射控制器。利用由贝尔态（Bell pairs）生成并受量子密钥分发保护的共享量子密钥，对传感器与执行器之间的在线控制计算进行加密。基于该架构，我们开发了一种轻量级加密显式 MPC 协议，证明了明文控制动作的精确恢复，并表征了其计算效率。数值结果表明，其在线复杂度低于传统的加密 MPC，同时从受控对象数据和控制输入的机密性方面讨论了安全性。

## Abstract
This paper studies quantum-encrypted explicit MPC for constrained discrete-time linear systems in a cloud-based architecture. A finite-horizon quadratic MPC problem is solved offline to obtain a piecewise-affine controller. Shared quantum keys generated from Bell pairs and protected by quantum key distribution are used to encrypt the online control evaluation between the sensor and actuator. Based on this architecture, we develop a lightweight encrypted explicit MPC protocol, prove exact recovery of the plaintext control action, and characterize its computational efficiency. Numerical results demonstrate lower online complexity than classical encrypted MPC, while security is discussed in terms of confidentiality of plant data and control inputs.

---

## 论文详细总结（自动生成）

以下是对论文《Explicit Model Predictive Control with Quantum Encryption》（具有量子加密的显式模型预测控制）的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着云端控制系统在智能电网、机器人和自动化领域的普及，网络通信面临着数据泄露、恶意篡改和隐私侵犯的风险。
*   **核心痛点**：
    1.  **计算开销**：现有的加密控制方案（如基于同态加密 HE 的方案）依赖复杂的大数算术运算，导致在线计算延迟高。
    2.  **量子威胁**：传统的公钥加密算法（如 RSA、Paillier）在未来量子计算机的攻击下具有脆弱性。
*   **研究目标**：提出一种基于量子密钥的轻量级加密显式 MPC 协议，旨在实现抗量子攻击的同时，降低在线计算负担并保证控制精度。

### 2. 方法论
*   **核心思想**：结合**显式 MPC（Explicit MPC）**的离线预计算特性与基于**量子密钥分发（QKD）**的指数-对数加密机制。
*   **关键技术细节**：
    *   **显式 MPC 转换**：将受限线性系统的二次规划（QP）问题离线求解，得到分段仿射（PWA）控制律：$u(k) = K(\sigma)x(k) + b(\sigma)$，其中 $\sigma$ 是状态所属的区域索引。
    *   **量子密钥生成**：传感器（S）与执行器（A）通过量子通道利用贝尔态（Bell pairs）纠缠特性生成共享的随机密钥流 $q(k)$。
    *   **加密协议（QE-MPC）**：
        1.  **传感器端**：测量状态 $x(k)$，识别区域 $\sigma$，利用密钥对 $x$ 和偏移量 $b$ 进行指数加密：$\tilde{z} = \exp(z/\beta)$。
        2.  **云端（控制器）**：在加密域执行幂运算（对应明文的乘法）：$\tilde{t} = \tilde{x}^{K}$。云端仅能看到加密数据和区域索引 $\sigma$，无法获知明文。
        3.  **执行器端**：接收加密结果，利用共享密钥进行对数运算还原线性项，并与偏移量求和，重构控制输入 $u(k)$。
*   **理论保证**：论文通过数学证明，该协议在解密后能 100% 精确恢复明文控制动作，消除了传统加密方案中的量化误差。

### 3. 实验设计
*   **实验场景**：混合电池-超级电容器电源系统的电流环路控制（电力电子能量管理标准基准）。
*   **Benchmark（基准）**：明文显式 MPC（Plaintext）。
*   **对比方法**：
    *   **Paillier 加密**（加法同态加密代表）。
    *   **RSA 加密**。
    *   **AES 加密**（对称加密代表）。
*   **评估维度**：控制跟踪误差（RMSE）、通信负载（Payload bits）、每周期执行耗时、抗最小二乘（LS）识别攻击的机密性。

### 4. 资源与算力
*   **硬件环境**：3.8 GHz AMD Ryzen 7 9700X 处理器。
*   **软件环境**：Python 实现。
*   **算力说明**：文中未提及 GPU 使用情况，因为该方案主要涉及在线代数运算而非深度学习训练。实验侧重于 CPU 上的单线程执行效率对比。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了闭环响应对比实验，验证控制效果。
    *   进行了 1000 次随机试验，评估在不同噪声（高斯、均匀、脉冲噪声）下的抗攻击能力。
    *   对比了不同精度要求（$\epsilon_q$）下的通信负载变化。
*   **充分性评价**：实验设计较为全面，不仅验证了功能（精确恢复），还从性能（速度/负载）和安全性（抗识别攻击）两个维度进行了客观对比，实验结果具有统计学意义。

### 6. 主要结论与发现
*   **精确恢复**：QE-MPC 的输入误差保持在机器浮点精度级别（约 $10^{-12}$），实现了对明文控制律的无损实现。
*   **高效性**：在相同精度下，QE-MPC 的在线计算复杂度为 $O(p^3)$（$p$ 为精度位数），远低于 Paillier 的 $O(L^3)$（$L$ 为大数模长）。实际测试中，QE-MPC 的总周期耗时比 Paillier 快约 8 倍。
*   **安全性**：在抗窃听者识别攻击实验中，QE-MPC 产生的状态误差与经典加密方案相当，有效保护了受控对象的机密性。

### 7. 优点
*   **轻量化**：避免了昂贵的大数模幂运算，非常适合计算资源受限的嵌入式控制器。
*   **抗量子性**：密钥生成基于物理定律而非数学难题，具备天然的抗量子攻击潜力。
*   **无量化损失**：相比于需要固定点编码的同态加密方案，该方法在理论上不引入额外的控制性能损失。

### 8. 不足与局限
*   **硬件门槛**：依赖量子密钥分发（QKD）基础设施，目前在工业现场大规模部署的成本和难度较高。
*   **信息泄露风险**：云端虽然无法获知数值，但仍能获知区域索引 $\sigma$。在某些极端情况下，攻击者可能通过 $\sigma$ 的切换频率推断系统的运行状态。
*   **适用范围**：目前仅针对线性受限系统（显式 MPC 适用范围），对于高度非线性或无法离线求解 PWA 律的系统，该方案难以直接应用。

（完）
