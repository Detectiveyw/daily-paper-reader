---
title: "FedPDPO: Federated Personalized Direct Preference Optimization for Large Language Model Alignment"
title_zh: FedPDPO：用于大语言模型对齐的联邦个性化直接偏好优化
authors: "Kewen Zhu, Liping Yi, Zhiming Zhao, Zhuang Qi, Han Yu, Qinghua Hu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19741v1"
tags: ["query:ppml"]
score: 8.0
evidence: 大语言模型的联邦个性化偏好对齐
tldr: 针对联邦学习中非独立同分布（non-IID）偏好数据导致的大模型对齐性能下降问题，本文提出FedPDPO框架。该框架采用LoRA参数高效微调架构，通过全局共享LoRA与个性化客户端头部、显式奖励头以及瓶颈适配器，实现了高效且个性化的偏好对齐。理论分析与实验证明，该方法在多场景下均优于现有方法，显著提升了对齐准确率。
selection_source: fresh_fetch
motivation: 传统的直接偏好优化（DPO）在联邦学习环境下难以应对高度异构的偏好数据，导致模型泛化能力受限且性能大幅下降。
method: 提出一种结合全局共享LoRA、个性化LLM头部、显式奖励头及瓶颈适配器的个性化联邦DPO框架，以平衡全局知识与局部特征。
result: "在多个偏好数据集上的实验显示，FedPDPO在域内和跨域设置下均达到SOTA性能，平均准确率提升高达4.80%。"
conclusion: FedPDPO通过创新的个性化策略有效解决了联邦偏好对齐中的数据异构挑战，为隐私保护下的大模型对齐提供了坚实的理论与实践基础。
---

## 摘要
在联邦学习（FL）中，由于偏好数据具有去中心化、隐私敏感且高度非独立同分布（non-IID）的特性，将大语言模型（LLMs）与人类偏好对齐具有挑战性。直接偏好优化（DPO）为基于人类反馈的强化学习（RLHF）提供了一种高效的替代方案，但其在联邦学习中的直接应用在非独立同分布数据下会遭受严重的性能退化，且隐式奖励的泛化能力有限。为了弥补这一差距，我们提出了 FedPDPO（联邦个性化直接偏好优化），这是一个用于大语言模型偏好对齐的个性化联邦框架。它采用参数高效微调架构，每个客户端维护一个冻结的预训练大语言模型主干，并辅以低秩自适应（LoRA）适配器，从而实现通信高效的聚合。为了解决非独立同分布的异构性，我们设计了：（1）全局共享的 LoRA 适配器与个性化的客户端特定大语言模型头部；（2）一种带有客户端特定显式奖励头部的个性化 DPO 训练策略，以补充隐式奖励并进一步缓解非独立同分布异构性；以及（3）一个瓶颈适配器来平衡全局和局部特征。我们提供了理论分析，确立了其概率基础和合理性。在多个偏好数据集上的广泛实验证明了其最先进的性能，在联邦域内和跨域设置中实现了高达 4.80% 的平均准确率提升。

## Abstract
Aligning large language models (LLMs) with human preferences in federated learning (FL) is challenging due to decentralized, privacy-sensitive, and highly non-IID preference data. Direct Preference Optimization (DPO) offers an efficient alternative to reinforcement learning with human feedback (RLHF), but its direct application in FL suffers from severe performance degradation under non-IID data and limited generalization of implicit rewards. To bridge this gap, we propose FedPDPO (Federated Personalized Direct Preference Optimization), a personalized federated framework for preference alignment of LLMs. It adopts a parameter-efficient fine-tuning architecture where each client maintains a frozen pretrained LLM backbone augmented with a Low-Rank Adaptation (LoRA) adapter, enabling communication-efficient aggregation. To address non-IID heterogeneity, we devise (1) the globally shared LoRA adapter with the personalized client-specific LLM head. Moreover, we introduce (2) a personalized DPO training strategy with a client-specific explicit reward head to complement implicit rewards and further alleviate non-IID heterogeneity, and (3) a bottleneck adapter to balance global and local features. We provide theoretical analysis establishing the probabilistic foundation and soundness. Extensive experiments on multiple preference datasets demonstrate state-of-the-art performance, achieving up to 4.80% average accuracy improvements in federated intra-domain and cross-domain settings.

---

## 论文详细总结（自动生成）

### 论文总结：FedPDPO —— 用于大语言模型对齐的联邦个性化直接偏好优化

#### 1. 核心问题与整体含义
论文探讨了在**联邦学习（FL）**环境下，如何将大语言模型（LLM）与人类偏好进行对齐。
*   **研究动机**：人类偏好数据通常分散在不同用户或机构中，具有高度的隐私敏感性。虽然直接偏好优化（DPO）比传统的RLHF更高效，但在联邦场景下直接应用DPO面临两大挑战：
    1.  **数据异构性（Non-IID）**：不同客户端的偏好分布差异巨大，导致全局模型性能严重下降。
    2.  **隐式奖励的泛化限制**：DPO依赖隐式奖励估计，在面对高度多样化的偏好分布时泛化能力较弱。
*   **核心目标**：提出一个既能保护隐私、又能应对数据异构性，且具备强泛化能力的联邦个性化偏好对齐框架。

#### 2. 方法论
论文提出了 **FedPDPO** 框架，其核心思想是结合**参数高效微调（PEFT）**与**个性化联邦学习（PFL）**。
*   **模型架构分解**：
    *   **冻结主干（Backbone）**：每个客户端保留一个预训练且冻结的LLM。
    *   **全局共享 LoRA**：通过低秩自适应（LoRA）捕获全局通用的偏好知识，并在服务器端进行聚合。
    *   **个性化双头（Dual-Head）**：每个客户端维护私有的**LLM头部**（生成Token概率）和**显式奖励头**（直接估计标量分数）。
    *   **瓶颈适配器（Bottleneck Adapter）**：位于主干与头部之间，用于融合并平衡全局特征（来自LoRA）与局部特征（来自冻结主干）。
*   **关键技术细节**：
    *   **PDPO 训练策略**：在标准DPO的隐式奖励基础上，引入显式奖励头的差值作为修正项，通过自适应缩放因子平衡两者的贡献。
    *   **交替优化**：本地训练时，先冻结LoRA训练个性化模块，再冻结个性化模块训练LoRA，实现全局协作与局部个性化的双向知识转移。
*   **理论支撑**：通过数学证明（Gumbel-Bradley-Terry 等价性），确立了显式奖励头作为加性修正项在概率模型上的合理性。

#### 3. 实验设计
*   **数据集与场景**：
    *   **域内（Intra-domain）**：IMDB（情感分析）、Code-Vulnerability-Security（代码安全）。通过奖励边界或编程语言进行 Non-IID 划分。
    *   **跨域（Cross-domain）**：WebGPT（问答）、PyDPO（Python代码）、UltraFeedback（指令遵循），每个客户端负责一个完全不同的领域。
*   **Benchmark 与对比方法**：
    *   **基线方法**：FedAvg (即 FedDPO)、Per-FedAvg、FedAMP、FedPer、FedRep。
    *   **优化目标对比**：对比了基于 PPO（需显式奖励模型）和 DPO 的不同联邦实现。
*   **评估指标**：偏好准确率（Preference Accuracy），即模型正确排序“优选回答”与“弃选回答”的比例。

#### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA GeForce RTX 3090 GPU (24 GB)** 上完成。
*   **模拟方式**：通过顺序执行多个客户端来模拟联邦学习环境。
*   **模型规模**：主要使用 **DistilGPT2** (约 82M 参数) 和 **TinyLlama-1.1B**。
*   **训练时长**：文中未明确给出总训练时长，但提到域内训练 20 轮，跨域训练 10 轮。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3、10、11 个客户端的不同规模设置。
*   **消融实验**：专门验证了“瓶颈适配器”和“显式奖励头”的必要性，结果显示移除奖励头对性能影响最大。
*   **超参数分析**：对 LoRA 秩（Rank）、奖励头维度、适配器维度、DPO 温度系数（$\beta$）进行了详尽的敏感性测试。
*   **评价**：实验设计较为全面，覆盖了从单域到跨域的多种异构场景，并使用了不同规模的基座模型，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：FedPDPO 在所有测试场景下均达到 SOTA 性能，相比最强基线，平均准确率提升了 **1.71% 至 4.80%**。
*   **应对异构性有效**：在跨域（极端 Non-IID）设置下，FedPDPO 的优势尤为明显，证明了个性化头部和显式奖励修正的有效性。
*   **收敛速度**：相比于 FedAvg 等方法，FedPDPO 在联邦轮次中表现出更快的收敛速度和更高的稳定性。
*   **DPO 优于 PPO**：在联邦环境下，基于 DPO 的方法普遍比基于 PPO 的方法更高效且稳定。

#### 7. 优点（亮点）
*   **架构创新**：首次将个性化联邦学习（PFL）与 DPO 结合，通过“全局 LoRA + 局部双头”解决了联邦对齐中的核心矛盾。
*   **双重奖励机制**：引入显式奖励头修正隐式奖励，增强了模型在分布偏移下的鲁棒性。
*   **通信高效**：仅聚合 LoRA 参数（不到总参数量的 1%），极大地降低了联邦学习的带宽需求。
*   **理论与实践结合**：不仅有显著的实验提升，还提供了严谨的概率论证明。

#### 8. 不足与局限
*   **模型规模限制**：实验主要集中在小型模型（1.1B 以下），尚未在 7B、70B 等更大规模的 LLM 上验证其扩展性。
*   **客户端数量**：实验中的客户端数量（最多 11 个）相对较少，在大规模超大规模联邦网络（如成千上万个移动设备）中的表现尚待观察。
*   **计算开销**：虽然通信高效，但每个客户端需要维护额外的瓶颈适配器和双头结构，增加了本地的存储和计算负担。

（完）
