---
title: Asymptotically Ideal Conjunctive Hierarchical Secret Sharing Scheme Based on CRT for Polynomial Ring
title_zh: 基于多项式环中国剩余定理的渐近理想合取分级秘密共享方案
authors: "Jian Ding, Cheng Wang, Hongju Li, Cheng Shu, Haifeng Yu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22001v1"
tags: ["query:fss-llm"]
score: 8.0
evidence: 基于中国剩余定理的分层秘密共享方案
tldr: 针对现有基于中国剩余定理（CRT）的合取层次秘密共享（CHSS）方案存在安全性漏洞或信息率低的问题，本文提出了一种基于多项式环CRT和单向函数的新型CHSS方案。该方案具有计算安全性，支持灵活的份额大小，且在份额等大时信息率趋近于1，实现了渐近理想的性能，有效提升了层次化秘密管理的效率与安全性。
selection_source: fresh_fetch
motivation: 现有的基于CRT的合取层次秘密共享方案在安全性和信息率（通常低于1/2）方面存在明显不足。
method: 利用多项式环上的中国剩余定理结合单向函数，构建了一种支持多级门限要求的秘密共享机制。
result: 该方案实现了计算安全性，并允许灵活的份额分配，在等长份额下信息率可达到1。
conclusion: 本文成功构造了一个渐近理想的CHSS方案，解决了传统方案在效率和安全性上的瓶颈。
---

## 摘要
合取分级秘密共享（CHSS）是一种将参与者划分为多个不同等级的秘密共享方式，每个等级都有特定的阈值。授权子集必须同时满足所有等级的阈值要求。现有的基于中国剩余定理（CRT）的 CHSS 方案要么存在安全漏洞，要么信息率低于 1/2。在这项工作中，我们利用多项式环上的中国剩余定理和单向函数构造了一种渐近完善的 CHSS 方案。该方案具有计算安全性，并允许灵活的份额大小。值得注意的是，当所有份额大小相等时，我们的方案是一个信息率为 1 的渐近理想 CHSS 方案。

## Abstract
Conjunctive Hierarchical Secret Sharing (CHSS) is a type of secret sharing that divides participants into multiple distinct hierarchical levels, with each level having a specific threshold. An authorized subset must simultaneously meet the threshold of all levels. Existing Chinese Remainder Theorem (CRT)-based CHSS schemes either have security vulnerabilities or have an information rate lower than $\frac{1}{2}$. In this work, we utilize the CRT for polynomial ring and one-way functions to construct an asymptotically perfect CHSS scheme. It has computational security, and permits flexible share sizes. Notably, when all shares are of equal size, our scheme is an asymptotically ideal CHSS scheme with an information rate one.

---

## 论文详细总结（自动生成）

这是一份关于论文《Asymptotically Ideal Conjunctive Hierarchical Secret Sharing Scheme Based on CRT for Polynomial Ring》（基于多项式环中国剩余定理的渐近理想合取分级秘密共享方案）的结构化分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在保证安全性的前提下，构建一个高效的**合取分级秘密共享（CHSS）**方案。
*   **研究背景**：在 CHSS 中，参与者被分为多个等级，只有同时满足所有等级门限要求的子集才能恢复秘密。
*   **研究动机**：
    *   现有的基于中国剩余定理（CRT）的方案存在两大缺陷：一是安全性漏洞（如 Tiplea 等人的方案被证明不安全）；二是效率低下，信息率（秘密长度与份额长度之比）通常低于 1/2（如 Ersoy 等人的方案）。
    *   现有的理想方案（信息率为 1）通常不支持灵活的份额大小分配。
    *   本文旨在利用多项式环上的 CRT 构造一个既安全、又具有高信息率（渐近理想），且支持灵活份额大小的 CHSS 方案。

### 2. 论文提出的方法论
核心思想是结合**多项式环上的中国剩余定理**与**单向函数（One-way Functions）**。

*   **关键技术细节**：
    1.  **初始化与身份分配**：在有限域 $F_p$ 上，为每个参与者分配互质的多项式 $m_i(x)$ 作为身份标识。
    2.  **秘密分割**：将秘密 $s(x)$ 拆分为 $m$ 个子秘密 $s_\ell(x)$。
    3.  **多项式构造**：利用随机多项式 $\alpha_\ell(x)$ 构造 $f_\ell(x) = s_\ell(x) + \alpha_\ell(x)x^{d_0}$，其中 $d_0$ 是秘密的阶数。
    4.  **份额生成与分层**：
        *   部分参与者持有随机多项式作为份额。
        *   利用单向函数 $h_\ell(\cdot)$ 处理份额，并发布公开值 $u_i^{(\ell)}(x)$。这些公开值允许授权用户通过自己的份额计算出对应的同余式。
    5.  **秘密重构**：授权子集通过收集足够的份额，利用多项式环 CRT 求解同余方程组，恢复各层的 $f_\ell(x)$，进而求和还原原始秘密 $s(x)$。

### 3. 实验设计
由于本论文属于**理论密码学**范畴，其“实验”主要体现为**数学证明**和**方案对比分析**，而非基于数据集的软件运行。
*   **对比场景**：合取分级访问结构下的秘密分发与重构。
*   **Benchmark（基准）**：对比了自 1979 年以来经典的及最新的 7 种分级秘密共享方案。
*   **对比方法**：包括 Tassa (2007)、Gyarmati (2025)、Chen (2022) 的理想方案，以及 Yuan (2022)、Chintamani (2024) 的计算安全方案，重点对比了同为 CRT 类的 Ersoy (2016) 和 Tiplea (2021) 方案。

### 4. 资源与算力
*   **算力说明**：论文中**未提及**任何关于 GPU、CPU 型号或训练时长的信息。
*   **原因**：该研究侧重于算法的逻辑构造、计算复杂度的理论分析以及数学上的安全性证明（如条件熵分析），不涉及大规模数值计算或深度学习训练。

### 5. 实验数量与充分性
*   **理论证明充分性**：论文通过三个核心定理（Theorem 1-3）严谨地证明了方案的**正确性**（授权集必能重构）、**渐近完善性**（非授权集无法获取信息）以及**渐近理想性**（信息率趋近于 1）。
*   **对比充分性**：论文通过一个详细的表格（Table 1）从安全性类型（无条件/计算安全）、份额灵活性、完善性、信息率四个维度进行了横向对比。
*   **客观性**：作者客观地指出了方案在计算安全（基于单向函数）与无条件安全之间的取舍，实验（证明）逻辑闭环，符合密码学论文的标准规范。

### 6. 论文的主要结论与发现
1.  **安全性**：该方案在计算意义上是渐近完善的，解决了之前 CRT 方案被攻击的安全漏洞。
2.  **高效性**：当所有份额大小相等时，该方案是**渐近理想**的，即信息率 $\rho = 1$。
3.  **灵活性**：与基于多项式插值的方案不同，本方案允许根据参与者的重要程度分配不同大小的份额（通过调整 $m_i(x)$ 的阶数实现）。
4.  **适用性**：利用多项式环 CRT 避免了整数环 CRT 中复杂的量程限制问题，使参数选择更加灵活。

### 7. 优点
*   **突破了信息率瓶颈**：在 CRT 类 CHSS 方案中首次实现了渐近理想的信息率（$\rho=1$）。
*   **兼顾灵活性与安全性**：既支持灵活的份额分配，又通过单向函数机制修补了前人方案的逻辑漏洞。
*   **数学构造精巧**：利用多项式阶数的控制巧妙地实现了层级间的合取逻辑。

### 8. 不足与局限
*   **公开值负担**：为了实现分层逻辑，方案需要发布较多的公开值 $u_i^{(\ell)}(x)$，这会增加系统的公共存储开销（作者在结论中也提到了这是未来的改进方向）。
*   **计算安全限制**：由于引入了单向函数，该方案属于计算安全而非无条件安全（Unconditional Security），在量子计算等极端威胁下的鲁棒性需进一步评估。
*   **实现复杂度**：相比简单的 Shamir 门限方案，多项式环上的 CRT 运算和单向函数的管理增加了工程实现的复杂度。

（完）
