---
title: "TrustFed: Enabling Trustworthy Medical AI under Data Privacy Constraints"
title_zh: TrustFed：在数据隐私约束下实现可信医疗人工智能
authors: "Vagish Kumar, Syed Bahauddin Alam, Souvik Chakraborty"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21656v1"
tags: ["query:ppml"]
score: 8.0
evidence: 用于隐私保护训练的联邦不确定性量化框架
tldr: TrustFed是一个针对医疗联邦学习的可靠性框架，旨在解决数据隐私、异构性和类别不平衡导致的预测不可靠问题。该框架通过引入表征感知的客户端分配机制和软最近阈值聚合策略，在无需集中数据的情况下，实现了具有统计保证的分布无关不确定性量化。在超过43万张跨6种模态的医学图像上进行的评估证明，TrustFed能提供稳健的覆盖率保证，推动了医疗AI向临床实用化迈进。
selection_source: fresh_fetch
motivation: 医疗联邦学习面临数据异构和类别不平衡挑战，导致现有不确定性量化方法失效，难以保证预测的可靠性。
method: 提出TrustFed框架，利用模型内部表征进行客户端分配，并结合软最近阈值聚合策略，实现分布无关的有限样本覆盖保证。
result: 在涵盖6种影像模态的43万余张医学图像上验证，TrustFed在各种不平衡场景下均展现出稳健的统计覆盖性能。
conclusion: 该研究证明了统计保证的不确定性是下一代医疗AI系统的核心要求，为跨机构、多模态的临床部署提供了可行方案。
---

## 摘要
保护患者隐私仍然是跨医疗机构扩展机器学习的基础性障碍，由于伦理、法律和监管约束，集中敏感数据通常是不可行的。联邦学习通过在不共享原始患者数据的情况下实现隐私保护的多机构训练，提供了一种极具前景的替代方案；然而，实际部署面临着来自数据异构性、站点特有偏差和类别不平衡的严峻挑战，这些挑战降低了预测的可靠性，并使现有的不确定性量化方法失效。在此，我们提出了 TrustFed，这是一个联邦不确定性量化框架，它在异构且不平衡的医疗数据下提供无分布、有限样本的覆盖率保证，且无需集中访问。TrustFed 引入了一种表征感知的客户端分配机制，利用内部模型表征来实现跨机构的有效校准，并结合软最近阈值聚合策略，在减少分配不确定性的同时生成紧凑且可靠的预测集。利用涵盖六种临床不同成像模态的超过 430,000 张医学图像，我们对医学影像中的不确定性感知联邦学习进行了最全面的评估之一，证明了在具有不同类别基数和不平衡状态的数据集上具有稳健的覆盖率保证。通过在这种规模和广度上验证 TrustFed，我们的研究将不确定性感知联邦学习从概念验证推向了具有临床意义、模态无关的部署，将具有统计保证的不确定性定位为下一代医疗 AI 系统的核心要求。

## Abstract
Protecting patient privacy remains a fundamental barrier to scaling machine learning across healthcare institutions, where centralizing sensitive data is often infeasible due to ethical, legal, and regulatory constraints. Federated learning offers a promising alternative by enabling privacy-preserving, multi-institutional training without sharing raw patient data; however, real-world deployments face severe challenges from data heterogeneity, site-specific biases, and class imbalance, which degrade predictive reliability and render existing uncertainty quantification methods ineffective. Here, we present TrustFed, a federated uncertainty quantification framework that provides distribution-free, finite-sample coverage guarantees under heterogeneous and imbalanced healthcare data, without requiring centralized access. TrustFed introduces a representation-aware client assignment mechanism that leverages internal model representations to enable effective calibration across institutions, along with a soft-nearest threshold aggregation strategy that mitigates assignment uncertainty while producing compact and reliable prediction sets. Using over 430,000 medical images across six clinically distinct imaging modalities, we conduct one of the most comprehensive evaluations of uncertainty-aware federated learning in medical imaging, demonstrating robust coverage guarantees across datasets with diverse class cardinalities and imbalance regimes. By validating TrustFed at this scale and breadth, our study advances uncertainty-aware federated learning from proof-of-concept toward clinically meaningful, modality-agnostic deployment, positioning statistically guaranteed uncertainty as a core requirement for next-generation healthcare AI systems.

---

## 论文详细总结（自动生成）

### TrustFed：在数据隐私约束下实现可信医疗人工智能 —— 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在医疗领域，由于法律（如 HIPAA、GDPR）和伦理约束，患者数据通常被隔离在不同的机构中。**联邦学习 (FL)** 虽然允许在不共享原始数据的情况下进行协作训练，但在实际临床场景中面临两大挑战：
*   **数据异构性与偏差：** 不同机构的采集协议、患者群体和疾病流行率各异，导致数据分布不一致（Non-IID）。
*   **预测不可靠：** 现有的不确定性量化（UQ）方法（如贝叶斯近似或深度集成）在异构环境下往往失效，要么过于自信（掩盖错误），要么过于保守（导致预测集过大，失去临床意义）。
**TrustFed 的核心目标**是提供一种具有统计学保证、分布无关（Distribution-free）且保护隐私的联邦不确定性量化框架，确保医疗 AI 的预测既准确又可靠。

#### 2. 论文提出的方法论
TrustFed 基于**符合性预测 (Conformal Prediction, CP)** 理论，提出了以下核心技术：
*   **表征感知的客户端分配 (Representation-aware client assignment)：** 
    *   利用全局模型的内部特征嵌入（Embeddings）来衡量测试样本与各客户端数据的相似性。
    *   每个客户端本地维护一个特征库，仅向服务器返回测试样本与本地特征的标量距离，无需共享原始图像或特征向量。
*   **软最近阈值聚合 (Soft-nearest threshold aggregation)：** 
    *   服务器根据距离选择最相关的 $N_k$ 个客户端。
    *   聚合这些客户端的本地符合性阈值（取最大值 $\tau = \max \tau_j$），以生成针对特定测试样本的自适应预测集。
*   **CortiNet 架构：** 采用一种基于小波变换的物理感知混合模型，通过频率分解捕获解剖结构（低频）和纹理细节（高频），提高模型在噪声环境下的鲁棒性。

#### 3. 实验设计
*   **数据集：** 使用了 MedMNIST v2 中的 6 种临床影像模态，包括血细胞显微镜、视网膜眼底、腹部 CT、皮肤病变、结肠组织病理学和肾脏组织显微镜。
*   **数据规模：** 总计超过 430,000 张医学图像，类别数量从 5 到 11 类不等。
*   **Benchmark 与对比方法：**
    *   **FCP (Federated Conformal Prediction)：** 基于全局池化校准分数的现有联邦方法。
    *   **Local：** 仅使用客户端本地数据进行校准的方法。
    *   **对比指标：** 经验覆盖率（Empirical Coverage，是否达到设定的置信水平）和预测集基数（Size/Cardinality，预测集越小越高效）。

#### 4. 资源与算力
*   **算力说明：** 论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率提及：** 作者强调了 CortiNet 是一种参数高效（Parameter-efficient）的架构，具有较低的推理延迟，适合在去中心化的联邦环境中部署。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 6 个完全不同的临床任务上进行了验证，涵盖了类别不平衡（Class Imbalance）和样本量不平衡（Sample Imbalance）等多种极端场景。
*   **消融与分析：** 进行了关于邻居数量 $N_k$ 的敏感性分析，并对比了基于特征空间与基于原始像素的客户端分配效果。
*   **充分性评价：** 实验设计较为充分且客观。通过跨模态的大规模数据验证，证明了该方法在不同异构程度下的普适性。

#### 6. 主要结论与发现
*   **覆盖率保证：** TrustFed 在所有任务中均实现了与名义保证（如 90% 置信度）一致的经验覆盖率，解决了传统方法在异构数据下的欠覆盖问题。
*   **预测集紧凑：** 相比于全局池化方法，TrustFed 生成的预测集更小，这意味着它在保持可靠性的同时提供了更有确定性的临床建议。
*   **鲁棒性：** 在极端类别不平衡（少数类样本极少）的情况下，TrustFed 依然能为少数类提供稳健的不确定性估计。

#### 7. 优点
*   **统计严谨性：** 提供了有限样本下的数学覆盖保证，而非启发式的置信度评分。
*   **隐私保护：** 整个校准过程仅交换标量距离和阈值，不涉及原始数据、标签或高维特征的泄露。
*   **自适应性：** 能够根据测试样本的特征动态调整校准策略，适应不同机构间的分布偏移。

#### 8. 不足与局限
*   **超参数依赖：** 邻居数量 $N_k$ 的选择目前主要依靠经验，缺乏自动化的理论指导。
*   **任务局限：** 目前仅验证了分类任务，尚未扩展到回归、目标检测或图像分割等更复杂的医疗任务。
*   **多模态缺失：** 虽然测试了多种模态，但每个联邦任务内部仍是单模态的，未探讨跨模态（如同时处理 CT 和报告）的联邦不确定性量化。

（完）
