---
title: "mmFHE: mmWave Sensing with End-to-End Fully Homomorphic Encryption"
title_zh: mmFHE：基于端到端全同态加密的毫米波感知
authors: "Tanvir Ahmed, Yixuan Gao, Adnan Armouti, Rajalakshmi Nandakumar"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22437v1"
tags: ["query:ppml"]
score: 8.0
evidence: 在不可信云端进行机器学习推理的全同态加密方案
tldr: 本研究提出了mmFHE，这是首个为毫米波雷达感知提供端到端全同态加密（FHE）支持的系统。它在边缘设备加密原始距离剖面，并在不可信云端对密文执行完整的信号处理和机器学习推理。通过7个可组合的FHE内核，mmFHE在保证输入隐私和数据不确定性的同时，实现了极高的计算精度。在生命体征监测和手势识别任务中，该系统表现出与明文相当的准确率，证明了在商用硬件上实现隐私保护毫米波感知的可行性。
selection_source: fresh_fetch
motivation: 旨在解决毫米波雷达数据在云端处理过程中面临的原始隐私泄露和数据依赖性攻击风险。
method: 构建了一个由7个可组合、数据无关的FHE内核组成的库，将标准DSP流程和机器学习推理转化为固定算术电路进行密文运算。
result: 在生命体征和手势识别任务中，加密处理的精度损失几乎可以忽略不计，且在商用GPU上实现了可行的端到端处理延迟。
conclusion: 该研究证实了在现有商用硬件上实现端到端、具备强隐私保证的毫米波雷达感知系统是完全可行的。
---

## 摘要
我们提出了 mmFHE，这是首个为端到端毫米波雷达感知实现全同态加密（FHE）的系统。mmFHE 在轻量级边缘设备上对原始距离剖面（range profiles）进行加密，并在仅对密文进行操作的不可信云端上，以同态方式执行整个毫米波信号处理和机器学习（ML）推理流水线。mmFHE 的核心是一个由七个可组合、数据无关（data-oblivious）的 FHE 算子组成的库，这些算子使用固定算术电路取代了标准的数字信号处理（DSP）例程。这些算子可以灵活地组合成不同的特定应用流水线。我们在两个代表性任务上演示了该方法：生命体征监测和手势识别。我们正式证明了由该库组装的任何流水线都具有两项密码学保证：输入隐私（云端无法获知任何传感器数据信息）和数据无关性（无论处理何种数据，云端的执行轨迹都是一致的）。这些保证有效地抵御了针对原始数据的各种监督式和非监督式隐私攻击，包括重识别和数据依赖型隐私泄露。在三个公开雷达数据集（270 条生命体征记录，600 次手势试验）上的评估表明，加密引入的误差微乎其微：与明文相比，心率/呼吸频率（HR/RR）的平均绝对误差（MAE）小于 10^-3 bpm，手势识别准确率为 84.5%（明文为 84.7%）；对于 10 秒的生命体征窗口和 3 秒的手势窗口，端到端云端 GPU 延迟分别为 103 秒和 37 秒。这些结果表明，在现有的商用硬件上实现隐私保护的端到端毫米波感知是可行的。

## Abstract
We present mmFHE, the first system that enables fully homomorphic encryption (FHE) for end-to-end mmWave radar sensing. mmFHE encrypts raw range profiles on a lightweight edge device and executes the entire mmWave signal-processing and ML inference pipeline homomorphically on an untrusted cloud that operates exclusively on ciphertexts. At the core of mmFHE is a library of seven composable, data-oblivious FHE kernels that replace standard DSP routines with fixed arithmetic circuits. These kernels can be flexibly composed into different application-specific pipelines. We demonstrate this approach on two representative tasks: vital-sign monitoring and gesture recognition. We formally prove two cryptographic guarantees for any pipeline assembled from this library: input privacy, the cloud learns nothing about the sensor data; and data obliviousness, the execution trace is identical on the cloud regardless of the data being processed. These guarantees effectively neutralize various supervised and unsupervised privacy attacks on raw data, including re-identification and data-dependent privacy leakage. Evaluation on three public radar datasets (270 vital-sign recordings, 600 gesture trials) shows that encryption introduces negligible error: HR/RR MAE <10^-3 bpm versus plaintext, and 84.5% gesture accuracy (vs. 84.7% plaintext) with end-to-end cloud GPU latency of 103s for a 10s vital-sign window and 37s for a 3s gesture window. These results show that privacy-preserving end-to-end mmWave sensing is feasible on commodity hardware today.

---

## 论文详细总结（自动生成）

### mmFHE：基于端到端全同态加密的毫米波感知论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：毫米波（mmWave）雷达在健康监测和手势识别中应用广泛，但由于边缘设备算力有限，通常需要将数据上传至云端处理。然而，原始雷达数据包含丰富的生物特征（如心跳、呼吸、步态），直接上传会导致严重的隐私泄露。此外，传统的信号处理（DSP）算法具有数据依赖性，攻击者可以通过执行轨迹（如耗时、内存访问）推断输入内容。
*   **研究动机**：现有的隐私保护方案（如 RPRS）仅对最后的神经网络推理进行加密，而前端的 DSP 处理仍为明文，存在侧信道泄露风险。本研究旨在实现**首个端到端全同态加密（FHE）毫米波感知系统**，确保从原始数据到最终结果的全过程在云端均以密文形式运行。

#### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用 CKKS 全同态加密方案，将复杂的雷达信号处理流程重新设计为**固定深度的算术电路**。由于 FHE 仅支持加法、乘法和旋转，研究者开发了 7 个数据无关（Data-oblivious）的 FHE 内核来替代标准 DSP 例程。
*   **关键技术内核**：
    1.  **能量积分 (K1)**：计算每个距离箱的信号功率。
    2.  **软功率注意力 (K2)**：用分支无关的多项式权重替代传统的 CFAR 阈值检测或硬最大值（argmax）操作。
    3.  **块对角矩阵乘法 DFT (K3)**：将 FFT 转化为矩阵向量乘法，以降低乘法深度。
    4.  **最高能量箱软 I/Q 提取 (K4)**：通过加权掩码提取目标信号，避免数据依赖的分支跳转。
    5.  **FIR 滤波 (K5)**：利用 Toeplitz 矩阵实现卷积滤波。
    6.  **陷波掩码 (K6)**：静态杂波抑制。
    7.  **泰勒级数差分相位提取 (K7)**：使用三阶泰勒展开近似 `arctan` 函数，解决 FHE 无法直接处理三角函数的问题。
*   **加密 ML 推理**：使用全连接网络，并将激活函数替换为平方函数（$x^2$），以适配 FHE 的多项式运算特性。

#### 3. 实验设计
*   **数据集与场景**：
    *   **Children 数据集**：50 名儿童的生命体征记录（60 GHz）。
    *   **Parralejo 数据集**：110 名受试者在不同姿势下的生命体征记录（60 GHz）。
    *   **Zenodo 60 GHz 数据集**：12 名用户、5 种手势、6 种环境下的 600 次试验。
*   **Benchmark（基准）**：
    *   **标准明文 DSP**：传统算法处理未加密数据。
    *   **mmFHE 明文版**：使用 mmFHE 设计的内核处理未加密数据（验证近似算法的有效性）。
    *   **RPRS (2025)**：仅对 CNN 推理阶段进行加密的对比方案。

#### 4. 资源与算力
*   **硬件配置**：
    *   **CPU**：AMD Ryzen 9 5950X (16核/32线程)。
    *   **GPU**：单张 **NVIDIA RTX 3090 Ti (24 GB)**。
*   **处理延迟**：
    *   **生命体征**：处理 10 秒窗口数据需 **103.32 秒**（GPU 加速）。
    *   **手势识别**：处理 3 秒窗口数据需 **36.56 秒**。
    *   **算力开销**：相比明文处理，加密处理存在约 2,700 倍至 37,000 倍的减速。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 270 条生命体征记录和 600 组手势试验，样本量在雷达感知领域属于中等偏上水平。
*   **充分性评价**：
    *   **多维度验证**：不仅测试了端到端准确率，还详细评估了每个内核引入的噪声（MSE < 10^-5）。
    *   **安全性分析**：通过形式化证明（定理 4.1 和 4.2）确保了输入隐私和数据无关性。
    *   **对比实验**：与最新的 RPRS 方案进行了公平对比，证明了在提升隐私强度的同时，准确率损失极小（< 0.2%）。实验设计客观且逻辑严密。

#### 6. 主要结论与发现
*   **精度极高**：加密引入的数值误差几乎可以忽略不计，心率/呼吸频率误差 < 10^-3 bpm，手势识别准确率（84.5%）与明文（84.7%）基本持平。
*   **隐私防御有效**：mmFHE 将用户重识别攻击的准确率从明文状态下的 99.9% 降低到了随机猜测水平。
*   **可行性验证**：证明了在现有商用 GPU 硬件上，通过优化 FHE 内核设计，实现端到端加密感知是可行的。

#### 7. 优点
*   **首创性**：首次实现了毫米波雷达感知的全流程（DSP + ML）同态加密。
*   **数学严谨性**：提供了输入隐私和数据无关性的密码学证明，解决了侧信道泄露问题。
*   **内核库设计**：提出的 7 个 FHE 友好型内核具有通用性，可灵活组合用于其他雷达任务。

#### 8. 不足与局限
*   **计算与通信开销**：延迟依然较高（秒级处理对应分钟级计算），且通信量巨大（每个 10s 窗口需上传约 4.3 GB 数据），限制了实时应用。
*   **多目标限制**：目前的“软功率注意力”机制在处理多目标场景时可能失效，会将多个目标坍缩为一个点。
*   **电路深度限制**：受限于 FHE 的乘法深度（Depth=11），无法直接支持更深层的神经网络（如大型 CNN 或 Transformer），除非引入极其耗时的自举（Bootstrapping）操作。

（完）
