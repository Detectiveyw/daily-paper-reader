---
title: "DriftGuard: Mitigating Asynchronous Data Drift in Federated Learning"
title_zh: DriftGuard：缓解联邦学习中的异步数据漂移
authors: "Yizhou Han, Di Wu, Blesson Varghese"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18872v1"
tags: ["query:ppml"]
score: 8.0
evidence: 针对数据漂移的联邦学习框架
tldr: 针对联邦学习中设备数据分布随时间异步偏移的问题，本文提出DriftGuard框架。该框架采用受混合专家模型（MoE）启发的架构，将全局共享参数与局部特定参数分离。通过全局重训练更新通用知识，并利用MoE门控机制识别设备簇进行局部重训练，从而在保证模型精度的同时，大幅降低了资源受限设备的计算开销，实现了高效的联邦持续学习。
selection_source: fresh_fetch
motivation: 联邦学习中不同设备的数据分布会随时间发生异步偏移，导致频繁重训练成本过高而频率过低则性能下降。
method: 提出一种基于MoE架构的联邦持续学习框架，通过分离全局共享参数与局部专家参数，实现全局和分组的选择性重训练。
result: "在多个数据集上，DriftGuard在保持或超越现有最优精度的前提下，将重训练成本降低了高达83%，单位成本精度提升达2.3倍。"
conclusion: DriftGuard通过高效的异步漂移缓解机制，实现了联邦学习中模型性能与计算成本之间的最佳平衡。
---

## 摘要
在实际的联邦学习（FL）部署中，参与训练的设备上的数据分布会随时间演变。这导致了异步数据漂移，即不同设备在不同时间向不同的分布发生偏移。缓解此类漂移具有挑战性：频繁的重训练会给资源受限的设备带来高昂的计算开销，而重训练频率过低则会降低发生漂移设备的性能。我们提出了 DriftGuard，这是一个能够高效适应异步数据漂移的联邦持续学习框架。DriftGuard 采用了一种受混合专家模型（MoE）启发的架构，将捕获全局可迁移知识的共享参数与适应特定群体分布的本地参数分离。这种设计实现了两种互补的重训练策略：(i) 全局重训练，当识别到系统范围的漂移时更新共享参数；(ii) 群体重训练，通过 MoE 门控模式识别设备集群，并在不共享原始数据的情况下选择性地更新本地参数。在多个数据集和模型上的实验表明，DriftGuard 在匹配或超过现有最先进技术准确率的同时，将总重训练成本降低了高达 83%。因此，它实现了单位重训练成本下的最高准确率，比最强的基准模型提升了高达 2.3 倍。DriftGuard 可从 https://github.com/blessonvar/DriftGuard 下载。

## Abstract
In real-world Federated Learning (FL) deployments, data distributions on devices that participate in training evolve over time. This leads to asynchronous data drift, where different devices shift at different times and toward different distributions. Mitigating such drift is challenging: frequent retraining incurs high computational cost on resource-constrained devices, while infrequent retraining degrades performance on drifting devices. We propose DriftGuard, a federated continual learning framework that efficiently adapts to asynchronous data drift. DriftGuard adopts a Mixture-of-Experts (MoE) inspired architecture that separates shared parameters, which capture globally transferable knowledge, from local parameters that adapt to group-specific distributions. This design enables two complementary retraining strategies: (i) global retraining, which updates the shared parameters when system-wide drift is identified, and (ii) group retraining, which selectively updates local parameters for clusters of devices identified via MoE gating patterns, without sharing raw data. Experiments across multiple datasets and models show that DriftGuard matches or exceeds state-of-the-art accuracy while reducing total retraining cost by up to 83%. As a result, it achieves the highest accuracy per unit retraining cost, improving over the strongest baseline by up to 2.3x. DriftGuard is available for download from https://github.com/blessonvar/DriftGuard.

---

## 论文详细总结（自动生成）

### 论文总结：DriftGuard - 缓解联邦学习中的异步数据漂移

#### 1. 核心问题与研究动机
*   **核心问题**：在真实的联邦学习（FL）场景（如物联网 IoT）中，设备端的数据分布会随时间演变，即**数据漂移（Data Drift）**。这种漂移通常是**异步的**：不同设备在不同时间发生漂移，且漂移后的分布各不相同。
*   **研究动机**：
    *   **计算开销大**：资源受限的 IoT 设备难以承受频繁的全局重训练。
    *   **性能权衡难**：减少重训练频率会导致发生漂移的设备性能长期低下；而传统的联邦持续学习（FCL）通常假设同步漂移，对异步场景处理效率极低。
    *   **知识共享不足**：现有的聚类方法将不同设备簇视为独立单元，忽略了全局可迁移知识的共享。

#### 2. 方法论
DriftGuard 的核心思想是利用**混合专家模型（MoE）**架构实现参数解耦，并根据漂移类型进行选择性重训练。
*   **核心架构**：
    *   **分支级解耦**：模型分为“共享分支”（捕获全局通用知识）和“局部分支”（适应特定群体分布）。
    *   **门控机制**：采用分支级软门控（权重分配）和层级硬门控（神经元激活控制）。
*   **关键技术细节**：
    *   **基于门控输出的聚类**：设备不共享原始数据，而是将层级门控的激活模式（聚合门控矩阵）发送至服务器。服务器利用层次聚类算法识别具有相似数据分布的设备组。
    *   **两级重训练策略**：
        1.  **全局重训练**：当系统整体平均精度下降时，触发所有设备参与，仅更新共享参数。
        2.  **群体重训练**：当特定设备簇精度下降时，仅该簇设备参与，仅更新其局部参数。
*   **算法流程**：推理监测 -> 门控特征上传 -> 服务器聚类 -> 触发判定（全局或局部） -> 执行参数受限的 FL 重训练。

#### 3. 实验设计
*   **数据集**：
    *   **DG5**：5 个手写数字域（MNIST, SVHN 等）。
    *   **PACS**：4 个物体识别域（照片、艺术画、卡通、素描）。
    *   **DomainNet**：6 个视觉风格域（真实、剪贴画、素描等）。
*   **模型**：基于 ResNet 和 ViT 的四种变体（cResNet-S/M, cViT-S/M）。
*   **对比方法（Baselines）**：
    *   **FCL-AveTrig/perDevice**：标准联邦持续学习（全局触发或单设备触发）。
    *   **PFL-AveTrig/perDevice**：个性化联邦学习（MoE 架构，但无针对性漂移处理）。
    *   **Cluster-based**：基于聚类的联邦学习（FedDrift 等）。
*   **场景模拟**：20 个设备，30 个时间步，模拟瞬时漂移和渐进式漂移。

#### 4. 资源与算力
*   **服务器端**：使用 2 台 **NVIDIA RTX A6000 GPU** (每台 48GB 显存) 和 AMD EPYC 7713P 64 核处理器进行模拟实验。
*   **设备端（原型验证）**：部署在 **20 台 Raspberry Pi 4** 组成的真实 IoT 测试床上，测量实际墙钟时间（Wall-clock time）。
*   **训练细节**：每次重训练执行 5 轮联邦聚合，每台设备本地训练最多 20 个 epoch，Batch Size 为 8。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流多域数据集、2 种主流架构（CNN 和 Transformer）以及 2 种模型规模，共 6 组主要配置。
*   **维度丰富**：除了精度和成本对比，还进行了消融实验（超参数敏感性分析，如全局触发阈值、聚类距离阈值）以及真实硬件上的端到端性能评估。
*   **评价客观性**：引入了“单位成本精度（Efficiency, E）”这一综合指标，公平地衡量了精度提升与计算开销之间的权衡。实验设计充分考虑了异步漂移的复杂性。

#### 6. 主要结论与发现
*   **高效性**：DriftGuard 在保持或提升精度的同时，将总重训练成本（FLOPs）降低了 **22% 至 83%**。
*   **单位成本收益**：在多数配置下，其单位成本精度比最强基准方法高出 **1.3 到 2.3 倍**。
*   **原型表现**：在树莓派测试床上，重训练时间缩短了高达 **20%**，且始终保持最高精度。
*   **漂移适应**：通过解耦参数，DriftGuard 能在不频繁触发全局更新的情况下，通过轻量级的局部更新快速恢复漂移设备的性能。

#### 7. 优点
*   **架构创新**：巧妙地将 MoE 门控机制从“推理加速”扩展到了“漂移感知”和“隐私聚类”。
*   **成本意识**：针对 IoT 场景，通过参数冻结和选择性参与，极大地缓解了计算瓶颈。
*   **隐私保护**：利用门控矩阵作为分布特征的代理，避免了原始数据或敏感梯度的泄露风险。
*   **实用性强**：在真实硬件原型上进行了验证，证明了其在资源受限环境下的可行性。

#### 8. 不足与局限
*   **架构依赖**：该方法高度依赖于 MoE 架构，对于不支持模块化解耦的传统紧耦合模型可能难以直接应用。
*   **设备规模**：实验主要在 20 个设备的规模下进行，在成千上万个超大规模设备节点下的聚类效率和通信开销尚待验证。
*   **超参数敏感性**：虽然有消融实验，但全局触发阈值 $\tau_{global}$ 的设定对最终成本影响较大，实际部署中可能需要复杂的调优。

（完）
