---
title: Incentive-Aware Federated Averaging with Performance Guarantees under Strategic Participation
title_zh: 策略性参与下具有性能保证的激励感知联邦平均方法
authors: "Fateme Maleki, Krishnan Raghavan, Farzad Yousefian"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20873v1"
tags: ["query:ppml"]
score: 8.0
evidence: 具有私有本地数据集的激励感知联邦平均
tldr: 针对联邦学习中参与者因成本与收益权衡而表现出的策略性行为，本文提出了一种激励感知的联邦平均（FedAvg）方法。该方法通过纳什均衡（NE）寻优规则动态调整客户端的数据参与量，并建立了凸与非凸目标下的性能保证。实验证明，该方法在保持全局模型性能的同时，能使参与者收敛到稳定的数据贡献策略，有效解决了策略性参与下的激励兼容问题。
selection_source: fresh_fetch
motivation: 传统的联邦学习忽略了参与者在贡献数据时会权衡学习收益与成本的策略性行为，导致难以维持长期参与。
method: 提出一种激励感知的FedAvg方法，利用纳什均衡寻优规则动态调整客户端上传的数据集大小。
result: 在MNIST和CIFAR-10数据集上的实验表明，该算法在实现全局模型高性能的同时，能使参与策略达到稳定。
conclusion: 该研究为存在策略性参与者的联邦学习系统提供了具有理论性能保证的激励机制和优化框架。
---

## 摘要
联邦学习 (FL) 是一种通信高效的协作学习框架，能够在拥有私有本地数据集的多个代理之间进行模型训练。虽然 FL 在提高全局模型性能方面的优势已得到公认，但单个代理可能会采取策略性行为，在学习收益与贡献本地数据的成本之间进行权衡。针对能够成功留住参与代理的 FL 框架的需求，我们提出了一种激励感知的联邦平均方法，在该方法中，在每个通信轮次，客户端向服务器传输其本地模型参数和更新后的训练数据集大小。数据集大小通过一种捕捉策略性数据参与的纳什均衡 (NE) 寻优更新规则进行动态调整。我们在凸和非凸全局目标设置下分析了所提方法，并为由此产生的激励感知 FL 算法建立了性能保证。在 MNIST 和 CIFAR-10 数据集上的数值实验表明，代理在收敛到稳定的数据参与策略的同时，实现了具有竞争力的全局模型性能。

## Abstract
Federated learning (FL) is a communication-efficient collaborative learning framework that enables model training across multiple agents with private local datasets. While the benefits of FL in improving global model performance are well established, individual agents may behave strategically, balancing the learning payoff against the cost of contributing their local data. Motivated by the need for FL frameworks that successfully retain participating agents, we propose an incentive-aware federated averaging method in which, at each communication round, clients transmit both their local model parameters and their updated training dataset sizes to the server. The dataset sizes are dynamically adjusted via a Nash equilibrium (NE)-seeking update rule that captures strategic data participation. We analyze the proposed method under convex and nonconvex global objective settings and establish performance guarantees for the resulting incentive-aware FL algorithm. Numerical experiments on the MNIST and CIFAR-10 datasets demonstrate that agents achieve competitive global model performance while converging to stable data participation strategies.

---

## 论文详细总结（自动生成）

以下是对论文《Incentive-Aware Federated Averaging with Performance Guarantees under Strategic Participation》（策略性参与下具有性能保证的激励感知联邦平均方法）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的联邦学习（FL）框架通常假设参与节点的本地数据集大小在训练过程中是固定不变的。然而，在现实场景中，参与者往往是“策略性”的：他们会权衡贡献数据带来的**学习收益**（模型精度的提升）与**参与成本**（数据收集、计算资源、隐私泄露风险等）。
*   **研究动机**：如果参与者发现减少数据贡献仍能获得足够的模型收益（“搭便车”行为），或者参与者之间存在竞争关系，他们就没有动力全额贡献数据。
*   **核心问题**：如何设计一个既能捕捉代理商策略性行为，又能同时保证全局模型收敛性和参与策略稳定性的联邦学习算法？

### 2. 方法论：核心思想与算法流程
论文提出了 **IncentFedAvg** 算法，其核心思想是将**协作式模型训练**与**非合作博弈论**相结合：
*   **双重更新机制**：在每个通信轮次中，客户端不仅更新模型参数（协作部分），还根据其效用函数动态调整参与训练的数据集大小 $N_i$（策略部分）。
*   **效用函数建模**：每个客户端最小化其“净损失”：$l_i(N) = c_i(N) - a_i(N)$，其中 $c_i$ 是参与成本，$a_i$ 是学习收益（如随机发现模型或随机覆盖模型）。
*   **纳什均衡（NE）寻优**：客户端利用投影梯度法更新其数据集大小 $N_i$，旨在收敛到数据参与博弈的纳什均衡点 $N^*$。
*   **算法流程**：
    1.  服务器广播当前全局模型 $\hat{x}_r$ 和参与规模估计 $\hat{N}_r$。
    2.  客户端执行 $H$ 步本地 SGD 更新模型。
    3.  客户端根据效用函数的梯度更新其下一轮的参与规模 $N_{i,r+1}$。
    4.  服务器根据新的 $N_{i,r+1}$ 计算加权比例，聚合模型参数。

### 3. 实验设计
*   **数据集**：使用了标准的 **MNIST**（手写数字识别）和 **CIFAR-10**（物体识别）数据集。
*   **模型架构**：MNIST 使用两层神经网络；CIFAR-10 使用卷积神经网络（CNN）。
*   **对比场景（Benchmark）**：
    *   主要对比了不同的本地更新步数 $H \in \{1, 5, 10, 20\}$ 对收敛速度的影响。
    *   验证了在不同成本系数和收益模型下，客户端参与规模 $N_i$ 的演化过程。
*   **收益模型**：采用了“随机发现（Random Discovery）”模型，即收益与所有参与者贡献的线性组合相关。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **实现细节**：实验侧重于验证算法的数值收敛性，而非大规模工程实现，因此主要关注通信轮次与损失函数、参与规模之间的关系。

### 5. 实验数量与充分性
*   **实验规模**：针对两个主流数据集进行了实验，涵盖了从简单线性模型到复杂 CNN 的场景。
*   **充分性评价**：
    *   **理论验证充分**：实验结果清晰地展示了全局损失的下降以及参与规模 $N_i$ 向纳什均衡点的线性收敛，有力支撑了文中的理论定理。
    *   **对比略显单一**：实验主要是在验证所提算法的有效性，缺乏与其他激励机制（如基于代币或区块链的 FL 激励方案）的横向对比。

### 6. 主要结论与发现
*   **双重收敛保证**：论文在理论上证明了该算法在非凸设置下能收敛到平稳点，在凸设置下能收敛到全局最优解。
*   **策略稳定性**：证明了参与者的策略向量 $N$ 以线性速率收敛到唯一的纳什均衡点。
*   **本地计算的影响**：实验观察到，增加本地更新步数 $H$ 可以显著加速全局模型的收敛，这与标准 FedAvg 的结论一致。
*   **异构性适应**：即使客户端具有不同的成本和收益偏好，算法也能引导系统达到一个稳定的协作状态。

### 7. 优点
*   **理论完备性**：首次在考虑策略性参与的同时，为全局学习目标和参与策略稳定性提供了同步的性能保证。
*   **动态性**：允许客户端在训练过程中实时调整参与度，比静态的激励分配机制更具灵活性。
*   **通用性**：框架支持多种效用函数建模，且适用于凸与非凸的损失函数。

### 8. 不足与局限
*   **强单调性假设**：理论证明依赖于博弈映射的强单调性（Assumption 2.2），在某些复杂的现实效用函数中，这一条件可能需要通过额外的正则化手段才能满足。
*   **隐私成本简化**：虽然提到了隐私是成本的一部分，但未深入探讨差分隐私（DP）等具体技术对博弈均衡和模型精度的耦合影响。
*   **实验广度**：实验仅限于 IID 或轻微非 IID 的模拟环境，未在高度异构或大规模真实工业数据集上进行验证。
*   **通信开销**：虽然增加了传输 $N_i$ 的开销（极小），但频繁的策略调整可能在极大规模网络中引入额外的协调负担。

（完）
