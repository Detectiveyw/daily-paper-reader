---
title: "HEART-PFL: Stable Personalized Federated Learning under Heterogeneity with Hierarchical Directional Alignment and Adversarial Knowledge Transfer"
title_zh: HEART-PFL：异构环境下基于分层定向对齐与对抗性知识迁移的稳定个性化联邦学习
authors: "Minjun Kim, Minje Kim"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24209v1"
tags: ["query:ppml"]
score: 9.0
evidence: 异构环境下的个性化联邦学习
tldr: 针对个性化联邦学习（PFL）中数据异构导致的对齐浅层化和蒸馏不稳定性问题，本文提出HEART-PFL框架。该框架通过分层定向对齐（HDA）在不同深度采用余弦相似度与MSE匹配策略以保留客户端特性，并利用对抗知识转移（AKT）在代理数据上进行对称KL蒸馏以稳定全局更新。实验表明，该方法仅需1.46M参数即可在多个数据集上达到SOTA性能，显著提升了模型的个性化准确率、鲁棒性与优化稳定性。
selection_source: fresh_fetch
motivation: 现有PFL方法在处理异构分布时面临原型对齐深度不足以及服务器端知识蒸馏过程脆弱且不稳定的挑战。
method: 提出一种双侧框架，结合了分层定向对齐（HDA）策略与基于对抗知识转移（AKT）的对称KL蒸馏机制。
result: 在CIFAR-100、Flowers-102和Caltech-101等数据集上均取得了SOTA的个性化准确率，并对域外代理数据表现出强鲁棒性。
conclusion: HEART-PFL通过协同增强个性化对齐与全局稳定性，为异构环境下的联邦学习提供了一个高效、稳健且可扩展的解决方案。
---

## 摘要
个性化联邦学习 (PFL) 旨在异构分布下提供有效的客户端特定模型，但现有方法面临浅层原型对齐和脆弱的服务端蒸馏问题。我们提出了 HEART-PFL，这是一个双侧框架，它：(i) 执行深度感知的分层定向对齐 (HDA)，在早期阶段使用余弦相似度，在深层阶段使用 MSE 匹配，以保留客户端特异性；(ii) 通过对抗性知识迁移 (AKT) 稳定全局更新，在干净和对抗性代理数据上进行对称 KL 蒸馏。通过仅包含 1.46M 可训练参数的轻量级适配器，HEART-PFL 在 Dirichlet 非独立同分布 (non-IID) 划分下的 CIFAR-100、Flowers-102 和 Caltech-101 数据集上分别实现了 63.42%、84.23% 和 95.67% 的最先进个性化准确率，并且对域外代理数据保持鲁棒。消融研究进一步证实，HDA 和 AKT 在对齐、鲁棒性和优化稳定性方面提供了互补的增益，并深入揭示了这两个组件如何相互增强以实现有效的个性化。总体而言，这些结果表明 HEART-PFL 同时增强了个性化和全局稳定性，凸显了其作为 PFL 强大且可扩展解决方案的潜力（代码见 https://github.com/danny0628/HEART-PFL）。

## Abstract
Personalized Federated Learning (PFL) aims to deliver effective client-specific models under heterogeneous distributions, yet existing methods suffer from shallow prototype alignment and brittle server-side distillation. We propose HEART-PFL, a dual-sided framework that (i) performs depth-aware Hierarchical Directional Alignment (HDA) using cosine similarity in the early stage and MSE matching in the deep stage to preserve client specificity, and (ii) stabilizes global updates through Adversarial Knowledge Transfer (AKT) with symmetric KL distillation on clean and adversarial proxy data. Using lightweight adapters with only 1.46M trainable parameters, HEART-PFL achieves state-of-the-art personalized accuracy on CIFAR-100, Flowers-102, and Caltech-101 (63.42%, 84.23%, and 95.67%, respectively) under Dirichlet non-IID partitions, and remains robust to out-of-domain proxy data. Ablation studies further confirm that HDA and AKT provide complementary gains in alignment, robustness, and optimization stability, offering insights into how the two components mutually reinforce effective personalization. Overall, these results demonstrate that HEART-PFL simultaneously enhances personalization and global stability, highlighting its potential as a strong and scalable solution for PFL(code available at https://github.com/danny0628/HEART-PFL).

---

## 论文详细总结（自动生成）

以下是对论文《HEART-PFL: Stable Personalized Federated Learning under Heterogeneity with Hierarchical Directional Alignment and Adversarial Knowledge Transfer》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在个性化联邦学习（PFL）中，如何解决由于客户端数据异构（Non-IID）导致的**原型对齐浅层化**（仅在单层对齐）和**服务端知识蒸馏脆弱性**（单向蒸馏易受教师偏差影响且不稳定）的问题。
*   **研究背景**：现有的 PFL 方法要么通信开销巨大（全模型个性化），要么在利用层级语义信息方面不足，且在服务端聚合时难以维持全局模型的泛化性与个性化模型特异性之间的平衡。

### 2. 方法论
HEART-PFL 提出了一个双侧增强框架，核心包含两个关键组件：
*   **分层定向对齐 (HDA, Client-side)**：
    *   **多层原型提取**：在客户端提取不同网络深度的类原型。
    *   **阶段性对齐策略**：在网络**早期阶段**使用**余弦相似度**进行定向对齐，以保留通用特征的几何一致性；在**深度阶段**使用 **MSE（均方误差）** 进行语义匹配，以确保类特定信息的精确性。
*   **对抗性知识迁移 (AKT, Server-side)**：
    *   **对称 KL 蒸馏**：在服务端利用代理数据集，通过双向（对称）KL 散度最小化全局模型与客户端集成模型之间的分布差异，避免单向蒸馏带来的偏差。
    *   **对抗样本增强**：引入对抗扰动生成代理数据的变体，通过在干净样本和对抗样本上同时进行蒸馏，提升全局适配器的鲁棒性和特征多样性。
*   **效率设计**：采用轻量级适配器（Adapter）架构，仅训练 1.46M 参数，冻结预训练的主干网络（Backbone），显著降低通信和计算成本。

### 3. 实验设计
*   **数据集**：CIFAR-100、Flowers-102、Caltech-101。
*   **异构场景**：使用 Dirichlet 分布（$\alpha \in \{0.1, 0.3, 0.5\}$）模拟不同程度的标签偏移（Label Skew）。
*   **对比基准 (Benchmarks)**：
    *   **通用 FL 方法**：FedAvg, FedProx, FedProto（及其个性化微调版本）。
    *   **SOTA PFL 方法**：LG-FedAvg, FedPer, Ditto, FedBABU, FedALA, PerAda。
*   **实验设置**：20 个客户端，每轮随机选择 8 个，进行 200 轮通信。

### 4. 资源与算力
*   **参数量**：明确指出 HEART-PFL 仅需 **1.46M** 可训练参数，远低于传统方法的 11.18M 或 22.36M。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到了优化器设置（客户端 SGD，服务端 Adam）以及 Batch Size（客户端 16，服务端 2048）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集上进行了 9 种异构配置的测试，并与 10 种基准方法进行了对比。
*   **消融实验**：
    *   验证了 HDA 和 AKT 各自的贡献。
    *   研究了 HDA 不同层级对齐策略的影响。
    *   测试了 AKT 中对称 KL 和对抗样本的必要性。
    *   **域外（OOD）鲁棒性测试**：验证了当服务端代理数据与客户端数据属于不同领域时（如用 Flowers 代理 CIFAR），模型依然保持高性能。
*   **客观性**：通过 3D 损失曲面可视化展示了方法对优化稳定性的提升，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **性能领先**：HEART-PFL 在所有数据集和异构设置下均取得了最高的个性化准确率（如 CIFAR-100 $\alpha=0.1$ 时达到 63.42%）。
*   **稳定性**：AKT 显著平滑了损失曲面，减少了异构客户端带来的聚合偏差，使训练过程更稳定。
*   **鲁棒性**：该框架对代理数据的领域依赖性较低，证明了其在实际应用中的灵活性。
*   **高效性**：在极低参数量的情况下超越了全参数微调的方法。

### 7. 优点
*   **双侧协同**：同时从客户端对齐和服务端蒸馏两个维度解决异构性问题。
*   **深度感知**：HDA 区分了浅层和深层特征的不同对齐需求，比单一层原型对齐更科学。
*   **鲁棒蒸馏**：AKT 结合对称 KL 和对抗训练，有效解决了服务端模型更新脆弱的问题。
*   **参数高效**：适配器设计使其非常适合资源受限的联邦学习场景。

### 8. 不足与局限
*   **代理数据依赖**：尽管证明了 OOD 鲁棒性，但服务端仍需一定规模的代理数据集（Proxy Data），这在某些极端隐私敏感的场景下可能难以获取。
*   **硬件细节缺失**：未提供具体的训练时间或能耗数据，难以评估其实际部署时的端到端效率。
*   **超参数敏感性**：HDA 中“早期”与“深层”阶段的划分界限可能需要针对不同架构进行手动调整。

（完）
