---
title: Private and Robust Contribution Evaluation in Federated Learning
title_zh: 联邦学习中隐私且鲁棒的贡献评估
authors: "Delio Jaramillo Velez, Gergely Biczok, Alexandre Graell i Amat, Johan Ostman, Balazs Pejo"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21721v1"
tags: ["query:ppml"]
score: 9.0
evidence: 与安全聚合兼容的联邦学习隐私稳健贡献评估
tldr: 针对跨机构联邦学习中安全聚合与贡献评估不兼容的问题，本文提出了两种与安全聚合兼容的边际差异贡献评分方法：Fair-Private 和 Everybody-Else。Fair-Private 满足公平性公理，而 Everybody-Else 通过消除自我评估增强了抗操纵性。该研究在保证隐私和计算效率的同时，实现了更准确的贡献排名和异常检测，为公平且鲁棒的联邦学习提供了理论与实践支持。
selection_source: fresh_fetch
motivation: 现有的联邦学习贡献评估方法与保护隐私的安全聚合技术不兼容，且现有的替代方案往往缺乏鲁棒性或依赖不可靠的自我评估。
method: 引入了两种与安全聚合兼容的边际差异评分机制，分别通过满足公平性公理和消除自我评估来确保评估的公正与抗操纵性。
result: 在多个医疗影像及CIFAR10数据集上的实验证明，该方法在客户端排名、模型性能提升和恶意行为检测方面均显著优于现有基准。
conclusion: 该研究成功地在联邦学习贡献评估中融合了公平性、隐私性、鲁棒性和高效性，为实际跨机构应用提供了原则性的解决方案。
---

## 摘要
跨机构（Cross-silo）联邦学习允许组织在不共享原始数据的情况下协作训练机器学习模型，但客户端更新仍可能通过推理攻击泄露敏感信息。安全聚合通过隐藏单个更新来保护隐私，但这使贡献评估变得复杂，而贡献评估对于公平奖励以及检测低质量或恶意参与者至关重要。现有的边际贡献方法（如 Shapley 值）与安全聚合不兼容，而实际的替代方案（如留一法 Leave-One-Out）较为粗糙且依赖于自我评估。我们引入了两种与安全聚合兼容的边际差异贡献评分。Fair-Private 满足标准的公平性公理，而 Everybody-Else 消除了自我评估并提供了抗操纵性，解决了长期以来被忽视的漏洞。我们为公平性、隐私性、鲁棒性和计算效率提供了理论保证，并在跨机构设置下的多个医学图像数据集和 CIFAR10 上评估了我们的方法。我们的评分始终优于现有基准，能更好地逼近由 Shapley 值引导的客户端排名，并提升了下游模型性能以及不当行为检测。这些结果表明，在联邦贡献评估中可以同时实现公平性、隐私性、鲁棒性和实际效用，为现实世界的跨机构部署提供了一种有原则的解决方案。

## Abstract
Cross-silo federated learning allows multiple organizations to collaboratively train machine learning models without sharing raw data, but client updates can still leak sensitive information through inference attacks. Secure aggregation protects privacy by hiding individual updates, yet it complicates contribution evaluation, which is critical for fair rewards and detecting low-quality or malicious participants. Existing marginal-contribution methods, such as the Shapley value, are incompatible with secure aggregation, and practical alternatives, such as Leave-One-Out, are crude and rely on self-evaluation.   We introduce two marginal-difference contribution scores compatible with secure aggregation. Fair-Private satisfies standard fairness axioms, while Everybody-Else eliminates self-evaluation and provides resistance to manipulation, addressing a largely overlooked vulnerability. We provide theoretical guarantees for fairness, privacy, robustness, and computational efficiency, and evaluate our methods on multiple medical image datasets and CIFAR10 in cross-silo settings. Our scores consistently outperform existing baselines, better approximate Shapley-induced client rankings, and improve downstream model performance as well as misbehavior detection. These results demonstrate that fairness, privacy, robustness, and practical utility can be achieved jointly in federated contribution evaluation, offering a principled solution for real-world cross-silo deployments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Leave-One-Out: Private and Robust Contribution Evaluation in Federated Learning》的结构化深入总结：

### 1. 核心问题与整体含义
在跨机构（Cross-silo）联邦学习中，**安全聚合（Secure Aggregation, SA）**是保护隐私的核心技术，它通过加密协议确保服务器只能看到聚合后的模型更新，而无法看到单个客户端的梯度。然而，这种“黑盒”属性与**贡献评估（Contribution Evaluation, CE）**产生了根本冲突：传统的评估方法（如 Shapley Value）通常需要访问单个客户端的更新来计算边际贡献。
目前唯一的兼容方案是“留一法”（Leave-One-Out, LOO），但它存在两个致命缺陷：
*   **评估粗糙**：仅考虑移除一个客户端后的变化，对 Shapley 值的近似效果差。
*   **易受操纵**：在 SA 环境下，LOO 依赖客户端“自我报告”评估结果，自私的客户端可以通过虚报分数来骗取更多奖励。

### 2. 方法论
论文提出了两种与安全聚合（SA）严格兼容的新型评分机制，计算复杂度均为 $O(N)$：

*   **Fair-Private (FP)**：
    *   **核心思想**：结合了“留一法”（LOO）和“加一法”（Include-One-In, IOI）。
    *   **技术细节**：客户端 $i$ 不仅计算移除自己后的模型性能变化，还计算仅包含自己时的模型性能提升。通过将这两个边际差异取平均并进行归一化处理，FP 能够满足公平性研究中的三个核心公理：效率（Efficiency）、零参与者（Null Player）和对称性（Symmetry）。
*   **Everybody-Else (EE)**：
    *   **核心思想**：**“互评”而非“自评”**。为了解决操纵问题，EE 故意不让客户端评估自己。
    *   **技术细节**：客户端 $i$ 的得分由除它之外的所有其他客户端 $j$ 的评估结果间接推导。具体而言，其他客户端通过计算包含/排除自身时的边际差异，由于客户端 $i$ 始终存在于这些对比的背景中，通过聚合这些差异可以提取出 $i$ 的贡献。
    *   **抗操纵性**：由于客户端 $i$ 无法通过任何手段影响自己的 EE 评分公式中的变量，从而实现了“操纵抗性”。

### 3. 实验设计
*   **数据集**：涵盖了三个医疗影像数据集（ISIC2019 皮肤癌、PatchChameleon 组织病理学、Brain-MRI 脑肿瘤）以及通用的 CIFAR10。
*   **实验场景**：跨机构联邦学习，采用非 IID 数据分布（Dirichlet 分布 $\mu=0.5$）。
*   **Benchmark（基准）**：
    *   **MR-SV**：多轮 Shapley 值近似（作为地面真值，但它不兼容 SA）。
    *   **True SV**：基于重新训练的真实 Shapley 值（计算成本极高）。
*   **对比方法**：LOO（留一法）、COS（余弦相似度评分）、FedAvg（等权聚合）。

### 4. 资源与算力
*   **模型架构**：使用了 ResNet50、ResNet18 以及 4 层简单的 CNN。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练总时长。但文中提到其算法复杂度为线性 $O(N)$，相比于 SV 的指数级复杂度，在计算效率上有显著优势。

### 5. 实验数量与充分性
*   **实验规模**：所有实验均重复运行 10 次并报告均值和方差，确保了结果的统计显著性。
*   **维度覆盖**：
    *   **相关性分析**：使用 Spearman、Kendall 和 Pearson 系数验证与 Shapley 值的排名一致性。
    *   **消融实验**：测试了不同训练轮次（第 5、10、15 轮）、不同客户端数量（6 到 15 个）、不同数据异质性（$\mu$ 取 0.1 到 1.0）对评分准确性的影响。
    *   **下游任务**：验证了基于评分进行加权聚合对模型性能的提升，以及对恶意攻击者（标签翻转攻击）的检测率。
*   **评价**：实验设计非常全面，不仅验证了理论属性，还通过实际的攻击场景测试了鲁棒性，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **排名准确性**：FP 和 EE 在所有数据集上的排名相关性（Spearman $\phi$）均显著优于 LOO 和 COS，非常接近不可计算的 MR-SV。
*   **性能增益**：使用 FP 或 EE 评分对客户端更新进行加权聚合，其模型收敛效果优于传统的 FedAvg，且几乎达到了 MR-SV 的性能水平。
*   **恶意检测**：在检测恶意客户端方面，FP 和 EE 的识别率远高于 LOO，能够有效识别并抑制低质量或攻击性更新。
*   **鲁棒性**：EE 成功实现了操纵抗性，客户端无法通过虚报数据提升自身得分。

### 7. 优点
*   **隐私与评估的平衡**：首次在严格遵守安全聚合（SA）的前提下，实现了高精度的贡献评估。
*   **理论完备**：对 FP 的公平性公理和 EE 的抗操纵性给出了严格的数学证明。
*   **实用性强**：计算开销极低（线性复杂度），无需改变现有的联邦学习架构即可部署。
*   **解决自私性问题**：EE 机制通过“互评”逻辑巧妙地解决了联邦学习中长期存在的客户端自我优化的道德风险。

### 8. 不足与局限
*   **公理权衡**：EE 机制为了获得抗操纵性，牺牲了“零参与者”（Null Player）公理，即完全不贡献的客户端也可能获得微小的正分数。
*   **依赖外部测试集**：方法假设存在一个代表性的全局测试集（或通过 GAN 生成的合成集），在某些极端隐私敏感的场景下，获取该集合可能存在挑战。
*   **场景限制**：目前主要针对跨机构（Full Participation）场景，尚未深入探讨在客户端频繁掉线（Churn）或大规模跨设备（Cross-device）采样场景下的表现。

（完）
