---
title: "DP-FedAdamW: An Efficient Optimizer for Differentially Private Federated Large Models"
title_zh: DP-FedAdamW：一种用于差分隐私联邦大模型的高效优化器
authors: "Jin Liu, Yinbin Miao, Ning Xi, Junkang Liu"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19945v1"
tags: ["query:ppml"]
score: 8.0
evidence: 差分隐私联邦大模型
tldr: 针对联邦学习中差分隐私（DP）导致AdamW优化器性能下降的问题，本文提出了DP-FedAdamW。该方法通过稳定二阶矩估计、消除DP偏差以及对齐局部更新来缓解客户端漂移，在保证隐私的同时提升了收敛效率。理论证明了其无偏性和线性加速收敛，实验显示在视觉和语言大模型任务上均显著优于现有SOTA方法。
selection_source: fresh_fetch
motivation: 传统AdamW在差分隐私联邦学习中面临二阶矩估计方差大、存在偏差以及客户端漂移加剧等挑战。
method: 提出了DP-FedAdamW优化器，通过稳定二阶矩方差、修正DP引入的偏差并对齐局部与全局下降方向来优化训练。
result: "在Tiny-ImageNet等数据集上，DP-FedAdamW相比现有最先进方法在精度上提升了高达5.83%。"
conclusion: DP-FedAdamW有效解决了差分隐私与联邦大模型优化之间的兼容性问题，为隐私保护下的高效联邦学习提供了强有力的工具。
---

## 摘要
在差分隐私（DP）下平衡收敛效率与鲁棒性是联邦学习（FL）中的核心挑战。虽然 AdamW 加速了大规模模型的训练和微调，但我们发现将其直接应用于差分隐私联邦学习（DPFL）会面临三个主要问题：(i) 数据异构性和隐私噪声共同放大了二阶矩估计的方差；(ii) DP 扰动使二阶矩估计产生偏差；(iii) DP 增强了 AdamW 对局部过拟合的敏感性，从而

## Abstract
Balancing convergence efficiency and robustness under Differential Privacy (DP) is a central challenge in Federated Learning (FL). While AdamW accelerates training and fine-tuning in large-scale models, we find that directly applying it to Differentially Private FL (DPFL) suffers from three major issues: (i) data heterogeneity and privacy noise jointly amplify the variance of second-moment estimator, (ii) DP perturbations bias the second-moment estimator, and (iii) DP amplify AdamW sensitivity to local overfitting, worsening client drift. We propose DP-FedAdamW, the first AdamW-based optimizer for DPFL. It restores AdamW under DP by stabilizing second-moment variance, removing DP-induced bias, and aligning local updates to the global descent to curb client drift. Theoretically, we establish an unbiased second-moment estimator and prove a linearly accelerated convergence rate without any heterogeneity assumption, while providing tighter $(\varepsilon,δ)$-DP guarantees. Our empirical results demonstrate the effectiveness of DP-FedAdamW across language and vision Transformers and ResNet-18. On Tiny-ImageNet (Swin-Base, $\varepsilon=1$), DP-FedAdamW outperforms the state-of-the-art (SOTA) by 5.83\%. The code is available in Appendix.

---

## 论文详细总结（自动生成）

### DP-FedAdamW：用于差分隐私联邦大模型的高效优化器论文总结

#### 1. 核心问题与研究动机
*   **研究背景**：在大规模模型（如 Transformer）的训练中，AdamW 优化器因其自适应学习率和解耦权重衰减的特性而成为主流。然而，在联邦学习（FL）结合差分隐私（DP）的场景下，直接应用 AdamW 效果极差，甚至不如 SGD。
*   **核心挑战**：论文识别了 AdamW 在 DPFL 中的三大痛点：
    1.  **方差放大**：非 IID 数据和 DP 噪声共同导致二阶矩估计（$v$）的方差剧增，使优化极不稳定。
    2.  **系统性偏差**：DP 的梯度裁剪和噪声注入为二阶矩估计引入了不可忽视的加性偏差。
    3.  **客户端漂移加剧**：DP 机制放大了 AdamW 对局部过拟合的敏感性，导致各客户端模型偏离全局最优方向。

#### 2. 论文提出的方法论：DP-FedAdamW
论文提出了首个专为 DPFL 设计的 AdamW 优化器，核心思想是通过三个维度修复 AdamW：
*   **块状二阶矩聚合（Block-wise Aggregation）**：为了稳定二阶矩并降低通信开销，不按参数聚合，而是按模型结构（如 Attention Head、MLP 层）划分为块，计算块内二阶矩的均值进行全局聚合。
*   **无偏二阶矩修正（Bias-Corrected Mechanism）**：在更新规则中显式减去由 DP 噪声引起的方差项 $(\sigma C / sR)^2$，从而消除 DP 注入的系统性偏差，恢复非隐私状态下的缩放行为。
*   **局部-全局更新对齐（Local-Global Alignment）**：在局部更新步骤中引入全局下降方向的估计项 $\gamma \Delta_G^t$，作为一种软正则化，强制局部更新向全局趋势靠拢，有效抑制客户端漂移。

#### 3. 实验设计
*   **数据集**：
    *   **视觉任务**：CIFAR-10、CIFAR-100、Tiny-ImageNet。
    *   **语言任务（GLUE 基準）**：SST-2、QQP、QNLI、MNLI。
*   **模型架构**：ResNet-18 (GN)、ViT-Base、Swin-Tiny/Base、RoBERTa-Base。
*   **对比方法（Benchmark）**：DP-FedAvg、DP-SCAFFOLD、DP-FedAvg-LS（拉普拉斯平滑）、DP-FedSAM（锐度感知最小化）、以及朴素的 DP-LocalAdamW。
*   **数据分布**：使用 Dirichlet 分布 $\alpha \in \{0.1, 0.6, 0.8\}$ 模拟不同程度的非 IID 异构性。

#### 4. 资源与算力
*   **硬件设备**：实验明确提到使用了 **NVIDIA RTX 4090 GPU**。
*   **实现细节**：基于 PyTorch 实现。虽然未详细列出总机时，但给出了具体的超参数设置：如 ResNet-18 训练 300 轮，Transformer 类模型微调 100 轮，Batch Size 为 16 或 50。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个主流数据集和 5 种代表性模型架构，涵盖了从卷积网络到视觉/语言 Transformer 的广泛领域。
*   **充分性**：
    *   进行了不同隐私预算（$\epsilon=1, 2, 3$）下的性能对比。
    *   针对不同异构程度（$\alpha$ 值）进行了鲁棒性测试。
    *   **消融实验**：分别验证了“块聚合”、“偏差修正”和“局部对齐”三个组件的贡献，证明了各部分的必要性。
    *   所有结果均基于 5 次运行的平均值并标注了标准差，实验设计客观且公平。

#### 6. 主要结论与发现
*   **性能领先**：DP-FedAdamW 在所有基准测试中均优于现有 SOTA。在 Tiny-ImageNet (Swin-Base, $\epsilon=1$) 上，比最强基准提升了 **5.83%**。
*   **隐私-效用权衡**：在强隐私保护（小 $\epsilon$）下，该方法的优势更加明显，证明其对 DP 噪声具有极强的鲁棒性。
*   **理论突破**：论文证明了该算法在非凸函数下具有线性加速收敛性，且**无需任何异构性假设**，这在自适应联邦优化理论中具有重要意义。

#### 7. 优点
*   **针对性强**：深入分析了 AdamW 在隐私场景下失效的底层数学原因，并给出了物理含义明确的修正方案。
*   **通信高效**：通过块状聚合策略，在不显著增加通信负担的前提下实现了二阶矩的稳定。
*   **理论与实践结合**：不仅有严谨的收敛性和隐私性（RDP）证明，还在大规模 Transformer 模型上验证了实效。

#### 8. 不足与局限
*   **超参数敏感性**：虽然引入了对齐系数 $\gamma$，但实验显示 $\gamma$ 的取值（如 0.5）对结果有影响，可能需要针对不同任务进行微调。
*   **微调限制**：实验主要集中在参数高效微调（LoRA）上，对于从头训练（From Scratch）超大规模模型在 DPFL 下的表现探讨较少。
*   **计算开销**：虽然通信效率高，但局部计算中涉及二阶矩的修正和对齐项的维护，相比简单 SGD 会略微增加客户端的计算负担。

（完）
