---
title: "HybridFL: A Federated Learning Approach for Financial Crime Detection"
title_zh: HybridFL：一种用于金融犯罪检测的联邦学习方法
authors: "Afsana Khan, Marijn ten Thij, Guangzhi Tang, Anna Wilbik"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19207v1"
tags: ["query:ppml"]
score: 9.0
evidence: 针对混合数据分布的隐私保护联邦学习
tldr: 本研究针对金融犯罪检测中数据同时存在横向（不同用户）和纵向（不同特征）切分的复杂场景，提出了混合联邦学习（HybridFL）框架。该框架通过整合横向聚合与纵向特征融合，在保护隐私的前提下实现了跨机构的联合建模。实验证明，HybridFL在AMLSim和SWIFT数据集上表现优异，显著提升了检测性能并接近中心化基准。
selection_source: fresh_fetch
motivation: 传统的联邦学习难以应对金融领域中数据既在用户维度横向分布又在特征维度纵向分布的混合切分挑战。
method: 提出HybridFL架构，通过集成横向模型聚合与纵向特征融合技术，在严格保护数据本地性的同时实现多方联合训练。
result: 在AMLSim和SWIFT数据集上的实验表明，HybridFL显著优于仅依赖本地交易数据的模型，且性能可比肩中心化训练。
conclusion: HybridFL为处理具有复杂分布特征的金融犯罪检测任务提供了一种兼顾隐私保护与模型效能的有效方案。
---

## 摘要
联邦学习（FL）是一种隐私保护型机器学习范式，使多个参与方能够在不共享原始信息的情况下，利用私有数据协作训练模型。虽然标准的联邦学习通常处理横向或纵向的数据划分，但许多现实场景表现出复杂的混合分布。本文提出了混合联邦学习（HybridFL），旨在解决跨不相交用户的横向划分以及跨互补特征集的纵向划分。我们在金融犯罪检测背景下评估了 HybridFL，其中交易方持有交易级属性，而多家银行维护私有的账户级特征。通过整合横向聚合与纵向特征融合，所提出的架构在严格保持数据本地性的同时实现了联合学习。在 AMLSim 和 SWIFT 数据集上的实验表明，HybridFL 的表现显著优于仅使用交易数据的本地模型，并达到了与中心化基准相当的性能。

## Abstract
Federated learning (FL) is a privacy-preserving machine learning paradigm that enables multiple parties to collaboratively train models on privately owned data without sharing raw information. While standard FL typically addresses either horizontal or vertical data partitions, many real-world scenarios exhibit a complex hybrid distribution. This paper proposes Hybrid Federated Learning (HybridFL) to address data split both horizontally across disjoint users and vertically across complementary feature sets. We evaluate HybridFL in a financial crime detection context, where a transaction party holds transaction-level attributes and multiple banks maintain private account-level features. By integrating horizontal aggregation and vertical feature fusion, the proposed architecture enables joint learning while strictly preserving data locality. Experiments on AMLSim and SWIFT datasets demonstrate that HybridFL significantly outperforms the transaction-only local model and achieves performance comparable to a centralized benchmark.

---

## 论文详细总结（自动生成）

### HybridFL：一种用于金融犯罪检测的联邦学习方法

#### 1. 核心问题与研究动机
*   **核心问题**：在金融犯罪检测（如反洗钱）中，数据通常以“混合”形式分布。**横向分布**体现在不同银行拥有不重叠的客户群体；**纵向分布**体现在交易处理机构（如 SWIFT）拥有交易级数据，而银行拥有账户级的历史行为数据。
*   **研究动机**：传统的联邦学习（FL）通常只针对单一维度（纯横向或纯纵向）。如果只看横向，会忽略跨机构的特征互补；如果只看纵向，则无法处理不重叠的样本空间。本文旨在提出一种**混合联邦学习（HybridFL）**框架，以应对这种复杂的现实数据切分，提升金融犯罪检测的准确性。

#### 2. 方法论
*   **核心思想**：将“纵向特征融合”与“横向模型聚合”集成在一个统一的训练流程中。
*   **角色分配**：
    *   **主动方（交易方）**：持有交易特征、标签、交易编码器和最终的融合预测头。
    *   **被动方（各银行）**：持有账户特征，并维护两个本地编码器（发款人编码器 $h_S$ 和收款人编码器 $h_R$）。
*   **关键技术细节**：
    1.  **嵌入交换（纵向维度）**：对于每一笔交易，交易方识别出涉及的发款行和收款行。各行计算账户特征的嵌入（Embedding）并发送给交易方。
    2.  **特征融合**：交易方将自身的交易嵌入与收到的账户嵌入拼接，通过融合头输出预测结果。
    3.  **梯度回传**：交易方计算损失并生成梯度，将对应的梯度传回各银行，用于更新其本地编码器。
    4.  **联邦聚合（横向维度）**：为了确保不同银行学习到的表示具有一致性，各银行的 $h_S$ 和 $h_R$ 编码器会定期通过 **FedAvg（联邦平均）** 算法进行同步。

#### 3. 实验设计
*   **数据集**：
    *   **AMLSim**：合成银行交易数据集，包含 6.3 万条交易，涉及 2 家银行。
    *   **SWIFT**：来自 NSF 隐私增强技术挑战赛的真实模拟数据集，包含 81 万条交易，涉及 10 家银行，正负样本比极度不平衡（1:903）。
*   **基准方法（Benchmarks）**：
    *   **中心化模型（Centralised）**：假设所有数据集中在一起训练，作为性能上限。
    *   **仅交易方模型（Transaction Party Only）**：仅使用交易级特征训练，作为性能下限。
*   **对比指标**：主要使用 **AUPRC**（精确率-召回率曲线下面积），辅以 Precision、Recall 和 F1-score。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **超参数**：提到了使用 Adam 优化器，Batch Size 为 128，学习率在 $10^{-5}$ 级别，SWIFT 数据集使用了 Focal Loss 以应对类别不平衡。

#### 5. 实验数量与充分性
*   **实验规模**：在两个不同规模和复杂度的合成数据集上进行了验证。
*   **充分性评价**：
    *   **优点**：对比了性能的上下限，证明了 HybridFL 的有效性；SWIFT 数据集的高不平衡性增加了实验的说服力。
    *   **不足**：实验组数相对较少，缺乏对“联邦聚合频率”或“参与银行数量”影响的消融实验；未对通信开销进行定量分析。

#### 6. 主要结论与发现
*   **性能提升显著**：HybridFL 在两个数据集上的表现均远优于仅使用交易数据的模型（如在 SWIFT 上 AUPRC 从 0.65 提升至 0.78）。
*   **接近中心化水平**：尽管存在数据隔离，HybridFL 的性能仍能较好地逼近中心化训练的上限。
*   **特征互补性**：实验证明账户级的行为特征（银行持有）对于识别复杂的金融犯罪至关重要，仅靠交易元数据（交易方持有）难以实现高召回率。

#### 7. 优点
*   **拓扑结构贴合现实**：模型设计精准匹配了现代跨国支付网络（发款行-中转机构-收款行）的实际业务逻辑。
*   **双编码器设计**：为发款和收款角色分别建立编码器，捕捉了账户在不同交易角色下的行为差异。
*   **隐私保护**：在不共享原始账户信息的前提下，实现了多方协作建模。

#### 8. 不足与局限
*   **隐私风险**：虽然不共享原始数据，但直接共享中间层嵌入（Embedding）和梯度仍存在反向推理攻击的风险。论文提到未来需加入差分隐私（DP）或安全聚合，但当前版本尚未实现。
*   **通信依赖**：由于每笔交易都需要跨机构交换嵌入和梯度，在实时性要求极高的金融场景中，通信延迟可能成为瓶颈。
*   **实验环境**：实验基于合成数据集，虽然具有代表性，但在真实生产环境中的鲁棒性仍待验证。

（完）
